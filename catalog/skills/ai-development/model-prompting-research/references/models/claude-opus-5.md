# Prompting Profile: claude-opus-5

**Platform**: claude-code
**Last verified**: 2026-09-08
**Roster provenance**: `config`

This file mirrors the `models["claude-opus-5"]` entry in `assets/profiles-index.json`. The index is authoritative; if the two disagree, regenerate this file from the index with `scripts/write_model_prompting_profile.py`.

## Verified prompting guidance

| Claim | Confidence | Scope | Primary source |
|---|---|---|---|
| Default user-facing responses run longer than prior Opus models, and effort controls how much the model thinks rather than how much it says, so lowering effort does not reliably shorten the visible response; prompt for conciseness explicitly. The effort-is-not-a-length-lever distinction is the actionable half: a reader who reaches for effort to shorten output will not get it. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| In a long system prompt, pair the conciseness instruction with a short reminder near the end of the prompt. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model narrates readily during agentic work and its per-message output in agentic sessions is often longer than prior models'; describe the cadence and shape of updates you want, and prefer positive examples of the style over instructions about what not to do. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| Files this model writes to disk (reports, Markdown documents, summaries) are often longer than on prior models, separately from conversational verbosity, so add explicit length calibration for Claude-authored documents. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model verifies its own work without being told to, so REMOVE explicit verification instructions and legacy harness verification scaffolding: they cause over-verification, and removing them reduces wasted tokens with no loss in quality. TENSION WORTH KNOWING, recorded rather than acted on: this cuts against the catalog's own [[verification-before-completion]] discipline, which requires a fresh proving command before a completion claim. The two are not the same thing -- the vendor claim is about redundant self-re-checks inside a turn, while the skill is about evidence for a claim made to a human -- but anyone reconciling them should read both before editing either. Deliberately scoped model-specific, so it can never reach a shared body through this layer. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model can expand the scope of a task, adding steps that were not requested; for narrow tasks constrain scope explicitly and tell it to stop short of actions clearly beyond what was asked. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model delegates to subagents more readily than prior models; give explicit criteria for which scenarios warrant delegation, or set deterministic caps, because delegation multiplies cost and time when applied to small tasks. On Claude Code and the Agent SDK the deterministic caps are CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH, CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS, and the SDK's max_budget_usd, requiring Claude Code 2.1.217 or later. Claude Code adds its own delegation instruction on this model only under the claude_code system-prompt preset. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model catches and fixes its own mistakes well without prompting, so avoid instructing re-checks such as "double-check your answer" or "re-verify before responding": they compound with the model's own behavior and add cost without improving results. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| This model narrates corrections to its earlier statements more than prior models; limit correction narration to errors that would change the user's code, conclusions, or decisions, and make silent fixes for slips that change nothing. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| In code review this model may follow a "only report high-severity issues" or "be conservative" instruction literally and report less; ask it to report everything and filter in a separate pass instead. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| `low` and `medium` effort produce strong quality at a fraction of the tokens and latency of higher settings and accuracy holds at lower effort; start at the default `high`, use `low` and `medium` liberally where quality holds, step up to `xhigh` for demanding agentic work, and re-run an effort sweep if defaults were carried over from a prior model. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |
| Thinking is on by default and can be disabled only at effort `high` or below; keep thinking enabled and control cost with lower effort instead, because thinking enabled at `low` effort performs better than thinking disabled at similar cost. With thinking disabled two artifacts can appear: a tool call written as user-facing text that never runs (and persists in agentic history), and internal XML tags leaking into the reply. A general instruction mitigates both; instructions that name thinking tags specifically are less effective. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-opus-5) |

## Does not apply to shared bodies

Every claim in this file is scoped to the model named in the H1. It must not be copied into a shared catalog body: a `SKILL.md`, a command file, or any of the five `base-*.md` instruction templates. Those artifacts are distributed verbatim to every supported platform, so a line naming one model is wrong for every reader running a different one, and `scripts/check_base_template_parity.py` fails the build when such a line diverges across the templates.

If a claim here turns out to be true of models generally rather than of this one, re-scope it to `model-agnostic-candidate` in `assets/profiles-index.json` and let the guard-gated auto-apply path propose the shared-body edit, so the change is branch-isolated, guard-checked, and reviewable.

## Schema

The field rules for this file and its index entry are documented in `references/schema.md`.
