# Prompting Profile: claude-fable-5-1

**Platform**: claude-code
**Last verified**: 2026-09-08
**Roster provenance**: `config`

This file mirrors the `models["claude-fable-5-1"]` entry in `assets/profiles-index.json`. The index is authoritative; if the two disagree, regenerate this file from the index with `scripts/write_model_prompting_profile.py`.

## Verified prompting guidance

| Claim | Confidence | Scope | Primary source |
|---|---|---|---|
| Start at the default effort level `high`, then sweep `low`, `medium`, `xhigh`, and `max` against your own evals; effort is the primary control for trading intelligence against latency and cost. Stated directly on the model's own prompting page. Model-specific because the page frames effort as this model's primary control and its level semantics as not transferable. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Re-run the effort sweep even if one was already run on Claude Fable 5, because effort level names do not correspond to the same amount of thinking across models. The non-transferability is itself the claim, so it cannot be generalized to a shared body. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| At `medium` effort this model roughly matches Claude Fable 5 at lower cost, and at `low` it is often competitive with Opus and Sonnet models on cost per task while scoring higher, so include `low` wherever a smaller model at higher effort would otherwise be used. Confidence medium rather than high: the comparison is a vendor performance claim about their own models, not a mechanical behavior a reader can verify from the page itself. | `medium` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| This model writes fewer user-facing updates during long tool-calling turns than Claude Fable 5, more pronounced at higher effort and in longer tool chains. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Before adding prompt text asking for progress updates, first confirm the client actually requests them: progress notes arrive as progress-update `thinking` blocks and are empty under the default `thinking.display` of `omitted`, so set `display: "updates"` or `"summarized"`. Ordering matters and is the point: the page says to check the transport before editing the prompt, because a prompt fix cannot surface blocks the client never asked for. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Audit the prompt for legacy lines that suppress narration (such as "hold all findings for the final response") and remove them before adding any instruction asking for updates. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Append each assistant turn to the conversation history exactly as the API returned it, thinking blocks included, and do not edit earlier turns between requests. For accounts created on or after 2026-08-31 a replayed thinking block whose prefix changed returns 400, or drops the block under `prefix_mismatch_behavior: "drop_block"`. The page states future models are expected to enforce this for all accounts, but that is an expectation about other models and is deliberately not recorded as a shared-body claim. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Send per-turn reminders as turn-scoped system messages and change instructions or tools with a mid-conversation system message rather than rewriting `system` or `tools`, because in-place history edits both trip the thinking-block prefix check and restart the prompt cache. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| This model's prose can be denser than Claude Fable 5's, with longer sentences and fewer paragraph breaks; an instruction that defines mannered prose as the anti-pattern helps, and the short form "Please remove all mannered prose" also tends to work. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| This model uses bold less and is less likely to reach for headers, lists, or quotation marks than earlier models, so remove legacy anti-formatting rules and replace them with a rule stating when specific formatting is appropriate. The actionable half is a REMOVAL of prompt text written for a previous generation, which is the opposite of the usual add-an-instruction fix. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| When summarizing documents this model is more likely than Claude Fable 5 to reproduce source passages without marking them as quotations; add one complete correct example to the system prompt containing the request, the response, and a rationale sentence. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| On complex asynchronous workloads, add a system-prompt block telling the model it is operating autonomously and must not end its turn on a plan, a question, or a promise about work it has not done; the opening sentence stating the user is not watching carries much of the effect. Scoped model-specific despite reading like general advice, per the ambiguity-resolves-to-model-specific rule. Nexus-Hub already ships equivalent text as the `## Autonomous Operation` and `Delivering work` blocks added in v4.7.0, so no shared-body edit is proposed: the shared surface already carries it. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| This model sometimes delivers more than the task asked for (fixing nearby code, extending unmentioned behavior, committing extra test files); an explicit instruction about what to leave out substantially reduces unrequested additions and committed test code with no measurable change in task success. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| At `low` effort this model calls search and retrieval tools less often and is more likely to answer from memory; raising effort for the affected turns is often simpler than a prompt change, and otherwise a verification nudge treats an unfamiliar name as the thing to search. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| Safeguard false positives are more likely with compile-check phrasing (ask "are there any bugs" rather than "does this compile without errors"), with lesser-known programming languages, and when tools return base64-encoded data into context. The page also states that finding vulnerabilities in source code is permitted and that a blocked request returns `stop_reason: "refusal"`. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| This model is more likely than Claude Fable 5 to rewrite an entire file for a small change; an instruction to minimize edit tokens and prefer surgical edits restores the earlier behavior for small and medium changes. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| At `xhigh` and especially `max` effort this model can draft a long deliverable in its thinking and then write it out again as the reply; run such requests at `high` unless a quality gain is measured, set `max_tokens` to cover thinking plus reply, and append the note explaining that both share one limit. | `high` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |
| When this model delegates to subagents, do not force the lead agent to wait for each one: have the start-subagent tool return immediately, pass each result back in a later `user` message, and give the lead a separate tool to call when it wants to wait. Confidence medium: the page reports lower average time to completion at similar quality and cost, and also notes the model still often chooses to wait, so the gain is described as coming from a subset of runs. | `medium` | `model-specific` | [source](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/prompting-claude-fable-5-1) |

## Does not apply to shared bodies

Every claim in this file is scoped to the model named in the H1. It must not be copied into a shared catalog body: a `SKILL.md`, a command file, or any of the five `base-*.md` instruction templates. Those artifacts are distributed verbatim to every supported platform, so a line naming one model is wrong for every reader running a different one, and `scripts/check_base_template_parity.py` fails the build when such a line diverges across the templates.

If a claim here turns out to be true of models generally rather than of this one, re-scope it to `model-agnostic-candidate` in `assets/profiles-index.json` and let the guard-gated auto-apply path propose the shared-body edit, so the change is branch-isolated, guard-checked, and reviewable.

## Schema

The field rules for this file and its index entry are documented in `references/schema.md`.
