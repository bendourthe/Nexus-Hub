interface:
  display_name: DevLog Generation
  short_description: Maintain docs/DEVLOG.md as a bounded per-release index - one line per release with a date, version, one-sentence summary, and links to that release's plan, development history,
    and known-gaps file. Narrative goes to the per-version history file, not the index.
