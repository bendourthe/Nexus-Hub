{
  "name": "trivia-quiz",
  "version": "0.1.0",
  "private": true,
  "description": "A tiny browser trivia quiz. Open index.html to play.",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
