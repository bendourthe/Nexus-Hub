# Glow Booth — start here

Open `index.html` in a browser. No build step.

Frozen bugs:

- A perfect set of five poses awards 4/5 stamps.
- Restart leaves the last pose on stage.

The local `/compare` target is `../glow-booth-shuffle-reference` (Shuffle poses plus a sparkle overlay, with both bugs already fixed).
