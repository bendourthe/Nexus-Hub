#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec python "$repo_root/scripts/check_commit_attribution.py" \
    --root "$repo_root" --pending-commit --message-file "$1"
