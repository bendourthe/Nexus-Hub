import type { ExtensionContext } from "vscode";

export function activate(_context: ExtensionContext): void {
  // Phase 5 wires the provider/store into Cursor UI surfaces.
}

export function deactivate(): void {
  // No runtime resources are registered during the Phase 4 data-layer scaffold.
}
