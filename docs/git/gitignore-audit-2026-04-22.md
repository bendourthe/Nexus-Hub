# .gitignore Audit - DevAI-Hub - 2026-04-22

**Repository:** `<project_root>` (DevAI-Hub)
**Scope:** full repo
**Mode:** --fix (user-invoked via /update-gitignore during v0.9.7 post-implementation docs phase)
**History scan:** no

---

## Summary

| Severity | Count |
|----------|-------|
| G0 CRITICAL | 0 |
| G1 HIGH | 2 |
| G2 MEDIUM | 3 |
| G3 LOW | 0 |
| **Total** | **5** |

Tracked files to remove from index: 0
.gitignore entries to add: 7 (grouped into 3 sections)
LFS candidates: 0

---

## Findings

| ID | Severity | Category | Location | Description | Recommended Action |
|----|----------|----------|----------|-------------|-------------------|
| G-001 | G1 | Build Artifact | `.pytest_cache/` (untracked; present after Phase 6 test runs) | pytest writes cache state here; should never be committed | Add `.pytest_cache/` to .gitignore |
| G-002 | G1 | Build Artifact | `extensions/claude-usage-monitor/*.vsix` (generated by installer's `npx vsce package` step) | VSIX packaging output is a build artifact rebuilt on each install run | Add `*.vsix` (or `extensions/**/*.vsix`) to .gitignore |
| G-003 | G2 | Build Artifact | `extensions/claude-usage-monitor/out/` (TypeScript compile output) | Installer runs `npm run compile` which emits here; not meant for commit | Add `extensions/**/out/` to .gitignore |
| G-004 | G2 | Build Artifact | Python venvs (`.venv/`, `venv/`, `env/`) in dev workspaces | Developers commonly create local venvs; none currently present but should be preemptively ignored | Add `.venv/`, `venv/`, `env/` patterns |
| G-005 | G2 | Build Artifact | `.mypy_cache/`, `.ruff_cache/` (untracked; present if contributors run ruff/mypy locally) | Type/lint caches pollute the working tree; stack-appropriate | Add `.mypy_cache/`, `.ruff_cache/` patterns |

**Intentionally NOT flagged**:
- `__pycache__/` directories - already covered by existing `*.py[cod]` wildcard which matches `.pyc`/`.pyo`/`.pyd` files inside the directories. (The directory itself remains empty after cleanup but does no harm; add `__pycache__/` explicitly anyway for defensive clarity.)
- `node_modules/`, `dist/`, `.idea/`, `.DS_Store`, `Thumbs.db`, `temp/`, `*.log` - already covered.
- Session-history markdown files and new SKILL.md files (the v0.9.7 untracked list) - these are version-controlled content, not build artifacts. They will be staged on the next commit.

---

## Proposed .gitignore Additions

```gitignore

# Python caches (pytest, mypy, ruff) - not covered by *.py[cod]
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Python virtual environments (developer-local)
.venv/
venv/
env/

# VS Code extension build output
*.vsix
extensions/**/out/
```

Total: 10 lines (3 sections, 7 unique patterns plus section headers). Appended to the existing .gitignore after the existing `temp/` entry at the bottom; no deletions, no re-ordering of prior content.

---

## Proposed `git rm --cached` Commands

None. No tracked files match any ignore pattern.

---

## LFS Recommendations

None. No files over 5 MB; no large binary asset categories present in the repo.

---

## Manual Steps Required

None. All findings are additive `.gitignore` entries; no history purge, no credential rotation, no LFS initialization.

---

## .gitignore Syntax Fixes

None. The existing `.gitignore` is syntactically clean.
