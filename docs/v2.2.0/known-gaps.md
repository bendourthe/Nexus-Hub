# Known Gaps -- v2.2.0

This file tracks per-version unfinished work, deferred items, deviations from plan, and bugs discovered during phase implementation. The next phase plan and the version-bump checklist read this file to decide what carries forward.

**Plan**: [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md)
**Status**: in-progress (Phases 1-5 closed; Phase 6 pending)
**Last updated**: 2026-05-22 (Phase 5 close -- Code-graph capabilities: three framework route extractors land under `extensions/nexus-code-search/src/nexus_code_search/frameworks/` (Django `path()`/`re_path()`/`url()`/`include()`/`as_view()`, FastAPI/Flask decorator-driven `@app.<method>` + `@router.<method>` discovery, Express `app.get`/`router.post` calls with middleware-chain `references` edges); `FrameworkResolver` base class invoked from the extraction orchestrator after per-language AST extraction, with deterministic local-index threading so multiple resolvers compose on the same file; new `graph/affected.py` reverse-import BFS algorithm with conservative module-candidate name resolution returns the test files transitively touched by a source change; `code_affected_tests` MCP tool + `scripts/nexus_hub_affected.py` CLI dispatcher (registered in both `installer.sh` and `installer.ps1`) expose the algorithm to agents and shells; synthetic-codebase eval harness lives at `src/nexus_code_search/eval/` with 4 fixture codebases (minimal / python_app / fastapi_app / ts_express, 18 questions total), tiny in-tree YAML subset parser to avoid an external dep, Markdown / JSON reporting, and a new `make eval` target wiring the runner to `docs/v2.2.0/eval-baseline.md` (baseline: 100% aggregate recall, 63.3% aggregate precision -- all four fixtures clear the 80% per-fixture recall gate); 223 nexus-code-search tests pass (32 net new: 5 Django + 6 FastAPI + 5 Express + 5 affected-algorithm + 4 affected-CLI + 2 server-handler + 5 eval-runner); JSON catalogs validate; no Phase 4 regressions.)

## Summary

| Category | Open | Resolved this version |
|---|---|---|
| NI -- Not implemented (skipped subtask) | 0 | 0 |
| DF -- Deferred (intentionally) | 2 | 0 |
| BG -- Bug or unresolved test failure | 0 | 3 |
| MT -- Missing tests / coverage gap | 1 | 0 |
| WN -- Warning or suppressed lint rule | 7 | 0 |
| QG -- Quality gate bypassed | 0 | 0 |
| **Total** | **10** | **3** |

## Open Items

| ID | Title | Category | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|---|
| DF-001 | Byte-identical parity migration of the original 4 platforms into the integration registry (part 2: instruction-file rendering) | DF | Phase 3 sub-task T021 (carried forward from v2.1.0/known-gaps.md DF-001) | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 3 sub-task 3.7 | Phase 3 sub-task 3.6 (T020) closed tree-mirror parity: SHA-256-identical output between the legacy bash `safe_folder_copy` blocks and the registry's `IntegrationBase._copy_tree` for `catalog/skills`, `catalog/commands`, `catalog/agents`, `catalog/rules` across claude / codex / cursor / gemini / opencode. Instruction-file parity was deliberately deferred: the legacy bash `render_template` substitutes 13+ placeholders (`{{PRIMARY_LANGUAGE}}`, `{{BUILD_CMD}}`, `{{OS_CONTEXT}}`, `{{SKILL_INDEX}}`, ...) plus appends language-specific coding snippets, whereas the Python `MarkdownIntegration._render` currently substitutes only `{{PROJECT_NAME}}`. Removing the legacy bash blocks without bringing the registry runner to feature parity would silently downgrade end-user instruction file content. | Extend `scripts/lib/integrations/runner.py::cmd_install` to accept and thread the full bash placeholder set (build/test/lint/language/skill-index/os-context); inline the per-language coding-snippet append into `MarkdownIntegration._write_instruction`; then add an instruction-file byte-parity assertion to `tests/integrations/test_parity_with_legacy_installer.py` and finally refactor the legacy claude / codex / gemini blocks in `scripts/installer.{sh,ps1}` into single `invoke_registry_platform` calls. Target: v2.3.0. |
| MT-1 | `Copilot` and `Cursor` retain bespoke install paths that bypass the new shared-mode marker write | MT | Phase 1 | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 1 sub-task 1.4 | Phase 1.4 routed `MarkdownIntegration._write_instruction` through `merge_marker_section`. Phase 3 hardened Copilot's bespoke flow so the first install now emits its `## Nexus-Hub Harness` marker (the contract suite's `test_install_idempotent` would otherwise fail), but Copilot still uses an append-after-heading pattern rather than the canonical marker-merge primitive. Cursor's AGENTS.md path was already updated to `merge_marker_section` in Phase 1; the bespoke `.mdc` rules pipeline still writes raw files (no markers needed since the files are dedicated). | Refactor Copilot's `install_workspace` to call `merge_marker_section` with a `legacy_header="## Nexus-Hub Harness"` so the marker block is migrated inline alongside the v2.1 `## Nexus-Hub` legacy header. Target: v2.3.0. |
| MT-2 | Instruction-file byte-parity tests are not yet asserted by the parity suite | MT | Phase 3 sub-task T020 | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 3 sub-task 3.6 | `tests/integrations/test_parity_with_legacy_installer.py::test_instruction_file_is_produced` only asserts that the instruction file exists and is non-empty. Byte equality with the bash sed-substituted output is the precondition for T021 (legacy bash block removal) and is the focus of DF-001-part2. | Same as DF-001 next step (close DF-001 and MT-2 simultaneously). |
| WN-1 | `pathspec` `GitWildMatchPattern` deprecation warning in `nexus-code-search` tests | WN | Phase 1.6 test review (pre-existing) | n/a (pre-existing) | The Phase 1.6 stabilization run surfaced 52 `DeprecationWarning: GitWildMatchPattern ('gitwildmatch') is deprecated` warnings from the `nexus-code-search` test suite. These predate Phase 1 (the warning lives in `pathspec/pattern.py:125`) but were not previously tracked. Tests pass; no functional impact. | Track as v2.3.0 cleanup; pin the `pathspec` API to `gitignore` mode when next touching `extensions/nexus-code-search`. |
| WN-2 | Antigravity CLI binary name unverified on a live VM | WN | Phase 2 sub-task 2.1 (T007) | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 2 sub-task 2.1 (T007) | The Antigravity CLI install-path probe ran as a static analysis (no live VM was available at authoring time on 2026-05-21). The probe inferred the binary name `antigravity` from the parallel Gemini CLI / Antigravity product naming, but this was not confirmed empirically. The new `antigravity-cli-diff-review.sh/.ps1` hook and the AGENTS.md sunset notice hardcode `antigravity` as the PATH name. | Once Google ships the Antigravity CLI to a verifiable user channel (announced for 2026-06-18 sunset cutover), re-run the probe on a live install, update [docs/v2.2.0/antigravity-cli-probe.md](antigravity-cli-probe.md) section 1, and rename the hook scripts + AGENTS.md references if Google ships a different binary name. |
| WN-3 | Antigravity CLI workflow file format unverified on a live VM | WN | Phase 2 sub-task 2.6 (T012) | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 2 sub-task 2.6 (T012) | The TOML-vs-Markdown commands schema analysis concluded that Antigravity CLI inherits Antigravity 2.0 desktop's Markdown workflow format (verbatim `.md` files under `~/.agent/workflows/`), not Gemini CLI's `.toml` schema. The conclusion is supported by the documented Antigravity 2.0 desktop behavior and the 2026-05-21 Google announcement of a shared backend, but it was not verified empirically against a live Antigravity CLI install. | Verify on the same live VM as WN-2; if the CLI ships a different format (e.g., custom JSON manifest), add a `_write_antigravity_commands` helper variant to `scripts/lib/integrations/antigravity.py` and re-open T012. |
| WN-4 | Antigravity CLI workflow front-matter / name-derivation schema unverified | WN | Phase 2 sub-task 2.6 (T012) | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 2 sub-task 2.6 (T012) section 6 | Whether the Antigravity CLI honors YAML frontmatter in workflow files, and the exact rule it uses to derive the workflow name (filename vs. first H1), are not empirically confirmed. The current Antigravity 2.0 + CLI install path mirrors `catalog/commands/*.md` verbatim, so any future schema rejection by Antigravity CLI would surface as a runtime warning rather than as an install error. | Same live-VM verification as WN-2 / WN-3. If frontmatter is rejected, add a strip pass to `SkillsIntegration._mirror_catalog` (or a dedicated Antigravity helper). |
| DF-002 | 18 deferred language extractors and 13 deferred framework extractors beyond Python / TypeScript / Django / FastAPI / Express | DF | Phase 4 sub-task T025 (forward-declared in [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) item N2) | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) "Items explicitly NOT adopted in v2.2.0 (deferred to v2.3.0+)" -> N2 | Phase 4.3 (T025) shipped `PythonExtractor` and `TypeScriptExtractor` only. The other 18 language extractors (Go, Rust, Java, C#, PHP, Ruby, C, C++, Swift, Kotlin, Scala, Dart, Lua, Luau, Svelte, Vue, Liquid, Pascal, plus plain JavaScript) and the 13 framework extractors beyond C3's Django / FastAPI / Express starter set were intentionally not implemented per the plan's deferral. The architecture is ready for them: adding a new language is a new `Extractor` subclass plus a registry entry in `LANGUAGE_EXTRACTORS`. | Add new extractors as v2.3.0+ tickets arrive. Each follows the same shape as `extraction/languages/python.py`: tree-sitter parser + per-NodeKind tree walk + in-file edge resolution. Track per-language demand against the eval baseline; ship the next batch in whatever order Nexus / Nexus-Hub user feedback prioritizes. |
| WN-5 | tree-sitter dependency pin tightened from the plan's `^0.23.0` to `>=0.24,<0.26` | WN | Phase 4 sub-task T023 | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 4 sub-task 4.1 (T023) | The plan suggested `tree-sitter = "^0.23.0"` with `tree-sitter-languages = "^1.10.0"`. The `tree-sitter-languages` 1.10 wheels target the tree-sitter 0.20-0.21 ABI and crash with `TypeError: Language.__init__() takes exactly 1 argument` on tree-sitter 0.23+. The Phase 4 install resolves to `tree-sitter>=0.24,<0.26` + `tree-sitter-python` + `tree-sitter-typescript` (the maintained per-language packages); the now-abandoned `tree-sitter-languages` umbrella package is not used. The wheels publish for Python 3.10 / 3.11 / 3.12 on Windows / macOS / Linux per `pip index versions`. | If a future tree-sitter major bump (0.26+) breaks ABI compatibility with `tree-sitter-python` / `tree-sitter-typescript`, widen the upper bound only after re-running `extensions/nexus-code-search/tests` to confirm. Track when the language packages publish a 0.26-compatible release. |
| WN-6 | Phase 4 in-file call resolution does not yet cover constructor instantiations or super-method overrides | WN | Phase 4 sub-task T026 | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 4 sub-task 4.4 | The Python `_collect_calls` and TypeScript `_collect_calls` paths emit `calls` for plain function calls and attribute-style method calls (`self.method()`, `obj.method()`). `instantiates` (Python `Class()` constructor invocation), `overrides` (method overriding a parent class's method in the same file), and cross-file `references` are not yet emitted; they show up either as `calls` to the class node (`main calls Service` when `Service()` constructs) or are dropped silently. The `EdgeKind` taxonomy includes both kinds so the storage path is ready; only the extraction logic needs an upgrade. | Add a constructor-detection branch in `_collect_calls` (`Class()` -> `instantiates` instead of `calls`), and add an `overrides` resolver that compares method names against the enclosing class's parent class (using the in-file `extends` edges already emitted). Both are local-resolution upgrades; no schema change. |
| WN-7 | Phase 5 eval baseline shows 63.3% aggregate precision because FTS5 returns symbol parameters and signature tokens alongside the target name | WN | Phase 5 sub-task T034 | [docs/v2.2.0/plans/codegraph-and-antigravity.md](plans/codegraph-and-antigravity.md) Phase 5 sub-task 5.6 | Aggregate recall is 100% on every fixture (>= 80% gate cleared) but precision sits at 63.3% because `code_search` matches against the FTS5 index of `(name, qualified_name, docstring)`. A search for `create_item` correctly returns `create_item` but also surfaces the parameter `name` (which appears in the function signature `create_item(name: str)`) -- a true positive against the index, a false positive against the expected-symbols answer key. The behavior is correct; only the fixture's expected-set narrowness produces the precision drop. | Either (a) tighten `code_search` to default to `kind` filtering when one is obvious (`*_item` -> filter to `function` / `method`), or (b) widen the fixture answer keys to include the parameter / variable matches the FTS index legitimately surfaces. Target: v2.3.0 (after the in-file resolution upgrades in WN-6 land, since they shift the recall surface anyway). |

## Resolved

| ID | Title | Category | Resolved in | Detail |
|---|---|---|---|---|
| BG-P3-1 | `merge_marker_section` truncated the block at the first nested mention of the end marker | BG | Phase 3 (T019 contract suite surfaced the failure) | The `gemini` integration's `base-gemini-ide.md` template references both `<!-- NEXUS_HUB_START -->` and `<!-- NEXUS_HUB_END -->` literally in its body to explain the marker mechanism to users. `_replace_between_markers` and `_strip_between_markers` used `text.index(end_marker, start)` (first occurrence), so the second install treated the nested mention as the boundary and rewrote a partial block. Fixed by switching both helpers to `text.rindex(end_marker, start)` so templates can quote the markers without breaking idempotency. |
| BG-P3-2 | Copilot first-install wrote the rendered template without a marker; subsequent installs appended the marker block to themselves | BG | Phase 3 (T019 contract suite surfaced the failure) | `CopilotIntegration.install_workspace` originally branched on `dst.exists()`: pre-existing file -> merge `marker + rendered`; absent file -> write `rendered` bare. The second install then saw a marker-less file and appended `marker + rendered`, leaving the file growing on every run. Fixed by always emitting `<marker>\n\n<rendered>\n` on first install so the second install sees the marker and short-circuits to `kept`. |
| BG-P4-1 | TypeScriptExtractor missed `extends` / `implements` clauses in tree-sitter-typescript 0.23+ | BG | Phase 4 (T028 stabilization surfaced the failure) | Initial Phase 4.3 (T025) implementation read the class heritage via `class_node.child_by_field_name("heritage")`, but tree-sitter-typescript 0.23+ exposes `class_heritage` as a named child without a field name (the older `heritage` field was removed). The extractor returned `None` for the heritage lookup so no `EXTENDS` / `IMPLEMENTS` edge was emitted. Fixed by walking `node.named_children` to find the `class_heritage` node, then iterating its `extends_clause` / `implements_clause` children and emitting one edge per base type. The two failing tests (`test_ts_extractor_emits_class_and_interface`, `test_ts_extractor_emits_extends_for_class`) pass after the fix. |

---

**File lifecycle**: This file is appended by `/implement-phase` Phase 8 step 8.4 (per-phase append), swept by `/wrap-up-session` Phase 6 (catch-all from live conversation), and finalized by `/update-version` at the v2.2.0 -> next-version bump. After finalization, the next plan run by `/generate-plan` will read this file to decide which items carry forward.
