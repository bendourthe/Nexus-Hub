# Session History - v3.11.0 Phase 8: Nexus-Hub self-application (dogfood)

**Date**: 2026-07-08
**Plan**: `docs/v3/v3.11/plans/v3.11.0-workflow-governance-refinements.md`
**Phase**: 8 of 8 - Nexus-Hub self-application (the mandatory final refactor phase)
**Status**: Complete (Exit Checklist PASS). Release-ready; `/update release` owns the commit / merge / tag / push. Phase 8.1 (active-docs migration) was already done in commits d1b49a0 + 84caded; this session did the 8.2-8.6 remainder (tracked as NI-5).

## Goal

Apply every new v3.11.0 rule to the Nexus-Hub repo itself so the codebase follows its own governance: normalize the archive to the canonical scheme with full link repair, run the upgraded refactor cleanup, finish CI/CD optimization, bump to v3.11.0 across every surface, and leave the full validator set green.

## What changed (by sub-task)

- **8.2 - Archive normalization (the highest-risk work)**: migrated `docs/archive/v<MAJOR>/v<MAJOR>.<MINOR>.<PATCH>/` to `docs/archive/v<MAJOR>/v<MAJOR>.<MINOR>/` (e.g. `v2/v2.1.0/` -> `v2/v2.1/`; `v0.8.1..v0.8.9` -> `v0/v0.8`; `v0.9.2..v0.9.7` -> `v0/v0.9`). A pre-migration inventory confirmed **zero filename collisions** (NI-5 had anticipated `known-gaps.md` / `docs-cleanup-report.md` collisions, but those files live in distinct minor dirs), so the migration was a clean many-to-one directory merge preserving each version dir's internal structure. **164 files moved with `git mv`** (history preserved; verified as 164 renames in `git status`). Then repaired **1028 archive-path references across 151 files** with a regex anchored to literal `docs/archive/vN/vN.M.P/` strings (so bare version numbers in prose were never touched), excluding `CHANGELOG.md` (intentional historical mentions). A functional test assertion (`test_installer_smoke.py` `V0_9_7_ARTIFACTS`) that pins `opus-4-7-migration.md` was correctly repaired to the new path. `archive-layout.md` already showed the new minor-grouped shape (no change needed); fixed one stale two-level example in `docs-layout-refactor/SKILL.md`.
- **8.3 - project-refactor cleanliness detectors on the repo**: ran the Phase 4 detectors (empty dirs, content-hash duplicates, structure-complexity chains) over 1502 tracked files outside `docs/`. Result: **the repo is already clean**. No empty dirs; the only "duplicates" are intentional (the 5 byte-identical `base-*.md` behavioral-guardrail templates that must exist as separate per-platform files, the catalog/extension logo+icon copies) or false positives (empty `__init__.py` / `.gitkeep` markers hashing identically); the only single-child chains are required conventions (`.cursor/rules/`, Python `src/<pkg>/`, `configs/permissions/` which is referenced by the installer, test fixtures). Nothing was safe to auto-apply; nothing needed maintainer escalation.
- **8.4 - CI/CD optimization**: added `paths-ignore: docs/**` to `ci.yml` push+PR triggers (docs-only pushes now skip the full job set - the biggest saver for a docs-heavy release; skill/template/README/CHANGELOG changes live outside `docs/` and still trigger the full run); added `cache: 'pip'` to the two pip-heavy jobs (`validate` keyed on `.pre-commit-config.yaml`; `tests` keyed on `extensions/*/pyproject.toml`, which does pytest + 5 editable installs); added a `concurrency` cancel-in-progress block to `codeql.yml`. `ci.yml` concurrency and the expensive-OS gating (bootstrap macOS, bootstrap-windows, install-smoke -> non-PR only) already landed in Phase 7.5. Did NOT add a CodeQL PR trigger (it would add minutes, against this phase's reduction goal). Both workflows parse and pass `validate_workflow_security.py`.
- **8.5 - Registries, version bump, CHANGELOG, manifest**: confirmed the 3 new delegate skills (`setup-project`, `analyze-codebase`, `implement-phase`) are registered consistently (263 skills across `skills.json` + `SKILL_INDEX.md`; marketplace category counts sum to 263). Did NOT run `make build-catalog` (not needed - registry already consistent; and it regenerates `security`/`long_description` for all 263 skills from source, a large out-of-scope diff). Bumped 3.10.3 -> 3.11.0 across all 6 `check_version_sync.py` surfaces (plugin.json, marketplace.json, installer.sh, installer.ps1, CHANGELOG heading, README+AGENTS markers). Reconciled the stale catalog prose (259 -> 263 skills) in plugin.json, marketplace.json, README.md (incl. a rewritten "What's New in v3.11.0"), and AGENTS.md. Added the `## [3.11.0]` CHANGELOG entry (fresh `## [Unreleased]` on top; promoted the parallel-adoption entries and fixed their now-moved `docs/v3.11.0/...` self-links). Regenerated `MANIFEST.sha256` (1112 entries). Assessed base templates: NO `base-*.md` change required (this release is command+skill behavior, not always-loaded instruction text; `check_base_template_parity.py` passes untouched).
- **Link consistency (8.6)**: fixed the Makefile `eval` / `compress-eval` output paths (`docs/v3.0.0/` -> `docs/v3/v3.0/`, `docs/v3.2.0/` -> `docs/v3/v3.2/`) and two broken links in `docs/CATALOG-COVERAGE.md` (to the now-archived `opus-4-7-migration.md`). Residual `docs/vX.Y.Z/` grep matches are all intentional: scheme descriptions in `docs-layout-refactor` / `implementation-plan`, historical `DEVLOG.md` / `CHANGELOG.md` entries, archived-doc self-references, guard-test fixtures, and tutorial examples.

## Verification (8.6)

- **Full `make validate` equivalent, green** (run individually - `make` is unavailable on the Windows host, WN-v36-1): JSON catalogs parse; `validate_skills --bundles-only` PASS and `--quality` PASS (0 errors, 263 skills); `validate_no_personal_paths`, `validate_unicode_safety`, `scan_supply_chain_iocs`, `validate_workflow_security`, `validate_solution_frontmatter` all PASS; `check_version_sync.py` clean at 3.11.0 (all 6 surfaces match); `check_base_template_parity.py` PASS; **compression accuracy-regression gate PASS** (CCR 100.0%, signatures 100.0%, reduction 45.8% - resolves WN-1).
- **Archive migration**: 164 files moved as git renames, no content lost (165th file is the untouched archive-root README); DoD grep confirms only `CHANGELOG.md` retains legacy `docs/archive/vN/vN.M.P/` paths.
- **Tests**: `test_installer_smoke.py` 28 pass (asserts the migrated archive path). The version bump uses parameterized test fixtures (`test_check_version_sync.py`), so no test hardcodes the old version. `tests/integrations` + `tests/installer` had only comment/docstring repairs (no behavioral change); their full run is subprocess-heavy and times out on the Windows host, so it defers to the CI `tests` job on Linux (WN-3).

## Notes and environment caveats

- `make` unavailable on the Windows dev host (WN-v36-1 class); every gate was run individually and all pass.
- Residual gaps recorded in `known-gaps.md`: DF-1 (external platform live-probes), WN-2 (Windows MAX_PATH deep-seed edge), WN-3 (full integrations/installer suite deferred to CI-Linux; `README_zh.md` catalog-count prose not re-synced to 263). WN-1 (compression eval) is now resolved.
- Nothing was auto-tagged or pushed. The version bump + CHANGELOG + manifest are committed on `develop`; the develop->main merge, tag, push, and GitHub Release are `/update release`'s job, behind its own confirmation gates.

## Next steps

- Hand off to `/update release` for the commit finalization / develop->main merge / `v3.11.0` tag / push / GitHub Release (never auto-tagged). It will find version-sync clean, the CHANGELOG finalized, the manifest current, the docs tree migrated, and CI optimized.
- Follow-ups (non-blocking): sync `README_zh.md` counts to 263; live-probe the DF-1 external contracts; consider Windows long-path support for the deep auto-seed (WN-2).
