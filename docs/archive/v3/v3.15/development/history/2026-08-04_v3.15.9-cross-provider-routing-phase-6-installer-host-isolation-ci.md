# Development Log: v3.15.9 Phase 6 Installer Host Isolation, Packaging, and CI E2E

**Date**: 2026-08-04
**Operator**: Repository maintainer
**Assisted by**: Cursor Grok 4.5, frontier tier / max effort (plan recommendation; Cursor picker-only)
**Objective**: Complete Phase 6 of `v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md` by isolating VS Code vs Cursor monitor installs, adding Cursor monitor CI/E2E degrade, and recording the live-smoke checklist.
**Outcome**: Tasks T031-T037 are complete on `feat/v3.15.9-cross-provider-routing`, ready for the maintainer-approved local phase commit. No push, pull request, release, or live credential probe is included.

---

## 1. Starting State

- **Branch**: `feat/v3.15.9-cross-provider-routing`
- **Starting commit**: `97b8bb71` (`feat(cursor-usage): add monitor UX shell and assets`)
- **Working tree**: clean before editing
- **Environment**: Windows 10, PowerShell 5.1, Node 22+, Python 3.12, pytest, Cursor
- **Prior session reference**: `docs/archive/v3/v3.15/development/history/2026-08-04_v3.15.9-cross-provider-routing-phase-5-cursor-monitor-ux-alerts.md`
- **Plan reference**: `docs/v3/v3.15/plans/v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md`

Phase 5 had shipped the Cursor UX shell with HO-5 still disabling live transport. Installers still treated Cursor as a VS Code-family fallback and did not install `cursor-usage-monitor`.

`is_final_phase` is false: Phase 7 remains the terminal architecture/known-gaps/CI/release gate.

---

## 2. Chronological Steps

### 2.1 T031-T032 - Dual-host installer resolution

**What happened**: Both installers now resolve `vscode_cli` / `$vscodeCli` (PATH `code` plus VS Code / Insiders / VSCodium paths only) and `cursor_cli` / `$cursorCli` (PATH `cursor` plus Cursor app/cmd paths) independently. Claude/Codex/GitHub monitors install only via the VS Code CLI; Cursor Usage Monitor installs only via the Cursor CLI under `ANYSPHERE`. Section banner renamed to `USAGE MONITOR EXTENSIONS`. Missing-CLI messaging uses the host label.

**Key files**: `scripts/installer.sh`, `scripts/installer.ps1`

### 2.2 T033 - Installer host-isolation tests

**What happened**: Extended `USAGE_MONITORS` with the Cursor row, kept Anthropic → OpenAI → GitHub → Anysphere order, and added `test_installers_isolate_vscode_and_cursor_hosts` asserting Cursor paths are absent from the VS Code resolver region and each monitor receives only its host CLI variable.

**Key files**: `catalog/hooks/tests/test_installer_smoke.py`

### 2.3 T034-T035 - Cursor monitor CI and E2E degrade

**What happened**: Added `.github/workflows/cursor-usage-monitor.yml` (Node 22, coverage, package, verify, artifact upload) plus `e2e-cursor-profile` that installs into `--user-data-dir` when `cursor` exists, otherwise skip-with-note to the live-smoke doc. Added Dependabot npm entry and workflow policy tests.

**Key files**: `.github/workflows/cursor-usage-monitor.yml`, `tests/workflows/test_cursor_usage_monitor_workflow.py`, `.github/dependabot.yml`

### 2.4 T036 - Live smoke checklist

**What happened**: Documented host isolation, personal meters/on-demand, theme smoke, commands, and HO-5 recording rules at `docs/v3/v3.15/development/cursor-usage-live-smoke.md`.

### 2.5 T037 - Stabilization and lifecycle

**What happened**: Focused installer/workflow suites green (123 with related policy suites); extension suite 132 green. Updated plan, known-gaps, DEVLOG, CHANGELOG, docs-cleanup audit, and this history. Commit deferred to the required 8.10 prompt.

---

## 3. Verification Gates

| Gate | Result |
|---|---|
| Installer smoke + host isolation | Pass |
| Cursor + sibling workflow policy | Pass |
| Cursor extension `npm test` | 132 passed |
| Live transport / secrets | None accessed; HO-5 honored |
| Push / release | Not performed |

---

## 4. Known Issues Carried Forward

- **HO-5**: Authorized Cursor session/dashboard probe still required before automatic credential reuse.
- **WN-4**: Transitive `@vscode/vsce` deprecation notices remain; audit stays clean.
- **Phase 7**: Architecture refactor, known-gaps reconciliation, CI optimization, and release-readiness handoff to `/update release`.

---

## 5. Deviations

None that change Phase 6 scope. VSCodium remains an accepted VS Code-family install host for the three VS Code monitors.

---

## 6. Next Steps

Begin Phase 7 from the completed local phase boundary: architecture refactor, known-gaps reconciliation, CI/CD finalize, and release-readiness handoff.
