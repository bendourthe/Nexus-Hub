# Development Log: v3.15.9 Phase 2 Model-Routing Skill and Command Wiring

**Date**: 2026-08-03
**Operator**: Repository maintainer
**Assisted by**: GPT-5.6 Sol in Cursor, frontier tier
**Objective**: Complete Phase 2 of `v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md` by implementing deterministic generic tier/effort scoring, a validated cross-provider model map with visible offline fallback, command/runbook wiring, and cross-platform tests.
**Outcome**: Tasks T007-T012 are complete on `feat/v3.15.9-cross-provider-routing`, including the maintainer-approved local phase commit. No push, pull request, release, installer, or version mutation is included.

---

## 1. Starting State

- **Branch**: `feat/v3.15.9-cross-provider-routing`
- **Starting commit**: `5abc395a` (`Merge develop into feat/v3.15.9-cross-provider-routing`)
- **Working tree**: clean before editing
- **Environment**: Windows 10, PowerShell 5.1, Python 3.12, pytest, Ruff, ShellCheck, Git Bash, Cursor
- **Prior session reference**: `docs/archive/v3/v3.15/development/history/2026-08-03_v3.15.9-cross-provider-routing-phase-1-routing-contract-and-plan-template-redesign.md`
- **Plan reference**: `docs/v3/v3.15/plans/v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md`

Phase 1 had locked the plan-document contract but deliberately left `model-routing` on its host-local "strongest / mid / cheapest" behavior. The branch and working tree matched the maintainer's required preconditions. The plan recommended `strong` / `high`; conservative implementation-time reassessment selected `frontier` / `high`, and the existing GPT-5.6 Sol session already met that tier.

---

## 2. Chronological Steps

### 2.1 T007-T009 - Routing Skill, Helpers, and Snapshot

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Rewrite `model-routing` for portable tier/effort scoring plus websearch map refresh, add cross-platform bundled helpers, and ship a dated fallback snapshot.

**What happened**: Split the skill into planning, implementation, and direct-switching modes. Defined deterministic scoring for all five rubric signals. Added `model-map.py` with `score`, `validate`, `render`, `fallback`, and `unavailable` commands; Bash and PowerShell wrappers; and a 2026-08-03 JSON snapshot grounded in official Anthropic, OpenAI, Google, and Cursor sources. The helper performs no network call: the harness researches public docs and the helper validates/renders the candidate.

**Key files changed**: `catalog/skills/ai-development/model-routing/SKILL.md`, `catalog/skills/ai-development/model-routing/scripts/model-map.py`, `catalog/skills/ai-development/model-routing/scripts/model-map.sh`, `catalog/skills/ai-development/model-routing/scripts/model-map.ps1`, `catalog/skills/ai-development/model-routing/references/last-known-model-map.json`, `data/SKILL_INDEX.md`, `data/skills.json`

**Troubleshooting**:

- **Problem**: ShellCheck rejected the new Bash wrapper with `SC1017: Literal carriage return`.
- **Root cause**: The Windows-created file used CRLF while the repository's Bash scripts require LF.
- **Resolution**: Normalized the wrapper to LF and reran ShellCheck successfully.

**Verification**: Direct Python, Bash, and PowerShell smoke commands validated the snapshot, scored a high-risk task as `frontier` / `high`, and rendered the exact offline fallback grammar.

---

### 2.2 T010 - Command and Retained Runbook Wiring

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Make `/plan`, `/implement`, and `/route` consume the new skill contract while keeping dispatchers thin and preserving no-degradation behavior.

**What happened**: `/plan` now requires candidate validation/rendering and the dated fallback commands. `/implement` refreshes or falls back, re-scores, validates the selected provider against the live surface, and preserves/upshifts tier. `/route` validates a plan map cell but continues to enumerate and switch only on the current host. The retained implement-phase runbook now contains the same pre-flight, closing the command-only behavior gap.

**Key files changed**: `catalog/commands/plan.md`, `catalog/commands/implement.md`, `catalog/commands/route.md`, `catalog/skills/workflow/implement-phase/references/implement-phase-runbook.md`

**Troubleshooting**: None.

**Verification**: Routing contract tests assert the new helper/snapshot references and continued host-native switching language.

---

### 2.3 T011 - Fixture and Cross-Platform Tests

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Cover rubric mapping, the four-by-four map schema, stale snapshot parsing, and empty-provider rejection without network flakiness.

**What happened**: Added synthetic valid and empty-provider fixtures, in-process and subprocess helper tests, and Bash/PowerShell wrapper parity tests. Focused coverage reaches 85% for `model-map.py`.

**Key files changed**: `tests/fixtures/model-routing/valid-map.json`, `tests/fixtures/model-routing/empty-provider-column.json`, `tests/plans/test_v3_15_9_model_routing_helpers.py`, `catalog/hooks/tests/test_model_routing_map.py`, `tests/plans/test_v3_15_9_routing_contract.py`

**Troubleshooting**:

- **Problem**: Ruff reported `PLW1510` for a subprocess call without explicit `check`.
- **Resolution**: Added `check=False`, formatted the files, and reran lint cleanly.
- **Problem**: The repository suite's Hermes layout test found `commit-sweep` without `SKILL.md`.
- **Root cause**: Eight pre-existing untracked empty skill scaffolds, already recorded in the v3.15.5 terminal audit, are visible to the flattening integration despite containing zero tracked files.
- **Resolution**: Temporarily isolated and then restored those empty directories; the Hermes assertion passed. No product or test code changed.

**Verification**: 25 focused plan/helper tests passed; 16 wrapper/switch tests passed; the complete hook and repository surfaces were exercised.

---

### 2.4 T012 - Stabilization and Lifecycle Records

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Run validation and test gates, update history, and create one local phase commit without pushing.

**What happened**: Ran every direct Windows equivalent of `make validate`, the security and compression gates, ShellCheck, PowerShell parsing, focused coverage, full hook/repository tests, and all five extension suites. Updated the plan, CHANGELOG, known-gaps checkpoint, docs cleanup report, DEVLOG, and this history. CI required no edit because its existing jobs collect both new test surfaces and already provide path filtering, concurrency cancellation, pip caching, and timeouts.

**Key files changed**: `CHANGELOG.md`, `docs/DEVLOG.md`, `docs/v3/v3.15/known-gaps.md`, `docs/v3/v3.15/docs-cleanup-report.md`, the Phase 2 plan, and this history

**Troubleshooting**:

- **Problem**: The skill-scanner pytest process remained alive after printing `89 passed in 2.65s`.
- **Resolution**: Captured the completed pytest result and terminated only the lingering process.
- **Problem**: The full catalog builder produced thousands of unrelated generated-data differences.
- **Resolution**: Reversed the broad regeneration, then used the existing builder to generate only the `model-routing` entry and aggregate size deltas in `data/skills.json` plus its single `data/SKILL_INDEX.md` row.

**Verification**: See the gate and testing tables below.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Branch and clean-start precondition | PASS - required branch; clean before edits |
| Focused routing/helper tests | PASS - 25 passed |
| New helper line coverage | PASS - 85% |
| Bash/PowerShell wrapper plus switch tests | PASS - 16 passed |
| Complete hook suite | PASS - 908 passed, 36 skipped |
| Repository suite | PASS after environmental isolation - 1,482 passed, 20 skipped; isolated Hermes assertion passed |
| Extension suites | PASS - 670 passed, 1 skipped |
| Ruff lint and format | PASS |
| ShellCheck and PowerShell AST parse | PASS |
| Skill bundle, quality, and routing gates | PASS |
| Security scan | PASS - no HIGH or CRITICAL findings |
| Compression accuracy gate | PASS - 100% CCR, 100% signatures, 45.8% reduction |
| Direct `make validate` equivalents | PASS |
| `git diff --check` | PASS before lifecycle docs; repeated before commit |
| CI/CD readiness and optimization | PASS - no workflow change required |

---

## 4. Known Issues

None introduced by Phase 2. The local empty skill scaffolds and the post-result skill-scanner process hang predate this phase and do not affect tracked repository content or CI.

---

## 5. Plan Discrepancies

None. The implementation used one Python core plus Bash/PowerShell wrappers instead of duplicating schema logic across shells; this satisfies the plan's cross-platform sibling requirement while keeping one deterministic implementation.

---

## 6. Assumptions Made

- **Agent-native refresh**: Web search belongs to the harness, while the bundled helper validates and renders researched facts. This avoids a new HTTP dependency or credential.
- **Snapshot scope**: The 2026-08-03 official-source map is a fallback, not a permanent authority; every online `/plan` still refreshes it in-session.
- **CI sufficiency**: Existing `ci.yml` collection is authoritative because it explicitly runs `tests/plans` and the complete `catalog/hooks/tests` tree on every non-doc change.
- **Environmental test isolation**: Temporarily moving and restoring empty untracked scaffolds is a test-environment correction, not a repository mutation.

---

## 7. Testing Summary

### Automated Tests

- Focused contract/helper coverage: 25 passed; `model-map.py` 85%.
- Wrapper and switch behavior: 16 passed.
- Complete hooks: 908 passed, 36 skipped.
- Repository: 1,482 passed, 20 skipped; the one environment-sensitive Hermes assertion then passed isolated.
- Nexus skill server: 43 passed.
- Nexus code search: 294 passed, 1 skipped.
- Nexus web fetch: 29 passed.
- Nexus skill scanner: 89 passed.
- Nexus context compressor: 215 passed.

### Manual Testing Performed

- Refreshed the provider roster from current official Anthropic, OpenAI, Google, and Cursor documentation.
- Validated the bundled snapshot through Python and rendered it through the Bash wrapper.
- Confirmed the PowerShell wrapper scores a high-risk signal as `frontier` / `high`.
- Confirmed unavailable fallback renders all 16 cells as `assess at implementation time`.

### Manual Testing Still Needed

None for Phase 2. Live provider picker availability is intentionally rechecked by `/implement` and `/route` at use time.

---

## 8. TODO Tracker

### Completed This Session

- [x] T007 Rewrite `model-routing` for generic scoring and cross-provider refresh.
- [x] T008 Add map scoring/validation/rendering helpers with Bash and PowerShell entry points.
- [x] T009 Add the dated bundled fallback snapshot.
- [x] T010 Wire `/plan`, `/implement`, `/route`, and the retained runbook.
- [x] T011 Add fixture-driven schema/rubric tests and cross-platform wrapper tests.
- [x] T012 Generate Phase 2 lifecycle records.

### Remaining

- [ ] Begin Phase 3 from the completed local phase boundary.

### Out of Scope

- [ ] Cursor usage data, auth/scrape, and visual contracts - Phase 3.
- [ ] Cursor usage extension provider/store and UX - Phases 4-5.
- [ ] Installer host isolation and Cursor packaging/CI E2E - Phase 6.

---

## 9. Summary and Next Steps

Phase 2 closes the gap between the portable plan contract and executable routing behavior. The skill now produces deterministic generic intent, validates fresh provider maps, degrades visibly offline, and keeps implementation/switching constrained to the active platform. Tests and validators are green after isolating documented local empty scaffolds; no new known gap or CI edit is required.

**Next session should**:

1. Start Phase 3 by defining Cursor personal-usage, auth/scrape, and visual contracts.
2. Keep installer host isolation deferred to Phase 6.
