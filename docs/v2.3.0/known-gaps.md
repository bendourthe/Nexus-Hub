# Known Gaps -- v2.3.0

This file tracks per-version unfinished work, deferred items, deviations from plan, and bugs discovered during phase implementation. The next phase plan and the version-bump checklist read this file to decide what carries forward.

**Plan**: [docs/v2.3.0/plans/adoption-ecc-cybersec-skills.md](plans/adoption-ecc-cybersec-skills.md)
**Status**: Phases 1-8 of 9 closed (skill-native foundations + CI validators + runtime learning + installer lifecycle & selective install + skill-quality tooling + framework coverage & defensive security content + installer instruction-file parity + code-graph quality & extractor expansion); Phase 9 pending
**Last updated**: 2026-05-29 (Phase 8 close -- code-graph quality & extractor expansion (WN-1 / WN-5 / WN-6 / WN-7 / DF-002 from v2.2.0 carryover, all closed). Scope was entirely within `extensions/nexus-code-search/` plus the Makefile `eval` target path and docs. (1) WN-1 / T026: `_load_ignore_spec` in `indexer.py` switched the pathspec factory from the deprecated `gitwildmatch` to `gitignore`; the 52 `GitWildMatchPattern` DeprecationWarnings are gone (full suite now passes under `-W error::DeprecationWarning`). (2) WN-5 / T027: the `tree-sitter>=0.24,<0.26` + per-grammar `<0.26` pins were re-verified on 2026-05-29 -- no 0.26-compatible release of core (latest 0.25.2), tree-sitter-python (0.25.0), or tree-sitter-typescript (0.23.2) has shipped, so the ceiling stays; a dated comment records the check. (3) WN-6 / T028: `python.py` and `typescript.py` now emit `instantiates` (a call/`new` whose resolved target is a class) and `overrides` (a method whose enclosing class extends, in-file and transitively, a parent defining a same-named method); `INSTANTIATES` was added to `traverser._IMPACT_KINDS` so re-typing a constructor call from `calls` does not drop it from `code_impact`. (4) WN-7 / T029: `traverser.search_fts` now scopes the default FTS match to the `name` column via a `name : (<query>)` wrapper (new `_scope_to_name` helper), with an `all_columns` opt-out threaded through `GraphQueryManager.search` and the `code_search` MCP tool as `all_fields`; aggregate eval precision rose 63.3% -> 96.2% with recall held at 100%. (5) DF-002 / T030: four new extractors (`go.py`, `rust.py`, `java.py`, `csharp.py`) registered in `LANGUAGE_EXTRACTORS` for `.go`/`.rs`/`.java`/`.cs`/`.csx`, each emitting the language's node kinds + `contains`/`calls`/`instantiates` (+ Java/C# `extends`/`implements`/`overrides`, Rust `implements`); four matching tree-sitter grammar deps (go 0.25, rust 0.24, java/c-sharp 0.23) added to `pyproject.toml` under the shared `<0.26` ceiling; four eval fixtures (`go_app`/`rust_app`/`java_app`/`csharp_app`) added, each clearing the 80% per-fixture recall gate (all 100%). The new deps auto-distribute via both installers' `pip install -e` of the copied package -- no installer copy-step edit needed (T030 added no standalone `scripts/*.py`). (6) `docs/v2.3.0/eval-baseline.md` created and the Makefile `eval` target repointed there. Tests: nexus-code-search 168 passed / 1 skipped (+32 new: 6 Go, 6 Rust, 6 Java, 6 C#, 3 Python-edge, 2 TS-edge, 2 name-scope, 1 recall-gate), zero DeprecationWarnings; eval 8/8 fixtures 100% recall; make-validate equivalent green (skills.json 227 OK, bundle audit 0/0, 4 CI validators rc=0). Two new low-priority gaps recorded below (DF-v23-4 remaining extractors + param-node parity; DF-v23-5 import-node search-precision residual).) Previously: 2026-05-29 (Phase 7 close -- installer instruction-file parity (DF-001 / MT-1 / MT-2 from v2.2.0 carryover, fully closed). (1) T022: the Python registry runner reached body parity with the legacy bash `render_template`. `MarkdownIntegration._render` now merges a built-in default placeholder map (mirroring the bash constant sed substitutions), auto-loads `{{SKILL_INDEX}}` from `data/SKILL_INDEX.md`, leaves unknown tokens literal (matching the bash sed list), and appends per-language coding snippets; `scripts/lib/integrations/runner.py` gained `--var KEY=VALUE` (repeatable), `--languages`, and `--instruction-only` on `install` (and `--var`/`--languages` on `print-config`); `InstallContext` gained `languages` and `instruction_only`. (2) Path/template reconciliation: claude and codex now render their workspace instruction file (CLAUDE.md / AGENTS.md) at the PROJECT ROOT via a new `instruction_workspace_dir=""` config key (skills/ still mirror under .claude/ / .codex/), matching where the tools actually read them and the legacy bash output; the gemini integration was repointed from the orphan stub `base-gemini-ide.md` to the canonical `base-gemini.md` (one of the five lock-step base templates), closing the template-divergence half of DF-001. (3) T024 / MT-1: `CopilotIntegration.install_workspace` was refactored from its bespoke append-after-heading flow onto the canonical `merge_marker_section` primitive with `legacy_header="## Nexus-Hub Harness"`, so re-installs settle to `unchanged` and teardown removes only the marker block (track_shared). (4) Removal: all six `render_template` (installer.sh) and `Render-Template` (installer.ps1) instruction-file calls were replaced by `invoke_registry_platform ... --instruction-only` / `Invoke-RegistryPlatform ... -InstructionOnly`, and both dead render functions were deleted; the registry runner is now the single instruction-file renderer shared by both installers (eliminating the prior bash-vs-PowerShell snippet-whitespace drift). `invoke_registry_platform` / `Invoke-RegistryPlatform` thread the detected globals (PROJECT_NAME, PRIMARY_LANGUAGE, BUILD_CMD, OS_CONTEXT, ...) to the runner, which also fixed a latent literal-placeholder bug for the already-registry-driven platforms (cursor/opencode/antigravity/nexus-ai). (5) T023 / MT-2: `tests/integrations/test_parity_with_legacy_installer.py` gained `test_instruction_body_parity_with_legacy_render` (claude/codex/gemini x global+workspace) asserting the registry's marker-body equals an INDEPENDENT naive-str.replace reference render (the parity oracle) plus a no-literal-placeholder completeness check; `test_instruction_file_is_produced`, `test_install_workspace.py`, `test_markdown_integration.py`, `test_contract.py`, and the Copilot `test_base_writeresult.py` case were updated for the new root paths and `unchanged` settle behavior. Tests: tests/integrations + tests/installer + tests/validators 304 passed / 0 failed; catalog/hooks/tests 392 passed + 3 skipped; make-validate equivalent (JSON + bundle audit 0 orphans + quality pass 0 new warnings + 4 CI validators rc=0) green; shellcheck `--severity=warning` on installer.sh/install.sh clean; bash -n + PowerShell parse OK. Zero new runtime dependencies; no new standalone scripts (so no installer copy-step registration needed); zero outbound calls. One new low-priority gap recorded below (DF-v23-3 orphaned base-gemini-ide.md template).) Previously: 2026-05-29 (Phase 6 close -- framework coverage + defensive security content shipped. (1) New `scripts/build_framework_coverage.py` reads the optional framework-mapping frontmatter (mitre_attack / atlas_techniques / d3fend_techniques / nist_csf / nist_ai_rmf) across `catalog/skills/` and emits a Markdown or JSON coverage matrix (now spanning 34 ATT&CK techniques + 6 D3FEND countermeasures + 10 NIST CSF categories from the new content), registered in both installers and covered by 6 pytest cases in `tests/validators/test_build_framework_coverage.py`. (2) New `security-operations` skill category (maintainer-approved 2026-05-29) holding 15 re-authored defensive skills -- batch 1 (DFIR / hunting / IR): memory-forensics, hunting-credential-dumping, disk-artifact-forensics, siem-detection-engineering, log-threat-hunting, lateral-movement-detection, ransomware-incident-response, persistence-mechanism-hunting, endpoint-edr-detection; batch 2 (cloud / endpoint / phishing): cloud-security-posture-detection, cloud-audit-log-detection, container-runtime-detection, phishing-analysis-and-defense, identity-threat-detection, malware-triage-analysis. Each carries a pushy description with trigger phrases and a SKIP clause, ATT&CK / D3FEND / NIST CSF framework-mapping frontmatter, a `references/standards.md` companion, a Common Rationalizations table, and a binary Verification checklist; all re-authored from public MITRE / NIST framework knowledge (no third-party SKILL.md text, no source attribution in the artifact per the Reverse-Engineering Attribution Rule), and all defensive-only per the mandate filter. (3) Two deterministic helper scripts with `.sh` + `.ps1` parity: `memory-forensics/scripts/volatility-runner.sh|.ps1` (read-only Volatility 3 triage wrapper, requires a local `vol`, fetches no symbol packs) and `log-threat-hunting/scripts/ioc-log-scan.sh|.ps1` (local read-only IOC sweep), both shellcheck-clean and functionally verified, each referenced from its parent SKILL.md so the orphan-bundle audit passes. (4) Registered all 15 skills in the three data files (`data/skills.json` 212 -> 227 entries, `statistics.total_skills` 208 -> 223, new `categories.security-operations` = 15; `data/marketplace.json` new "Security Operations" category, skill_count 15; `data/SKILL_INDEX.md` +15 rows, 211 -> 226 skills / 22 -> 23 categories) and documented the new category in `AGENTS.md`. Tests: tests/ 298 passed (was 291), tests/validators 44 passed (+6 new), 4 CI validators rc=0, bundle audit 0 orphans, quality-heuristics pass 0 new warnings (the new skills clear it cleanly; the pre-existing 574-warning grandfathered debt is unchanged). Zero new runtime dependencies; everything stdlib / shell; zero outbound calls anywhere. Two new gaps recorded below (QG-v23-1 CI lint coverage; DF-v23-2 optional skill-body enrichment).) Previously: 2026-05-28 (Phase 5 close -- skill-quality tooling shipped. (1) New `catalog/skills/workflow/skill-stocktake/SKILL.md`: a two-mode (Quick Scan / Full Stocktake) holistic skill-quality audit that caches per-skill results under `.nexus/skill-stocktake/results.json` and delegates the deterministic checklist to the validator's new `--quality` pass. (2) New `catalog/skills/workflow/skill-create/SKILL.md`: a local-analysis-only (git log/diff, zero outbound) skill that drafts a conformant SKILL.md from recurring git-history patterns and surfaces it for maintainer review (skill-only form per user decision; no scripts/skill_create.py). (3) `scripts/validate_skills.py` gained a non-blocking `--quality` heuristics pass (missing Common Rationalizations, prose-only Verification, over-long Tier-1 fields, missing Related Skills links) wired into `make validate`; 6 new pytest cases in `tests/validators/test_validate_skills_quality.py`. (4) Both new skills registered in the three data files (skills.json 210 -> 212; workflow `skill_count` 25 -> 27; SKILL_INDEX total 209 -> 211). (5) Three pre-existing personal-path false positives (prose in `docs/DEVLOG.md`, `docs/v2.3.0/known-gaps.md`, and the Phase 4 session history that literally wrote the real `/Users/<user>/...` form while describing redacting it) were redacted to the `<user>` placeholder so `make validate` is green on the v2.3.0 tree. Tests: tests/ 291 passed, catalog/hooks/tests 392 passed + 3 skipped, tests/validators 37 passed, nexus-skill-server 43 passed. Zero new runtime dependencies; zero outbound calls; no new standalone scripts (so no installer registration needed). Previously: 2026-05-28 Phase 4 close -- installer lifecycle & selective install shipped. (1) Install-state manifest: `scripts/lib/integrations/manifest.py` extended additively with `record_actions(integration_key, file_actions)` capturing `{path, action, sha256, mtime}` per write; the legacy `_tracked` / `_shared` / `_logs` fields are untouched so the 50-case integration contract suite still passes (191/191 in tests/integrations green). The runner auto-records actions after each integration install. (2) Lifecycle subcommands: new `scripts/lib/integrations/lifecycle.py` ships `doctor` (diagnose drift / missing managed files vs. recorded SHA-256), `repair` (re-run install for drifted/missing integrations through the regular install pipeline so `merge_marker_section` semantics still apply), and `list_installed` (enumerate the manifest). All three exposed as new subcommands on `scripts/lib/integrations/runner.py` (`doctor`, `repair`, `list-installed`) with `--json` / `--quiet` flags. (3) Selective install: `data/bundles.json` schema bumped to 1.4.0 with two new top-level keys, `profiles` (minimal / core / full) and `modules` (testing / code-review / security-ops / ai-engineering / infrastructure / documentation). The existing `bundles` array is untouched. (4) Consult advisor: new `scripts/nexus_hub_consult.py` is a local, read-only natural-language matcher over skills / bundles / profiles / modules with a token-overlap + id-exact + tag-boost baseline scorer (the actual ranking heuristic is marked as a user-contribution slot for future tuning). (5) Harness audit: new `scripts/harness_audit.py` is a deterministic scorer over the manifest, scoring presence / integrity / coverage / marker_integrity per integration and emitting an aggregate 0-100 score (Markdown by default; `--json` available; `--min-score N` exits non-zero below threshold). (6) Installer wiring: `scripts/installer.sh` and `scripts/installer.ps1` register the two new scripts in lockstep with the existing v2.3.0 CI-validator block. (7) Test coverage: 33 new pytest cases across `tests/integrations/test_lifecycle.py` (13), `tests/integrations/test_consult.py` (12), `tests/integrations/test_harness_audit.py` (8). Full integration suite: 191 passed (was 158, +33 new). Pre-existing `/Users/<user>/...` findings in `docs/DEVLOG.md` (1) and `docs/v2.3.0/development/history/2026-05-28_phase-3-runtime-learning.md` (3) redacted to `<user>` so `make validate` stays clean on the v2.3.0 tree. Zero new runtime dependencies; everything stdlib; no outbound network calls anywhere; the additive-manifest guarantee in the plan is honored.) Previously: 2026-05-28 Phase 3 close -- runtime-learning subset shipped. (1) Memory-persistence hooks: `catalog/hooks/session-summary.sh` now also writes a compact project-scoped digest at `.nexus/context/last-session.md` (atomic via mktemp + rename) capturing branch, working-tree status, last five oneline commits, and up to 30 files-touched names; `catalog/hooks/session-start.sh` reads it back on `SessionStart` capped at `NEXUS_SESSION_START_MAX_CHARS` (default 8000). PowerShell siblings `session-start.ps1` / `session-summary.ps1` ship in lockstep. `catalog/hooks/settings.json` adds `PreCompact` and `SessionEnd` registrations for `session-summary.sh` alongside the existing `Stop` registration. (2) Continuous-learning skill + capture hooks: new `catalog/hooks/learning-capture.sh` / `.ps1` reads a Claude Code payload from stdin and appends one JSONL record (`ts`, `event`, `tool`, `prompt_sample`) to `.nexus/observations.jsonl` (auto-truncated at `NEXUS_LEARNING_MAX_BYTES`, default 1 MiB), wired to `UserPromptSubmit` and `Stop`. The new `catalog/skills/workflow/continuous-learning/SKILL.md` is the analysis half: read the JSONL on demand, surface candidate patterns, mint each as `.nexus/instincts/<slug>.yaml`, regenerate `.nexus/instincts/_index.md`, and draft new SKILL.md / commands for maintainer review when a high-confidence cluster forms. Hard constraint, stated in the body and the Common Rationalizations table: no background observer, no upload, no cross-project sharing -- the only observer is the agent itself, in-session. Skill count: 209 -> 210; workflow `skill_count`: 24 -> 25. (3) Test coverage: 22 new pytest cases under `catalog/hooks/tests/` (`test_session_digest.py` 11 cases + `test_learning_capture.py` 11 cases) exercising syntax, round-trip, runtime controls, off-switches, size-caps, and a static-analysis no-network-token check. Pre-existing personal-path findings in two Phase 2 docs (the v23-1 DF item description) redacted to use the `<user>` placeholder so `make validate` is clean on the v2.3.0 tree. Full hook sweep: 392 passed, 3 skipped (was 370 + 3 skipped at Phase 2). Zero new runtime dependencies; all reads/writes are local; no outbound network calls anywhere.)

## Summary

| Category | Open | Resolved this version |
|---|---|---|
| NI -- Not implemented (skipped subtask) | 0 | 0 |
| DF -- Deferred (intentionally) | 5 | 2 |
| BG -- Bug or unresolved test failure | 1 | 0 |
| MT -- Missing tests / coverage gap | 0 | 2 |
| WN -- Warning or suppressed lint rule | 4 | 4 |
| QG -- Quality gate bypassed | 1 | 0 |
| **Total** | **11** | **8** |

_(Phase 8 closed five v2.2.0 carryover items (WN-1, WN-5, WN-6, WN-7, DF-002 -- see Resolved) and added two new low-priority gaps (DF-v23-4, DF-v23-5). Phase 7 previously closed DF-001, MT-1, MT-2. The remaining open items apply to Phase 9 (WN-2/WN-3/WN-4/WN-8 live-environment verification) plus the pre-existing catalog-quality debt (BG-v23-1, WN-v23-1..4, DF-v23-1..5, QG-v23-1) that is not a release blocker.)_

## Open Items

| ID | Title | Category | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|---|
| BG-v23-1 | `scripts/validate_skills.py` reports 7 pre-existing "Generic secret assignment" false positives in unrelated skills | BG | v2.3.0 Phase 1 (sub-task T003) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The strict validator flags 7 lines across `ai-development/google-antigravity-sdk/SKILL.md`, `documentation/user-documentation/SKILL.md` (2), `infrastructure/cd-pipeline-generator/SKILL.md` (2), and `infrastructure/rollback-strategy-advisor/SKILL.md` (2) as potential generic-secret assignments. Manual inspection confirms each is a documentation example, not a real secret (e.g., `password = "..."` inside a fenced code block teaching against the pattern). The Phase 1 work did not introduce any of these; they predate the phase. They block the strict-mode validator but not `make validate` (which runs `--bundles-only`). | Either (a) refine `SECRET_PATTERNS` in `scripts/validate_skills.py` to ignore matches inside fenced code blocks in `.md` files, or (b) add an in-skill suppression mechanism. Track as a quality-tooling pass; non-blocking for Phase 2. |
| WN-v23-1 | `data/skills.json` skill entry count drifted from `statistics.total_skills` before Phase 1 | WN | v2.3.0 Phase 1 (sub-task T003) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | At Phase 1 start, the array contained 207 skill entries while `statistics.total_skills` was `206`. The Phase 1 work added 2 entries (now 209) and bumped the statistic from `206` -> `208` per the plan's explicit instruction, leaving the same 1-skill drift behind. Drift was not introduced by Phase 1; the rebaseline in v2.2.0 known-gaps line 6 set `total_skills` to 207, and `data/skills.json` was bumped one entry beyond that without updating the statistic. | At the next `make build-catalog` / data rebaseline (likely Phase 5 or 6 of this plan), reconcile `statistics.total_skills` against `len(d['skills'])` and re-derive the per-category counts; the build script under `infrastructure/tools/build_skills_catalog.py` is the natural home. |
| WN-v23-2 | `templates/ai-instructions/**/*.md` ship with a UTF-8 BOM | WN | v2.3.0 Phase 2 (sub-task T006) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The new `validate_unicode_safety.py` flags 15 `.md` files under `templates/ai-instructions/coding-instructions/` and `templates/ai-instructions/legacy/coding-instructions/` plus `templates/ai-instructions/generic-instructions.md` as starting with a `U+FEFF` (UTF-8 BOM). BOM is exempted for `.ps1` (Windows PowerShell convention) but not for Markdown; these files predate Phase 2 and a BOM in distributed Markdown can confuse renderers. To unblock Phase 2 the Makefile excludes the whole `templates/ai-instructions` subtree from the Unicode pass. | Strip the BOMs (`python -c "p=open('file','rb').read(); open('file','wb').write(p.lstrip(b'\\xef\\xbb\\xbf'))"` per file), drop the `--exclude templates/ai-instructions` from the Makefile call, and re-run `make validate`. Out of scope for Phase 2; aim for Phase 5/6 (skill-quality tooling) or earlier as a stand-alone cleanup. |
| WN-v23-3 | `templates/development/compliance-review/**/*.md` ship with non-ASCII punctuation (em-dashes) | WN | v2.3.0 Phase 2 (sub-task T006) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The new `validate_unicode_safety.py` emits ~1034 WARN-level findings on em-dashes and curly quotes across English Markdown in `templates/development/compliance-review/` and elsewhere. Warnings are not blocking (exit 0) but they violate the CLAUDE.md "Critical Rules" ASCII-only constraint for English Markdown. | Run `validate_unicode_safety.py --strict --path templates/development/compliance-review/` to enumerate, then bulk-replace em-dashes with `--` and curly quotes with straight ASCII via a one-shot script. Out of scope for Phase 2 (per the user-CLAUDE.md "every changed line must trace to the user's request" rule). |
| DF-v23-1 | `catalog/hooks/tests/test_classification_audit.py` contains real personal paths in test fixtures | DF | v2.3.0 Phase 2 (sub-task T006) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The new `validate_no_personal_paths.py` correctly flags 8 occurrences of `/Users/<user>/...` (real username redacted in this gap entry) in this test file. These are legitimate user-reported real-world test cases (the file's docstring records them as "User-reported mislabeled commands"). Redacting them in place would require modifying upstream test fixtures, which is out of scope for Phase 2 and risks invalidating the audit. The Makefile excludes `catalog/hooks/tests/` from the no-personal-paths pass to unblock the gate. | Either (a) replace the real username with a placeholder (`<user>`) in the test fixtures and confirm the classification logic still parses them correctly, or (b) leave the fixtures as-is and keep the exclusion. Decide as part of Phase 5/6 skill-quality work. |
| WN-v23-4 | New skill-quality heuristics pass surfaces 574 warnings across grandfathered skills | WN | v2.3.0 Phase 5 (sub-task T016) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The non-blocking `python scripts/validate_skills.py --quality` pass added in T014 reports 574 warnings across the catalog (missing Common Rationalizations tables, prose-only Verification sections, missing Related Skills links, and a handful of over-long Tier-1 fields). These are pre-existing quality debts in skills that predate the size/section norms; the pass is intentionally warnings-only (exit 0) so it does not break CI or WIP branches. Both new Phase 5 skills (`skill-stocktake`, `skill-create`) clear the pass with zero warnings. This is the surfacing tool BG-v23-1's note anticipated, but it does NOT resolve BG-v23-1 (those are secret-scan false positives in the strict validator, a separate mechanism). | Run the new `skill-stocktake` skill (Full Stocktake) to rank the catalog worst-first, then remediate the lowest-scoring skills in batches (add Common Rationalizations tables, convert prose Verification to binary checklists, wire Related Skills links). Grandfathered per the AGENTS.md forward-looking size/section norm; prioritize during a dedicated catalog-quality pass, not as a release blocker. |
| QG-v23-1 | CI shellcheck job does not lint per-skill helper scripts | QG | v2.3.0 Phase 6 (sub-task T020 / step 8.3) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The two new defensive-skill helper scripts (`catalog/skills/security-operations/memory-forensics/scripts/volatility-runner.sh` and `catalog/skills/security-operations/log-threat-hunting/scripts/ioc-log-scan.sh`) are shellcheck-clean locally (`--severity=warning`, 0 findings), but the CI `shellcheck` job in `.github/workflows/ci.yml` only lints `scripts/installer.sh`, `install.sh`, and `catalog/hooks/*.sh` (`find catalog/hooks -name '*.sh'`). New per-skill helper `.sh` files are therefore not gated in CI, so a future regression in them would pass CI unnoticed. Not auto-applied per the step-8.3 "never silently rewrite CI/CD configs" rule. | Broaden the "Lint hook scripts" step in `.github/workflows/ci.yml` from `find catalog/hooks -name '*.sh'` to `find catalog -name '*.sh'` (or add a dedicated step linting `catalog/skills/**/scripts/*.sh`); re-run CI to confirm. Low risk -- the only current targets are already clean. |
| DF-v23-2 | security-operations skill bodies are concise; optional enrichment deferred | DF | v2.3.0 Phase 6 (sub-tasks T018 / T019) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | The 15 re-authored defensive skills (89-156 physical lines each) are complete and pass the strict validator (0 errors), the orphan-bundle audit (0 orphans), and the quality-heuristics pass (0 warnings each), but sit below the 160-280-line authoring band suggested in the drafting brief, partly because the project's no-hard-wrap rule keeps each paragraph and bullet on one physical line. They are fully conformant -- the AGENTS.md SKILL.md size norm is an upper bound (<=500 target, 800 soft cap), not a lower bound -- so this is an enrichment opportunity, not a defect. | In a future catalog-quality pass, optionally deepen the highest-traffic skills (e.g., `siem-detection-engineering`, `endpoint-edr-detection`, `cloud-audit-log-detection`) with platform-specific query examples (Splunk SPL / KQL / Sigma snippets) under each skill's `references/` subdir. Not a release blocker. |
| DF-v23-3 | `templates/ai-instructions/base-gemini-ide.md` is orphaned as an instruction template | DF | v2.3.0 Phase 7 (sub-task T022) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | Phase 7 repointed the `gemini` integration from `base-gemini-ide.md` (a static `@base-google-shared.md` import + 6-line IDE stub) to the canonical `base-gemini.md` to reach body parity with the legacy bash installer. `base-gemini-ide.md` is therefore no longer rendered by any integration; it is now referenced only by a code comment in `gemini.py` and the `instruction_merge.py` docstring (which cites it as the historical example of a template that literally contains the marker strings). `base-google-shared.md` remains in active use by `base-antigravity-10.md`, `base-antigravity-20.md`, and `base-gemini-cli.md`, so only the `-ide` stub is orphaned. Harmless (it ships but is never selected). | In a future templates cleanup, either delete `base-gemini-ide.md` (and update the two doc/comment references that name it) or repurpose it. Out of scope for Phase 7 (the change must trace to DF-001 parity, not template housekeeping). Not a release blocker. |
| DF-v23-4 | nexus-code-search extractor coverage is partial: 4 of ~18 languages shipped, no framework extractors in the new batch, parameter nodes omitted for the new languages | DF | v2.3.0 Phase 8 (sub-task T030) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | T030 (DF-002 successor) shipped Go + Rust + Java + C# extractors per the maintainer-confirmed priority batch. Three coverage edges remain deferred: (a) ~14 languages still lack extractors (PHP, Ruby, C, C++, Swift, Kotlin, Scala, Dart, Lua, Luau, Svelte, Vue, Liquid, Pascal, plain JavaScript); (b) the 13 deferred framework extractors (beyond the existing Django / FastAPI / Express resolvers) were not part of this batch; (c) to avoid re-introducing the FTS parameter-noise that T029 just fixed, the four new extractors deliberately do NOT emit `parameter` nodes, unlike `python.py` / `typescript.py` which do. None of these is a defect -- the architecture (new `Extractor` subclass + `LANGUAGE_EXTRACTORS` registry entry + eval fixture) is proven and each shipped extractor clears the 80% recall gate. | Implement the next language batch (recommend Go-adjacent demand: Ruby + PHP + C/C++) following `extraction/languages/go.py`, each with an eval fixture clearing the 80% recall gate; add framework resolvers as a separate sub-effort; and, if parameter-level search becomes a requirement, emit `parameter` nodes for the new languages and confirm name-scoped search (T029) keeps them out of default results. Not a release blocker. |
| DF-v23-5 | `code_search` precision residual: import-statement nodes surface under a name-scoped query | DF | v2.3.0 Phase 8 (sub-task T029 / T031) | [adoption-ecc-cybersec-skills](plans/adoption-ecc-cybersec-skills.md) | After T029 scoped the default FTS match to the `name` column, aggregate eval precision rose to 96.2%; the only sub-100% fixture is `python_app` (70%). The residual noise is import-statement nodes whose `name` is the dotted import path (e.g. `service.make_user`, `models.AdminUser`): the FTS tokenizer splits the dotted name so a search for `make_user` legitimately matches the import site as well as the definition. This is honest import-site surfacing, not a false positive in the extractor, and it does not affect recall (still 100%). | Either (a) widen the affected `python_app` answer keys to accept the import-site hits the FTS index legitimately surfaces, or (b) rank/exclude `import`/`export`-kind nodes in default `code_search` results (they are references, not definitions) while keeping them reachable via `all_fields=true`. Decide as part of a future search-quality pass; the 96.2% aggregate already clears the >=80% recall gate comfortably. Not a release blocker. |

## Resolved

| ID | Title | Category | Resolved in | Detail |
|---|---|---|---|---|
| DF-001 | Installer instruction-file byte parity (registry runner vs legacy bash `render_template`) | DF | v2.3.0 Phase 7 (sub-tasks T022 + removal) | Carried forward from v2.2.0 known-gaps DF-001 part 2. The Python `MarkdownIntegration._render` previously substituted only `{{PROJECT_NAME}}`, leaving 14+ placeholders literal; it now threads the full bash placeholder set (defaults + installer-detected `--var` values + auto-loaded `{{SKILL_INDEX}}` + per-language coding-snippet append) and leaves unknown tokens literal exactly as the bash `sed` list did. claude/codex render to the project root (`instruction_workspace_dir=""`) and gemini uses the canonical `base-gemini.md`, eliminating the path and template divergences. The six legacy `render_template` / `Render-Template` calls were replaced by `invoke_registry_platform ... --instruction-only`, the dead render functions were deleted, and the registry runner is now the single shared instruction-file renderer across both installers. Gated by the T023 body-parity test (below). |
| MT-1 | Copilot `install_workspace` does not use the canonical `merge_marker_section` primitive | MT | v2.3.0 Phase 7 (sub-task T024) | Carried forward from v2.2.0 known-gaps MT-1. `CopilotIntegration.install_workspace` was refactored from its bespoke append-after-heading flow onto `merge_marker_section(..., legacy_header="## Nexus-Hub Harness")`, matching Cursor. The v2.1 `## Nexus-Hub Harness` legacy header now migrates inline into the marker block, re-installs settle to `unchanged`, and teardown removes only the marker block (`track_shared`). The 50-case contract suite (idempotency + sibling preservation + partial-state recovery) passes for copilot. |
| MT-2 | `test_parity_with_legacy_installer.py` did not assert instruction-file byte parity | MT | v2.3.0 Phase 7 (sub-task T023) | Carried forward from v2.2.0 known-gaps MT-2. Added `test_instruction_body_parity_with_legacy_render` (claude/codex/gemini x global+workspace): it installs each integration in isolation with the full placeholder set, extracts the marker-delimited body, and asserts byte equality against an INDEPENDENT naive-`str.replace` reference render (the bash-semantics oracle), plus a no-literal-placeholder completeness check. This is the precondition that authorized the legacy-block removal under DF-001 above. |
| WN-1 | 52 `GitWildMatchPattern` ('gitwildmatch') deprecation warnings from pathspec | WN | v2.3.0 Phase 8 (sub-task T026) | Carried forward from v2.2.0 known-gaps WN-1. `_load_ignore_spec` in `extensions/nexus-code-search/src/nexus_code_search/indexer.py` switched the pathspec pattern factory from the deprecated `gitwildmatch` to `gitignore` (verified to match identically on the indexer fixtures). The 52 warnings are gone -- the full nexus-code-search suite (168 passed / 1 skipped) now runs clean under `pytest -W error::DeprecationWarning`. |
| WN-5 | tree-sitter dependency pin needs periodic re-verification for a 0.26-compatible release | WN | v2.3.0 Phase 8 (sub-task T027) | Carried forward from v2.2.0 known-gaps WN-5. Re-verified 2026-05-29: no 0.26-compatible release of `tree-sitter` (latest 0.25.2), `tree-sitter-python` (0.25.0), or `tree-sitter-typescript` (0.23.2) has shipped, so the `>=0.24,<0.26` / `>=0.23,<0.26` ceiling stays. A dated comment in `pyproject.toml` records the check; the four new Phase-8 grammar deps were added under the same shared `<0.26` ceiling. |
| WN-6 | `_collect_calls` did not emit `instantiates` / `overrides` edges despite the taxonomy supporting them | WN | v2.3.0 Phase 8 (sub-task T028) | Carried forward from v2.2.0 known-gaps WN-6. `python.py` and `typescript.py` now emit `instantiates` (a call / `new` expression whose resolved target is a class) and `overrides` (a method whose enclosing class extends, in-file and transitively, a parent class defining a same-named method, via a new `_resolve_overrides` pass). `INSTANTIATES` was added to `traverser._IMPACT_KINDS` so re-typing a constructor call from `calls` does not silently drop it from `code_impact`. Covered by new unit tests in `test_python_extra.py` / `test_typescript_extra.py` and the Java/C# `code_impact` eval questions. |
| WN-7 | `code_search` aggregate precision was 63.3% due to FTS matching qualified_name ancestor segments | WN | v2.3.0 Phase 8 (sub-task T029) | Carried forward from v2.2.0 known-gaps WN-7. `traverser.search_fts` now scopes the default FTS match to the `name` column (`name : (<query>)` via a new `_scope_to_name` helper), with an `all_columns` opt-out threaded through `GraphQueryManager.search` and the `code_search` MCP tool as `all_fields`. Aggregate eval precision rose 63.3% -> 96.2% with recall held at 100%. Two new `test_traverser.py` cases assert name-scoping excludes method/parameter ancestor matches and that `all_columns=True` restores the wide match. |
| DF-002 | only Python + TypeScript extractors shipped; 18 languages + 13 framework extractors deferred | DF | v2.3.0 Phase 8 (sub-task T030) | Carried forward from v2.2.0 known-gaps DF-002. Shipped the maintainer-confirmed priority batch: Go (`go.py`), Rust (`rust.py`), Java (`java.py`), and C# (`csharp.py`) extractors registered for `.go`/`.rs`/`.java`/`.cs`/`.csx`, each emitting the language's node kinds + `contains`/`calls`/`instantiates` (+ Java/C# `extends`/`implements`/`overrides`, Rust `implements`). Four eval fixtures (`go_app`/`rust_app`/`java_app`/`csharp_app`) each clear the 80% per-fixture recall gate (all 100%); 24 new extractor unit tests added. The remaining languages, framework extractors, and parameter-node parity are re-deferred as DF-v23-4 (above). |

---

**File lifecycle**: This file is appended by `/implement-phase` Phase 8 step 8.4 (per-phase append), swept by `/wrap-up-session` Phase 6 (catch-all from live conversation), and finalized by `/update-version` at the v2.3.0 -> next-version bump. After finalization, the next plan run by `/generate-plan` will read this file to decide which items carry forward.
