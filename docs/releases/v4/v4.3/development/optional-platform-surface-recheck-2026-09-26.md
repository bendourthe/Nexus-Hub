# v4.3 WN-3 Optional Platform Surface Recheck, 2026-09-26

This source recheck narrows the remaining v4.3 platform-contract warning for the integration maintainer. It compares current vendor documentation with Nexus-Hub's existing installer paths; it does not claim a live Antigravity, Cursor, Gemini Code Assist, or Nexus-AI read-path test. The 2026-09-24 [bounded recheck](../../../../archives/v4/v4.3/development/optional-platform-surface-recheck.md) remains the earlier evidence, not a result overwritten by this note.

| Surface | Current source and installed path | Bounded verdict |
|---|---|---|
| Antigravity CLI custom agents | [Google's CLI `/agents` reference](https://antigravity.google/docs/cli/commands/agents/) names `~/.gemini/config/agents/<name>/agent.md` and `.agents/agents/<name>/agent.md`. `Antigravity20Integration` already writes the shared global config and workspace agent trees. | The destination is source-qualified; no CLI runtime smoke was run. The separate `~/.gemini/antigravity-cli` compatibility tree is not established by this source. |
| Antigravity legacy workflows | [Google's migration guide](https://antigravity.google/docs/migration/workflows-to-skills/) names `~/.gemini/config/workflows/<name>.md` and `.agents/workflows/<name>.md`, both emitted by the adapter. It says workflow indexing and slash invocation retire on November 1, 2026; skills at `~/.gemini/config/skills/<name>/SKILL.md` and `.agents/skills/<name>/SKILL.md` remain the migration path. | The current legacy paths are source-qualified but time-limited. Do not add a new workflow-only default or promise post-retirement discovery. A live CLI smoke and a post-retirement contract decision remain open. |
| Cursor legacy commands | The former [Cursor commands documentation URL](https://docs.cursor.com/en/agent/chat/commands) redirects to the general docs landing page. A [Cursor staff reply](https://forum.cursor.com/t/commands-are-not-detected-in-the-global-cursor-directory/150967/4) says global `~/.cursor/commands` should work but acknowledges a discovery bug and recommends project `.cursor/commands` as a workaround. Nexus-Hub writes both and also publishes command-skills. | The project path has vendor-staff support, but neither a live-host read-back nor a reliable global discovery guarantee was established. Keep the compatibility writes marked unverified. |
| Gemini Code Assist IDE extras | [Google's IDE guide](https://docs.cloud.google.com/gemini/docs/codeassist/chat-gemini) instructs VS Code users to create custom commands and rules through Gemini Code Assist settings and IntelliJ users through its Prompt Library and Rules settings. It does not identify the Gemini CLI's skill, workflow, agent, rule, or hook files as Code Assist IDE discovery paths. | `GEMINI.md` remains the separately qualified instruction path. Do not attribute CLI file mirrors to the Code Assist IDE without an IDE-specific read-path test or vendor contract. |
| Nexus-AI project-local mirror | No new source or live-app result was obtained for project-local `.nexus-ai/catalog/` or its other mirrors in this pass. The earlier global-skill reader finding remains valid only at its pinned source commit. | Still unverified; do not infer project-local discovery from the global reader. |

WN-3 remains open for the live-host paths and the Nexus-AI project-local mirror. The adapter was not changed, and this note does not upgrade the repository's full platform-read contract. A future integration change must first choose supported destinations against current vendor docs, then run the matching live host and installer tests.
