# Session History -- v4.4.2 Phase 4: Models, Agentic Platform, and the Layered Harness Animation

**Date**: 2026-09-02
**Plan**: [v4.4.2-guide-production-ready-rebuild.md](../../plans/v4.4.2-guide-production-ready-rebuild.md)
**Phase**: 4 of 8
**Branch**: `feat/v4.4.2-guide-production-ready-rebuild`
**Driver mode**: `full` (on `claude-fable-5-1`, the plan's recommended frontier tier)
**Outcome**: Phase 4 complete. The Models and Agentic Platform flows are joined by connectors computed from live geometry so no arrow can sit on a card; the fourth output is labelled `Video` and the audio output draws a live waveform while it plays; and the two harness scenes now share one layered diagram, model inside platform harness inside Nexus Hub harness, choreographed in seven steps with a static end state. Gate: 288 passed, 1 skipped. One local commit; nothing pushed.

## Sub-task 4.1 -- Connectors and media

`window.NexusFlow` replaces the seven inline arrow `<div>`s. Each `[data-flow-root]` gets one overlay SVG sized to itself, and consecutive `[data-flow-node]` children are joined bottom-centre to top-centre from `getBoundingClientRect`, recomputed on resize, on `ResizeObserver`, and after fonts load. Because the Models flow nests its later stages inside the request region, that region is itself a flow root with its own overlay, so the diagram has three roots and seven connectors (1 + 2 + 4). A test asserts, at six widths, that no connector's box overlaps any node's box except the two edges it joins. The Models and Agentic entry motifs stay byte-identical (the Agentic one is wrapped in a node `<div>` rather than flagged) so the v4.4.1 matched-emission test still holds.

`Moving image` is now `Video`; playback is still on request only. The audio output gains a `canvas.fx-wave`: the static waveform is decoded straight from the embedded 8-bit PCM bytes, so it paints with no gesture and no `AudioContext`; on `play`, an `AnalyserNode` fed by `createMediaElementSource` drives a live time-domain trace until `pause` or `ended`, guarded by a generation counter; if Web Audio is unavailable or refuses the `data:` source the static trace stays and the native element still plays. Reduced motion never enters the live state. Nothing fetches: the request capture stayed empty.

## Sub-task 4.2 -- The layered harness

One SVG, emitted twice from the same source. Scene 7 (Harnesses) shows it static and fully lit with the platform ring in focus and the Nexus Hub ring dashed; scene 8 (Nexus Hub Harness) shows it in motion with the outer ring solid and a seven-step `NexusSeq` timeline: the prompt and context chips enter; `skills` lights with `matched procedures load`; the chips pass into the platform ring and `context`, `tools`, `permissions` light; the model's abstract work cycle pulses; `execution`, `observations`, and `hooks` light with `every action checked`; `gates` and `artifacts` light with `definition of done met`; and `reviewed, verified work` leaves while a dashed ghost chip labelled `raw answer`, captioned `platform alone`, shows what a platform without the outer ring returns. The `data-phase3-harness-layer` markers moved onto the SVG groups and the five repository-anchored claims stayed as a list beneath, so the v4.4.1 structural assertions held without edits. Capability language is unchanged (`can`, `when supported`, `when permitted`) and the cycle keeps its `not hidden chain-of-thought` note.

## Defects found and closed this phase

- **`BG-14`**: the ghost group was dimmed with `opacity: .55` and the contrast sweep caught its text at 2.4:1 in light and 3.3:1 in dark. Same rule as `BG-13`: text never loses contrast; the ghost is now a dashed chip with `--ink-dim` text at full opacity.
- Two captions overran the 540-unit viewBox at the right and bottom edges; repositioned, and a six-width test now asserts every SVG `text` stays inside its box.
- A helper rule whose selector contained `.fx-region-tag` shadowed the original in a first-match regex test; the helper was dropped rather than the test loosened.
- The waveform test first muted the element, which feeds the analyser silence, then assumed consecutive live frames differ; headless Chromium advances a media element slowly with no output device, so the test now proves what is provable: frames keep drawing from the analyser, the live trace differs from the static one, and drawing stops on pause.
- The uppercase `PLATFORM LOOP + NEXUS-HUB` label had survived the case-sensitive Phase 1 rename; it now reads `NEXUS HUB`, the two assertions follow, and the rename test is case-insensitive.

## Verification

```text
NEXUS_REQUIRE_RENDER=1 pytest -q tests/guides/ tests/verification/test_visual_defect_detector.py
288 passed, 1 skipped in 229.16s

tests/guides/test_v442_phase4_media.py    17 passed
  connectors never cross a card at six widths; entry and cycle bytes matched; labels Text/Image/Video/Audio;
  static waveform without a gesture, live frames while playing, frozen on pause, no fetch; reduced motion static;
  layered diagram carries model/platform/nexus-hub groups and nine ports; seven-step choreography reaches the
  fully lit end state; reduced-motion end state; SVG text inside its viewBox at six widths

Byte ledger
page bytes: 336,063 | Phase 4 delta from 319,422: +16,641 of 38,000 | headroom 163,937
```

## CI impact

One new test file under `tests/guides/` (covered fail-closed by `guide-render`). Web Audio and canvas run in headless Chromium without flags. No new command, dependency, environment variable, or workflow edit.

- [x] Connectors from live geometry; no card crossing at six widths
- [x] Video label; live waveform with static fallback and reduced-motion state
- [x] One layered harness diagram, two instances, seven-step choreography
- [x] One local commit; no push, pull request, or remote CI
