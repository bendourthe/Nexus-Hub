# Session History -- v4.4.3 Phase 9: Verification and Closeout

**Date**: 2026-09-03
**Plan**: [v4.4.3-guide-illustration-clarity-rebuild.md](../../plans/v4.4.3-guide-illustration-clarity-rebuild.md)
**Phase**: 9 of 9
**Branch**: `feat/v4.4.3-guide-illustration-clarity-rebuild`
**Phase 9 verification base SHA**: `1fe0322952e83b09509b5ac5b8b20b45d46de571`
**Driver mode**: `full` (on `claude-opus-5`, the plan's frontier tier at high effort)
**Outcome**: Phase 9 complete. The declared matrix runs 164 cases clean, the guides suite and the visual detector pass at 347, the validation profiles pass, the byte ledger is closed at 370,271 bytes, and the maintainer README describes every contract this version shipped. GO is recorded for the operator's review and NO-GO for publication until that review happens, which is what the operator asked for. One local commit; nothing pushed.

## The matrix had to grow, not just re-run

Three of the rebuilt figures were built with class selectors only, and the declared matrix addresses
a sequence root by id, so it could not drive them. Rather than leave those scenes uncovered, the
figures gained ids and the matrix gained two groups: the chatbot comparison at 420 and 1440 in both
themes, mid-sequence and at its end state, and the platforms flow at three of its five stages. The
harness group now drives six stops instead of seven, because the merged figure has six.

```text
python tests/guides/tools/browser_matrix.py --label phase-9
cases: 164/164   screenshots: 138/138
runtime: 183s / 1200s    evidence: 13.7 MiB / 30 MiB
failures: 0
```

A matrix that still declared 150 cases would have reported success while three rebuilt scenes went
unphotographed. The declared count is the honest number here.

The tool also had the previous version's slug hardcoded in two places, so this run wrote its
evidence into `guide-production-ready-rebuild/renders/`. Both the output path and the plan label
now derive from one constant, and the run was moved under this plan's own slug. The 138 PNGs are
regenerable and are NOT committed: the v4.4.2 set is already 28 MB, and a second full set per
visual iteration grows the repository faster than the evidence gets read. The per-run summary
JSON is committed and is the authoritative record; the reasoning is in the renders README, and
committing the images later is a deliberate choice rather than a default.

## The gate

```text
NEXUS_REQUIRE_RENDER=1 pytest -q tests/guides/ tests/verification/test_visual_defect_detector.py
                                                        347 passed, 1 skipped (5m51s)
python scripts/ci/run.py --profile fast --quiet          PASS: 13 passed, 0 failed
python scripts/ci/run.py --profile full --only docs,catalog,security --quiet
                                                        PASS: 16 passed, 0 failed
pytest -q tests/validators/                              1139 passed, 2 skipped
pytest -q tests/workflows/                               97 passed, 13 skipped
pytest -q catalog/hooks/tests/test_installer_smoke.py    33 passed
python scripts/stamp_guide_counts.py --check             OK -- 5 markers match the catalog
validate_unicode_safety.py --strict, validate_no_personal_paths.py, check_docs_conventions.py   clean
```

`make` is not on this host's PATH, so the validation groups were run through the repository-native
CI profile runner instead, which is what the `validate` job actually executes. That is a deliberate
substitution and it is recorded rather than papered over.

## Byte ledger, closed

345,623 to **370,271**, which is 129,729 under the 500,000 ceiling and 35,352 under this plan's own
target. The interesting number is that the file is SMALLER than at Phase 7 close: two SVG figures
retired in Phase 8 returned more bytes than the four inlined platform marks cost in Phase 6.

## What the README now records

Ten rules, each written as a rule rather than as a description of the current state: the two heading
tokens; the measured no-wrap rule with its floor and its stated deviation; never measure available
space against a track the measured element can widen; text belongs in boxes that size to it; the
product never addresses the reader, with the honest scope of that claim; a model name is read, never
recalled; a vendor mark is reused from the ledger, never re-sourced; motion that plays itself with
the access half kept; and every class in the markup has a rule. Foundations is documented as seven
scenes, each rebuild described.

## The honest state of this version

Every Definition-of-Done clause is measured except `D2`, which is met at 720px and wider and relaxed
below it with the arithmetic in the phase contract. Two items are the operator's judgement rather
than defects, and both are flagged: the label now renders larger than the title, which is exactly
what the review's two numbers produce, and the platform chips carry vendor marks under product names
because the linked aggregator serves a client-rendered page with no stated licence.

`HT-4` records the gap that matters most: no reader has been asked whether the rebuilt pictures
land. Geometry and contrast are measured; comprehension is not. The operator's review is the next
step, and it is why nothing here was pushed.

- [x] T021 declared matrix, visual detector, and the full guides suite
- [x] T022 README synced, byte ledger closed, evidence and known gaps recorded
- [x] One local commit; no push, pull request, or remote CI
