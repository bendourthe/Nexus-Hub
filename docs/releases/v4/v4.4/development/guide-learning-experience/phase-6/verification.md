# Phase 6 verification

Status: locally complete. Frontier/max retained. Guide SHA-256 `d5673b540efc9313ce1e83bd5424d443c2107317595cfe675183c13433db9635`; 352,924 bytes.

## Observable results

Training now teaches Understand / Change / Verify with concise instructions for all eight commands. The offline simulation has pending, running, blocked, complete, and failed states. Files and success claims follow completed work; skipping a prerequisite is held, and restarting a step invalidates downstream evidence. The deterministic first-hit button demonstrates the bug and fix without gaming skill. Live reduced motion finishes the finite output; navigation and hidden-state events cancel it. Progress and Outline restore keyboard focus.

The integrated Training/lesson selection passed 80 tests in 78.71s. The fullscreen suite passed 21 checks. Fresh captures exercised 32 before/after scene cases and 10 native/fallback fullscreen cases, with zero script errors. The 18-case Training geometry audit has zero overflow, external requests, and hidden essentials. The independent adversarial review reproduced and then verified fixes for skipped-test false success and keyboard focus loss. Its final report and raw correction receipt are retained here. Actual desktop occlusion event delivery remains a final environment check; the event handler was exercised with an instrumented hidden state, not misrepresented as native occlusion.

## Visual review

Inspected normal desktop and mobile introductions, all eight desktop completed scenes, long mobile plan and implementation results, and 1280x720 fullscreen. Both themes were sampled. Fixed the excessive gap before the file preview, fullscreen check truncation, and light-theme deleted-code contrast. The complete capture set is retained for final review; capture existence alone is not a claim that every image was inspected. The final cross-page matrix and older assertion migrations continue in Phase 7.

## Closeout

CI impact: existing guide-render job; no pipeline edit, dependency, or gitignore change. Source and inline JSON remain equal and safely escape HTML-significant characters. README, tracker, known gaps, and assertion migration record updated. This report is the phase session history. One local commit; no push or remote CI.
