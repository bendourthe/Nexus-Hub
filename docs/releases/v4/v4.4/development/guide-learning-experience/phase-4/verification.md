# Phase 4 verification

Status: four Foundations lessons locally complete. Frontier/max recommendation retained. Guide SHA-256 `76b66ec72cf3e81fce500707ca6a3e4a486193cfefd41d7fac1588eb671461f5`, 358,278 bytes.

## Outcome and evidence

Model, tokens, prompt, and context now precede the two remaining legacy scenes. Training and use are visibly separate; illustrative continuation probabilities are labeled; context does not imply retraining; media are capabilities, not a maturity ladder. The main token example uses the shared meeting-notes task and was generated with the installed cl100k_base tokenizer. Its eight IDs and exact pieces are retained in tests/guides/fixtures/meeting-note-tokens.json. The earlier verified ten-token example remains in a short disclosure. Token selection uses keyboard-accessible buttons and preserves the complete source text.

The focused lifecycle, lesson interactions/geometry, static guide, and retained media-provenance suite passed 122 tests with one optional skip in 25.25s. The 18-case audit recorded zero overflow, errors, external requests, or hidden essentials. All four changed lesson compositions were inspected at 1440x900 and 420x900 in both themes, including the mobile lower diagram portions. A second pass moved the model explanation above a wider two-part figure and shortened prompt labels to avoid needless wrapping. Captures and exact-file verdicts are adjacent. Final integrated word/height budgets remain Phase 5 gates.

## Plan delta

Tokens use direct selection instead of a cosmetic replay: each selected chip highlights its exact source span without moving or hiding the sentence. A new verified shared-example split supplements the preserved old fixture. Model replay emphasizes the already-readable result rather than fabricating a live inference. Historical model GIF, modality ladder, giant annotations, and context inventory assertions were retired individually in the migration register; durable bounds, no-JS, reduced-motion, offline, media ledger, and non-coding teaching checks remain.

## Closeout

CI impact: existing guide-render job; no pipeline or dependency changes. README and tracker updated; no new known gap or gitignore change. This file is the phase session history. Phase 3's final whitespace-only cleanup changed its committed source hash to 5a1d2a57fa53643c12a28fa5dd32195955f6d12ad1aecd99ef85c61f2df278f5; its screenshots retain their actual pre-cleanup hash. One scoped local commit; no publication.
