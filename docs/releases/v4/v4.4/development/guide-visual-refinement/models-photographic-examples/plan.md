# Models photographic examples

## Scope

Refine only Models: focus the opening row on pre-training and reinforcement learning, simplify the language flow, replace basic drawings with photographic assets, preserve room details across views, use voice plus a UI screenshot for multimodal, and distinguish the four illustrated architectures. Keep the guide offline and under the existing 500,000-byte release ceiling.

## Implementation and visual checks

- [x] Add text/image/code icons and a try-score-improve reinforcement learning illustration.
- [x] Show one language request and put the growing response above its candidate probabilities.
- [x] Accelerate diffusion and replace the sailboat drawing with a realistic harbor image.
- [x] Replace inconsistent geometry with a single photographic room source and continuous camera framing.
- [x] Demonstrate a voice request plus booking UI screenshot through shared weights, with text and redesigned UI output.
- [x] Inspect all examples in dark/light desktop/mobile layouts, verify motion controls and offline behavior, record evidence and commit locally.

## Assets

Two new images were generated with the built-in imagegen tool, then mechanically resized and encoded as WebP in the browser for inline delivery. Original generated PNG files remain in the tool output directory. The workspace WebP assets are embedded in the guide; there are no runtime image requests.
