# Models flow refinement

## Scope and visual direction

Rework only the Foundations Models section. Reuse the guide typography, teal accents and dark/light tokens. Use an asymmetric request/network/output flow, labeled teaching illustrations and slow purposeful motion. Keep all other sections unchanged. construction-debt: inline HTML, CSS, SVG and a small local canvas renderer; no dependency or runtime network additions.

## Work and verification

- [x] Replace ambiguous training icons with labeled examples and floating triangle connectors.
- [x] Unify all four demonstrations around request, neural network and generated output; loop token prediction with a pause control.
- [x] Render a furnished room with translation, camera turns and a frame strip; show native multimodal input and multiple outputs.
- [x] Identify both providers, show increasing conceptual model scale, animate internal reasoning and remove both picnic fragments.
- [x] Inspect dark/light desktop and mobile screenshots, intermediate animation states and all model types; repair wrapping, clipping and alignment.
- [x] Verify keyboard controls, motion preferences, offscreen suspension, offline behavior, size, syntax and unchanged neighboring sections; record evidence and commit locally.

## Accuracy

Reasoning is internal processing, not literal self-chat or a fixed number of loops. Model graphics convey a conceptual capability scale, not parameter counts or benchmark equivalence. Candidate probabilities are illustrative. Sources are linked in the guide.
