# v4.11.0 interactive handbooks: Phase 6 continuation

**Outcome: Phase 6 remains incomplete.** Work is preserved on feat/v4.11.0-interactive-handbooks in the isolated worktree. Phases 1-5 have their local commits; no Phase 6 commit or remote publication occurred.

The continuation added complete reading/slide measurement, frozen neutral source inputs and deterministic extraction/native-generator/checkout tests. It fixed sticky toolbar occlusion, long-path wrapping, compact slide shrinkage and the detector's false positive for native vertical scrolling. The live handbooks gained retained explanatory compositions and fresh build/rendered/source receipts. Both final matrices cover 200 states with zero reported failures; the read-only pre-version gate passes.

The earlier preflight found that the installed old CLI was incompatible with the requested model/effort. A temporary current CLI can call the model, but its native Windows sandbox cannot create a file in the trial workspace and its available skill list omits the installed presentify entry. At that earlier checkpoint, no frozen authoring case had started. Qualification is unverified, not waived or counted as first-build success.

See [Phase 6 evidence](../interactive-handbooks/phase-6-evidence.md) for actual commands, bounded observations, the remaining feature/export/native-authoring gates, and recovery steps. Repair native sandbox and skill discovery, refresh all three installed trial bundles, then complete Phase 6 before beginning Phase 7 integration.

## Native recovery continuation

The IDE-visible plan rename and reference repair are preserved separately in the primary checkout. The feature branch already owns the canonical v4.11.0 filename. Temporary current-CLI profiles now exercise real Windows workspace writes, explicit installed-skill reads and Chromium, with read/write boundary denial probes. All three original authoring invocations timed out at their declared 2,100-second limits and remain non-passes. Two earlier simultaneous starts failed before model turns. Fresh serialized retries use the same frozen prompts and source inputs with corrected shared components and predeclared 3,600-second limits, retaining the three-repair ceiling. A Windows short-path mismatch in the prepared repository retry profile was corrected before model turns. Runtime recovery is not an authoring pass.

The SVG detector now transforms graphic bounds into the outer SVG coordinate system. This exposed and repaired a genuinely clipped y-axis label. An independent report first-build inspection reproduced a chart axis-title/tick collision at a valid 28-pixel SVG label size; its regression failed before the shared gutter correction and passed afterward. Independent final-report grading also reproduced a fullscreen focus race and rapid-close/reopen race. The shared runtime now restores focus after browser fullscreen exit and rejects stale exit events. Three real Chromium regressions failed before correction. The refreshed broad suite passes 497 tests; 21 runtime tests pass with 98.21% precise line coverage, and the combined runtime/measurement group passes 33 tests. All three maintained rendering matrices were regenerated: the rich fixture passes 240 states and both living handbooks pass 200 states. Full freshness passes.

Native PowerPoint rejects the original presentation first and final exports. A diagnostic reproduction identified custom curved geometry in a connector as the cause; editable freeform geometry opens normally. Repair-mode opening discarded most of a slide and is excluded from qualification. The shared export owner now specifies valid native containers and ordinary open without repair. Original files remain unchanged. Native Office automation is unavailable in the sandbox logon session, so independent host-side native grading is recorded separately. The original report's PDF and native Word rendering were inspected successfully, but those partial results do not qualify the timed-out invocation.

Word QA set the persistent UpdateLinksAtOpen preference to false without recording its previous value. The current value was verified and the user was asked which value to retain. That preference question remains separate from artifact qualification; no prior setting is inferred.

## Print and native boundary correction

The completed repository retry stopped after three repairs with print contrast unresolved. The shared renderer now preserves the reading palette, keeps semantic figure controls visible, exposes collapsed data tables and expands scroll regions in print. Two JavaScript/no-JS regressions failed before correction and pass afterward; an actual six-page PDF was inspected. All 497 affected tests and 30 catalog checks pass. All three artifacts also pass the named 1024x640 viewport; native Chromium tab zoom at 125% is verified for all sixteen slides, separately from visual scaling. An exploratory higher-zoom sweep was invalidated when file navigation reset the level and remains a private diagnostic.

A fresh report preflight twice reproduced readable cross-case files despite configured denials. The 0.153.4 native sandbox subcommand supplied an empty deny-read list; upstream 0.154.0 contains the forwarding correction. A temporary 0.154.0 installation now denies all five tested forbidden reads and passes the complete workspace/outside-write preflight. Earlier permission failures alone were insufficient proof and the prior receipt is preserved as superseded. The global CLI and default user configuration remain unchanged. Report authoring has started with the final installed owners; presentation and repository trials remain serialized.

## Plan delta

The presentation retry completed within its hour and opened normally in PowerPoint, but all native effect sequences were empty. Timed playback confirmed fully drawn stationary figures after the slide crossfade. The shared PPTX owner now exposes a portable automatic-fade helper with actual native process/chart-series playback proof, sixteen passing affected tests and 96.59% helper coverage. The failed author's files are unchanged; a fresh presentation invocation is queued after the report. The first video capture was rejected because all seek results reported time zero; linear playback with strictly increasing frame timestamps supplies the valid evidence. The primary checkout's tracker also gained separate v4.12 planning work, which must be preserved during integration; six other rename-related files still match the earlier hashes.

The report retry completed within its declared hour and passed independent HTML geometry, navigation, detached-file and retained-rebuild checks. Native Word inspection found a table painted beyond the physical page edge because header cell widths differed from the grid. The shared DOCX sizing example and native-layout verification instructions were corrected; four new regression tests and twelve affected checks pass. An isolated diagnostic native render proves the width correction, without modifying or accepting the author output. The report remains a non-pass and a fresh report invocation is queued after the presentation and repository cases. A private review-queue prefix bug that skipped report-native inspection was also corrected; ordinary native Word opening now has actual evidence.

Incomplete environment assumption: project-level Codex skill exclusions were not applied by the current native host, Windows read-denial ACL state persists across profile changes, and Office COM is unavailable to the sandbox logon session. Qualification therefore uses explicit opt-in user profiles with resolved paths, a tested absolute Python runtime, serialized startup and independent native grading of unchanged collected exports. Native trials exposed shared renderer/runtime defects that required fresh installed attempts. The three prompts and source facts remain frozen before model turns. No requirement is weakened; Phase 7 stays closed until the three native results and remaining Phase 6 evidence pass.

The repository final attempt timed out after 3,600.75 seconds without qa/result.json. Independent inspection of unchanged HTML reproduced 72-pixel admission-panel overflow and 20-pixel source-panel overflow at 2560x720, with content reaching the navigation bar. Its nine navigation/Escape/focus checks pass, but those do not qualify the unverified alternate states. The QA owner now requires alternate-panel inventory before first QA. A fresh repository invocation is queued with a predeclared 5,400-second limit, the same frozen input/prompt and three-correction ceiling. The active report profile and installed bundle are unchanged; failed artifacts remain preserved. [Repository attempt evidence](../interactive-handbooks/phase-6-native-attempts/repo-final/summary.json).

The fresh report invocation is accepted after three internal repairs in 3,247.39 seconds. Independent review confirms complete source facts, intact input copies, 200 default rendered states, 404 control/paint-order checks, native two-page Word rendering with Arial and no outside-page text, two PDF engines plus QPDF, and byte-identical HTML/PDF/DOCX rebuilding from retained sources. Ten critical installed files remain unchanged. The native host recovered a transient remote-compaction WebSocket reset inside the same invocation without configuration changes; full wrapper duration and terminal token usage are recorded. The first build remains a failure, and no operator repaired the final artifacts. See [accepted report](../interactive-handbooks/phase-6-native-attempts/report-final/summary.json) and the [R01-R30 ledger](../interactive-handbooks/phase-6-requirement-evidence.md). The presentation is active and the repository case queued; accepted cases are 1/3, so Phase 6 is still incomplete.

The presentation-final invocation finished in 3167.30 seconds with one internal correction. Its unchanged PPTX opens normally and actual playback verifies eight automatic process effects and one simultaneous chart-series fade. Native slide 4 nevertheless exposes an unreadable black automatic chart title on the dark background, so the invocation is a non-pass. A separate diagnostic proves that explicitly setting chart.has_title = False disables the generated title without losing values; it earns no qualification credit. The retained export instruction now requires explicit chart-title visibility and native color review. A fresh presentation-readable run is queued after repo-extended finishes and is collected, with unchanged frozen inputs/prompt, a 3600-second limit and three internal repairs. Accepted input families remain 1/3. [Native title finding and diagnostic](../interactive-handbooks/phase-6-native-attempts/presentation-final/summary.json).

The repo-extended invocation finished in 3770.25 seconds with three internal corrections. Independent inspection found all five brands on the left and one whole-figure process fade without staggered semantic steps. The unchanged output is a non-pass against R26/R23. The shared HTML contract now states the headerless conditional upper-right rule and hands motion to the existing navigation owner; that owner explicitly requires individual semantic targets and inspected intermediate frames. A fresh repo-semantic invocation is queued after presentation-readable collection with unchanged frozen inputs/prompt, a 5400-second limit and three internal repairs. No failed output or operator correction is supplied. Accepted input families remain 1/3. [Repository branding and motion evidence](../interactive-handbooks/phase-6-native-attempts/repo-extended/summary.json).

The full local profile completed in 5281.81 seconds with 42 passing commands and two failures. The repository suite reported 5221 passed, 77 skipped and one failed cross-link assertion. The presentify dispatcher now explicitly hands responsive layout to its canonical rule. The interpreter failure was PATH selecting an unusable WSL Bash; the affected process now resolves the existing Git Bash first, without changing user or machine PATH. The interpreter gate and 56 focused cross-link/intake/interpreter/both-installer tests pass after correction. The original full run remains recorded as failed, with separate successful resolution receipts; it is not restamped. [Full-profile results and affected repairs](../interactive-handbooks/phase-6-full-profile-resolution.json).

The read-only freshness gate subsequently rejected outdated content-review hashes. Both source/build/output snapshots were unchanged; the previously completed screenshot-review paragraph had changed the dedicated review file. Forty individual captures, eight contact sheets and both output hashes matched before rebinding that existing review. The freshness gate passes after this evidence-only correction, without new rendering claims. [Binding proof](../interactive-handbooks/phase-6-freshness-binding-resolution.json).
