# Pre-v4.12 repository integration

The maintainer requested that outstanding work be tracked and merged into develop, followed by removal of confirmed merged branches, before starting the v4.12.0 attribution plan. This record covers that preparation, not implementation of the attribution rewrite.

## Reconciled work

- Back-merge PR #214 was merged after its current-head required checks passed. It contained no file changes relative to develop.
- PR #197 was closed as superseded: its target-manifest test file was byte-identical to develop, and its patch was reported already applied by `git cherry`.
- PR #203's planning documents and weekly-bar rendering tests were merged into this integration candidate after refreshing its branch from develop. Local duplicate planning files were retained in an external backup; the published copies repair a renamed plan link and redact a personal path.
- The existing slide-build commit `b878a681` and seven additional lines of annotation and pacing guidance were preserved. The older plan remains an implementation record, not a claim that every proposed rendered check is implemented.
- The preserved cache branch `c8f18b58` was merged. Rename conflicts were resolved using the current v4.11.1 plan name and its existing 9/25 completed tasks. Six historical evidence files now link to the current plan and retain their original test claims as historical evidence. Diagram and final-integration phases remain open.

## Verification

- `python -m pytest tests/skills/test_cache_share_example.py tests/skills/test_presentify_slide_mode_scorer.py tests/skills/test_presentify_visual_qa.py -q --disable-warnings --maxfail=1`: 151 passed on the combined cache/scorer tree.
- The scorer's real CLI, `visual_qa_score.py <fixture.html> --json`, accepted the declared presentation with exit 0 and rejected the undeclared deck with exit 1 and a high-severity `presentation-declared` finding. This is structural CLI evidence, not rendered visual certification.
- `npm test -- --reporter=dot` in `extensions/claude-usage-monitor` on the refreshed PR #203 branch: 20 passed across six test files.
- `python scripts/ci/run.py --profile fast --quiet`: the first candidate failed on three personal paths in the inherited slide-build plan and an empty directory left by parking duplicate files. After portable-path repairs and removal of the confirmed empty directories, all 14 checks passed. The failed receipt remains separate from the passing rerun.
- Before changes, the integration-base validator and installer-smoke baseline passed 1,576 tests with two skips using `--import-mode=importlib`; the default import mode failed collection because an installed tests package shadowed this checkout. This baseline is not substituted for outgoing-change verification.

## Preservation and cleanup boundary

A dated external backup contains all pre-cleanup refs in a verified Git bundle, the original outstanding working files, and the parked local planning copies. Existing stash entries and backup tags are retained. A branch is eligible for ordinary deletion only after its tip is an ancestor of the refreshed integration branch and its worktree has no unique uncommitted files. Unmerged historical branches are retained rather than represented as merged.

## Open

The final integration PR, its required checks, post-merge verification, return to develop, and branch cleanup remain pending at this commit. The v4.12.0 preliminary inventory was paused before any implementation or history rewrite; it must be refreshed after integration.
