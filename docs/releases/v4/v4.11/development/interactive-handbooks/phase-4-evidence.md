# Phase 4 - Presentation intake and compatible output

The command and direct HTML skill now resolve one complete reading page with presentation included by default, explicit opt-out, conditional theme/depth questions, and retained choice provenance. This record covers T012-T014 and distinguishes runtime/assembly checks from the native authoring qualification still required in Phase 6.

## Verification

- Broad presentify and extractor regression: 395 passed, two slow installer cases deselected. After the final SVG style rejection, 111 affected builder, intake, foundation and installer tests passed with no skips. The final frontmatter/body clarification passed 86 intake and first-shot hardening tests.
- Real Chromium exercises cover all nine theme/depth combinations, explicit No, stable mixed assignments, reading completeness, layout independence, scoped zoom, visible branded wordmarks and painted diagram connectors. Both Git Bash and PowerShell installs resolve retained settings, build/check the fixture and exercise its presentation in isolated temporary workspaces.
- Final measured line coverage: assembler 89.4%, resolver 96.4%; see [coverage](phase-4-coverage.json). The earlier broad run measured the scorer at 95%. Coverage is not a claim of native agent selection or authoring quality.
- Final build followed by read-only check passed, output SHA-256 `de14411d2ce7e5b26fbad19989a33dc34c3cbd89cecfe372b9e53f872316607f`; [build record](phase-4-build-record.json) retains dependency hashes. Output is in a temporary workspace, not a published artifact.
- Ruff, JavaScript syntax, registry structure/reachability and bundle audit pass. Bundle audit: 336 skills, zero errors, 64 existing warnings. Registry edits are limited to three changed fields and one index row.

## Resolved failures

The expanded tests found stale output-budget and cinematic-confirmation wording, a missing executable-bit check for newly untracked bundled scripts, and inherited CSS defeating figure zoom. Each was corrected. A source SVG gradient with zero-height object bounds failed to paint connectors; explicit user-space geometry and separate arrow paths now pass a pixel probe with a deliberately invisible negative control. A light-reading brand variant now has visible ink while retaining the supplied contours. An SVG style-element probe attempted one request, intercepted and blocked by the browser route; the assembler now rejects style elements and the regression passes. No request reached the probe destination. A final test command named a nonexistent file and collected nothing; its corrected invocation passed 86 tests.

## CI impact and limits

Existing tests/skills discovery and the explicit slow installer selection own coverage. No pipeline, service, secret or environment configuration was added. Optional export remains delegated to existing format owners, with native editable marks, declared unavoidable raster exceptions, retained rebuild provenance and playback verification. No native PowerPoint export or native one-invocation authoring qualification is claimed by this phase. Phase 5 owns candidate-aware handbook freshness and Phase 6 owns the frozen authoring corpus. No push, PR or remote CI ran; version metadata remains unchanged. No gate bypass or deferred implementation gap was accepted.
