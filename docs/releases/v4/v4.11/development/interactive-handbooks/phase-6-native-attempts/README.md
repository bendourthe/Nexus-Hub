# Native authoring attempt evidence

This directory preserves qualification attempts for Phase 6 of the interactive-handbooks plan. Read it when checking first-build defects, repair limits and final acceptance; an artifact's presence does not establish a passing invocation.

The three `*-original` directories contain exact first-build HTML bytes, per-file hashes and available first-build findings. The presentation directory also retains its original first-build PPTX. All three original invocations reached the 2100-second limit and are non-passes. Their first-build rate remains 0/3 regardless of later repairs or retries. The `repo-repaired` invocation finished within its 3600-second limit but returned a non-pass after three internal repairs with unresolved print contrast findings.

Raw authoring JSONL, collected archives, full screenshot sets and Office playback artifacts are retained in the private qualification workspace. Promoted JSON records identify the relevant hashes and observations. Exact HTML/PPTX files in non-pass directories are historical evidence, not recommended deliverables. Operator-created diagnostic repairs are kept separate and cannot qualify an authoring result.

The [report-final case](report-final/summary.json) is independently accepted after three internal repairs in 3247.39 seconds. Its final HTML/PDF/DOCX rebuild byte-identically; native Word and two PDF engines confirm complete readable output. The presentation and repository retries still require independent final source-content, rendered-behavior, design, rebuild and applicable native-export checks. Phase 6 cannot close until all three required source cases pass within their declared invocation and repair budgets.

The `report-repaired` directory records a completed 3162.33-second invocation with three internal corrections. Independent HTML and delivered-PDF checks pass their recorded scopes, but native Word renders the appendix table beyond the page edge. Its final accepted verdict is non-pass, overriding the author's qualified-pass label. The after-image is explicitly a diagnostic copy that proves the shared grid/cell width correction; it is not an accepted deliverable or an extra author repair round.

The `presentation-repaired` directory records a completed 3272.41-second invocation with three internal corrections. Ordinary native PowerPoint opening succeeds, with correct editable chart values and visible directed edges. Actual timed playback exposes missing automatic process and chart-series builds: all five native effect sequences are empty. The invocation remains a non-pass. A shared native fade helper now has separate diagnostic playback proof; a fresh presentation invocation must demonstrate that authors use it or another valid native implementation. The initial browser seek-based video capture returned repeated time-zero frames and is excluded; only advancing timestamps from linear playback support the recorded finding.

The `repo-final` directory records a 3600.75-second timeout without a final result record. Independent browser inspection reproduces two alternate-panel overflows at 2560x720, including content covering navigation. The case remains a non-pass despite successful default navigation checks. The next repository invocation retains the frozen corpus and three-correction limit, with a 5400-second wall-time limit declared before it starts. It receives the clarified QA instruction to inventory alternate panels before the first pass; failed output is not carried into that fresh workspace.

The [presentation-final case](presentation-final/summary.json) records a completed 3167.30-second invocation with one internal correction. Native playback now proves the required process and chart-series motion, but PowerPoint generates a black chart title on its dark background. That readability defect keeps the case non-pass. The separate after-image proves the explicit title-visibility correction without repairing the qualifying output. A fresh presentation-readable invocation is queued with unchanged inputs and prompt after repository collection.
