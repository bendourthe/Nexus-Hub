# v4.11.1 -- last-phase evidence

Final-phase record for `v4.11.1-adoption-cache-and-diagram-quality`. Phases 1-3 were implemented on 2026-09-10; phases 4-6 were implemented on 2026-09-21 and are recorded here. One duty per section, each quoting what was actually run.

## Architecture refactor

Detectors run over `docs`, `catalog`, `scripts`, `tests`.

Empty directories: two, both PRE-EXISTING and neither created by this plan.

```
docs/decisions/proposed/tooling
docs/releases/v4/v4.15/development
```

They are reported rather than deleted, because `project-refactor` is propose-then-apply and neither is this plan's to remove.

Orphan bundled files in the skill this plan touched: none. Every `references/*.md` in `document-to-interactive-html` is referenced from its `SKILL.md`, including the two extended in Phase 4.

## Known-gaps reconciliation

`docs/releases/v4/v4.11/known-gaps.md` carries 18 entries after this pass. Open at close: `SEC-1` (CodeQL alerts, pre-existing), `MT-5`, `MT-9`, and the new `MT-10`. `WN-3` and `QG-2` are closed with stated dispositions rather than silently dropped.

**`MT-10` is new and was found by the Tier 3 pass below**, not predicted: the two geometry checks this plan adds cover none of this repository's own diagrams, because they use `<path>`. The gap records the measurement, and records that widening the envelope by approximating curves is the wrong fix.

## Living docs architecture

No living-docs change was required. Phase 4 extended two existing `references/` files in place and added one handoff clause to `SKILL.md` step 4; it created no new subtree and no decision record. `docs/handbooks/` and `docs/decisions/` are unchanged by this plan.

## Git-tree hygiene

```
python scripts/check_release_preconditions.py --branches --repo-settings
```

Reporting only; nothing was deleted. `delete_branch_on_merge` is enabled.

One finding worth carrying out of this plan, because nothing automated can fix it:

```
NOTE: the repository description disagrees with README.md:
  - skills: description says 336, README.md declares 337
```

The GitHub repository DESCRIPTION is not a version-carrying surface, so `check_version_sync.py` cannot see it. It needs a manual edit, and per `AGENTS.md` only the skills count is authoritative.

## Terminal CI/CD reconciliation

```
Required-check coverage: OK -- 10 declared context(s) across 2 branch(es), every one produced unconditionally.
```

Workflow files changed by this plan: **0**. Nothing here needed a pipeline change: the new checks run inside `visual_qa_score.py`, which the existing `tests` group already exercises through `tests/skills/test_presentify_visual_qa.py`. The canonical contract is therefore unchanged and no reconciliation edit was required.

## Tier 3 deep pass

Every artifact this plan produced, exercised through its real boundary rather than its unit tests.

**The two checks, inside the real `score_html` pipeline, against a real repository handbook** (`docs/handbooks/distribution.html`, 5 SVG blocks):

```
svg-label-occlusion:   unchecked
svg-connector-routing: unchecked
unchecked_checks = 2
page_pass = False
```

Two things this establishes, and one it disproves:

- **The `unchecked` path works end to end on a real artifact**, reporting "carries geometry this check cannot decide" per SVG rather than guessing.
- **`page_pass = False` is NOT caused by this plan.** The page's only high-severity finding is the pre-existing `image-sizing`; neither new check contributes a severity, which is the backward-compatibility property the design depends on.
- **It disproves the assumption that these checks would cover the repository's own diagrams.** They cover none of them. That is `MT-10`.

**The Phase 4 reference additions**, verified present and reachable:

```
svg-diagram-quality 'Structure follows the relationship': present=True
figure-reconstruction part 5a:                            present=True
SKILL.md step 4 handoff:                                  present=True
```

## Independent Goal-vs-codebase review

The plan's Goal covers cache accounting (phases 1-3, implemented 2026-09-10) and diagram semantics and fidelity (phases 4-5).

Re-reading the Goal against the tree as if the phases had not been implemented:

- **Semantic selection before decoration** -- landed. `svg-diagram-quality.md` gains a section placed BEFORE rule 1, carrying the three named cases. It states what each structure must make visible and declines to mandate connector shape, node count, type size or palette, which the prompt forbade.
- **Fidelity and simplification made inspectable** -- landed. `figure-reconstruction.md` part 5a defines the four-column ledger and the derived-summary handoff. The lossless invariant in part 5 path 1 and in `SKILL.md` step 4 is unchanged; the summary is barred from the gate rather than the gate being widened.
- **Geometry verification** -- landed with a measured limitation. The checks exist, are wired into `score_html`, and are proven by 13 tests and two mutations. Their coverage on real repository artifacts is zero, recorded as `MT-10`.

No Goal clause is unmet without a recorded gap.

## Human and manual testing suggestions

Automated coverage cannot reach these:

1. **Author a diagram that follows the shape vocabulary** (rects, text, lines/polylines, translate/scale) with a label deliberately covered by a later panel, and confirm the scorer reports `svg-label-occlusion` on a real authoring run rather than only on a fixture.
2. **Judge whether the three semantic-selection examples change what gets drawn.** The text is testable for presence; whether it actually redirects an author away from the default row of boxes is a human judgement.
3. **Work one faithful reconstruction and one derived summary against the same dense source**, per part 5a, and confirm the ledger is usable at the size a real figure produces. The section asserts the ledger is small; only a real figure shows whether it is.

## Full-suite testing and stabilization

```
tests/skills/test_presentify_visual_qa.py                     93 passed
tests/skills/test_presentify_visual_qa.py + test_deck_integrity.py   100 passed
test_presentify_first_shot_hardening / _presentation_intake / _slide_mode_scorer   121 passed
scripts/ci/run.py --profile full --only docs                   8 passed
scripts/ci/run.py --profile fast                              15 passed
```

Mutation checks, run because a green suite proves nothing about a check that cannot fail:

- `_geom_inside` forced to `False`: 3 occlusion detection tests fail, the near-miss controls stay green.
- the routing `crosses` branch forced to `False`: the crossing test fails, the attaching control stays green.

A defect the fixtures caught before the tests existed: `_geom_walk` pushed children onto a LIFO stack in document order, so it visited siblings in REVERSE. Paint order is the whole basis of the occlusion check, so every verdict was inverted -- "occluded" passed and "clean paint order" failed. Fixed, with the reason recorded at the call site.

## Publication and integration

Completed 2026-09-22 as part of the shared historical-closure [PR #232](https://github.com/bendourthe/Nexus-Hub/pull/232). The feature branch targeted protected `develop`; every final required check passed, including Linux tests (14m42s), Windows tests (20m40s), guide rendering (7m42s), `claude-plugin-load`, the Ubuntu/macOS/Windows bootstrap and installer matrices, CodeQL, and the aggregate `ci-required` gate. GitHub merged the PR as `c54dbeb4e68296ac95ec3adb79777b4d674503c5`.

The first hosted `claude-plugin-load`, Presentify `verify`, and handbook `validate` failures were reproduced and fixed in commits `a547f01a`, `871ea4ee`, and `197ca22d`; none was blindly rerun. Their detailed immutable sequence is recorded in the shared v4.10.1 `## Publication and integration` evidence because that plan owned the CI changes.

Post-merge run [35789442554](https://github.com/bendourthe/Nexus-Hub/actions/runs/35789442554) passed with only provenance and merged-tree fast-profile smoke jobs. It did not rerun the complete suite. T025 is complete. The existing `MT-5`, `MT-9`, and `MT-10` limitations remain explicit and do not invalidate the implemented goals. No v4.11.1 tag is planned because this work is historical closure inside the later released v4.11 family.
