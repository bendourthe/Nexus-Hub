# Plan -- Harness Economics and Portable Engineering Systems

**Version**: v4.17.3

**Status**: queued

**Created**: 2026-09-21

**Target branch**: feature branch from `develop`; integrate through one final pull request to `develop`

**Seeded from**: [`../comparisons/v4.17.3-comparison-harness-economics-and-portable-engineering-system.md`](../comparisons/v4.17.3-comparison-harness-economics-and-portable-engineering-system.md)

**Strategy grounding**: No `STRATEGY.md` or v4.17 strategy artifact exists. This plan therefore derives its Goal and acceptance criteria from the approved comparison and records that missing strategy anchor as a planning limitation, not as inferred product strategy.

---

## Overview

Nexus-Hub already supplies planning, testing, review, memory, security, reusable skills, structural harness auditing, and cross-platform instruction installers. The approved comparison found three narrower gaps: unattended routines do not explicitly report no-data and partial-completion outcomes; maintainers cannot measure a `model x harness x workload` pairing with one local, versioned receipt; and supported Claude Code project sessions still receive a generated `CLAUDE.md` even though Claude Code v2.1.277 can fall back to the repository's `AGENTS.md`.

This plan closes those gaps without importing ECC, adding a hosted benchmark service, copying a public ranking, or creating a second routine framework. Structural conformance remains owned by `scripts/harness_audit.py`; the new evaluator owns observed local behavior. Project instruction content gains one platform-neutral shared owner, while platform integrations become consumers. Supported Claude Code sessions consume that shared `AGENTS.md`; unsupported or disabled-fallback sessions retain `CLAUDE.md`.

## Review corrections

The mandatory coherence, scope, feasibility, design, product, security, and adversarial review ran against the first draft. The revised plan applies every P1 and P2 finding:

- Added terminal-outcome precedence so malformed input cannot masquerade as no data.

- Added execution modes, environment identity, paired ordering, a three-repeat minimum, named 95% interval methods, predeclared acceptance gates, Pareto reporting, and a restrained recommendation rule.

- Reduced the evaluator to a fresh serial repository-local MVP; resumability, parallel scheduling, quarantine, installer distribution, and a dashboard are deferred.

- Separated workspace hygiene from enforceable live isolation and delegated the live containment contract to `[[agent-execution-isolation]]`.

- Separated time, turn, and spend enforcement; hard cost claims now require provider- or adapter-enforced rejection before spend is incurred, while cancellation-only telemetry is bounded-overshoot observational evidence.

- Moved workload evaluators to a hashed read-only trusted root and required allowlisted, bounded, redacted, escaped output before any persistence.

- Added credential-free Claude Code and Codex profiles so the first real harness comparison does not require a custom bridge.

- Replaced the nonexistent single `AGENTS.md` owner with a proposed platform-neutral shared resource, per-platform semantic inventories, reachable platform-specific owners, persisted consumers, a capability-decision record, four Claude migration states, last-consumer teardown protection, and deterministic release gates.

- Added missing stabilization and specialized lifecycle task lines and reconciled one initial publication with scoped corrective re-pushes after failed required checks.

**Interview decision - approved 2026-09-21**: Use the platform-neutral `base-agents.md` owner proposed here. Every integration currently capable of writing root `AGENTS.md` becomes a consumer of the shared resource, with platform-specific guidance preserved through a verified native sidecar or platform-owned global surface. Claude-only path detection was rejected because it would leave competing writers and teardown ambiguity in place.

## Goal

Give Nexus-Hub maintainers an honest, local way to decide whether a model-harness pairing earns its cost for a representative workload, while reducing duplicate project instructions for supported Claude Code sessions and making unattended-loop outcomes explicit when inputs are absent, stale, or only partly processed.

## Personas and user stories

- **US1 - Routine operator**: As a maintainer running an unattended routine, I can distinguish no data, stale input, partial completion, and full completion without reading raw logs.

- **US2 - Harness evaluator**: As a maintainer selecting a coding-agent harness, I can run repeated bounded trials over the same workload, with enforceable isolation for live runs, and compare success, cost, latency, context payload, and failure fingerprints from versioned local receipts.

- **US3 - Multi-tool repository maintainer**: As a maintainer using Claude Code with other coding tools, I can keep one project instruction file where Claude supports `AGENTS.md`, while preserving `CLAUDE.md` for unsupported providers, disabled fallback, and global Claude instructions.

## Non-goals

- Do not import ECC, its catalog, its runtime, or its installer.

- Do not adopt a shared cloud computer, shared credentials, a hosted benchmark service, or any search-, scraping-, embedding-, or generation-as-service MCP.

- Do not encode Arena's reported ratios or a permanent harness ranking.

- Do not build against the unverified Claude Code "mods" claim.

- Do not replace `scripts/harness_audit.py`; structural audit and behavioral evaluation remain separate.

- Do not make a minimal harness the automatic winner. Lower overhead is one measured input, not the decision rule.

## Queued-plan assessment

Queue membership comes from each plan's declared status or explicit user direction, never from unchecked-task counts. The 2026-09-21 enumerator result found the following interactions.

| Plan | Declared status | Verdict for v4.17.3 | Evidence and sequencing consequence |
|---|---|---|---|
| v4.15.0 | unknown | Unknown membership; no direct dependency | It touches planning, memory, and product-pulse surfaces but not the three owners changed here. If activated, re-run the queue assessment before implementation. |
| v4.15.1 | queued | No material impact | Its verification and construction rules remain shared gates; it does not own loop outcomes, harness pairing receipts, or Claude instruction paths. |
| v4.16.0 | unknown | Content and ordering impact if active | Its instruction-necessity review touches `base-claude.md` and shared-instruction policy. Phase 4 must consume its landed decision or record why it did not land before changing project instruction ownership. |
| v4.16.1 | unknown | No material impact if active | Model-adaptive runtime work does not own the evaluator or instruction-file selection. Re-check only if its implementation changes the harness adapter contract. |
| v4.16.2 | unknown | Content and ordering impact if active | It touches model routing, the Claude integration, both installers, and installer smoke tests. Phase 4 starts only after reconciling its final tree or documenting that it remains inactive. |
| v4.16.3 | queued | Direct predecessor and content impact | It owns durable harness-cost guidance. Its relevant guidance must land before Phases 2-3, or those phases stop and record the dependency; this plan adds measurement and does not duplicate the guidance. |
| v4.17.0 | unknown | Content impact if active | Its evidence vocabulary and functional-verification work should be reused by Phases 2-3 and the terminal phase if landed. Do not invent a competing evidence vocabulary. |
| v4.17.1 | unknown | No material impact if active | Access modes and presets are adjacent but do not own these capabilities. |
| v4.17.2 | queued | No material implementation overlap | Its memory and model-routing work remains separate. The evaluator may record model-map identity but does not change routing policy. |

**Required implementation-time recheck**: Run `python scripts/enumerate_plan_queue.py --root . --json` at Phase 1 entry and Phase 4 entry. A changed status or overlapping landed diff changes sequencing, not the approved scope.

## Definition of Done

1. `[[loop-engineering]]` defines narrow-trigger test runs and observable `no-data`, `stale-input`, `partial`, `complete`, and `failed` outcomes, with regression tests that reject ambiguous success.

2. A local evaluator accepts an explicit workload and harness-adapter manifest, validates it before execution, and produces deterministic machine-readable receipts for synthetic fixtures without making provider calls.

3. Trusted synthetic trials run in workspace-separated temporary roots. Live trials run only through an enforceable isolation backend from `[[agent-execution-isolation]]` that constrains filesystem access, process trees, network destinations, and credential injection. Command execution avoids shell interpolation; typed redaction occurs before persistence; unavailable measurements remain explicitly unavailable.

4. Aggregation reports task success, latency, cost, cost per successful task, initial instruction bytes, tool-schema bytes, repeated-trial uncertainty, and normalized failure fingerprints. Each workload predeclares acceptance gates and a decision policy; reports refuse a recommendation for incomparable, insufficient, or non-qualifying samples.

5. `scripts/harness_audit.py` remains a structural auditor and reports the effective project instruction path by capability rather than treating both `AGENTS.md` and `CLAUDE.md` as simultaneously required.

6. A platform-neutral shared-instruction resource owns the project `AGENTS.md` managed block and persists its active consumers. Supported Claude Code v2.1.277+ project sessions with fallback enabled consume it without becoming a writer. Global Claude instructions and Bedrock, Vertex, Foundry, older, fallback-disabled, or unresolved-precedence project sessions retain `CLAUDE.md`.

7. Bash and PowerShell installers, integration registry behavior, read-path verification, merge preservation, smoke postconditions, and legacy-parity tests agree on the effective Claude instruction path and preserve user-authored content. A per-platform semantic inventory proves that universal content moved to shared `AGENTS.md` and platform-specific instructions retained on a native sidecar or platform-owned global surface remain reachable for every migrated consumer.

8. A before-and-after evaluation records the instruction payload change and behavioral result for at least one approved local pairing. Fresh supported installs must have one active managed project body; deterministic synthetic outcomes must be identical; shared instruction bytes must not grow. A failed gate blocks convergence. If a live provider run is unavailable, the result is `needs-live-validation`; synthetic success is not reported as live evidence.

9. Every phase has focused tests, a CI-impact record, a session-history entry, and exactly one planned local phase commit. No phase before the terminal publication task pushes or starts remote CI. A failed required remote check reopens Phase 5 and permits only a separately recorded corrective commit and re-push.

10. The terminal phase completes architecture review, known-gap reconciliation, living-doc checks, git hygiene, CI/CD reconciliation, the Tier 3 deep pass, an independent Goal-vs-codebase review, human testing suggestions, a full local profile at one revision, and approval-gated publication.

## Architecture and ownership decisions

| Concern | Owner | Decision |
|---|---|---|
| Unattended-loop outcome semantics | `catalog/skills/workflow/loop-engineering/SKILL.md` | Extend the existing owner; do not create another routine framework. |
| Structural harness conformance | `scripts/harness_audit.py` | Keep deterministic file and configuration checks here. Do not add live task execution. |
| Behavioral pairing evaluation | `scripts/evaluate_harness_pairings.py` | Add one stdlib-first, serial local orchestrator with a versioned manifest and receipt contract. |
| Evaluation audit and routing | `catalog/skills/ai-development/eval-pipeline-audit/SKILL.md` | Detect whether behavioral pairing evidence exists and route to the local evaluator; do not absorb runner mechanics. |
| Project `AGENTS.md` managed block | Proposed `templates/ai-instructions/base-agents.md` shared resource | One platform-neutral writer with persisted consumers. Claude and every confirmed root writer become consumers; teardown cannot remove the block while a consumer remains. |
| Claude-specific instructions | `scripts/lib/integrations/claude.py` and `templates/ai-instructions/base-claude.md` | Keep global and exception-path `CLAUDE.md` behavior. |
| Capability truth | `docs/policy/platform-read-contracts.md` plus a persisted capability decision | Version, provider surface, user fallback setting, evidence source, confidence, effective path, and decision time govern install, repair, audit, migration, and teardown. |

## Current model map

**Model map status**: fresh as of 2026-09-21; sources cited below.

| Tier | Anthropic | OpenAI | Google | Cursor |
|---|---|---|---|---|
| frontier | `claude-fable-5-1` | `gpt-6-astra` | `gemini-3.8-flash` | `cursor-grok-4.6` |
| strong | `claude-opus-5` | `gpt-5.6-sol` | `gemini-3.7-flash` | `cursor-grok-4.5` |
| standard | `claude-sonnet-5` | `gpt-5.6-terra` | `gemini-3.6-flash` | `composer-2.5` |
| fast | `claude-haiku-4-5` | `gpt-5.6-luna` | `gemini-3.5-flash-lite` | `composer-2.5-fast` |

### Model map sources

- Anthropic: https://platform.claude.com/docs/en/models/overview

- OpenAI: https://developers.openai.com/api/docs/models and https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra

- Google: https://ai.google.dev/gemini-api/docs/models

- Cursor: https://cursor.com/docs/models-and-pricing

## Phases at a Glance

| Phase | Goal | Tier | Effort | Scoring signals: context, uncertainty, risk, breadth, verification |
|---|---|---|---|---|
| 1 | Lock ownership and explicit loop outcomes | strong | high | medium, low, medium, low, medium |
| 2 | Define the behavioral evaluation contract and independent fixtures | frontier | max | medium, high, medium, medium, high |
| 3 | Build and exercise the isolated local pairing runner | frontier | max | high, high, high, high, high |
| 4 | Converge supported Claude project sessions on one `AGENTS.md` owner | frontier | max | high, medium, high, high, high |
| 5 | Architecture refactor, known gaps, CI/CD, publication, and integration | frontier | max | high, high, high, high, high |

---

## Phase 1: Ownership baseline and explicit loop outcomes

**Goal**: Reconcile the live queue and landed instruction work, then make unattended-loop outcomes explicit inside the existing loop owner.

**Prerequisites**: Approved comparison; clean understanding of the current dirty worktree; no destructive cleanup.

**Stability Gate**: Focused loop bundle tests prove each terminal outcome and reject a success claim for stale, absent, or partly processed input. Queue and ownership decisions are recorded in the phase evidence.

**Verification Expectation**: Run the loop bundle validator against positive and negative fixtures, including zero records, stale records, one failed item among successes, an expired budget, and a repeated idempotent retry.

**Recommended model tier**: strong

**Recommended effort level**: high

**Rationale**: The change is narrow and localized, but outcome semantics are trust-bearing and must remain compatible with existing loop budgets and termination rules.

### Sub-tasks

#### 1.1 -- Reconcile the queue and freeze ownership

**Prompt**:

> Re-run the queue enumerator, inspect landed diffs for v4.16.0, v4.16.2, v4.16.3, and v4.17.0, and record exact effects in `phase-1-evidence.md`. Preserve the ownership table above unless a landed artifact proves a conflict; in that case amend the plan before code changes.

**Failure modes**:

- Malformed or absent queue metadata yields `unknown`, never an inferred status.
- An unreachable plan file is recorded with its path and blocks only the affected dependency decision.
- Conflicting landed owners stop the affected task until one owner is selected; they are not merged silently.

#### 1.2 -- Add explicit loop outcomes

**Prompt**:

> Extend `[[loop-engineering]]` and its schema reference with a closed outcome vocabulary: `no-data`, `stale-input`, `partial`, `complete`, and `failed`. Add a precedence table: malformed, unreadable, schema-invalid, or unverifiable input is `failed`; a valid query returning zero records is `no-data`; any stale required source is `stale-input` unless fresh records were processed, in which case unfinished work is `partial`; any progress followed by budget exhaustion is `partial`; only all-valid and all-processed work is `complete`. Require a narrow trigger, one bounded test run before scheduling, processed/failed/skipped counts, a last-success timestamp where applicable, and a next action for every non-complete outcome.

**Failure modes**:

- Missing or malformed input becomes `failed`; only a valid empty result becomes `no-data`.
- A stale source becomes `stale-input` even when cached output exists.
- Conflicting retries preserve the idempotency key and report partial progress rather than double-counting work.

#### 1.3 -- Prove the outcome vocabulary

**Prompt**:

> Extend `tests/validators/test_loop_engineering_bundle.py` with independent positive and negative controls. Assert exact required fields and demonstrate that deleting or weakening each outcome rule fails at least one test. Cover mixed fresh/stale records, zero processed after retries, corruption versus valid-empty input, and partial progress followed by budget exhaustion.

**Failure modes**:

- A malformed fixture must fail with an actionable field path.
- A slow or unavailable external source is represented by a fixture and consumes no network access.
- Tests must reject simultaneous terminal outcomes and mismatched processed/failed totals.

#### 1.4 -- Testing and Stabilization

**Prompt**:

> Run the focused loop tests, the relevant skill validators, Markdown checks, and strict Unicode validation for every touched path. Fix failures, record CI impact, generate the phase session history, and create exactly one local commit. Do not push.

**Failure modes**:

- A missing test dependency is recorded and blocks the phase rather than being treated as a skip.
- A slow validator is bounded and its timeout is a failure.
- Conflicting unrelated worktree changes stay unstaged and out of the phase commit.

### Phase 1 Exit Checklist

- [ ] Queue status and landed predecessor effects recorded
- [ ] One owner named for every concern
- [ ] Five loop outcomes documented with unambiguous fields
- [ ] Independent positive and negative controls pass
- [ ] CI impact and session history recorded
- [ ] Exactly one local commit; no push

---

## Phase 2: Behavioral evaluation contract and fixtures

**Goal**: Define a versioned, fail-closed contract for comparable harness trials before implementing any live orchestration.

**Prerequisites**: Phase 1; v4.16.3 harness-cost guidance landed or the phase stops with a recorded dependency; reuse v4.17.0 evidence vocabulary if it landed.

**Stability Gate**: Schema and fixture tests distinguish valid, incomparable, no-data, partial, budget-exhausted, and malformed results without invoking a provider or harness binary.

**Verification Expectation**: Parse frozen positive and adversarial manifests and receipts; recompute every aggregate with an independent oracle; prove that pricing, payload, or sample gaps remain unavailable rather than becoming zero.

**Recommended model tier**: frontier

**Recommended effort level**: max

**Rationale**: Cross-harness comparability, uncertainty, pricing, and failure normalization have high semantic uncertainty and can create convincing but false evidence if the contract is weak.

### Sub-tasks

#### 2.1 -- Define manifest, trial, and receipt schemas

**Prompt**:

> Add `guides/reference/HARNESS_PAIRING_EVALUATION.md` with the durable system contract and add versioned JSON fixtures under `tests/fixtures/harness_pairings/`. Bind each run to workload hash, repository revision, harness and model identity, adapter version, effective instruction hash, tool-schema hash, pricing snapshot, trial seed, isolation mode, operating system and architecture, harness configuration, model parameters, cache policy, concurrency level, retry policy, trial order, budgets, and timestamps.

**Failure modes**:

- Missing required identity or budget fields reject the manifest before execution.
- Unavailable version or pricing data is explicit and makes affected comparisons `incomparable`.
- Conflicting trial IDs, workload hashes, or schema versions are rejected rather than merged.

#### 2.2 -- Define outcomes and objective metrics

**Prompt**:

> Define task success from a workload-owned deterministic evaluator, not harness self-report. Define latency, observed provider cost, cost per successful task, initial instruction bytes, tool-schema bytes, trial counts, uncertainty intervals, and normalized failure fingerprints. Keep every raw trial beside aggregates. Separate `synthetic`, `metered-live`, and `observational-live` modes: synthetic cost is not applicable; metered live requires provider- or adapter-enforced spend control; observational live may report cost but cannot satisfy a hard-cost claim or recommendation gate.

**Failure modes**:

- No successful task makes cost per success unavailable, not zero or infinity presented as a score.
- Missing provider usage data preserves success and latency while marking cost unavailable and blocks a cost-based recommendation.
- Conflicting evaluator and harness claims resolve in favor of the workload-owned evaluator with both values retained.

#### 2.3 -- Define comparison eligibility and ranking restraint

**Prompt**:

> Require the same workload revision, evaluator revision, environment identity, model parameters, cache policy, retry policy, paired seeds, trial-order policy, budget class, and pricing basis for a direct comparison. Default to three repeats per pairing, a 95% Wilson interval for binary success, and a fixed-seed percentile bootstrap interval for continuous metrics. Alternate or randomize pairing order with the recorded seed. Each workload must predeclare minimum success, maximum latency and cost where applicable, and a tie-break policy. Report qualifying pairings and their Pareto frontier; name one recommendation only when the predeclared tie-break applies and the relevant 95% intervals do not overlap.

**Failure modes**:

- Fewer than three repeats cannot yield a recommendation.
- A timed-out pairing remains in the report as partial evidence.
- Conflicting price dates or budget classes split comparison groups rather than being normalized silently.

#### 2.4 -- Define the adapter and isolation boundary

**Prompt**:

> Define adapters as argv arrays plus a narrow JSON input/output protocol. Forbid `shell=True`, inline secrets, inherited environment by default, and receipt capture of credential values. Treat all adapter bytes as hostile: bound output before parsing, accept allowlisted typed fields only, redact in memory before any persistence, escape control sequences and Markdown/HTML, and keep raw stdout/stderr ephemeral unless an explicitly protected diagnostic channel is selected. Require disposable workspace roots, a sentinel before cleanup, an environment allowlist, and explicit network/provider permission.

**Failure modes**:

- String commands, traversal paths, missing sentinel, or unknown environment names fail before process start.
- An unavailable or slow harness exits with a bounded typed failure and retained logs.
- Colliding runs cannot share a mutable workspace or receipt path.

#### 2.5 -- Add independent contract tests and negative controls

**Prompt**:

> Add `tests/integrations/test_harness_pairing_contract.py` with an independent aggregate oracle and mutations for absent values, inconsistent totals, forged success, secret-shaped fields, invalid UTF-8, NULs, ANSI escapes, forged Markdown headings, oversized fields, path escape, budget overrun, mixed comparison groups, and environment or trial-order drift.

**Failure modes**:

- A malformed fixture names its schema path and never reaches execution.
- Tests use frozen local data and remain valid without network or provider credentials.
- Mutually inconsistent outcomes cannot pass because both producer and test use the same helper.

#### 2.6 -- Route audit findings without merging owners

**Prompt**:

> Update `[[eval-pipeline-audit]]` to recognize missing cross-harness behavioral evidence and route it to the local evaluator. State that `scripts/harness_audit.py` remains structural and the evaluator remains behavioral. Link the reference contract without embedding runner mechanics in the audit skill.

**Failure modes**:

- An absent evaluator is reported as not covered, not reconstructed by the audit skill.
- An unreachable reference produces an explicit broken-link failure.
- Conflicting structural and behavioral results are shown side by side, not collapsed.

#### 2.7 -- Testing and Stabilization

**Prompt**:

> Run contract tests, skill bundle validation, documentation links, schema mutations, Markdown checks, and strict Unicode validation. Fix failures, record CI impact, generate the phase session history, and create exactly one local commit. Do not push or call a provider.

**Failure modes**:

- An unavailable optional parser cannot weaken the stdlib validation path.
- A slow mutation suite is bounded and fails visibly.
- Unrelated dirty files stay outside the commit.

### Phase 2 Exit Checklist

- [ ] Durable manifest and receipt contract exists
- [ ] Independent oracle recomputes all aggregates
- [ ] Missing data never becomes zero
- [ ] Comparison eligibility and no-rank states are explicit
- [ ] Adapter trust boundary rejects shell interpolation and secret capture
- [ ] Structural and behavioral owners remain separate
- [ ] Exactly one local commit; no push

---

## Phase 3: Isolated local pairing runner

**Goal**: Implement a serial local runner that executes trusted synthetic adapters with workspace hygiene, executes live adapters only behind proven isolation and budget capabilities, and emits comparable receipts redacted before persistence.

**Prerequisites**: Phase 2 contract frozen and green; at least one deterministic synthetic workload, two fake adapters, and credential-free conformance profiles for the Claude Code and Codex CLIs.

**Stability Gate**: A complete serial synthetic matrix run and an interrupted run produce schema-valid receipts; independent recomputation matches; cleanup touches only sentinel-owned temporary roots; canary secrets and hostile control bytes appear in no persisted artifact; live mode is rejected unless containment and budget capabilities are proven.

**Verification Expectation**: Exercise the command end to end with fake adapters that succeed, fail, hang, emit malformed or hostile output, omit pricing, and attempt host escape. Run credential-free `--version` and `--dry-run` conformance for Claude Code and Codex. A paid live trial remains opt-in and is allowed only when the isolation backend, enforceable budget, credentials, and workload are approved.

**Recommended model tier**: frontier

**Recommended effort level**: max

**Rationale**: This phase crosses subprocess, filesystem, network, credential, cost, and evidence boundaries. A plausible-looking receipt without real isolation would be worse than no evaluator.

### Sub-tasks

#### 3.1 -- Implement preflight and manifest validation

**Prompt**:

> Create `scripts/evaluate_harness_pairings.py` with stdlib-first parsing, strict schema validation, executable discovery, version capture, execution-mode checks, budget-capability checks, path containment, output collision detection, and `--dry-run`. Preflight every pairing before starting any trial. The MVP runs serially and starts fresh; resumability and parallel scheduling are deferred.

**Failure modes**:

- Malformed or absent manifests fail before workspace creation.
- Missing binaries, unavailable version probes, or slow probes yield typed preflight failures.
- Duplicate output paths or concurrently active run IDs fail closed.

#### 3.2 -- Separate workspace hygiene from enforceable isolation

**Prompt**:

> Create a unique temporary workspace per trusted synthetic trial, copy only declared workload inputs, write a sentinel, pass an allowlisted environment, and clean only sentinel-owned roots. Label this `workspace-separated`, never `isolated`. Define an isolation-backend capability contract owned by `[[agent-execution-isolation]]` for filesystem confinement, complete process-tree containment, network destination policy, credential injection, and cleanup. Live trials require a backend that proves every capability; otherwise reject live execution.

**Failure modes**:

- Missing sentinel, path escape, junction/symlink escape, or unexpected root blocks cleanup and records manual cleanup needed.
- A slow process is terminated at the declared timeout with bounded, typed, redacted diagnostics retained; raw stdout and stderr remain ephemeral unless the separately protected diagnostic channel was explicitly selected.
- Attempted writes to an external sentinel or disallowed outbound access fail the live isolation proof.

#### 3.3 -- Execute adapters with hard budgets

**Prompt**:

> Launch argv arrays without a shell and terminate the complete process tree on timeout or budget exhaustion. Model enforcement explicitly: time is runner-enforced; turns require a native pre-request limit or adapter events with cancellation; a hard spend ceiling requires a provider or adapter limit that rejects additional requests, tokens, or spend before it is incurred. Streaming telemetry plus cancellation is bounded-overshoot observational control only: record its maximum exposure and never let it satisfy a strict hard-cost gate. Reserve the serial trial's maximum exposure before launch. Reject a `metered-live` run whose declared hard limit is post-hoc or cancellation-only. Accept only bounded protocol output.

**Failure modes**:

- Malformed adapter output becomes `adapter-protocol-error` and cannot assert success.
- Missing cost telemetry cannot bypass a hard cost ceiling; it rejects `metered-live` and may run only as explicitly approved `observational-live` evidence that cannot support a recommendation.
- Simultaneous timeout and budget exhaustion preserve both facts under one primary terminal outcome.

#### 3.4 -- Evaluate workloads independently

**Prompt**:

> Keep evaluator code, expected results, and workload policy under a hashed read-only trusted root outside the adapter workspace. After each adapter exits, give the evaluator read-only access to trial outputs and let only the parent runner write receipts. Keep harness claims as diagnostic fields only. Normalize failure fingerprints from bounded, in-memory-redacted evidence without storing secrets or full prompts.

**Failure modes**:

- Missing evaluator output makes task success unavailable and the trial non-rankable.
- Slow evaluators use a separate timeout and cannot inherit provider credentials.
- Conflicting harness and evaluator success remains an explicit mismatch, and an adapter attempt to modify evaluator code or expected results is a containment failure.

#### 3.5 -- Aggregate trials and render reports

**Prompt**:

> Redact and sanitize every allowlisted field in memory before writing immutable per-trial JSON, one aggregate JSON receipt, and a concise escaped Markdown report. Compute metrics only for eligible comparison groups; show acceptance gates, Pareto status, intervals, sample sizes, unavailable fields, partial runs, and failure-fingerprint overlap. Include hashes, not secret-bearing instruction or schema payloads, in the distributable report. The serial MVP rejects output collisions and does not resume or quarantine duplicate receipts.

**Failure modes**:

- Zero eligible trials yields a no-data report and nonzero command status unless explicitly requested as a probe.
- A partial run retains completed immutable trials but a new invocation starts a new run identity.
- Conflicting duplicate trial receipts fail closed and are never merged.

#### 3.6 -- Add synthetic adapters and end-to-end tests

**Prompt**:

> Add deterministic fake adapters and integration tests covering success, failure, hang, malformed output, missing cost, secret-shaped output, invalid encodings and control bytes, payload-size variation, collision, evaluator tampering, external-sentinel writes, disallowed egress, and cleanup escape. Add built-in Claude Code and Codex adapter profiles with credential-free version and dry-run conformance tests. Prove at least one mutation is caught by a workload evaluator rather than the adapter.

**Failure modes**:

- Tests fail if a provider credential or network call is required.
- Hanging fakes are bounded by short test timeouts.
- Parallel test runs use unique temporary roots and cannot consume one another's receipts.

#### 3.7 -- Document the repository-local MVP

**Prompt**:

> Keep the Phase 3 MVP repository-local; installer distribution is deferred until usage evidence proves a downstream need. Add a copy-pasteable runbook with safe synthetic defaults, Claude Code and Codex conformance checks, the explicit live-run opt-in, required isolation and budget capabilities, expected outputs, and cleanup behavior.

**Failure modes**:

- A request for installed distribution is recorded as deferred scope rather than implemented implicitly.
- An unavailable live harness leaves the synthetic path fully usable and records `needs-live-validation`.
- Conflicting documentation and CLI defaults fail contract tests.

#### 3.8 -- Testing and Stabilization

**Prompt**:

> Run focused unit and integration tests, canary-secret assertions over every persistence path, process-tree termination checks, path-containment and host-sentinel tests on Windows and POSIX fixtures, Claude Code and Codex conformance checks, lint/type checks, Markdown checks, and strict Unicode validation. Observe the synthetic command end to end. Record CI impact, generate the phase session history, and create exactly one local commit. Do not push.

**Failure modes**:

- Missing platform coverage is a known gap, not a cross-platform pass.
- A slow end-to-end test is bounded and retains failure artifacts.
- Unrelated dirty files remain unstaged.

### Phase 3 Exit Checklist

- [ ] Preflight fails before execution on malformed or unsafe input
- [ ] Synthetic trials are workspace-separated; live trials require a proven isolation backend
- [ ] Time, turn, and cost claims name their enforcement capability
- [ ] Workload evaluator, not harness self-report, owns success
- [ ] Raw and aggregate receipts are immutable, redacted, and schema-valid
- [ ] Synthetic success, failure, timeout, hostile output, and cleanup paths observed end to end
- [ ] Claude Code and Codex profiles pass credential-free conformance
- [ ] Repository-local MVP cut line is explicit
- [ ] Exactly one local commit; no push

---

## Phase 4: Provider-aware Claude `AGENTS.md` convergence

**Goal**: Establish one platform-neutral owner for the project `AGENTS.md` block, then let supported Claude Code sessions consume it without breaking global Claude instructions, unsupported providers, user-disabled fallback, teardown, or user-authored content.

**Prerequisites**: Phases 1-3; re-run queue assessment; reconcile landed v4.16.0 and v4.16.2 changes; re-verify Claude Code's current official read contract and supported-provider exceptions.

**Stability Gate**: Capability-matrix and migration-state tests prove the effective instruction path for supported, old, disabled, Bedrock, Vertex, Foundry, global, fresh, legacy-managed-only, mixed, and user-authored cases. Installer smoke tests prove one shared writer, persisted consumer dependencies, teardown safety, and byte preservation outside owned blocks.

**Verification Expectation**: Install into disposable roots with both Bash and PowerShell paths, inspect the actual emitted files and managed blocks, run read-path verification, then measure before-and-after instruction payloads with the Phase 3 evaluator. Live behavior remains `needs-live-validation` unless an approved real Claude session is exercised.

**Recommended model tier**: frontier

**Recommended effort level**: max

**Rationale**: One incorrect fallback assumption can silently remove all project instructions. The change spans integration abstractions, two installers, audits, policy, and preservation tests.

### Sub-tasks

#### 4.1 -- Refresh and encode the Claude capability matrix

**Prompt**:

> Verify the current official Claude Code documentation and release notes. Define a capability decision object containing version, provider surface, fallback setting, evidence source, confidence or unknown state, effective path, decision timestamp, and prior-decision identity. Pass it explicitly through the installer runner and install context, persist it in the install manifest, and make install, repair, doctor, summaries, migration, audit, and teardown consume the same decision. Keep unknown states conservative: use or preserve `CLAUDE.md` until capability is proven.

**Failure modes**:

- Missing or malformed version data selects the conservative exception path.
- Unreachable official documentation blocks a claim of newly verified support but does not erase the last dated contract.
- Conflicting capability signals prefer the path that preserves instructions and report the conflict; upgrade, downgrade, changed-setting, and unavailable-probe transitions require a new recorded decision.

#### 4.2 -- Add a shared instruction resource and scope-aware seam

**Prompt**:

> First inventory every confirmed root writer's current template section by section. Extract only universal content into one platform-neutral `base-agents.md` resource. Retain platform-specific guidance on a documented native sidecar or platform-owned global surface, and do not migrate a consumer whose required content has no reachable owner. Add persisted consumer ownership for the root `AGENTS.md` managed block and prevent teardown while any consumer remains. Refactor the minimum integration seam needed for global and workspace scopes to select different instruction behavior. Global Claude keeps `~/.claude/CLAUDE.md`; supported workspace Claude consumes shared `AGENTS.md`; exception workspace Claude retains root `CLAUDE.md` from `base-claude.md`.

**Failure modes**:

- Missing capability context falls back to `CLAUDE.md` without deleting `AGENTS.md`.
- An unavailable shared resource blocks consumer-only mode rather than creating an empty file.
- Confirmed root-writing integrations cannot write competing content to the same marker or remove it while another consumer remains; a missing sidecar owner blocks that platform's migration rather than dropping content.

#### 4.3 -- Update both installers and migration behavior

**Prompt**:

> Update `scripts/installer.sh` and `scripts/installer.ps1` through the shared integration runner. Implement four explicit project states: fresh supported installs create no project `CLAUDE.md`; legacy Nexus-managed-only files can migrate only through an explicit reversible action that removes the owned block and deletes the file only when Nexus created it and it is otherwise empty; mixed user-plus-managed files remove only the owned block when explicitly requested and remain `CLAUDE.md`-precedence while user bytes remain; user-authored files are preserved and remain `CLAUDE.md`-precedence. Unsupported sessions continue to manage `CLAUDE.md`.

**Failure modes**:

- Malformed existing managed blocks stop with repair guidance and preserve bytes.
- An unavailable capability probe selects the exception path, and unresolved `CLAUDE.md` precedence cannot be called converged.
- Concurrent or repeated installs are idempotent and cannot duplicate either managed block.

#### 4.4 -- Make harness audit capability-aware

**Prompt**:

> Update `scripts/harness_audit.py` so Claude's required project instruction file is the persisted capability-selected path. Keep global and exception expectations distinct. Report inactive duplicate files as precedence risks without claiming both are required. Report shared owner and consumer state separately from file presence.

**Failure modes**:

- Missing capability inputs produce `unknown` or conservative results, not a false pass.
- Unreachable instruction files produce path-specific findings.
- Conflicting active instruction files report precedence and shadowing rather than adding both scores.

#### 4.5 -- Update policy and read-path verification

**Prompt**:

> Update `docs/policy/platform-read-contracts.md` and `tests/installer/test_verify_read_paths.py` with the dated fallback, exceptions, user toggle, precedence, one-writer rule, and verification commands. Keep unsupported-provider evidence separate from ordinary Claude Code evidence.

**Failure modes**:

- An unverified platform claim remains explicitly unknown.
- A slow or unavailable platform command does not convert documentation evidence into runtime proof.
- Conflicting documentation and executable expectations fail the verifier.

#### 4.6 -- Prove merge preservation and cross-platform parity

**Prompt**:

> Extend integration, merge, smoke, lifecycle, organization-materialization, and legacy-parity tests for scope-aware behavior. Cover every confirmed root writer as a shared consumer, its universal-section inventory, its platform-specific sidecar or global owner, and its effective read path. Also cover pre-existing user `CLAUDE.md`, pre-existing user `AGENTS.md`, all four migration states, malformed markers, repeat install, upgrade, downgrade, changed fallback setting, unavailable probe, unsupported provider, fallback disabled, consumer removal, and last-consumer teardown.

**Failure modes**:

- Malformed user files remain byte-preserved outside any valid managed block.
- Missing Bash or PowerShell runtime is recorded as unverified platform coverage, not a pass.
- Parallel installer operations cannot interleave writes or remove another integration's content.

#### 4.7 -- Measure the before-and-after payload and behavior

**Prompt**:

> Use the Phase 3 evaluator with paired seeds and alternating order over a frozen synthetic workload to compare the old Claude project layout and supported `AGENTS.md` consumer layout. The release gate requires one active managed project body on fresh installs, exact deterministic synthetic outcome parity, no shared-instruction byte growth, and successful migration-state preservation tests. Any miss blocks convergence. Record tool-schema bytes if observable, latency, success, and failure fingerprints. Run an opt-in live Claude trial only when credentials, enforceable isolation, metered budgets, and an approved workload are available.

**Failure modes**:

- Missing payload instrumentation is `unavailable`, not zero.
- An unavailable or slow live provider yields `needs-live-validation` with synthetic evidence retained separately.
- Conflicting synthetic and live results remain separate evidence classes.

#### 4.8 -- Testing and Stabilization

**Prompt**:

> Run focused integration and installer tests on both supported shells, harness-audit tests, read-path verification, preservation tests, policy link checks, the synthetic before-and-after evaluation, lint/type checks, and strict Unicode validation. Observe disposable install outputs directly. Record CI impact, generate the phase session history, and create exactly one local commit. Do not push.

**Failure modes**:

- A missing target OS blocks cross-platform completion.
- A slow installation is bounded and leaves its disposable root for diagnosis when cleanup is unsafe.
- Unrelated dirty files stay out of the commit.

### Phase 4 Exit Checklist

- [ ] Current official Claude fallback contract re-verified and dated
- [ ] One platform-neutral resource owns root `AGENTS.md`; every migrated writer has a no-loss semantic inventory and reachable platform-specific owner
- [ ] Capability decisions persist across install, repair, audit, migration, and teardown
- [ ] Global and unsupported-provider `CLAUDE.md` paths preserved
- [ ] Four migration states preserve user bytes and report unresolved precedence
- [ ] Both installers and audits agree on effective paths
- [ ] Before-and-after release gates pass; live status is reported honestly
- [ ] Exactly one local commit; no push

---

## Phase 5: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Goal**: Leave the project well-organized, its known gaps reconciled, its living docs current, its artifacts exercised through the Tier 3 deep pass, and its pipeline reconciled, with independent evidence that the Goal landed, then publish and integrate.

**Prerequisites**: All prior phases.

**Stability Gate**: `docs/releases/v4/v4.17/development/last-phase-evidence.md` exists with one quoted section per duty, including `## Tier 3 deep pass`. A missing Goal, a Goal-review miss without a recorded known gap, or a missing evidence section blocks `/update release`.

**Verification Expectation**: Run `[[functional-verification]]`'s Tier 3 procedure from its `references/deep-pass.md` against every artifact the plan produced and observe a clean or bounded terminal result; quote it under `## Tier 3 deep pass`.

**Recommended model tier**: frontier

**Recommended effort level**: max

**Rationale**: Repo-wide refactor, Tier 3 exercise, Goal review, known-gap reconciliation, and release gating carry high context volume and blast radius.

**Scoping note**: This is the repository's standard fail-closed contract. Duties 5.1, 5.3, and 5.5 are scoped to artifacts this plan touched, and a no-change result is acceptable only with quoted scan evidence.

### Sub-tasks

#### 5.1 -- Architecture refactor

**Prompt**:

> Identify deprecated or obsolete files, empty directories, redundant files, and overcomplicated structure, then refactor via `[[project-refactor]]` and `[[docs-layout-refactor]]` using propose-then-apply with confirmation and full reference repair. Scope to artifacts this plan touched. Write detector output under `## Architecture refactor`; an empty finding requires quoted scan output.

**Failure modes**:

- Missing paths are recorded and cannot be silently ignored.
- Slow or unavailable detectors are bounded and reported.
- Conflicting moves or live references block the refactor until resolved.

#### 5.2 -- Known-gaps reconciliation

**Prompt**:

> Via `[[known-gaps-tracker]]`, reconcile this version's known gaps and every other `docs/**/known-gaps.md` whose Status is in progress or whose Open Items remain. Glob canonical and legacy layouts. Create `docs/releases/v4/v4.17/known-gaps.md` if absent. Carry unavailable live harness evidence, unsupported platform validation, deferred strategy grounding, and any predecessor dependency with an owner and next step.

**Failure modes**:

- An absent ledger is created; a malformed ledger blocks derived counts.
- An unreachable historical file is named and the glob evidence retained.
- Conflicting dispositions remain open until one owner and next step are selected.

#### 5.3 -- Living docs architecture

**Prompt**:

> Check `docs/handbooks/`, `docs/decisions/`, `guides/reference/`, and living `docs/README.md`, `docs/DEVLOG.md`, and `docs/todos.md`. Confirm the harness-pairing reference has a dense header summary and remains living documentation. Self-gated: never invent `docs/testing/` or `docs/validation/`. Invoke `[[technical-documentation]]` through its handbook-refresh reference before local completion. Quote the result under `## Living docs architecture`.

**Failure modes**:

- Missing living roots are reported before creating any new taxonomy.
- Unreachable link targets fail documentation validation.
- Conflicting Markdown and generated HTML resolve from Markdown or fail visibly.

#### 5.4 -- Git-tree hygiene

**Prompt**:

> Run `python scripts/check_release_preconditions.py --branches --repo-settings`. Quote the output under `## Git-tree hygiene`. Report only; never delete branches or user work.

**Failure modes**:

- Malformed Git state or broken worktree pointers are findings.
- Unreachable remote settings remain unverified.
- Conflicting local work is preserved and excluded from publication.

#### 5.5 -- Terminal CI/CD reconciliation

**Prompt**:

> Invoke `[[cicd-architect]]` in existing-pipeline comparison mode and run its six steps in order: DETECT the active provider; COMPARE every canonical field; PROPOSE each difference with cost and risk; obtain explicit APPROVAL per change; APPLY and re-run the local gate; RECORD declined differences as known gaps with owners. A green pipeline that checks nothing is not evidence.
>
> Confirm by direct inspection whether this plan touched `scripts/installer.sh` or `scripts/installer.ps1`. Phase 4 is expected to touch both, so cross-installer parity is mandatory and must prove absolute postconditions on target operating systems. Cross-link `[[platform-contract-verification]]`. Quote the per-field result under `## CI/CD coverage`.

**Failure modes**:

- No detected provider is recorded as none, not guessed.
- An unavailable target OS blocks parity completion.
- Conflicting pipeline proposals require explicit approval and cannot be bundled silently.

#### 5.6 -- Tier 3 deep pass

**Prompt**:

> Invoke `[[functional-verification]]` and follow its Tier 3 runbook at `catalog/skills/testing/functional-verification/references/deep-pass.md` against every artifact this plan produced. Use its blast-radius decision, delegated owners, plan audits, and global iteration budget exactly. Quote the terminal result under `## Tier 3 deep pass`. Record every remaining finding through `[[known-gaps-tracker]]` with an owner and next step.

**Failure modes**:

- Missing artifacts or malformed evidence fail closed.
- An unavailable delegated verifier is named as not covered.
- Conflicting results remain unresolved findings until adjudicated.

#### 5.7 -- Independent Goal-vs-codebase review

**Objective**: Prove the Goal landed with questions whose answer can be no even when the planned text exists.

**Prompt**:

> Re-read the Goal and Definition of Done. Inspect the codebase as if this agent had not implemented it. Write `## Goal-vs-codebase review` with the Goal restated, satisfying artifacts, and gaps. Do not merely re-run prior stability gates.
>
> 1. For DoD 1, exercise absent, stale, partial, complete, and failed loop inputs and confirm their receipts cannot collapse to one success state.
> 2. For DoD 2-4, run the same frozen workload through two fake harness adapters with repeated trials, verify the independent oracle matches, then mutate price, workload hash, secret-shaped output, and sample count and confirm ranking becomes unavailable or the run fails.
> 3. For DoD 3, split proof by mode. Synthetic mode must prove path validation, safe cleanup, process-tree timeout, and rejection of live execution without an isolation backend. Only a trial executed through the isolation backend may prove blocked external-sentinel writes and disallowed egress. If no live backend is available, record containment as not verified and keep live execution disabled.
> 4. For DoD 5, run structural audit and behavioral evaluation over the same synthetic pairing and confirm they produce distinct evidence without overwriting each other.
> 5. For DoD 6-7, install supported and exception Claude configurations into disposable roots with both installers and confirm the exact effective file, one managed-block writer, and preservation of user bytes.
> 6. For DoD 8, inspect the before-and-after receipt and confirm unavailable live evidence is labeled `needs-live-validation` rather than success.
> 7. For DoD 9-10, confirm one commit per phase, no early push, every evidence section, and one full-profile result at the reviewed revision.
>
> A miss is a blocking finding or a new known gap, not a pass.

**Failure modes**:

- A missing or malformed Goal is a fail-closed finding.
- An unavailable runtime boundary is recorded as not verified.
- Conflicting evidence is adjudicated or remains open; task completion is not proof.

#### 5.8 -- Human/manual testing suggestions (last phase only)

**Prompt**:

> Write `## Human testing` with only checks that require judgment. At minimum: judge whether the evaluation report supports a real harness choice without overstating uncertainty; inspect whether a supported Claude project remains understandable with one shared `AGENTS.md`; and decide whether exception messaging is clear enough that a Bedrock, Vertex, Foundry, older-version, or fallback-disabled user will not remove `CLAUDE.md`. This is the first phase permitted to emit these suggestions.

**Failure modes**:

- Missing human access is recorded as pending, not passed.
- A slow live provider check remains optional and budgeted.
- Conflicting judgments are retained with the decision owner.

#### 5.9 -- Testing and Stabilization

**Prompt**:

> Run the complete local gate at one revision: `python scripts/ci/run.py --profile full`. Do not substitute individually run steps for the repository-native profile. Fix every failure and re-run until the whole profile is green at one revision.
>
> Also run `python scripts/validate_unicode_safety.py --strict --root . --path docs/releases/v4/v4.17`.
>
> Quote the exact commands, exit codes, and skips under `## Full-suite testing`. Record final CI impact, generate final session history, and create exactly one local commit.

**Failure modes**:

- Missing dependencies or skipped required tests block the phase.
- A timeout is a failure with bounded, typed, redacted diagnostics; raw process output follows the protected-channel rule.
- Concurrent worktree changes stay outside the commit and evidence claim.

#### 5.10 -- Publication and integration

**Prompt**:

> This is the only sub-task permitted to push, open a pull request, or start remote CI.
>
> Obtain explicit user approval, then perform one initial branch push and open the integration pull request to `develop` via `[[pr-description-writer]]`. This pull request is the plan's first remote validation and runs against the merge result. Do not perform routine intermediate pushes.
>
> Run `nexus-hub attribution check` before pushing. Commit messages are ASCII-only with no `Co-Authored-By` line or AI attribution footer.
>
> Wait for required checks. A red required check reopens this phase: reproduce locally, create one scoped corrective commit with fresh evidence, then re-push and record the exception. Do not bypass a required check or use an administrator override.
>
> `/update release` starts only after the integration result is green and merged. Do not tag or release from here.

**Failure modes**:

- Missing approval blocks publication without blocking local completion evidence.
- Unavailable remote checks remain pending and cannot be called green.
- Conflicting branch or attribution state must be resolved before push.

### Phase 5 Exit Checklist

- [ ] `last-phase-evidence.md` exists with every named section
- [ ] `## Tier 3 deep pass` has a bounded terminal result
- [ ] Known gaps reconcile live, unavailable, and predecessor evidence
- [ ] Both real installers pass cross-platform parity
- [ ] Goal review answers behavioral questions rather than restating tasks
- [ ] Human testing suggestions appear only in this phase
- [ ] `python scripts/ci/run.py --profile full` is green at one revision
- [ ] `nexus-hub attribution check` passed before push
- [ ] One initial branch push and one integration pull request; any corrective re-push is scoped and recorded; required checks green

---

## Complexity tracking

| Decision | Selected rung | Construction debt avoided |
|---|---|---|
| Loop behavior | Reuse and refine `[[loop-engineering]]` | No second scheduler or routine framework |
| Behavioral evaluation | One stdlib-first repository script plus fixtures | No service, database, daemon, dashboard, or new MCP |
| Harness support | Narrow argv/JSON adapter contract | No bundled provider SDKs or imported harness runtime |
| Project instructions | One platform-neutral `base-agents.md` resource with persisted consumers | No competing platform-specific writers for the shared managed block |
| Reporting | JSON receipts plus concise Markdown | No web UI or permanent leaderboard |

## Considered and not changed

- ECC remains source material only. Its package, agents, hooks, skills, and installers are not adopted.

- Grokbot's shared cloud host and shared credential pattern remains rejected.

- Arena's reported cost ratios remain bounded source evidence and are not routing constants.

- Claude Code instruction mods remain deferred until a primary source documents them.

- Existing project `CLAUDE.md` files are preserved because precedence and user intent cannot be inferred safely.

- Dynamic Workflows are optional for Phase 4's platform matrix only after one pairing is calibrated serially; they are not required to implement the plan.

## Task lines

- [ ] T001 [US1] Re-run the queue assessment and record landed predecessor effects docs/releases/v4/v4.17/development/phase-1-evidence.md
- [ ] T002 [US1] Freeze rule ownership and the shared project instruction decision docs/releases/v4/v4.17/plans/v4.17.3-adoption-harness-economics-and-portable-engineering-system.md
- [ ] T003 [US1] Add the closed loop outcome vocabulary and precedence table catalog/skills/workflow/loop-engineering/SKILL.md
- [ ] T004 [US1] Extend the loop schema and bounded test-run contract catalog/skills/workflow/loop-engineering/references/loop-schema.md
- [ ] T005 [US1] Add independent loop outcome and precedence controls tests/validators/test_loop_engineering_bundle.py
- [ ] T006 [US1] Stabilize Phase 1, record CI impact and history, and create one local commit docs/releases/v4/v4.17/development/phase-1-evidence.md
- [ ] T007 [US2] Write the durable harness-pairing evaluation contract guides/reference/HARNESS_PAIRING_EVALUATION.md
- [ ] T008 [P] [US2] Add versioned valid, partial, and incomparable manifests tests/fixtures/harness_pairings/
- [ ] T009 [P] [US2] Add versioned trial and aggregate receipt fixtures tests/fixtures/harness_pairings/
- [ ] T010 [US2] Define execution modes, objective metrics, uncertainty, acceptance gates, and Pareto policy guides/reference/HARNESS_PAIRING_EVALUATION.md
- [ ] T011 [US2] Define argv, containment, environment, redaction, secret, and cleanup boundaries guides/reference/HARNESS_PAIRING_EVALUATION.md
- [ ] T012 [US2] Add an independent contract oracle and adversarial mutations tests/integrations/test_harness_pairing_contract.py
- [ ] T013 [US2] Route missing behavioral evidence without merging audit owners catalog/skills/ai-development/eval-pipeline-audit/SKILL.md
- [ ] T014 [US2] Stabilize Phase 2, record CI impact and history, and create one local commit docs/releases/v4/v4.17/development/phase-2-evidence.md
- [ ] T015 [US2] Implement evaluator preflight, execution modes, and strict manifest validation scripts/evaluate_harness_pairings.py
- [ ] T016 [US2] Implement workspace hygiene and enforceable isolation-backend checks scripts/evaluate_harness_pairings.py
- [ ] T017 [US2] Implement shell-free execution, process-tree termination, and capability-backed budgets scripts/evaluate_harness_pairings.py
- [ ] T018 [US2] Implement trusted-root workload evaluation and normalized failure fingerprints scripts/evaluate_harness_pairings.py
- [ ] T019 [US2] Implement pre-persistence redaction, immutable receipts, aggregation, and escaped reporting scripts/evaluate_harness_pairings.py
- [ ] T020 [P] [US2] Add deterministic benign and hostile fake adapters tests/fixtures/harness_pairings/adapters/
- [ ] T021 [P] [US2] Add credential-free Claude Code and Codex adapter profiles configs/harness-adapters/
- [ ] T022 [US2] Add end-to-end tests for outcomes, containment, budgets, hostile output, and cleanup tests/integrations/test_harness_pairings.py
- [ ] T023 [US2] Document the repository-local serial MVP and explicit live-run gate guides/reference/HARNESS_PAIRING_EVALUATION.md
- [ ] T024 [US2] Stabilize Phase 3, observe the command end to end, record CI impact and history, and create one local commit docs/releases/v4/v4.17/development/phase-3-evidence.md
- [ ] T025 [US3] Re-verify the Claude contract and define the persisted capability decision docs/policy/platform-read-contracts.md
- [ ] T026 [US3] Add the platform-neutral shared project instruction template templates/ai-instructions/base-agents.md
- [ ] T027 [US3] Inventory universal and platform-specific instructions for every confirmed root writer docs/releases/v4/v4.17/development/shared-agents-inventory.md
- [ ] T028 [US3] Add shared-resource consumers, lifecycle ownership, and scope-aware path selection scripts/lib/integrations/base.py
- [ ] T029 [US3] Implement Claude global, consumer, exception, and migration states scripts/lib/integrations/claude.py
- [ ] T030 [US3] Apply persisted capability and reversible migration behavior in Bash scripts/installer.sh
- [ ] T031 [US3] Apply persisted capability and reversible migration behavior in PowerShell scripts/installer.ps1
- [ ] T032 [US3] Make structural harness audit report effective path and shared ownership scripts/harness_audit.py
- [ ] T033 [US3] Add capability and ownership-aware harness audit tests tests/integrations/test_harness_audit.py
- [ ] T034 [US3] Update executable read-path verification tests/installer/test_verify_read_paths.py
- [ ] T035 [US3] Add byte-preserving four-state migration and repeat-install tests tests/installer/test_instruction_merge.py
- [ ] T036 [US3] Add capability-matrix and shared-owner smoke postconditions tests/installer/test_installer_smoke_postconditions.py
- [ ] T037 [US3] Extend integration contract tests for persisted scope-aware decisions tests/integrations/test_contract.py
- [ ] T038 [P] [US3] Extend shared-resource lifecycle and last-consumer teardown tests tests/integrations/test_lifecycle_block_rendering.py
- [ ] T039 [P] [US3] Extend organization materialization tests for the shared instruction resource tests/integrations/test_org_materialization.py
- [ ] T040 [P] [US3] Extend legacy parity tests for supported and exception paths tests/integrations/test_parity_with_legacy_installer.py
- [ ] T041 [US3] Measure the release-gated synthetic before-and-after result docs/releases/v4/v4.17/development/claude-agents-evaluation.md
- [ ] T042 [US3] Stabilize Phase 4 on both installers, record CI impact and history, and create one local commit docs/releases/v4/v4.17/development/phase-4-evidence.md
- [ ] T043 Run the architecture refactor scan and quote it docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T044 Reconcile known gaps and derived counts docs/releases/v4/v4.17/known-gaps.md
- [ ] T045 Verify living documentation architecture and refresh the dashboard docs/todos.md
- [ ] T046 Report git-tree and repository-settings hygiene docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T047 Reconcile the pipeline and prove cross-installer parity docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T048 Run the Tier 3 deep pass and quote its terminal result docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T049 Answer the seven behavioral Goal-review questions docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T050 Write the last-phase-only human testing suggestions docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T051 Run the full local profile green at one revision and create the planned Phase 5 commit docs/releases/v4/v4.17/development/last-phase-evidence.md
- [ ] T052 Perform one initial publication, record any corrective re-push, and open the integration pull request docs/releases/v4/v4.17/development/last-phase-evidence.md
