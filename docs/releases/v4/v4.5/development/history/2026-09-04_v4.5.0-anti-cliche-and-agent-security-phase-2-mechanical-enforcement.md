# Session History - v4.5.0 Phase 2: Mechanical Enforcement Across All Twelve Templates

**Date**: 2026-09-04
**Plan**: `docs/releases/v4/v4.5/plans/v4.5.0-anti-cliche-and-agent-security.md`, Phase 2 of 7
**Driver**: `/implement v4.5.0-anti-cliche-and-agent-security in-full`, worktree `nh-v45`, branch `feat/v4.5.0-anti-cliche-and-agent-security`
**Model routing**: plan asked strong / high; session ran `claude-fable-5-1` (frontier) at high, the same or stronger tier, so no change.

## Outcome

The Writing Discipline block can no longer drift or vanish from any platform without a test failing. The parity guard now requires the heading and byte-locks the body across the five lockstep templates, and a companion validator holds the seven unguarded templates to the same body, forbids the block in the four include-only shims, and asserts the self-check clause that binds live replies. Every negative case was observed failing on the real tree and restored, not reasoned about.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T006 | `"Writing Discipline"` added to `REQUIRED_HEADINGS` and `INVARIANT_SECTIONS` with the v4.5.0 rationale comment in the existing style, referencing the companion validator. In the same deliberate edit the file's two pre-existing ruff findings (`UP035`, `UP045`) were fixed and it was formatted, closing `WN-1`; the whole diff is 15 insertions, 3 deletions. | `scripts/check_base_template_parity.py` |
| T007 | Companion validator modelled on `test_construction_discipline_rule.py`: presence in all twelve (parametrized), body identical across twelve, absent from the four shims (which must carry an `@base-` include), self-check clause present by stable substring, non-empty body capped at six lines, ASCII only, and the guard's two lists asserted through the imported module rather than its source text | `tests/validators/test_writing_discipline_rule.py` |
| T008 | Collection confirmed: `make validate` runs the parity guard; CI's `tests` job runs the whole `tests/` tree through `scripts/ci/run.py --profile full`, and `tests/workflows/test_ci_runs_every_repo_test.py` (4 passed) holds that property, so the new module is collected with no workflow change. Installer impact: none. `templates/ai-instructions/` is already distributed and the validator is a test under `tests/`, not a script under `scripts/`, so `DEV_ONLY_SCRIPTS` does not apply; the installer smoke suite passes unchanged. | none (verified, no edit) |
| T009 | The four temporary tests appended to the parity test module in phase 1 were removed and replaced by two negative parity cases (a one-word reword in `base-gemini.md`, a renamed heading in `base-codex.md`), so the invariant lives in one permanent place | `tests/validators/test_check_base_template_parity.py` |

## Negative cases observed (the point of this phase)

| Mutation (real tree, restored after) | Observed |
|---|---|
| Delete the block from `base-cursor.md` | guard exit 1: `DRIFT: base-cursor.md: missing required section heading: 'Writing Discipline'` |
| Change one word in `base-opencode.md` | guard exit 1, naming `base-opencode.md` and `Writing Discipline` |
| Remove the block from unguarded `base-qwen.md` | validator: 3 failed, 29 passed |
| Reword one phrase in unguarded `base-aider.md` | validator: 1 failed, 31 passed (identity test) |
| Paste the block into shim `base-gemini-cli.md` | validator: 1 failed, 31 passed (include-only test) |
| Drop the self-check clause from all twelve | validator: 12 failed, 20 passed (parametrized self-check test) |

After every mutation `git status` showed 0 changed template files and the validator returned to 32 passed.

## Plan delta

**Disposition: No delta.**

Evidence: every sub-task landed as specified, the precedent the plan named (`Construction Discipline` and its test) fit without adaptation, and the 2.3 assumption that no installer or workflow change is needed was verified rather than assumed (CI collects the whole `tests/` tree; the validator is not a distributed script). The plan's Stability Gate was met exactly: guard fails on removal or alteration in any lockstep file, validator fails on the seven others, both pass on the clean tree, `make validate` runs the guard and CI's `tests` job runs the validator.

Consequence for remaining phases: none. Phase 1's word-ceiling delta stands as recorded; this phase added no template text.

## Verification

- `python scripts/check_base_template_parity.py`: pass on the clean tree; fails as tabled above under mutation.
- `python -m pytest tests/validators/test_writing_discipline_rule.py tests/validators/test_check_base_template_parity.py tests/validators/test_construction_discipline_rule.py`: 66 passed.
- Full `validate` target (35 commands): 0 failed.
- `ruff check` and `ruff format --check`: clean on all three Python files touched.
- `tests/workflows/test_ci_runs_every_repo_test.py`: 4 passed (CI collects the new module).
- `catalog/hooks/tests/test_installer_smoke.py`: 33 passed (unchanged; the validator is not a distributed script).
- Post-phase 8.2 re-run of the full validators tier: 1173 passed, 2 skipped (3m06s).
- Functional smoke (gate 5): the phase's feature is the guard failing on drift, and the six real-tree mutations above are that exercise; expected and observed behavior matched in every row.

## CI impact record (8.3)

No new command, dependency, environment variable, or artifact. One new test path (`tests/validators/test_writing_discipline_rule.py`) in a tree CI already runs whole. No pipeline file changed; reconciliation is phase 7 duty 5.

## Deviations

None marked `# DEVIATION:`. The WN-1 lint fixes in the parity script were planned by that gap's own suggested next step, not a deviation.

## Next

Phase 3: extend the `anti-slop-editing` catalog with the roughly twenty uncovered cliche patterns, including the mannered-prose cluster with its vendor-supplied definition, in a new `references/cliche-patterns.md`. Plan asks frontier / max; the session will surface the `/effort max` keystroke at the boundary.
