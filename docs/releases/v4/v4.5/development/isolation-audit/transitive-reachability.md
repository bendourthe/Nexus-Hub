# v4.5 DF-2 Transitive-Reachability Assessment

This record answers what the repository's current local agent and CI configurations can send to, and where a true allowlist-union proof stops. It is a bounded assessment for the maintainer, observed at 2026-09-24 19:51 UTC. No internal network was scanned, credential value was inspected, or egress policy was changed.

| Executor or path | Direct destination evidence | Downstream reach and limit | Verdict |
|---|---|---|---|
| Local Windows agent shell | An authenticated `gh api` request reached GitHub during this audit. The active task environment permits network access; no out-of-process egress proxy or destination allowlist was evidenced in the inspected configuration. | The token's permitted GitHub operations and any further destinations available to the host were not enumerated. A successful GitHub call proves at least that route, not a finite boundary. | No certified allowlist or transitive union. |
| `nexus-web-fetch` MCP | The registry declares user-specified outbound HTTP(S) URLs, with in-process SSRF guards that block local and private ranges by default; a user YAML override is possible. | An arbitrary allowed public endpoint may itself link or redirect to other services. The guard is useful input validation, but it is inside the agent-invoked process, not the independent proxy required by the isolation skill. Runtime egress was not exercised in this audit. | Public egress is configured; downstream union not bounded. |
| GitHub-hosted CI runners | Workflows use `ubuntu-latest` and `windows-latest`, install packages and Chromium, and upload reports or release artifacts. | The repository YAML does not provide a network namespace, external proxy allowlist, or host-level routing map for GitHub's runner infrastructure. GitHub services, package endpoints, and their downstream reach cannot be reduced to a proved finite set from YAML. | Runner transitive reach unverified. |
| GitHub Actions cache and artifact services | Local authenticated repository API calls returned 75 caches and 411 artifacts. | GitHub's branch and repository permissions define who can restore or download bytes; the service's own infrastructure reach is not inspectable from this repository. | Shared-service read/write scope is assessed separately; network union remains unknown. |

The real reachable set includes at least the successful GitHub API route; public-web fetch and CI dependency routes are configured but were not exercised for this audit. No out-of-process destination allowlist was evidenced for local or CI execution, so the skill's instruction to enumerate every allowlisted destination has no finite input set here. Calling the union complete would imply an unevidenced boundary. Local RFC-1918, loopback, management-plane, DNS, proxy, and credential-mediated routes remain untested or unknown; this record does not infer either reachability or denial for them.

## Required disposition

DF-2 remains open. To establish a pass, first choose and implement per-session executor and credential boundaries, force outbound calls through an independent egress mediator, and define a reviewable allowlist. Then enumerate each allowed destination's own reachability, record the union, and back the proxy with network segmentation. This changes the development environment and security posture; the current read-only audit does not authorize or perform it.
