# Session History - v4.2.3 Phase 5: Cheatsheets single-column scopes and terminal usage

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.3-guide-refinement.md`
**Phase**: 5 of 7

## Subtasks

- **5.1 Single column and terminal usage** (T020): `.cs-scopes` dropped its `repeat(auto-fill, minmax(300px, 1fr))` grid for a single column, so a reader scans straight down instead of across columns. Every one of the 21 commands gained a small terminal captioned "type it like this", showing a real invocation with the shared `.term` chrome - reused rather than reinvented, so the guide has one terminal language across Home, Training, and Cheatsheets. Inside each, the invocation convention colours the command in accent and its argument in plain ink, which is what makes the ordering legible: the scope visibly follows the command rather than floating as a bare token. Copy affordances inherit Phase 1's rules, so the inline cells use the bare icon and the terminal rows keep the labelled button, matching Home.

## Deviations

- None. The transformation was scripted across all 21 command entries rather than hand-edited, so no entry could be missed.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **73 passed, 1 skipped**.
- New Phase 5 tests: `.cs-scopes` carries no `grid-template-columns`; every command has a `cs-usage` block and a `term--mini`; the old `cs-run` row is gone; every example with an argument splits it into `.inv-cmd` and `.inv-arg`; and copy payload parity survives the split markup.
- The scope anti-drift guard from v4.2.2 still passes, so no scope token was paraphrased during the restructure.
- Rendered QA: `renders/v423-phase-5/`, plus a close crop of a single command entry to confirm the scope list and terminal read clearly at real size.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, environment variables, test paths, or artifacts. Render PNGs only.

## Known gaps

- None new.

## Next steps

- Phase 6: cross-page accessibility re-verification after this release's colour and layout changes, hallmark audit, and the README update.
