# Session History - v4.2.2 Phase 4: Training rebuild

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.2-guide-cinematic-rebuild.md`
**Phase**: 4 of 7

## Subtasks

- **4.1 Narrative and scene data** (T011): `development/guide-rebuild/training-script.md` written, and `guides/website/example/training-scenes.json` redesigned around a learner-facing walkthrough. Each of the eight scenes now carries `title`, second-person `intent`, `command`, `tools`, an `output` line list written as the assistant would report it, `editor`, `artifact`, `gate`, `beats`, and a new `booth` block (`captured`, `fixed`, `shuffle`, `sparkle`, `highlight`, `note`) that drives the mockup. Bugs stay visible through `/review` and `/plan`; `fixed` flips at `/implement` and stays true.
- **4.2 Glow Booth mockup** (T012): a self-contained interactive booth - stage with tinted pose cards, five pose buttons, stamp pills and counter, Restart, mode line, and a live region. It re-implements `example/glow-booth/logic.js` faithfully, including both frozen bugs: `computeStamps` walks `captured.length - 1` while `fixed` is false, and Restart keeps `lastPose` while `fixed` is false.
- **4.3 Simulated terminal** (T013): install-terminal chrome, pre-filled command with a slim copy button, Run affordance, typed echo then staggered output lines. Every line is written with `textContent`; the engine assigns `innerHTML` nowhere. Running mid-flight completes the previous output instantly instead of interleaving. Reduced motion prints instantly.
- **4.4 Sequencer and slide mode** (T014): Previous / Next / Restart / Outline, per-scene progress bars, step-and-beat counter, `#training/<scene>?beat=n` deep links with clamping, and a Present mode that requests fullscreen on the training root and applies a viewport overlay class either way, so a denied or unavailable Fullscreen API still presents. Esc, `fullscreenchange`, and the toggle all restore normal flow.

## Verification beyond the static suite

Drove the page in headless Chromium (12 interaction checks, all passed):

- buggy booth: five poses -> counter reads **4**; Restart leaves a pose on stage
- fixed booth: five poses -> counter reads **5**; Restart clears the stage
- terminal echoes `> /describe full`, renders `<img onerror>` as literal text, and injects **0** elements from fixture data
- `syncFromHash('nope-not-a-scene', 99)` clamps to a valid step; `('test', 42)` clamps to beat 2 / 2
- ArrowRight advances a beat; Present applies the overlay in headless (where fullscreen is denied); Escape exits

## Deviations

- None. The plan's "no ninth command" rule is preserved: `/compare` still teaches the shuffle adoption without adding a scene.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **50 passed, 1 skipped, 0 xfailed**. Both strict xfails are now un-xfailed and passing.
- New Phase 4 tests: walkthrough fields on every scene, bug-visibility ordering (`fixed` false before `/implement`, true after), frozen-bug simulation present in the engine, booth + terminal + present-mode + outline markup, deep-link clamping, and the textContent/no-innerHTML contract.
- Two assertions were corrected as TEST-class findings, not implementation changes: the innerHTML check now matches an assignment rather than the word (a code comment tripped it), and the overlay check became an allowlist so Present mode's deliberate, user-invoked fullscreen overlay is permitted while Foundations still has none.
- Inline scene JSON re-synced from the example file; parity test green.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, env vars, or test paths. Guide is 350 KB against a 500 KB budget. Phase-4 render PNGs added as evidence.

## Next steps

- Phase 5: Cheatsheets rebuild (intent-named sections, per-scope descriptions derived from `catalog/commands/`).
