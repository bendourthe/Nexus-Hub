# Development Log: v4.2.0 Phase 5 - Interactive IDE Training Workbench

**Date**: 2026-08-29
**Operator**: maintainer
**Assisted by**: Cursor Grok 4.6
**Objective**: Replace the 31-slide deck with one data-driven IDE workbench for the closed eight-scene set.
**Outcome**: `training-scenes.json` is the maintainer source of truth; the guide inlines it; `applyState` paints explorer, editor, assistant, terminal, artifact, timeline, and gate via `textContent`.

---

## 1. Starting State

- **Branch**: `feat/v4.2.0-interactive-guide-redesign`
- **Starting tag/commit**: `800b221a` (Phase 4)
- **Environment**: Windows 11, PowerShell, Python, pytest, Node 24 for `node --check`. No browser tools (DF-1).
- **Prior session reference**: `docs/releases/v4/v4.2/development/history/2026-08-29_v4.2.0-interactive-guide-redesign-phase-4-foundations.md`
- **Plan reference**: `docs/releases/v4/v4.2/plans/v4.2.0-interactive-guide-redesign.md`

Model-routing: plan says frontier / max. Session remains `cursor-grok-4.6`. No downshift.

---

## 2. Chronological Steps

### 2.1 Scene fixtures

**Plan specification**: T019.

**What happened**: Authored eight scenes from Trivia Quiz (`logic.js` off-by-one and restart leak), `quiz-shuffle-reference` shuffle, and current command names. Hostile `<img onerror>` and `</script>` live in the describe narration. Inline copy is structurally equal to the JSON file. `</script>` is encoded as `<\/script>` in the HTML script block.

**Key files changed**: `guides/website/example/training-scenes.json`, `guides/website/nexus-hub-guide.html`

### 2.2 Workbench

**Plan specification**: T020.

**What happened**: Removed `.ts-slide` markup. Added a namespaced `wb-` shell. `applyState` is idempotent and writes every pane from the scene snapshot. No `innerHTML` for fixture strings.

### 2.3 URL and keys

**Plan specification**: T021.

**What happened**: `#training/<scene-id>` plus `?beat=n` via `replaceState`. Scene changes use `location.hash` (history entry). Invalid scene/beat clamps to describe beat 0. Keys disengage inside `[data-nhg-keys=self]` panes. No timers, so no interval cleanup beyond "do not start them". No autoplay, speed, or fullscreen.

### 2.4 Tests

**Plan specification**: T022.

**What happened**: Removed Phase 5 xfail markers. Added inline-equals-file, no slide markup, hostile-string, and textContent contract tests. `node --check` on the extracted controller. 27 passed. CI IMPACT: existing `pytest tests` job; no new workflow. File size ~420KB with excerpts, not video. No size known-gap.

**Verification**: `python -m pytest -q tests/guides/test_nexus_hub_guide.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused guide tests | PASS (27 passed) |
| `node --check` workbench JS | PASS |
| Browser visual QA | NOT RUN (DF-1) |

---

## 4. Known Issues

DF-1 remains. Unused `.nht` CSS still in the file (no slide elements).

---

## 5. Plan Discrepancies

- Compare scene records shuffle as the adopted outcome and states that a follow-on `/implement` is the same command, not a ninth scene, per the content-map decision.
- Dead `.nht` rules were left in place rather than risking a large CSS delete in the same commit as the controller.

---

## 6. Assumptions Made

- Two beats per scene is enough for the workshop loop; more beats can be added in the JSON later without a new page model.

---

## 7. Testing Summary

### Automated Tests

- `tests/guides/test_nexus_hub_guide.py`: 27 passed
- `node --check` on extracted workbench controller: passed

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session

- [x] T019-T022

### Remaining

- Phases 6-7

### Deferred

- Browser workbench QA and 25px projector check (DF-1)

---

## 9. Summary and Next Steps

Phase 5 is the training surface. Next: Phase 6 maintainer README, publication check, and product-currency rows for `/org` and `/tune-prompting`.
