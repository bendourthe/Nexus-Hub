# Session History - v4.2.3 Phase 2: Home install verify and animated comparison

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.3-guide-refinement.md`
**Phase**: 2 of 7

## Subtasks

- **2.1 Install verify block** (T006): replaced the dense wrapped callout with a two-step ordered sequence. Step one runs either verification command (each in its own copy cell on its own line, not two commands jammed mid-sentence); step two restarts the assistant, with the reason stated. Both `.vs-do` and `.vs-note` use the same 15 px body size, so spacing and a numbered marker carry the hierarchy rather than font-size changes. The old `.verify-callout` rules were deleted, not orphaned.
- **2.2 Animated comparison** (T007): replaced the five-row `.nhg-compare` table. Each of the five concerns is now a row reading without-then-with: a dashed amber-tinted statement, a connector that draws, then a solid accent statement that fades up. Rows stagger on entry via the shared reveal observer. It is deliberately neither a two-card grid nor a stat-pill row, both of which are hallmark gates the v4.1.2 baseline failed. Under 720 px the pair stacks and each side gains its own label via CSS `::before`, so the without/with reading survives without the header row.

## Deviations

- None.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **60 passed, 1 skipped**.
- New Phase 2 tests: the verify block is exactly two numbered steps with matching type sizes and both copy payloads intact; the comparison has five rows, orders `cmp-a` before `cmp-b`, carries the staggered entry and connector-draw animations, and has a reduced-motion static state for every animated class.
- One test earned its keep immediately: `test_install_verify_is_a_two_step_sequence` failed on the first run because the old `.verify-callout` CSS was still in the file after its markup was gone. Dead rules removed.
- Rendered QA: `renders/v423-phase-2/`, reviewed at dark 1440 and light 420. Mobile stacking verified.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, environment variables, test paths, or artifacts. Render PNGs only.

## Known gaps

- None new.

## Next steps

- Phase 3: Foundations. Note for that phase: Home's closing heading still says "run it on your repo", which the project-generic pass must catch.
