# Development Log: v4.2.0 Phase 6 - Maintainer Docs and Copy Contract

**Date**: 2026-08-29
**Operator**: maintainer
**Assisted by**: Cursor Grok 4.6
**Objective**: Document the redesigned guide and give maintainers a Nexus-Hub-local publication check that does not require the sibling portfolio.
**Outcome**: `guides/website/README.md` matches Home, Foundations, and the IDE workbench. Every catalog command is a Training scene, a Reference row, or a documented decline. The publication check skips the portfolio copy when `NEXUS_HUB_PORTFOLIO_ROOT` is unset.

---

## 1. Starting State

- **Branch**: `feat/v4.2.0-interactive-guide-redesign`
- **Starting tag/commit**: `866a9684` (Phase 5)
- **Environment**: Windows 11, PowerShell, Python, pytest. No browser tools (DF-1). No `NEXUS_HUB_PORTFOLIO_ROOT`.
- **Prior session reference**: `docs/releases/v4/v4.2/development/history/2026-08-29_v4.2.0-interactive-guide-redesign-phase-5-workbench.md`
- **Plan reference**: `docs/releases/v4/v4.2/plans/v4.2.0-interactive-guide-redesign.md`

Model-routing: plan says strong / high. Session remains `cursor-grok-4.6` (frontier). No downshift. Cursor has no scriptable model switch.

---

## 2. Chronological Steps

### 2.1 Copy contract and publication check

**Plan specification**: T023.

**What happened**: Documented canonical source `guides/website/nexus-hub-guide.html`, destination `<portfolio-root>/nexus-hub/index.html`, byte-identical copy with an allowlisted favicon head delta, hand copy or a later portfolio-side script, and that this release does not add `scripts/sync-nexus-hub-guide.mjs`. Extended `tests/guides/test_nexus_hub_guide.py` with a self-contained offline check and an env-gated sibling diff that skips when the env var is unset. The check never fetches the guide from the network.

**Key files changed**: `guides/website/README.md`, `tests/guides/test_nexus_hub_guide.py`

### 2.2 Maintainer README and living dashboard

**Plan specification**: T024.

**What happened**: Rewrote `guides/website/README.md` for concise Home with install, platform reachability, model-versus-harness Foundations, the eight-scene workbench, keyboard controls, fixture maintenance, browser verification, command inventory, and the copy contract. Removed Guided-tour / 31-slide / fullscreen-control / Installation-nav instructions from the website README. Updated the leftover `.nht` CSS comment in the guide so it no longer describes a live slide-show. Refreshed `docs/todos.md` to 6/7.

**Key files changed**: `guides/website/README.md`, `guides/website/nexus-hub-guide.html`, `docs/todos.md`, `docs/releases/v4/v4.2/development/guide-redesign-content-map.md`

### 2.3 Product-currency gate

**Plan specification**: T025.

**What happened**: Added Reference rows for `/org`, `/tune-prompting`, `/commit`, `/commands`, and `/constitution`. Added a test that every `catalog/commands/*.md` basename is a Training scene id, a Reference token, or listed in the README / content-map. Focused tests: 30 passed, 1 skipped (portfolio copy). Did not run Lighthouse. Did not require the sibling `build-site.mjs`.

**Verification**: `python -m pytest -q tests/guides/test_nexus_hub_guide.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused guide tests | PASS (30 passed, 1 skipped) |
| `python scripts/check_docs_conventions.py` | PASS |
| `python scripts/validate_unicode_safety.py --strict` | PASS |
| Full `make validate` | NOT RUN as a merge gate; Phase 6 did not change catalog JSON or installers |
| Browser visual QA | NOT RUN (DF-1) |
| Portfolio byte-diff | SKIPPED (`NEXUS_HUB_PORTFOLIO_ROOT` unset) |

---

## 4. Known Issues

DF-1 remains. Unused `.nht` / `.ts-` CSS remains in the HTML; last-phase refactor may delete it.

---

## 5. Plan Discrepancies

- Full `make validate` was not executed; Phase 6 added no catalog, installer, or workflow files. Focused tests plus docs-convention and unicode guards were used instead.
- Root `README.md` still says "click-through walkthrough" / simulated VS Code. The plan named `guides/website/README.md` and the guide itself as owned stale copy; the root blurb is left for Phase 7 living-docs if the Goal review requires it.

---

## 6. Assumptions Made

- CI will not set `NEXUS_HUB_PORTFOLIO_ROOT`, so the optional copy leg stays skipped on Nexus-Hub-only checkouts.
- An allowlisted favicon-only head delta is the only permitted drift when the env var is set.

---

## 7. Testing Summary

### Automated Tests

- `tests/guides/test_nexus_hub_guide.py`: 30 passed, 1 skipped

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session

- [x] T023-T025

### Remaining

- Phase 7

### Deferred

- Browser QA and workshop study (DF-1)
- Optional deletion of dead `.nht` CSS

---

## 9. Summary and Next Steps

Phase 6 is the maintainer contract. Next: `/implement v4.2.0-interactive-guide-redesign in-full` continues at Phase 7 (last phase: evidence file, local gate, publish with approval, integration PR to `develop`).
