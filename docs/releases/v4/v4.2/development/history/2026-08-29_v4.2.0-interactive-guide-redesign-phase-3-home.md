# Development Log: v4.2.0 Phase 3 - Concise Home and Embedded Installation

**Date**: 2026-08-29
**Operator**: maintainer
**Assisted by**: Cursor Grok 4.6
**Objective**: Let a first-time visitor understand, compare, install, and start the guide without a separate Installation page.
**Outcome**: Home is definition, comparison, one-command install, platform reachability, six-node ribbon, and next actions. `#page-setup` is gone from markup and routing.

---

## 1. Starting State

- **Branch**: `feat/v4.2.0-interactive-guide-redesign`
- **Starting tag/commit**: `5c34aa0e` (Phase 2)
- **Environment**: Windows 11, PowerShell, Python, pytest. No browser tools (DF-1).
- **Prior session reference**: `docs/releases/v4/v4.2/development/history/2026-08-29_v4.2.0-interactive-guide-redesign-phase-2-shell.md`
- **Plan reference**: `docs/releases/v4/v4.2/plans/v4.2.0-interactive-guide-redesign.md`

Model-routing: plan says standard / medium. Session remains `cursor-grok-4.6` (frontier). Cursor has no scriptable switch. No downshift.

---

## 2. Chronological Steps

### 2.1 Rewrite the Home narrative

**Plan specification**: T011.

**What happened**: Replaced the marketing catalog (stale counts, equal feature cards, world-experts cadence) with two short definition paragraphs and a five-row comparison (portability, reusable procedures, deterministic enforcement, durable artifacts, team consistency). Building-block vocabulary is a disclosure.

**Key files changed**: `guides/website/nexus-hub-guide.html`

### 2.2 Fold installation into Home

**Plan specification**: T012.

**What happened**: Canonical curl and irm commands sit on Home with OS tabs. `data-copy` is on a `<code>` whose text equals the payload. wget and flags are in "Need another option?" plus Reference. Platform slash reachability and `/skills list` / `/commands` verify live on Home. Untrusted-origin warning is visible until JS hides it on localhost or `bendourthe.github.io`. `file://` keeps the warning. `#page-setup` removed. `#home/install` scrolls to the install block. Copy success is announced on `#nhg-copy-status`.

**Key files changed**: `guides/website/nexus-hub-guide.html`

### 2.3 Six-node ribbon

**Plan specification**: T013.

**What happened**: Map and evaluate, Plan, Implement, Harden, Ship, Document. Selecting a node reveals one sentence and a `data-go` link. No autoplay. Communicate / `/presentify` is present.

### 2.4 Tests

**Plan specification**: T014.

**What happened**: Removed Phase 3 xfail markers. Copy-payload test required moving `data-copy` off the wrapper that contained the `$` prompt span. 17 passed, 6 xfailed (Phases 4-5). CI IMPACT: existing `pytest tests` job; no new workflow. Browser QA still DF-1.

**Verification**: `python -m pytest -q tests/guides/test_nexus_hub_guide.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Focused guide tests | PASS (17 passed, 6 xfailed) |
| Browser visual QA | NOT RUN (DF-1) |

---

## 4. Known Issues

DF-1 remains: no rendered screenshots this session.

---

## 5. Plan Discrepancies

- Copy buttons append into the `<code data-copy>` node so the static parser's visible text equals the payload (the `$` / `>` prompt stays outside).
- `#home/install` is a Home subpath, not a new page id.

---

## 6. Assumptions Made

- Documented trusted origins are `localhost`, `127.0.0.1`, and `bendourthe.github.io`. Other https hosts keep the warning.

---

## 7. Testing Summary

### Automated Tests

- `tests/guides/test_nexus_hub_guide.py`: 17 passed, 6 xfailed

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session

- [x] T011-T014

### Remaining

- Phases 4-7

### Deferred

- Browser Home QA (DF-1)

---

## 9. Summary and Next Steps

Phase 3 is the onboarding surface. Next: Phase 4 Foundations (model versus harness, user-initiated comparison, handoff copy "Now watch the experience layer work").
