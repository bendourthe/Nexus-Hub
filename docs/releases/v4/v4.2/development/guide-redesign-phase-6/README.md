# Phase 6 evidence - Maintainer docs and copy contract

**Date**: 2026-08-29

Canonical source remains `guides/website/nexus-hub-guide.html`. `guides/website/README.md` documents Home, Foundations, the IDE workbench, the eight-scene set, keyboard controls, fixture maintenance, browser verification, and the copy path to `<portfolio-root>/nexus-hub/index.html`. This release does not add a portfolio-side sync script.

Reference now includes `/org`, `/tune-prompting`, and explicit alias rows. Product-currency and publication checks live in `tests/guides/test_nexus_hub_guide.py`. The sibling portfolio copy is optional via `NEXUS_HUB_PORTFOLIO_ROOT`.
