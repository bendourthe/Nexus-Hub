# Development Log: v4.1.0 Phase 6 - Terminal Reconciliation

**Date**: 2026-08-28
**Operator**: maintainer
**Objective**: Reconcile architecture, known gaps, CI/CD, installer parity, and the plan goal against the final tree.
**Outcome**: Architecture and docs require no moves; prior goal-review misses are corrected; CI coverage and installer parity are complete; the final full-profile result is recorded in `development/last-phase-evidence.md`.

---

## 1. Starting State

- **Branch**: `feat/v4.1.0-adoption-skill-trial-records-and-low-evidence-ts`
- **Starting commit**: `fb48aed8` (`docs(decisions): decline oxlint vendor surface`)
- **Prerequisite**: Phases 1 through 5 locally committed
- **Plan reference**: `docs/releases/v4/v4.1/plans/v4.1.0-adoption-skill-trial-records-and-low-evidence-ts.md`

---

## 2. Terminal Corrections

The independent Goal-vs-codebase review found that Phase 2's ownership checklist contradicted valid runtime parsers and that TypeScript Tier-3 examples still taught low-evidence dictionaries and assertions. Ownership now distinguishes boundary narrowing from downstream uncertainty; the complete TypeScript reference tree is guarded so every non-const assertion requires an adjacent `SAFETY:` invariant.

The review also found that Phase 4 documented raw-memory dispatch without an executable creation path and scored any present directory as a run. The existing optimizer executable now resolves eval-relative sources, dispatches the optional arm through its existing CLI branches, writes response metadata, skips missing or undecodable sources, and leaves grading to the established grader. Aggregation labels incomplete, mismatched, or failed artifacts invalid and excludes them from metrics.

The historical v4.0 README count was restored to 325 while the live repository count remains 326. Browser QA then exposed a narrow-viewport source-path overflow; the viewer now inserts safe separator breakpoints and constrains long components without changing result layout.

---

## 3. Architecture and Known Gaps

The docs-layout inventory and reference graph found no v4.1 placement or reference defect. Repository structure scans found only intentional distribution copies, package boundaries, ignored/generated workspaces, host-local directories, and declared taxonomy placeholders. No tracked move or deletion was proposed.

The v4.1 ledger is finalized with one warning: the remote GitHub description still says 324 skills. v3.21 DF-2 is resolved on its original ledger; v3.21 DF-1 and the plan-explicit v3.20 follow-ups remain on their owning ledgers.

---

## 4. CI/CD and Installer Coverage

The full CI profile lists catalog, security, workflow, platform-contract, documentation, version, hook, repository, six extension, and compression gates. Current workflow classification, cancellation, and caching already cover the changed surfaces without a required-check path-filter hazard. No workflow or Oxlint job was justified.

The first full run found that extension commands inherited ambient Python import state. That made code-search and web-fetch unavailable from a bare `src` checkout and made context-compressor lose its sibling AST dependency. The repository-native profile now isolates every extension command to its local `src` tree and gives context-compressor the code-search source it deliberately reuses. A CI engine regression test locks the path contract; the corrected extension profile passed all seven commands.

The next full run proved that correction but exposed insufficient timeout headroom in `repo-tests`: 41 commands passed and the repository suite alone reached the 3,600-second ceiling. Its prior passing duration was 3,341.7 seconds, so the command-specific ceiling is now 4,500 seconds with a CI engine contract assertion. This changes no test selection or assertion behavior.

`check_installer_parity.py` passed, platform-contract verification covered 10 platforms, and required-check auditing found 10 unconditional contexts across 2 protected branches. Recursive skill copying required no installer registration change.

---

## 5. Verification and Handoff

- Focused correction suite: 61 passed.
- Python compilation: PASS for all three eval-loop scripts.
- `git diff --check`: PASS with expected Windows line-ending notices only.
- Independent Goal-vs-codebase review: PASS with no unresolved miss.
- Desktop and mobile browser captures: PASS for three-arm rendering and responsive flow.
- Checkout-local extension profile: 7 commands passed in 62.0 seconds.
- Full repository-native profile: 42 passed, 0 failed, 0 skipped, 0 advisory in 4,723.9 seconds; see `development/last-phase-evidence.md`, Section 8.

No push, pull request, tag, release, or remote setting change was performed. The next step is the explicit publication approval gate after the local Phase 6 commit.
