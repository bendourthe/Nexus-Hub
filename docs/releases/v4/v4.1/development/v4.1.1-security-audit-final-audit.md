# v4.1.1 Security-Audit Final Audit

**Date**: 2026-08-28
**Branch**: `feat/v4.1.1-adoption-openworker-security-refinement`
**Plan**: `docs/releases/v4/v4.1/plans/v4.1.1-adoption-openworker-security-refinement.md`
**Commits on this branch**: `24771ad2`, `7dbcc896`, `b2f8d0c3`, `a94bef54`

## Ownership map

| Concern | Single owner | Notes |
|---|---|---|
| Receipt schema and closure diffs | `security-review` `references/closure-gate-review-record.md` plus `docs/releases/v4/v4.1/development/v4.1.1-security-audit-contract.md` | Gate does not hard-code scanner names |
| Closure gate executable | `catalog/skills/code-review/security-review/scripts/closure-gate.py` | Schema 1 and 2; unknown versions exit 2 |
| Semgrep and gitleaks recipes | `security-review` `references/local-scanner-recipes.md` | Never auto-install; gitleaks redacts values |
| OSV-Scanner, npm audit, pip-audit, Trivy vuln | `dependency-security-audit` | Reachability handoff preserves severity |
| Trivy config and Checkov | `cloud-security-posture-detection` | `NOT_APPLICABLE` without IaC; never apply |
| Ordered workflow sequence | `agent-presets` `security-audit` preset, aligned with `data/workflows.json` id `security-audit` | Detection before remediation; re-scan before close |
| Remediation | `security-patch-advisor` | User approval required |
| Independent verification | `security-reviewer` plus `adversarial-verifier` | Read-only; fixer cannot be the only verifier |
| Focused install membership | `security-specialist` bundle | Includes the audit owners after Phase 4 |
| User-facing procedure | `guides/reference/SECURITY_AUDIT.md` | Generic Nexus-Hub terms only |

`pre-commit-checklist` points at gitleaks and does not own receipts.

## Layout audit

`project-refactor` and `docs-layout-refactor` were run in propose-only mode over files this plan touched. No move or deletion is proposed:

- New files sit in existing skill trees, `tests/`, `guides/reference/`, or `docs/releases/v4/v4.1/`.
- Recursive skill copy already delivers `scripts/`, `references/`, and `evals/`.
- No installer copy line was added.
- The untracked Ponytail comparison is outside this plan and was not staged.

`python catalog/skills/code-cleanup/docs-layout-refactor/scripts/audit-docs.py inventory --root docs` exited 0.

`python catalog/skills/code-cleanup/docs-layout-refactor/scripts/audit-docs.py refgraph --root docs` exited 0.

No confirmation gate is requested because nothing is moved or deleted.

## CI/CD comparison (no pipeline mutation)

Provider detected: GitHub Actions (`.github/workflows/*.yml`).

The 23-field existing-pipeline comparison lives in `docs/releases/v4/v4.1/development/last-phase-evidence.md` section 5. Summary:

| Field | Existing evidence | This plan |
|---|---|---|
| Profiles | `scripts/ci/run.py` fast/full/platform/report/release | New tests are under `tests/skills/` and `tests/installer/`; existing `full` already runs them |
| Events | `ci.yml` `pull_request` + `merge_group` only; post-merge is separate | No change |
| Required aggregate | `check_required_check_coverage.py`: 10 contexts, unconditional | No change |
| Permissions | `contents: read` on CI | No change |
| Caching | pip cache on CI jobs | No change |
| Concurrency | cancel superseded PR runs; release and post-merge do not cancel | No change |
| Scanner execution in CI | Not present | Must not be added; recipes are optional local CLIs |
| Installer parity | `check_installer_parity.py` PASS | No copy-line delta |
| Report upload | Human summary on `$GITHUB_STEP_SUMMARY`; artifacts not uploaded | Inherited v4.0 DF-1; declined to reopen |

No pipeline file change is proposed. Silence is not approval; none is requested.

## Goal versus codebase

Plan goal: a local security-audit workflow that proves which deterministic scanners ran, separates remediation from verification, and requires a same-detector post-fix re-scan, without OpenWorker, MCP, hosted scanning, or auto-install.

Satisfied by schema-v2 receipts, per-scanner owners, the ordered preset, role-separated reviewer, inert fixtures, and the generic user guide. Remaining gaps are environment-limited (DF-1, DF-2, QG-1) and remote integration, which this audit does not claim.
