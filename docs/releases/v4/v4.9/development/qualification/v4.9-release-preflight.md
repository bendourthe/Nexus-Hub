# v4.9.0 Release Preflight

Release preparation evidence for maintainers promoting the integrated application-security audit to v4.9.0. This record separates local verification from the later PR, tag, published archive, and back-merge results, which belong in the release PR handoff.

## Scope and integration prerequisite

The approved release starts at develop commit `bd2c896892203ce7a5adf0a103242c1bfe68244a`, the merge of [PR 190](https://github.com/bendourthe/Nexus-Hub/pull/190). Its 28 applicable checks passed, one unrelated Presentify check skipped, and post-merge smoke and provenance passed in [run 34289027085](https://github.com/bendourthe/Nexus-Hub/actions/runs/34289027085). All seven audit phases and 31 tasks are integrated. The separate interactive-handbooks plan remains queued at 0/7 phases and 0/31 tasks.

Release preparation updates the canonical version carriers, approved changelog, README feature summary, DEVLOG index, progress and known-gap records, manifest, and platform verification. The release follow-up corrects two discrepancies discovered by the [same-session platform sweep](v4.9-platform-verification.md): Copilot new-install defaults now use the documented string `disable`, and Antigravity 2 global workflows use `~/.gemini/config/workflows/`. Existing user settings and legacy workflow files are retained. An ownership-aware write protects existing user workflows at the newly corrected destination and still refreshes files recorded as installer-owned.

## Verification

| Gate | Local evidence |
|---|---|
| Platform regression selection | 158 tests passed across Antigravity, defaults seeding, lever declarations, and generated defaults. |
| Installer read paths | 24 tests passed, including rejection of the obsolete Antigravity directory. |
| Workflow ownership correction | 54 adapter, Antigravity, and installer read-path tests passed; real global installation preserves user and legacy files and refreshes an owned file. |
| Broader local checks | 35 catalog, hygiene, interpreter, security, workflow, platform, documentation, and version checks passed. The initial interpreter failure selected an unusable Windows Bash; verification used the installed Git Bash first on the process PATH. |
| Release profile | Three checks passed: version synchronization, platform contract freshness, and release precondition advisory. |
| Platform contracts and parity | All 14 repository contract rows match; installer parity and generated defaults pass. Documentary verification is distinct from native-host execution. |
| Capability notes | All five usage elements pass for security-audit, Local SARIF export, Copilot CLI defaults, and Antigravity 2 global workflows. |
| Unicode | Strict release-artifact scan passes with zero files repaired. |
| Benchmark integrity | Candidate `bd37a8b8e91ba5c28001d068445a6b3e65a97fada223e5c7b1441ab6bdb3506c` verifies with four retained entries. Candidate bytes and private answer archive are unchanged. |

The repository-native generators/checkers cover registry metadata, framework coverage, guide counts, and default configuration derivation. `docs/handbooks/markdown/` has no authored pages, so handbook HTML generation is a documented no-op. The living handbook admission reference is snapshotted under `docs/archives/v4/v4.9/handbooks/`; no pending plan or immutable benchmark is archived or rewritten.

## Review and governance

Six independent lenses reviewed the release diff against `bd2c8968`: correctness, testing, project standards, maintainability, security, and API contract. Testing found the stale Antigravity verification fixture; standards found the living AGENTS path, historical path rewrite, and incomplete Phase 7 exit checkboxes. All were corrected. Security reproduced an unowned-workflow overwrite, then independently verified the ownership-aware correction. No unresolved release-diff finding remains. Findings used deterministic or evidence-based verification at confidence 100, so no confidence promotion or suppression changed their disposition.

Considered but rejected: existing Copilot values are intentionally preserved; Antigravity 1 and Windsurf retain their independent legacy directory contracts; keeping separate IDE and workspace config keys avoids unrelated refactoring; generated hook JSON changed only formatting; old workflow files are deliberately retained; native-host benchmark results are not inferred from fixture tests.

The [final-phase layout inventory](v4.9-layout-public.md) remains the repository-wide disposition baseline. Release changes add only this qualification record and the correctly placed reference snapshot; existing files retain their roles. No duplicate consolidation, broad archival, ignored-file cleanup, or structural rewrite is justified. The Git ignored/tracked audit reports no tracked ignored files. Pipeline files are unchanged: the final-phase reconciliation already supplies the required aggregate and supported-OS installation checks. Release preparation rechecks workflow security and required-check production rather than rewriting the pipeline.

## Advisories and remaining external gates

The local Codex CLI enumerated `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.3-codex`, `gpt-5.2`, and `codex-auto-review`; the profile check reports DRIFTED against the stored cross-provider roster. This is a CLI-scoped discrepancy, not evidence of global removal. The separate tier map was refreshed from the official Anthropic, OpenAI, Google, and Cursor model pages on 2026-09-08; all mapped families remain listed and tier placements remain unchanged operator judgments. No prompting-profile freshness marker was fabricated.

Native-provider benchmark attempts remain unavailable and informational. The private synthetic cleanup residue remains ignored after an automatic approval review rejected cleanup. These limits do not establish native-host execution or accuracy claims.

Before publication, the parent must stage the final files, regenerate the manifest from staged bytes, and verify an extracted Git archive. Protected integration and release PR checks, exact main SHA, annotated tag, GitHub Release, downloaded-archive verification, and main-to-develop ancestry are recorded in the subsequent external handoff rather than claimed here.

## PR 191 correction

The first hosted Linux run, [34295151959](https://github.com/bendourthe/Nexus-Hub/actions/runs/34295151959), passed 4,825 repository tests and skipped 260 but failed the existing newest-model-map-note regression. The refresh note omitted the explicit Astra identifier and second official guide URL required by that contract. A local reproduction confirmed one failure and two passes. The note now records the retained `gpt-6-astra` frontier judgment and both fetched official sources; model IDs and tier assignments are unchanged. Affected model-map checks, manifest regeneration, and staged-archive verification are repeated before corrective publication.
