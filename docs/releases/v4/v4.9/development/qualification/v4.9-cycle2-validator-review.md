# Cycle 2 Unicode Validator Review

This bounded independent review covers the current diff in `scripts/validate_unicode_safety.py` and `tests/validators/test_validate_unicode_safety.py`: native Windows long-path conversion at reads, writes, traversal, exclusions, root admission, and display. No runtime source was edited by this reviewer.

**Verdict**: PASS for the reviewed change. Zero findings.

## Verification

- `python -B -m pytest -q tests/validators/test_validate_unicode_safety.py --disable-warnings --tb=short`: 37 passed, one skipped, exit 0, in 8.68 seconds. The skip is `test_fix_preserves_executable_bit`, explicitly limited to POSIX file modes.
- The new real long-path test passed scanning, explicit subtree exclusion, repair, relative explicit-file selection, an absolute explicit-file selection, and a long directory used as the scan root. It confirmed the unsafe character was detected as content rather than an I/O error and the repaired file had the expected contents.
- Six independent lexical conversion checks passed: ordinary drive path, ordinary UNC path, already-extended drive path, already-extended UNC path, relative parent-segment path, and already-extended parent-segment path. Every result was idempotent; UNC conversion used the UNC namespace form, existing prefixes were preserved, and tested parent segments normalized away. These checks did not access a network share.
- Six independent real CLI exclusion checks passed: root-relative target, absolute target, already-extended absolute target, case-varied target, a prefix-sibling directory, and a relative root argument. The intended subtree was excluded in the five equivalent routes; the prefix-sibling directory remained scanned and returned the expected finding exit code.
- `git diff --check -- scripts/validate_unicode_safety.py tests/validators/test_validate_unicode_safety.py` passed. Git reported ordinary configured line-ending conversion notices only.

## Considered but rejected

| Candidate | Actual route and observed reason |
|---|---|
| Reapplying conversion duplicates a native prefix or corrupts UNC spelling. | Direct `filesystem_path` calls covered ordinary and already-extended drive/UNC strings. All six lexical controls were idempotent, and the UNC result used the correct namespace form. |
| Prefix conversion makes equivalent paths bypass `--exclude`. | Real CLI calls with relative, absolute, extended, case-varied target paths and a relative root all excluded the same subtree. Both sides of the relative comparison now use the same Windows namespace form. |
| Excluding one directory accidentally suppresses its similarly named sibling. | The independent prefix-sibling control still detected unsafe content and exited 1. Exclusion uses path-component ancestry rather than a string prefix. |
| Detection works on long paths but repair or explicit-file modes still fail. | The new real long-path regression completed detection, exclusion, atomic repair, reread, explicit relative/absolute selection and long-root selection successfully. |
| The helper changes POSIX path normalization or write behavior. | The first branch returns the original `Path` unchanged whenever `os.name != "nt"`; all new call sites therefore preserve the prior object and POSIX behavior. This is source-level verification, not a claim that POSIX execution occurred. |

## Limits

Live UNC-share I/O was not attempted; only UNC lexical conversion and idempotence were verified. No POSIX host was available, and the POSIX executable-bit test remained skipped. The parent owns the narrowed validator rerun and full final gate. This reviewer did not retry cleanup of the earlier policy-blocked private harness and did not access or change that directory.
