# v4.9 layout preflight public evidence

Read-only Phase 7 architecture and documentation preflight of commit `ae630cd50cf1458e6c836a4265ab34eed4920925`. The [complete bounded machine-readable findings](v4.9-layout-public.json) preserve every candidate, type, source line where available, and disposition. Raw inventories, reference graphs, copied historical ledgers, ignored private paths and absolute user paths remain local diagnostics and are not promoted.

## Full scan summary

> tracked_files: 5218
> tracked_bytes: 196824280
> tracked_docs: 2351
> duplicate_groups: 72
> duplicate_files: 183
> empty_directories: 6
> placeholder_directories: 4
> single_child_directories: 84
> deep_directories: 769
> deprecated_term_candidates: 762
> zero_lexical_inbound_candidates: 1441
> historical_lifespan_candidates: 1383
> historical_unresolved_links: 811
> known_gap_ledgers: 37

The lexical-reference supplement was collected during concurrent parent edits and is a working-tree observation, not an immutable base-tree graph. The authoritative source inventory SHA-256 is `4351e1553cae074a083dfa5ef09e60e22c8fc6209fc8955d836924d2cba6b5af`. It hashes every tracked file, including all 24 benchmark receipts omitted by the existing docs helper on Windows long paths. The helper's 2,756 disk records additionally include 429 nontracked artifacts; these are not source counts. Generated tracked catalog/coverage exports and release manifests were inventoried but not proposed for handwritten edits. No fixture source or answer map was executed or copied.

## Detector outcomes

The JSON includes complete stdout/stderr for the small retention, co-location, conventions, decisions and bundle checks, with original output hashes. The large inventory/refgraph diagnostics retain hashes and explicit private-only disposition; all 1,383 lifespan and 811 unresolved-link records are reproduced as bounded candidates without source-text copies. Retention reports nothing due under published v4.8; conventions also select v4.8 and do not prove v4.9 coverage. Decisions pass for 38 records. Bundles pass with zero errors and 64 warnings. The earlier path-form wrapper failure is private diagnostic history, not a passing result.

The initial co-location failure was sent to the parent. The current recheck returns **0** after the parent created a v4.9 adoption comparison record; the historical v4.8 original was not moved. Living README navigation and the inherited todos catalog count remain assigned to the parent for the final Phase 7 documentation refresh. Final CI/platform qualification is outside this preflight's completion claim.

## Candidate dispositions and ownership

Every outside-scope candidate is explicitly retained under its originating owner. Duplicate skill resources have independent callers; fixtures and screenshots preserve distinct tests/receipts. Placeholder directories and the documented catalog handbook scaffold are intentional. Catalog bundle nesting, package import paths, and candidate/attempt/ledger addressing are structural contracts. Deprecated wording and zero lexical inbound references do not establish dead code; those candidates remain ambiguous or intentional entry points. No move, deletion, consolidation, or flattening is proposed.

The historical lifespan/link totals are complete baselines, not new v4.9 defects: both have zero v4.9 source candidates. Migration commit `c2656ce8` explains the sampled historical timestamp signal, already owned by v4.0 WN-2; 1,145 timestamp candidates are retained v4.4 records requiring their guide owner's judgment. No historical ledger or archive was edited.

All 37 canonical/archive ledgers have a short recorded status, source SHA-256, recorded open IDs with line numbers, and current owner/disposition in the JSON. Legacy singular archive/version locations were searched and have no tracked ledgers. Recorded open IDs are historical source evidence, not asserted current defect totals; explicit cross-ledger overrides are retained. The v4.8 installer follow-up closures remain closed; the v4.9 model-prompting limits retain their separate owner; audit MT-2 and QG-1 remain with Phase 7. The handbook atlas exception remains with the separately owned handbook plan. No named owner is invented where the source names none.

Private ignored-path candidates use stable opaque IDs rather than exposing local directory names; each candidate and its type/disposition remains countable. Public tracked candidate paths are preserved. Raw source-text and private path lists are unnecessary for reviewing these dispositions and are intentionally absent.

## Verification and next step

The public JSON contains 5,332 candidate records and 37 ledger summaries. It is 1,327,548 bytes, SHA-256 `50f2c5c73f099c2567e28d555c071122a8268fb18c80b9f8de792c12c838b9f5`. The projection checks exact lifespan/link counts, all ledger records, absence of absolute user paths, and absence of copied source sections. The parent promotes only this Markdown and JSON, then verifies its final navigation/qualification changes. This report is Cat 4 active release evidence.
