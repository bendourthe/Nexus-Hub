# Final v4.9 implementation convergence

Independent, read-only Goal-vs-codebase review for the application-security audit plan, including the Cycle 1 graph corrections and candidate `bd37a8b8e91ba5c28001d068445a6b3e65a97fada223e5c7b1441ab6bdb3506c`. This assesses implemented requirements T001-T021 and identifies the remaining Phase 7 duties separately. It does not declare the release ready or replace the final full-profile, installer, CI, approval, and integration gates.

## Goal-vs-codebase review

The stated Goal is a local-first, provider-neutral application-security pipeline that conditionally routes existing specialists, retains qualified graph evidence and truthful schema-v2 health, exports redacted SARIF, and measures observations on inert Python/TypeScript fixtures without a new provider runtime, credential, service, dependency, default mutation path, or expansion into v4.5 boundary ownership.

The Goal is present and well formed. The current code and contracts implement that feature scope. No deterministic missing, partial, contradicting, or unrequested feature was established in this pass. The pending Phase 7 qualification/publication work remains mandatory and is not converted into a deferral.

| Classification | T001-T021 implementation gaps |
|---|---:|
| missing | 0 |
| partial | 0 |
| contradicts | 0 |
| unrequested | 0 |

These counts apply to implementation convergence, not to completion of all release gates. US4 still requires its final execution evidence and green integration before release handoff.

## Inspected scope and evidence

Intent came from the current plan header Goal, US1-US4, Definition of Done, all T001-T021 task requirements, informational characterization formulas, approved assurance decision, approved A1 amendment, non-deferrable gates, and the remaining Phase 7 requirements. No governing constitution was found; the catalog's constitution command is not a project constitution.

This reviewer previously performed bounded independent reviews of the implemented phases. This pass freshly inspected the current graph projector/validator and regression cases; closure graph conflict detection, health precedence and summary failure path; normalized finding and qualified-flow binding; scorer admission, context binding, corrected-finding eligibility, semantic/location matching and metric formulas; SARIF output; routing resolver and host contract; collector boundary entry points; distribution assertions; user guidance; and the current benchmark report. Earlier phase reviews supply the deeper shared filesystem/Git/corpus boundary examination; this pass does not claim a fresh line-by-line security audit of every unchanged helper.

| Requirement | Present implementation and acceptance evidence |
|---|---|
| T001-T003; US2 identity and compatibility | `closure-gate-review-record.md`, `closure-gate.py`, `_strict_json.py`, `_safe_artifact.py`, and `_target_manifest.py` provide the additive profile, strict input bounds, contained observation and target identity. Profile, strict-decoder, safe-artifact, target-manifest and legacy closure tests remain separate proving surfaces. Ordinary schema-v1/v2 behavior is preserved outside opt-in summary mode. |
| T004-T007; US1 routing | `security-audit-routing.json` owns the nine surfaces, predicates, owners, fixed stages, order and four-worker cap. `collect-security-audit-inventory.py` observes bounded source/configuration evidence; the resolver validates receipts and returns an ordered queue without reading the target or invoking owners. Unsupported/unclassified input remains incomplete. The preset, workflow metadata and read-only `/review` dispatch use current owners and retain missing/declined work. Routing, inventory and dispatch tests cover this contract. |
| T008-T010; US2 graph evidence | `code-search-seeding.md`, `_graph_receipt.py`, closure validation and the approved `GraphQueryManager.explore` change reuse the six existing public graph operations. Qualified identities are opaque in retained receipts, ambiguity is preserved, quality remains separate from operation availability, and no result cannot dismiss a finding. The Phase 3 public-MCP probe and focused extension tests remain the real-boundary evidence for A1. |
| T011-T013; US2 health/envelope | `closure-gate.py`, `_audit_envelope.py` and `_normalized_audit.py` enforce receipt identity, required stages, target/content/scope/routing/artifact binding, severity/disposition rules and structured qualified flow. Invalid/conflicting evidence precedes failure, degradation and completeness. Self-attested host execution cannot be upgraded by a content hash. Summary mode rejects invalid cross-run/target bindings without partial output; deterministic normalized-envelope goldens cover complete local, degraded, failed and rejected-input cases. |
| T014-T016; US2 interoperability | `application-security-sarif.md` and `emit-sarif.py` consume only the normalized envelope, retain its health and provenance, emit active findings with portable once-encoded locations and preserve inactive disposition accounting. The emitter reads stdin and does not dereference result locations. SARIF goldens, strict-schema/path cases and recursive-copy assertions exercise this boundary. |
| T017; US3 fixed safe corpus | `_benchmark_corpus.py`, `build-security-audit-projection.py` and the repository-only source/answer fixture directories implement the exact 16-seed/16-benign, two-language/eight-kind/four-holdout cut-line. Structural validation, separate opaque source projection and answer map, contained copying, original-content checks and owned cleanup are present. This review did not execute fixture source or disclose per-seed answers. |
| T018; US3 scoring | `_benchmark_scoring.py` and `score-security-audit.py` match findings only from the envelope and require SARIF equivalence. Paths are translated before matching; benign identities cannot satisfy seed recall. Language/kind and required qualified flow govern detection, then expected identity/region governs location accuracy. Recall uses all 16 seeds; duplicate/ambiguous or unbound inputs invalidate scoring. Required-stage, closure and artifact denominators remain nonzero. |
| T019; US3 retained observations | `_benchmark_protocol.py`, `_benchmark_lifecycle.py` and `manage-security-audit-benchmark.py` freeze the candidate and two mode-specific attempts, retain immutable terminal/outcome entries, validate local hash-chain consistency, recompute evaluator outcomes and render the report. The latest candidate was independently verified in this pass, with exactly four entries. |
| T020-T021; US4 guidance/distribution | `guides/reference/SECURITY_AUDIT.md` explains inventory, routing, health, SARIF, approval, limitations and lifecycle commands. `test_cross_platform_flatten.py` checks bundled script/reference byte equality across six integration surfaces and rejects corpus filenames, answer maps, run plans and ledger artifacts in installed skill output. Platform/installer parity and the full suite are separate final Phase 7 execution duties currently owned by the parent. |

## Cycle 1 recheck and fresh proving commands

The graph corrections address three concrete false-evidence cases: an absent/blank query seed cannot become a qualified receipt; a zero-match result cannot retain a location; and changing a receipt's symbol cannot bypass conflict detection for the same operation, parameters and index. `_graph_receipt.validate()` now rejects inconsistent zero-match metadata, and closure keys query conflict detection independently of the producer's symbol label.

Fresh focused regression execution:

```text
python -m pytest -q tests/skills/test_graph_receipts.py tests/skills/test_security_audit_benchmark_lifecycle.py -k "zero_match or absent_query or symbol_substitution or real_closure_producer or corrected_requires"
13 passed, 104 deselected in 40.97s
```

This includes the real raw-record -> closure summary -> SARIF -> scorer contract, preserving genuine producer bytes, plus equivalent scanner tool/owner/version/configuration and independent-verifier cases for corrected findings. These are deterministic synthetic contract tests, not host benchmark measurements.

The existing `verify-candidate` operation was independently invoked against the current candidate and explicit contained evidence inputs. Its complete result was:

```json
{"candidate_id":"bd37a8b8e91ba5c28001d068445a6b3e65a97fada223e5c7b1441ab6bdb3506c","entries":4,"state":"verified"}
```

No full-suite or live installer result is inferred from these focused commands. No model, scanner, source application, or fixture application was launched by this review.

## Informational host results

The current generated benchmark report binds plan digest `94ab1b0054ede1330323496fe38037e0bf9c991e7ddb574d5e58c3cdb210c397`, subject digest `1bdbc22f8b3a46e5dd898ad5e37d3ed0641c7a8df4ede106f990e365dd295ed7`, and final ledger hash `21cfc32b955b23f657f9edf7c21e01e9c7cba0c4d93f40eb746fb52c6ed646b1`.

| Declared attempt | Workers | Terminal characterization | Code search | All nine targets |
|---|---:|---|---|---|
| sequential | 1 | PRE_SCORING_UNAVAILABLE | UNAVAILABLE | unavailable / unscorable |
| concurrent-four | 4 | PRE_SCORING_UNAVAILABLE | UNAVAILABLE | unavailable / unscorable |

The nine targets are seed recall, blocking-seed recall, location accuracy, blocking-location accuracy, routing coverage, required-stage completeness, closure completeness, artifact consistency and benign high/critical false positives. No observed accuracy, concurrency benefit or successful graph execution is claimed. The report expressly limits execution claims to self-attestation, content checks to observed bytes, and ledger assurance to local declared-set consistency. It does not claim independent blindness, an external anchor, prevention of outside answer access, discovery of omitted launches, or universal application safety. This is consistent with the approved assurance decision; unavailable host performance is not a missing deterministic feature or a release pass/fail result.

## Remaining Phase 7 duties

The parent still owns final Tier 3 evidence, current-host installer postconditions, full local profile/stabilization, platform-specific skip/remote-job accounting, final candidate revalidation after any subject/context/protocol change, and the v4.5 sequencing recheck. Terminal CI changes remain subject to the plan's per-change approval; no approval or remote result is supplied by this review.

Publication still requires the authorized final local commit, explicit first-push approval, exact-SHA PR/check/merge evidence, and the required green integration/release handoff. Human/manual workflow observations must remain distinguishable from automated and synthetic tests. These duties are already assigned in T026-T031; they are neither new convergence tasks nor deferrable implementation gaps. A later failure of any non-deferrable gate must block publication.

## Considered but rejected

- **Unavailable attempts mean the benchmark was not implemented:** rejected because the approved protocol explicitly retains unavailable declared attempts and separates deterministic evaluator correctness from informational host performance. The current ledger verifies both attempts and both outcomes.
- **A content-observed artifact can upgrade host execution to complete:** rejected after inspecting health precedence and the normalized provenance contract; content identity and execution assurance stay separate.
- **A no-result graph receipt can supply a scored source/sink location:** rejected by the corrected zero-match validation and fresh regressions; malformed graph metadata cannot supply a qualified flow through normalization.
- **Detection should require the expected vulnerable location:** rejected because the plan deliberately separates semantic recall from location accuracy. Current scoring applies those stages in that order and excludes benign identities first.
- **Corrected findings can satisfy recall with a different scanner or the fixer as verifier:** rejected by the equivalent tool/owner/version/config checks, independent read-only verifier condition and fresh positive/negative tests.
- **The A1 extension edit is unrequested:** rejected because the plan contains the explicitly approved, bounded qualified-explore amendment and its public-boundary evidence.
- **Missing final CI/install/publication evidence can be treated as an ancillary gap:** rejected. Those duties remain mandatory Phase 7 gates; this feature-convergence result cannot waive them.

## Verdict

**Converged for T001-T021: 0 missing / 0 partial / 0 contradicts / 0 unrequested feature gaps.** No new convergence task is warranted. This reviewer wrote only this report and did not change the plan or runtime. Proceed with the parent's remaining Phase 7 verification and approval gates; overall release completion is not yet asserted.
