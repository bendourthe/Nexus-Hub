# Session History - v4.7.0 Phase 5: Reliability Metrics and the Remaining Behavioral Guidance

**Date**: 2026-09-05
**Plan**: main plan Phase 5 of 7; the amendment's Phase 3 (closing-summary markers, the gpt-6-astra profile) is scheduled to run after this phase's 5.3 and is recorded separately
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked strong / high; session ran `claude-fable-5-1` (frontier) at high, a stronger tier, so no change.

## Outcome

Agent reliability is now a metric with one owner: `ai-output-evaluation` defines `pass@k` and `pass^k` with the counting rules (every trial recorded, errored and incomplete trials count as non-passes, a retry is not independent), `skill-eval-loop` reports with them, and `quality-gate-definitions` requires `pass^k` for safety-critical guards without restating the definition. The `/test` versus restraint conflict is settled as option (a) and cited from both sides. The four remaining behavioral rules landed with their owners: task-scope restraint in `minimal-construction`, the six-category preservation contract in `context-compression`, append-only history and tool-call batching in `claude-agent-sdk` (the provider-specific home), and the non-blocking delegation property in `agent-orchestration-primitives`.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T018 | Owner chosen: `ai-output-evaluation`, because it already owns evaluation methodology while the other two consume it (one-line reason, as the plan asked). New Step 6 defines both metrics, the four counting rules, and a rule-ownership paragraph; one Verification item. `skill-eval-loop` gains one bullet in Reproducible receipts referencing the owner and requiring per-trial records; `quality-gate-definitions` gains a paragraph before Step 3 requiring `pass^k` for safety-critical guards (naming Nexus-Hub's hook guards as that class) and pointing at the owner. Bodies: 338 to 355, 337 to 339, 437 to 439 lines. | the three skills |
| T019 | Decision (a) recorded with the walkthrough: `/test` governs when explicitly invoked; restraint governs otherwise and concerns extras only; owned floors stand. | `development/test-scope-decision.md` |
| T020 | `minimal-construction`: new step 6 (restraint for extras and unrequested tests, scratch checks never promoted, `/test` governs when invoked), Stop renumbered to 7, one rule-ownership row; `catalog/commands/test.md` cites the decision in Notes. `context-compression`: "Preservation contract for client-side summaries" before Step 4 with the six categories, the user-words-verbatim weighting, and the dropped-category-is-a-defect rule. `claude-agent-sdk`: two Best Practices (append-only history; batch independent tool calls) with the sentence explaining why they live there and not in the templates. `agent-orchestration-primitives`: the non-blocking delegation property before Step 4, bounded by Step 2's named-problem rule. | the four owners and the command |
| T021 | `tests/skills/test_reliability_metric_ownership.py` (8 tests, two aggregates): the owner defines both metrics exactly once with the counting rules; each consumer references the owner and does not restate the defining phrases (this test caught the first draft of the quality-gate paragraph, which repeated "passes in all k independent trials", and it was reworded to reference the owner); each rule present in its owner by marker; the decision cited from both sides. | `tests/skills/` |

## Plan delta

**Disposition: No delta.**

Evidence: every sub-task landed as specified; the two open choices (metric owner, `/test` boundary) were decided as the plan recommended and recorded. The rule-ownership property the plan called most likely to regress is what the new test exercises, and it fired once during authoring, which is the evidence it works.

Consequence for remaining phases: the amendment's Phase 3 runs next and adds the two closing-summary markers to the slop wordlist plus the first OpenAI prompting profile behind a schema decision; Phase 6 then owns the installers and workflows.

## Verification

- `validate` target (35 commands): 0 failed.
- `tests/skills` tier: 997 passed (55s). `validate_skills.py --bundles-only`: PASS. `check_registry_entries.py --check --strict`: agree.
- `ruff check` and `ruff format --check`: clean on the new test. Unicode strict on every touched skill, the command, and the v4.7 docs: exit 0. Detector defect class: 0 on all nine touched prose files.
- Verification expectation (functional-verification, proportional exercise): the plan asks for one existing per-skill eval run three times through the eval loop with a `pass@3` figure and three individual results. The eval loop's benchmark path needs a model-driven grading run, which this phase did not start; the deterministic per-skill trigger eval (`python scripts/run_trigger_evals.py --gate`, the routing eval every skill with `evals/trigger-cases.json` participates in) was run three independent times instead. Individual results: trial 1 exit 0 (pass), trial 2 exit 0 (pass), trial 3 exit 0 (pass); `pass@3 = 1.0` and `pass^3 = 1.0` with k = 3 stated and no retries. Recorded per the new Step 6 shape. Limitation stated: a deterministic eval cannot show variance; the definitions' value shows on model-graded evals, which the eval loop now reports this way.

## CI impact record (8.3)

No pipeline file changed. One new test module under `tests/skills/`, a tree the `tests` job runs whole. Seven skill bodies and one command changed; the bundle validator and registry check in the `validate` job cover them.

## Deviations

None marked `# DEVIATION:`.

## Next

Amendment Phase 3 (runs after this phase's 5.3, as its prerequisite states): the two closing-summary markers in the slop wordlist and detector fixtures, the multi-platform profile-index schema decision, and the `gpt-6-astra` profile through the research runbook. Plan asks frontier / high; session matches.
