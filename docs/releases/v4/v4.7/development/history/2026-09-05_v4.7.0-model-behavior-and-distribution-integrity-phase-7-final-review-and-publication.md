# Session History - v4.7.0 Phase 7: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Date**: 2026-09-05
**Plans**: main plan Phase 7 of 7 (final); the amendment plan's Phase 4 is discharged here with one shared evidence file, per that plan's note
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked frontier / max; session ran `claude-fable-5-1` (frontier) at high. The `/effort max` keystroke was surfaced at the boundary and the phase proceeded at high under the in-full driver; recorded under `WN-1`.

## Outcome

The release was reconciled against both plans' Goals, the codebase, every open ledger, and the pipeline, and each duty of the fail-closed last phase has quoted evidence in `last-phase-evidence.md`. The independent review found ten of ten Goal clauses converged (five per plan). Two report items the plan excluded on purpose (E5, E6) are recorded as owned gaps with next steps, the four template validators' shared shape is recorded as a consolidation gap with its trigger, and the two new CI jobs are recorded as unobserved until publication. Publication has not happened: the driver stops for explicit approval before the single push.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T027 / T051 | Refactor scans quoted (empty dirs none, docs conventions OK, retention nothing due); the four-validator duplication assessed and recorded as `DF-3` with `construction-debt:` rather than applied in the final phase. | `last-phase-evidence.md` (Architecture refactor) |
| T028 / T052 | Seven ledgers reconciled: v4.7 finalized with derived counts (DF 5, WN 1, MT 1); v4.4 `DF-1` carried unchanged, `WN-1` closable as superseded by the map fix, `WN-2` closable; v4.1 `DF-1` narrowed; v4.5 `WN-3` re-checked and still open; E5 and E6 recorded as `DF-4` and `DF-5`. The v4.4 ledger and `docs/todos.md` were not edited from this branch because a concurrent session holds them modified. | `known-gaps.md` |
| T029 / T053 | Living docs checked: handbooks are scaffolds; two decision records validated (33 OK); DEVLOG line at release. | evidence file |
| T030 / T054 | Release-preconditions report quoted; v4.5 and v4.6 confirmed committed on `develop`. | evidence file |
| T031 / T055 | Pipeline compared field by field; the two Phase 6 workflow changes are the only pipeline changes and each field has observable evidence; required set unchanged (10 contexts); installer parity PASS. | evidence file |
| T032 / T056 | Tier 3 deep pass: verdict run; nine inventory rows exercised through their real boundaries (the autonomy break test repeated this phase; the bash bootstrap leg by hand); adversarial pass on the bootstraps, the CLI, and the detector; convergence and sufficiency answered. Terminal disposition: no unresolved finding. | evidence file |
| T033 / T057 | Goal-vs-codebase review for BOTH plans, clause by clause against the tree. | evidence file |
| T034 / T058 | Six human tests emitted, including the pinned install on each operating system and the Codex entitlement check. | evidence file |
| T035 / T059 | Full local suite quoted (below); capability gate OK on the two new opt-in surfaces. | evidence file |
| T036 / T060 | Publication: pending explicit approval; ticked only when the pull request is merged. | evidence file |

## Plan delta

**Disposition: No delta.**

Evidence: every duty ran as the runbook and both plans specify; the two report items the plan told this phase to record were recorded; the plan's list of v4.4 items to review matched the ledger this time. The one judgment (defer the validator consolidation rather than rename cited modules in a final phase) is recorded as a gap with its trigger, which is the plan's own convention.

Consequence for remaining phases: none remain. For `/update release`: the round-trip step gains a second check (the published `SHA256SUMS` against the downloaded tarball) now that a release publishes one, and `MT-1` closes when the two new jobs are observed.

## Verification

- `validate` target (35 commands): 0 failed, re-run after this phase's writes.
- Full suite per directory plus the six extension suites:

```text
catalog/hooks/tests: 1280 passed, 33 skipped (9m24s)
tests/validators: 1225 passed, 2 skipped (3m28s)
tests/verification: 83 passed (34s)
tests/workflows: 107 passed, 15 skipped (2s)
tests/plans: 91 passed (3s)
tests/guides: 340 passed, 1 skipped (5m48s)
tests/ci: 88 passed (4s)
tests/installer: 469 passed, 43 skipped (4m17s); the org-CLI git-source test that failed once by ordering during Phase 6 passed in this run
tests/skills: 1006 passed (1m05s)
tests/integrations: 740 passed, 4 skipped (32m42s)
extensions/nexus-skill-server: 43 passed; nexus-skill-scanner: 89 passed; nexus-memory: 53 passed, 1 skipped
extensions/nexus-code-search: collection error; nexus-web-fetch: 3 collection errors; nexus-context-compressor: 3 failed (AST path resolved to regex), 234 passed
The three extension results are ENV, identical to the v4.5.0 record: git diff origin/develop..HEAD -- extensions/ is empty, and ci.yml pip-installs those packages in editable mode with [dev] extras, which this shell does not have.
```

- Detector defect class on the evidence file, this history, and the ledger: 0 (the mandated `##### ID - Title` lines are advisories). Unicode strict on `docs/releases/v4/v4.7`: exit 0. `validate_no_personal_paths.py`: clean. `check_docs_conventions.py --root docs/releases/v4/v4.7`: OK.

## CI impact record (8.3)

No pipeline file changed in this phase; the two Phase 6 changes were reconciled above. No new command, dependency, environment variable, test path, or artifact.

## Deviations

None marked `# DEVIATION:`.

## Publication record (9F, in progress)

Pushed once after approval; PR #167. First remote validation returned one red required check (`tests` on ubuntu, one fixture case in the new bootstrap-verification suite where PowerShell 7 on Linux normalizes a literal backslash in a directory name); classified TEST, not reproducible on this Windows host by construction, fixed in one stabilization commit that changes no product code. Details in `last-phase-evidence.md`.

## Publication record (9F, complete)

Re-pushed after approval; all required checks green; merged with approval as `ca8e663e` into `develop` at 2026-09-05T22:55:48Z; post-merge ran only `provenance` and `smoke` (green). Details in `last-phase-evidence.md`.

## Next

Publication and integration are complete. What follows, as originally listed: push once, open the pull request to `develop`, wait for the required checks against the merge result, merge on green, record the results in `last-phase-evidence.md`, tick T036 and T060, then hand off to `/update release`. After the plan closes: remove the `nh-v45` worktree and the helper scripts outside the repository.
