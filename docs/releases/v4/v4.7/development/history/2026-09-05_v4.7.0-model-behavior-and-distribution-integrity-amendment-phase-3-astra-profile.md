# Session History - v4.7.0 Amendment Phase 3: Closing-Summary Markers and the First OpenAI Prompting Profile

**Date**: 2026-09-05
**Plan**: `v4.7.0-adoption-gpt-6-astra-prompting.md` Phase 3, run after the main plan's sub-task 5.3 as its prerequisite states; folded into the main plan's sequence between Phase 5 and Phase 6
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked frontier / high; session ran `claude-fable-5-1` (frontier) at high. Agreed, no change.

## Outcome

The slop wordlist and the offline detector cover the two labelled closing-summary markers. The prompting profile index moved to schema 1.1.0 with an optional per-platform roster array, all four readers understand it, and the layer holds its first OpenAI profile: twelve `gpt-6-astra` claims, each verified on a second refutation-oriented fetch of the vendor's primary pages, written through the deterministic writer with the Codex roster enumerated live. One honest wrinkle is recorded rather than smoothed over: the Codex CLI does not yet list `gpt-6-astra`, so the codex platform entry reads DRIFTED against the live CLI roster until it does.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T046 | The closing-summary family in `references/slop-wordlist.md` gains `Bottom Line:` and `In short:` with a smell note and a before/after pair. The detector gains `closing-summary-marker` (advisory; a line-leading `Bottom Line:` or `In short:`), `seeded.md` plants one of each, and `SEEDED_COUNTS` pins the count at 2. | `anti-slop-editing/references/slop-wordlist.md`, `scripts/detect_prose_cliches.py`, `tests/verification/` |
| T047 | Decision (a): schema minor bump to 1.1.0 with an optional `meta.platforms` array; legacy single-platform keys stay authoritative for the primary platform. Options (b) one index per platform and (c) a major bump recorded with the reasons they lost. | `development/profile-index-multi-platform-decision.md` |
| T048 | `verify_model_prompting_profiles.py`: `platforms` accepted as an optional meta key, each entry validated with the same roster and hash rules plus unique platform ids. `write_model_prompting_profile.py`: `SCHEMA_VERSION` 1.1.0; a write for the primary platform behaves as before, a write for another platform upserts its `platforms` entry (live roster widened by the models profiled on that platform, the layer's existing invariant); mirrors take their provenance from the matching entry; `plan --platform <id>`. `check_model_prompting_freshness.py`: `--platform <id>` selects the entry; messages name the block. `apply_prompting_edits.py` does not read the index (confirmed by grep), so three readers changed and one was confirmed unaffected. `references/schema.md` documents the field and the flags. Existing suites for all readers pass unchanged (the one message-wording test was kept green by preserving the legacy `meta.roster` wording). | `scripts/`, the skill's `scripts/` and `references/` |
| T049 | Roster enumerated live with `enumerate-models.sh codex` (`codex debug models`, provenance `api`): `codex-auto-review`, `gpt-5.2`, `gpt-5.3-codex`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.5`; `gpt-6-astra` absent. `plan --roster <live> --only gpt-6-astra` reported `in_roster: false`, recorded as such. Research against primary sources only (the model guide, the models catalog, the API changelog); twelve claims, each re-fetched with a refutation prompt that quoted the supporting sentence verbatim (twelve of twelve confirmed, zero refuted), so every claim carries `confidence: high` and `scope: model-specific`; the four prompting-guidance claims note where their platform-agnostic form landed in this plan, and the parallelize-by-delegation claim notes that it was deliberately not adopted. Written with `write --dry-run` then `write`; the writer regenerated `references/models/gpt-6-astra.md`. `verify_model_prompting_profiles.py`: OK, schema 1.1.0, 2 profiled of 4 rostered on the primary platform. `plan --platform codex`: the six live models remain targets, `gpt-6-astra` does not. Freshness: `--platform codex` with the six live ids reports DRIFTED (removed: `gpt-6-astra`), recorded as `DF-2`; the legacy check stays IN SYNC. | `assets/profiles-index.json`, `references/models/gpt-6-astra.md` |
| T050 | `tests/skills/test_prompting_profile_gpt_6_astra.py` (9 tests): schema 1.1.0 with a codex entry and untouched primary platform; the shipped layer passes the gate; a per-platform hash mismatch fails it naming `meta.platforms[0]`; `plan --platform codex` omits `gpt-6-astra`; every claim vendor-sourced, scoped, and not unverified with the seven API-level markers present; the mirror reflects the entry; no template, command, or `SKILL.md` gained the string `gpt-6-astra`; `--platform` freshness reads the entry; the two markers are flagged once each as advisories. | `tests/skills/` |

## Plan delta

**Disposition: Incomplete (non-blocking, resolved inside the phase).**

Evidence: the plan expected `gpt-6-astra` to be reachable from the Codex roster once the availability gate lifted; the vendor's API catalog lists it, but the Codex CLI's live enumeration on 2026-09-05 does not. The plan's provenance rule was followed exactly (enumerate live, record honestly), and the layer's existing invariant (the index never claims a model it has no roster entry for) widened the codex entry to include the profiled model, which is why the per-platform freshness check reads DRIFTED. The plan did not name this state; it is recorded as `DF-2` with the re-check condition.

Consequence for remaining phases: Phase 7's known-gaps reconciliation narrows v4.1.0 `DF-1` (OpenAI ids unprofiled) to "the Codex CLI's six live models remain unprofiled", since the first OpenAI profile now exists; the deep pass should run the structural gate and the per-platform freshness check as this phase did.

## Verification

- `validate` target (35 commands): 0 failed, including `verify_model_prompting_profiles.py` on the 1.1.0 index and the bundle orphan audit on the new mirror.
- Tiers: skills: 1006 passed (56s); validators: 1223 passed, 2 skipped (3m46s); verification: 83 passed (36s).
- Profile-layer and detector suites run together (`test_verify_model_prompting_profiles.py`, `test_check_model_prompting_freshness.py`, `test_model_prompting_research.py`, `test_prompting_profile_gpt_6_astra.py`, `test_prose_cliche_detector.py`): 169 passed.
- Verification expectation (functional-verification): the detector on a fixture holding `Bottom Line: ship it` and `In short: done` reported two advisory findings, one per line (`1:1` and `3:1`), 0 defects; `references/models/gpt-6-astra.md` lists twelve claims including the seven API-level ones (effort migration and no `none`, `configuration_update`, Responses API for tools, async tools with `call_id`, removed `temperature`/`top_p`/`top_logprobs`, EU fast-tier limit, `prompt_cache_options.ttl` 30m), each with a `developers.openai.com` source and `model-specific` scope. Expected and observed match.
- `ruff check`: clean on the edited scripts for the lines this phase added; two findings in `verify_model_prompting_profiles.py` (`F401`, `SIM102`) pre-exist at `HEAD` in untouched lines and are left alone. `ruff format --check` reports all three edited scripts as unformatted, exactly as they were at `HEAD`; reformatting them would rewrite pre-existing code outside this scope.
- Unicode strict on both skill bundles, the v4.7 docs, the two scripts, and the new test: exit 0. `validate_no_personal_paths.py`: clean. Detector defect class: 0 on the decision note, the schema doc, the mirror, and the wordlist.

## CI impact record (8.3)

No pipeline file changed. One new test module under `tests/skills/`; one detector fixture line pair and one seeded-count entry under `tests/verification/`; both trees run whole in the `tests` job. Two repo-level validators changed behavior additively (`verify_model_prompting_profiles.py` runs in the `validate` job; `check_model_prompting_freshness.py` is deliberately outside CI). One data file (`profiles-index.json`) and one generated mirror changed.

## Deviations

None marked `# DEVIATION:`.

## Next

Main plan Phase 6: distribution integrity (the scheduled supply-chain watch, the attested release artifact, artifact verification and a pinned-version path in both installers). Plan asks frontier / max; the `/effort max` keystroke is surfaced at the boundary.
