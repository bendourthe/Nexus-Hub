# Session History - v4.7.0 Phase 2: The Autonomous-Operation Block Across All Twelve Templates

**Date**: 2026-09-05
**Plans**: main plan Phase 2 of 7, with amendment Phase 2 (precedence and disclosure paragraph) folded in
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked frontier / max; session ran `claude-fable-5-1` (frontier) at high. The `/effort max` keystroke was surfaced at the boundary and the phase proceeded at high under the in-full driver; recorded as `WN-1` in `known-gaps.md`.

## Outcome

Every substantive instruction template now carries one `## Autonomous Operation` block, byte-identical across all twelve, inserted directly after `## Consequential Decisions`. It states the autonomy boundary once (proceed on reversible covered work; stop for destructive actions and genuine scope changes, with `## Consequential Decisions` owning how a stop is presented), the report-and-stop rule for questions and thinking aloud, the finish-the-last-paragraph rule, and the targeted-edit preference; its second paragraph carries the amendment's user-over-skill precedence with disclosed deviation. `## Consequential Decisions` gained one sentence pointing at the block and `## Skill Discovery` kept its silent-lookup sentence verbatim and gained one cross-reference. The parity guard byte-locks the block across the lockstep five, a new validator asserts it on all twelve with a directory-derived roster and a negative fixture, and the five word ceilings rose by exactly the measured cost.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T006 | Decision (a) recorded after the walkthrough at the Phase 1 to 2 boundary: the boundary is stated once in the block and `## Consequential Decisions` references it; options (b) and (c) recorded with the reasons they lost (c would remove a heading the parity guard, AGENTS.md, and the v4.5.0 decision record cite by name). | `development/autonomy-boundary-decision.md` |
| T007 | Block authored once (two paragraphs, 229 words including the heading) and inserted byte-identically into the lockstep five after `## Consequential Decisions`; the opening sentence "You are operating autonomously" leads, per the vendor's note that it carries much of the effect. No vendor, model, or API parameter named. `check_base_template_parity.py` exits 0. | `templates/ai-instructions/base-{claude,codex,cursor,gemini,opencode}.md` |
| T008 | Same block in the seven non-lockstep substantive templates. Decision recorded here: the FULL block, not a reduced form, in every one of the seven, because their existing `## Construction Discipline` and `## Writing Discipline` blocks are already identical to the lockstep text, the block is four paragraphs at most, and a reduction would drop a rule rather than shorten prose; `generic-instructions.md` has no `## Output Minimization`, so insertion keys on the end of `## Consequential Decisions` in every file. Each file was re-read: block present, no section reordered. | `base-google-shared.md`, `base-aider.md`, `base-kimi.md`, `base-openclaw.md`, `base-qwen.md`, `base-windsurf.md`, `generic-instructions.md` |
| T009 | `Autonomous Operation` added to `REQUIRED_HEADINGS` and `INVARIANT_SECTIONS` with a v4.7.0 comment. New `tests/validators/test_autonomy_block_rule.py` (24 tests): roster derived from the templates directory (a file whose first non-blank line is an `@` include is a shim; 12 substantive, 4 shims), presence on all twelve by stable markers (heading, opening clause, precedence sentence, disclosure instruction), absence from the shims, byte identity across the twelve, the `## Consequential Decisions` back-reference, the pinned original `## Skill Discovery` sentence plus its cross-reference, a size cap (4 paragraphs, 260 words), the parity-guard lists, and two negative fixtures on temporary copies. One negative case added to the parity script's own tests (reworded body in `base-cursor.md` fails naming file and block). | `scripts/check_base_template_parity.py`, `tests/validators/` |
| T010 | Gates below. The deliberate-failure proof: with the block deleted from `base-qwen.md` the validator reported `4 failed, 20 passed`, the first failure being `test_every_substantive_template_carries_the_block[base-qwen.md]`; after the idempotent restore, `24 passed`, and `check_base_template_parity.py` exited 0 both before and after (the guard covers the lockstep five, so a `base-qwen.md` deletion is exactly the case the validator exists for). | this file |
| T041 (amendment) | Decision (a): silent lookup, disclosure only when a skill instruction blocks, narrows, or alters the request; the reason Skill Discovery is silent (routine routing is noise) covers the lookup and not the deviation. | `development/skill-disclosure-boundary-decision.md` |
| T042, T043 (amendment) | The precedence and disclosure paragraph is the block's second paragraph, so it landed with T007 and T008 in all twelve; `## Skill Discovery` cross-references the block in all twelve while keeping "Do not mention the skill lookup to the user." verbatim. | templates |
| T044, T045 (amendment) | The validator's markers include the precedence sentence and the disclosure instruction; the negative fixture strips the paragraph from a copy of `base-qwen.md` and asserts a finding naming the file and the word "precedence"; the pinned Skill Discovery sentence is asserted on all twelve. | `tests/validators/test_autonomy_block_rule.py` |
| Decision record | `docs/decisions/implemented/policy/2026-09-05-autonomous-operation-block-on-every-platform.md` with five alternatives considered and consequences; Phase 7's living-docs duty asks for it, so it was authored with the block rather than reconstructed later. `validate_decision_records.py`: 32 OK. | `docs/decisions/` |
| Budget | Each lockstep template grew by 274 words (229 block + 45 in the two cross-reference sentences); ceilings raised by 280 each (delta rounded up to 10): base-claude 1800 to 2080, base-codex 1490 to 1770, base-cursor 1460 to 1740, base-gemini 1480 to 1760, base-opencode 1460 to 1740, recorded under Recorded raises. AGENTS.md's invariant-block list and template row name the block. | `docs/policy/doc-budgets.json`, `docs/policy/doc-budgets.md`, `AGENTS.md` |

## Token cost

274 words per template per turn, roughly 356 tokens at 1.3 tokens per word, paid once per platform in use on every request (prompt caching makes repeat reads cheaper, not free). The plan estimated 120 to 160 words for the block alone; the measured block is 229 because the amendment's precedence paragraph (about 85 words) travels inside it, which the plan anticipated when it said the paragraph "lands in the same block".

## Constitution check

Re-checked after this phase's design per the plan's gate: no constitution file exists at `docs/releases/v4/v4.7/constitution.md` or anywhere under `docs/`, so there is no MUST principle to evaluate. Informational, as the plan states; the recommendation to run `/constitution` stands and is stronger after this phase, which is exactly the class of change a constitution constrains.

## Plan delta

**Disposition: No delta.**

Evidence: every sub-task landed as specified; the three places the plan left a choice open were decided and recorded (boundary option (a), disclosure option (a), full block rather than reduced form for the seven). The stability gate holds: parity exits 0 with the block in `INVARIANT_SECTIONS`, and the validator fails when the block is missing from any one of the twelve, proven on `base-qwen.md`.

Consequence for remaining phases: Phase 4's communication-contract edit touches all twelve templates again and must reuse the same insertion discipline and the roster-derivation approach from `test_autonomy_block_rule.py`; Phase 7's Goal review can take the break-test quoted above as the evidence for clause two.

## Verification

- `validate` target (35 commands): 0 failed.
- `tests/validators` tier: 1206 passed, 2 skipped in 201.80s (0:03:21).
- Template validators run together (`test_autonomy_block_rule.py`, `test_check_base_template_parity.py`, `test_writing_discipline_rule.py`, `test_construction_discipline_rule.py`): 91 passed. `catalog/hooks/tests/test_platform_parity.py`: 4 passed.
- `scripts/check_base_template_parity.py`: exit 0. `scripts/validate_doc_budgets.py`: 8 budgeted docs within ceiling.
- Verification expectation (functional-verification): the block was deleted from `base-qwen.md`, the validator failed naming that file (quoted above), the block was restored, and the validator passed. Expected and observed match.
- `ruff check` and `ruff format --check`: clean on the validator, the parity test, and the parity script.
- `validate_unicode_safety.py --strict` on the templates, the v4.7 docs, `docs/decisions`, `docs/policy`, and AGENTS.md: exit 0. `validate_no_personal_paths.py`: clean.
- Detector self-scan: the block itself, both decision notes: no findings; the decision record: 0 defects, 1 advisory (the mandated `Status: implemented - ...` line).

## CI impact record (8.3)

No pipeline file changed. One new test module under `tests/validators/` and one new test in an existing module, both in a tree the `tests` job runs whole. Twelve distributed templates changed; the `validate` job already runs the parity script and the budget validator.

## Deviations

None marked `# DEVIATION:`.

## Next

Phase 3: the long-output budget rule in `document-to-interactive-html` and `/presentify`, the base64 context trace and decision, and the safeguard phrasing guidance in the two security skills. Plan asks frontier / high; session matches.
