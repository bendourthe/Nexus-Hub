# Session History - v4.7.0 Phase 1: Routing Data Correctness and the Effort-Level Contract

**Date**: 2026-09-05
**Plans**: `docs/releases/v4/v4.7/plans/v4.7.0-adoption-model-behavior-and-distribution-integrity.md` (main, Phase 1 of 7) with `v4.7.0-adoption-gpt-6-astra-prompting.md` (amendment Phase 1) folded in
**Driver**: `/implement v4.7.0-adoption-gpt-6-astra-prompting in-full`, maintainer chose to run the full main plan with the amendments folded in; worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity` off `develop` at `76bcf614` (post v4.5.0 back-merge and plans migration)
**Model routing**: plan asked frontier / high; session ran `claude-fable-5-1` (frontier) at high. Agreed, no change.

## Outcome

The routing data now names only models the vendors list today, with every one of the sixteen cells re-fetched and recorded as changed or unchanged after verification. `gpt-6-astra` enters the map at frontier from quoted evidence gathered one day after the vendor lifted its rollout gate, and the OpenAI column becomes four distinct ids. The Claude effort-level contract is corrected: the vendor page documents `max` for the environment variable and not for the settings keys, the reverse of what the 2026-09-04 statement recorded. `model-routing` gains the two rules it lacked, and the main plan's Phase 2 and Phase 5 prompts now cite a second vendor.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T001 | Fetched `code.claude.com/docs/en/model-config` and `/settings` on 2026-09-05. Option (a) holds with a twist: the reference was fetched and the statement was stale, but inverted. Quoted: "set `effortLevel` to `low`, `medium`, `high`, or `xhigh` ... `max` isn't accepted in either key" and "Unless you set it through the `CLAUDE_CODE_EFFORT_LEVEL` environment variable, Claude Code applies `max` to the current session only." | `development/effort-level-contract.md` |
| T002 | Snapshot re-verified cell by cell (Anthropic overview and Mythos 5.1 page, OpenAI catalog and Astra guide, Google models page, Cursor models page). Anthropic, Google, Cursor unchanged after verification; OpenAI frontier `gpt-6-astra`, strong `gpt-5.6-sol`, standard `gpt-5.6-terra`, fast `gpt-5.6-luna`, ordered by the vendor's own descriptions. `verified_as_of` 2026-09-05; note `refresh_2026_09_05_v470`; `deliberately_unmapped` re-verified with the Mythos 5.1 page ("Invite only. Released September 1, 2026." / "by invitation only, as part of Project Glasswing") and the Astra history. `model-map.py validate`: `{"valid": true, "verified_as_of": "2026-09-05", "tiers": 4, "providers": 4}`. The two frontier moves the plan named (fable-5-1, gemini-3.8-flash) had already landed in the v4.4.5 release refresh, so they were verified rather than made. | `references/last-known-model-map.json` |
| T003 | Claude `doc_statement` rewritten to the quoted contract, `verified` 2026-09-05; seeded `high` accepted by every surface so no seeded value changed. `sync_platform_defaults.py --apply`: "Already in sync -- no derived artifact changed"; `--check`: OK, 13 platforms. Upgrade-pair rule confirmed unchanged: both levers still declared together, missing partner never added. Lever contract's Claude section corrected and a dated log row added. | `configs/platform-defaults.json`, `docs/policy/platform-defaults-levers.md` |
| T004 | New subsection "Two verification rules the map depends on" before Step 5: effort levels are not comparable across models (re-sweep on a mapped-model change; start at the documented default; include mid and low), and a recognized name is not a known name (search the name as written; familiarity is not a reason to skip). One Verification item added. Body 197 to 204 lines. | `catalog/skills/ai-development/model-routing/SKILL.md` |
| T005 | `tests/validators/test_model_map_snapshot.py` (5 tests: helper validation, `verified_as_of` floor, no tier cell names `claude-fable-5`, no live surface names it, `sync --check` exits 0) and the amendment's `test_model_map_astra_note.py` (3 tests). Gates below. | `tests/validators/` |
| T037 (amendment) | Astra decision from both OpenAI pages fetched 2026-09-05: the catalog lists `gpt-6-astra` as "Our most capable model, built for the hardest end-to-end work" with no trusted-access or "coming days" sentence; the guide states no restriction. Mapped at frontier; `gpt-5.6-sol` to strong on the vendor's ordering. | `development/astra-routing-decision.md`, snapshot note |
| T038 (amendment) | v4.8.0 plan: map is merged and historical, so a dated note under it cites the decision rather than editing the cell. v4.4.6 plan: its map already agrees (Astra at frontier) but the file exists only on the concurrent guide branch, so it was not edited from this branch; recorded as `DF-1` in `known-gaps.md`. | `docs/releases/v4/v4.8/plans/...`, `known-gaps.md` |
| T039 (amendment) | Main plan 2.2 prompt gains both sources and the "can you / help me" example sentence; 5.3 prompt gains the Astra guide beside the test-scope rule with the vendor's sentence. | main plan |
| T040 (amendment) | Tests above; `check_docs_conventions` OK on v4.7 and v4.8. | `tests/validators/test_model_map_astra_note.py` |

## Plan delta

**Disposition: Incomplete (non-blocking, resolved inside the phase).**

Evidence, three items. First, the plan's two named frontier moves were already in the snapshot from the v4.4.5 release refresh (2026-09-04), so sub-task 1.2 became a full re-verification plus the Astra admission rather than the edit the plan describes; the plan predates that refresh. Second, the plan's stability gate "no file in the repository names `claude-fable-5` as a frontier tier value" is unsatisfiable as written: twelve historical plans under `docs/releases/v3/` carry `claude-fable-5` in their own dated maps, which are records and are not edited. The test is scoped to live routing surfaces (the snapshot's cells, the platform-defaults source, the templates, the skill body) and the history is left alone. Third, sub-task 1.1 framed the question as whether `max` had been added to the settings key; the vendor page shows the opposite arrangement (environment variable yes, settings keys no), so option (a) applies with the correction running the other way.

Consequence for remaining phases: Phase 2's block and `model-routing` may say `/effort max` only as a Claude Code session keystroke, never as a settings value; Phase 7's Goal review should read the effort contract from the decision note, not from the plan's framing. Amendment Phase 3 (the `gpt-6-astra` profile) cites the routing decision as planned.

## Verification

- `validate` target (35 commands): 0 failed.
- `tests/validators` and `tests/plans` tiers: 1272 passed, 2 skipped (3m21s), including the 8 new tests and the existing routing-helper tests that render the snapshot.
- `python catalog/skills/ai-development/model-routing/scripts/model-map.py validate <snapshot>`: valid, 4 tiers, 4 providers.
- Verification expectation (functional-verification, Tier 1 exercise): `model-map.py fallback` rendered `**Model map status**: offline fallback; stale as of 2026-09-05.` with the frontier row `claude-fable-5-1 | gpt-6-astra | gemini-3.8-flash | cursor-grok-4.6`; `sync_platform_defaults.py --check` exited 0. Expected and observed match, with the `verified_as_of` one day later than the plan's 2026-09-02 because the phase ran on 2026-09-05.
- `ruff check` and `ruff format --check` clean on the two new tests.
- `validate_unicode_safety.py --strict` on the touched docs, the skill, and the platform-defaults source: exit 0. `validate_no_personal_paths.py`: clean.
- Detector self-scan on the two decision notes and the skill body: no findings.
- Derived artifacts (`catalog/hooks/settings.json`, `scripts/lib/integrations/claude.py`) unchanged, as the seeded values did not move.

## CI impact record (8.3)

No new command, dependency, environment variable, or pipeline change. Two new test modules under `tests/validators/`, a tree the `tests` job already runs whole. One data file (`last-known-model-map.json`) and one source file (`configs/platform-defaults.json`) changed; both are already covered by `make validate` (`sync_platform_defaults.py --check`) and the existing routing tests.

## Deviations

None marked `# DEVIATION:`. The scoping of the legacy-id test is recorded above as a Plan delta, not a deviation.

## Next

Phase 2: the autonomous-operation block across all twelve templates, with the maintainer's decisions already recorded: (a) state the autonomy boundary once in the new block and have Consequential Decisions reference it; (a) silent skill lookup with disclosure only when a skill blocks, narrows, or alters the request; ceilings raised by the measured delta. Plan asks frontier / max; the `/effort max` keystroke is surfaced at the boundary.
