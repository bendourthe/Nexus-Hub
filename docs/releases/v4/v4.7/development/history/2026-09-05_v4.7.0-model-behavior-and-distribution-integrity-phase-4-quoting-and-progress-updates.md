# Session History - v4.7.0 Phase 4: Source-Quoting, Search-Triggering, and Progress-Update Discipline

**Date**: 2026-09-05
**Plan**: main plan Phase 4 of 7 (no amendment sub-tasks in this phase)
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked strong / high; session ran `claude-fable-5-1` (frontier) at high, a stronger tier, so no change.

## Outcome

The two research skills now teach quotation by a complete worked example each (request, response, rationale), organized around where sources agree and differ, with exactly one short marked quotation and everything else reworded. The communication contract gains its missing half in all twelve templates, the style guide, and the skill: one line before starting and brief progress notes on a long tool-calling turn, stated as the agent's own narration so `## Output Minimization` is untouched, plus the formatting rule as a positive statement (lists where asked or multifaceted, plain prose where minimal formatting is requested or the exchange is conversational). The additions fit inside the existing word ceilings, so no budget changed this phase.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T015 | `deep-research-compilation`: a worked example under Step 3 in the skill's inline `[N]` citation style, with a templated Step 2 manifest, three invented field reports on an invented irrigation subject, one marked phrase attributed to one source, and a rationale sentence; states that zero marked phrases is a valid outcome. `trend-research`: a worked example under section 3 with templated native-search excerpts (no real community or blog named), one marked phrase, and a rationale. Neither introduces a second citation style. | `deep-research-compilation/SKILL.md` (120 to 134 lines), `trend-research/SKILL.md` (145 to 163) |
| T016 | Two bullets appended to `## Communication Contract` in all twelve substantive templates, identical text (60 words): the forward-looking line and progress notes with the Output Minimization reconciliation, and the formatting rule. The style guide (the contract's source of truth) gains `## 8. Progress narration and formatting` with the harness note (tell the agent when the interface hides tool output; framed for whoever configures the harness, no vendor parameter named) and two Verification items; the skill gains step 8 and the same Verification items. Existing step 2.5 and the style guide's "do not narrate your own process" were scoped to the retrospective account so the two rules do not read as contradicting each other. The legacy anti-formatting grep the plan asked for (`do not use bullet|avoid markdown|no bullet|never use headers|plain prose only` across `templates/ai-instructions/`, `agent-communication`, and `catalog/style-guides/`) returned nothing, so the addition is purely additive and the vendor's usual first step (deleting old anti-formatting rules) does not apply. `check_base_template_parity.py` exits 0. | twelve templates, `catalog/style-guides/agent-communication.md`, `agent-communication/SKILL.md` |
| T017 | `tests/validators/test_communication_contract_rule.py` (17 tests: directory-derived roster of twelve, the three markers inside the contract section of each, the style guide's section and harness note, the skill's step 8 and the scoped step 2.5, and a complete request / response / rationale example with one marked quotation in each research skill). Body identity across the twelve stays owned by the existing rollout test, which still passes. | `tests/validators/` |

## Plan delta

**Disposition: No delta.**

Evidence: every sub-task landed as specified. The plan's expectation that no legacy anti-formatting language exists was verified by the grep and held. The one judgment recorded here: the contract bullets were authored at 60 words so they fit the headroom left after the Phase 2 raise, leaving 15 to 28 words of headroom per lockstep template; a later addition to a lockstep template will need a recorded raise.

Consequence for remaining phases: Phase 5's rules land in skills, not templates, so no further template edit is planned; Phase 7's Goal review can take the rollout and contract-rule tests as evidence for the "communication contract covers mid-turn narration" clause.

## Verification

- `validate` target (35 commands): 0 failed. `scripts/validate_doc_budgets.py`: 8 within ceiling (headroom after this phase: base-claude 28, base-codex 15, base-cursor 16, base-gemini 15, base-opencode 16 words).
- Tiers: validators: 1223 passed, 2 skipped in 251.71s (0:04:11);skills: 989 passed in 75.81s (0:01:15). `catalog/hooks/tests/test_platform_parity.py`: 4 passed.
- Template validators run together (`test_communication_contract_rule.py`, `test_communication_contract_rollout.py`, `test_autonomy_block_rule.py`, `test_writing_discipline_rule.py`, `test_check_base_template_parity.py`): 122 passed.
- `validate_skills.py --bundles-only`: PASS. `check_registry_entries.py --check --strict`: agree.
- `ruff check` and `ruff format --check`: clean on the new test. Unicode strict on the templates, the style guide, and the three skills: exit 0. Detector defect class: 0 on the four edited prose surfaces.
- Verification expectation (functional-verification, proportional exercise): the deliverable of this phase is agent prose, so the exercise was performed as the skill directs rather than through a script. Two invented fixture sources were compiled: source A ("the March survey found that 62 percent of the forty respondents preferred the shorter form, and the author called the longer form a relic of the paper era") and source B ("the April follow-up, with twenty-eight respondents, found preference split evenly and attributed the March result to the sample's age skew"). Compiled response: "The two surveys agree that form length mattered to respondents and differ on how much. The March survey found a clear majority for the shorter form, which its author dismisses the longer form against as "a relic of the paper era" [1]; the smaller April follow-up found an even split and attributes March's majority to the age of that sample rather than to the form [2]." Observed: organized around agreement and difference; each source in one sentence of indirect speech; exactly one short marked phrase, attributed to one source; the numbers reworded and kept (a clear majority, forty and twenty-eight respondents implied by "smaller"); no other passage reproduced. Expected and observed match.

## CI impact record (8.3)

No pipeline file changed. One new test module under `tests/validators/`, a tree the `tests` job runs whole. Twelve distributed templates, one style guide, and three skill bodies changed; the `validate` job's parity script, budget validator, and bundle validator already cover them.

## Deviations

None marked `# DEVIATION:`.

## Next

Phase 5: the reliability-metric owner and definitions (`pass@k`, `pass^k`), the `/test` versus restraint decision (option (a), already recorded by the maintainer), and the compaction, batching, and delegation rules with their owners. Plan asks strong / high; the session's frontier tier meets it.
