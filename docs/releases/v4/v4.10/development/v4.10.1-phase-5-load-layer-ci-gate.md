# v4.10.1 Phase 5 - Load-layer CI gate

This record covers T017-T020 of the v4.10.1 eval-isolation and adaptive-compaction plan. It records the manifest validation profile, scratch-config host-load job, fail-open wiring guards, functional exercise, validation, and CI impact in the enclosing commit named `ci(plugins): verify host load layer`.

## Completed work

- Added `claude plugin validate .` to the repository-native full profile and the existing unconditional `validate` job.

- Kept a missing optional vendor CLI visible as `SKIP` locally while preserving fail-closed behavior for every command that does not opt into that narrow exception.

- Replaced the invalid directory-valued plugin `agents` field with the explicit sorted list of all 23 agent files accepted by the Claude Code host.

- Added `claude-plugin-load`, a bounded Ubuntu job that installs the vendor CLI without lifecycle scripts, uses an isolated `CLAUDE_CONFIG_DIR` under `runner.temp`, installs this checkout through its marketplace, and parses the host listing to require exactly one enabled `nexus-hub@nexus-hub` entry.

- Routed the new job through the existing `ci-required` aggregate without adding a required context. The job is filtered at job level, has a 15-minute timeout, grants only `contents: read`, and disables persisted checkout credentials.

- Added regression tests for optional-tool visibility, exact profile membership, the 23-agent manifest roster, aggregate-only reporting, job-level filtering, permissions, timeout, scratch configuration, and enabled-state parsing.

## Functional exercise - host validation and isolated load

- **Revision**: the enclosing commit named `ci(plugins): verify host load layer`.
- **Artifact and boundary**: `.claude-plugin/plugin.json`, the `claude-plugin` profile group, and the `claude-plugin-load` workflow job.
- **Command or action**: ran `claude plugin validate .`, ran `claude plugin validate .claude-plugin/plugin.json`, then installed the checkout into a newly created scratch `CLAUDE_CONFIG_DIR`, listed installed plugins as JSON, and removed the verified temporary directory.
- **Input**: the current checkout, Claude Code CLI on Windows, and an empty host configuration directory under the system temporary root.
- **Expected contract**: marketplace validation passes; direct plugin validation accepts the explicit agent roster; scratch installation reports exactly one enabled `nexus-hub@nexus-hub` entry; no real user configuration is touched.
- **Exit code or measurement**: both validation commands exited 0; scratch marketplace add, install, and list exited 0; the parsed entry reported version `4.13.0` and `enabled: true`.
- **Observed output or state**: `claude plugin validate .` reported `Validation passed`; direct manifest validation passed with one non-fatal warning that a root `CLAUDE.md` is not plugin project context; the isolated host accepted and enabled the plugin.
- **Comparison**: matches.
- **Environment**: Claude Code CLI and Python 3 on Windows PowerShell in the isolated v4.10.1 worktree.
- **Evidence paths**: this record, `.claude-plugin/plugin.json`, `.github/workflows/ci.yml`, `scripts/ci/profiles.py`, and the four Phase 5 test modules.
- **Delegates**: `[[functional-verification]]` procedure applied.
- **NOT COVERED**: the new hosted job has not run remotely. Per the plan lifecycle, its first remote execution belongs to the single Phase 7 publication.

## Verification

- `python -m pytest -q tests/ci/test_ci_engine.py tests/validators/test_ci_required_gate.py tests/workflows/test_workflow_policy_repo_wide.py tests/validators/test_claude_plugin_manifests.py`: 169 passed, 17 skipped in 16.11 seconds.

- `python scripts/check_required_check_coverage.py`: passed with 10 declared contexts across two branches, all produced unconditionally. The required-check count remains five per branch.

- `python scripts/ci/run.py --profile full --only claude-plugin`: 1 passed, 0 failed, 0 skipped in 0.6 seconds.

- `claude plugin validate .`: passed.

- `claude plugin validate .claude-plugin/plugin.json`: passed with the single root-`CLAUDE.md` warning described above.

- The full fast profile passed: 15 passed, 0 failed, 0 skipped, 0 advisory in 173.8 seconds. The longest commands were `validate_unicode_safety` at 100.8 seconds and `check_merge_conflict_markers` at 61.3 seconds.

- The first monolithic full-profile attempt remains a non-pass: all groups except `repo-tests` passed, `hook-tests` passed in 1380.9 seconds, and `repo-tests` reached 61 percent before its unchanged 4500-second safety timeout. The retained output contained one failure marker before timeout, so the run is not presented as success.

- The retained failure was reproduced as a stale test path to an archived v4.7 decision and fixed separately in `b896caa8`. Bounded corpus partitions then exposed and resolved three more pre-existing defects in separate commits: stale archived release paths and snapshot schema in `1266aa6c`, an incidental repository-depth assumption in the Windows long-path guard in `fb15b609`, and an overlong v4.13 DEVLOG summary in `ae64beca`. A Windows code-page warning in the prose detector was resolved and verified with warnings-as-errors in `d416a1c5`.

- The 5,934-test repository corpus was exercised in bounded partitions without changing the shared timeout: skills reported 2,145 passed and 16 skipped; installers, integrations, and plans reported 1,327 passed and 48 skipped; CI and guides reported 435 passed and 1 skipped; the validator/verification/workflow suffix reported 1,324 passed and 20 skipped after a 613-pass prefix and a focused 10-of-10 DEVLOG repair. The counts overlap only where a failed module was rerun after repair, so they are evidence records rather than a fabricated aggregate total.

- The CI-and-guide partition printed its 435-pass terminal summary but left the pytest process alive without child processes. It was interrupted after the summary. `tests/ci` then exited cleanly at 95 passed, isolating the teardown anomaly to the guide side or the combined interaction. This is not counted as a clean monolithic profile exit and remains input to Phase 7's terminal verification.

- `git diff --check` passes before evidence recording and is rerun at the commit gate.

## CI impact

CI/CD is this phase's explicit deliverable. The existing unconditional validation job gains one repository-native profile group. One new hosted load job proves the achieved host property that structural checks cannot, then reports into the existing aggregate. The required-check manifest, workflow trigger, deployment boundaries, and branch context count are unchanged. The terminal field-by-field pipeline reconciliation remains Phase 7 work.

## Session history

The phase began from clean Phase 4 commit `37cda405`. Direct host validation disproved the original directory-valued `agents` assumption, so the manifest now lists the live 23-file roster and a test derives the same list from disk. The scratch load exercise passed after that correction. The monolithic profile preserved a timeout and a real stale-path failure rather than being smoothed into success; the repository corpus was partitioned without weakening CI limits, and each deterministic defect received a focused repair and proof. No branch push, pull request, tag, release, paid model call, or remote CI run occurred.
