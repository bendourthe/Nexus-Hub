# v4.10.1 Phase 2 - Eval runner isolation and model pinning

This record covers T005-T009 of the v4.10.1 eval-isolation and adaptive-compaction plan. It records the official-source decision, runtime isolation boundary, model-pin invariant, historical-result consequence, functional exercise, mutation proofs, validation, and CI impact in the enclosing commit named `fix(evals): isolate runners and require model pins`.

## Completed work

- Re-verified all four runner surfaces from official vendor documentation on 2026-09-21 and recorded the result in `docs/decisions/implemented/tooling/2026-09-11-eval-runner-configuration-isolation.md`.

- Added Claude isolation with `--setting-sources ""` and Codex isolation with `--ignore-user-config --ignore-rules --ephemeral`.

- Required one explicit run-level model pin for every provider-backed arm and rejected conflicting per-eval model values.

- Made Gemini and OpenCode fail closed because their checked official documentation does not expose an all-configuration isolation flag. Their limitations are recorded as EV-1 and EV-2 in the v4.10 known-gap ledger.

- Added the selected model to raw-memory metadata and optimizer iteration records.

- Documented baseline self-contamination and stated that pre-isolation paired results are not comparable. A repository inventory found no committed skill-eval-loop paired results or baselines to re-capture; deterministic compressor and code-search baselines are separate harnesses and are unaffected.

## Functional exercise - isolated eval CLI boundary

- **Revision**: the enclosing commit named `fix(evals): isolate runners and require model pins`.
- **Artifact and boundary**: CLI script `scripts/optimize_skill_description.py` and its public argument parser/command builder.
- **Command or action**: ran a valid pinned Claude dry run over a one-case temporary eval file, built Claude and Codex argv values through `build_cli_command`, invoked a provider-backed Gemini run, and invoked a Claude run without `--model`.
- **Input**: one valid eval entry, the shipped skill-eval-loop SKILL.md, runner names `claude`, `codex`, and `gemini`, and model `fixture-model`.
- **Expected contract**: dry run succeeds and records the model; isolated argv values contain the documented flags and model; Gemini fails before subprocess invocation; missing model fails before subprocess invocation.
- **Exit code or measurement**: dry run 0; command construction 0; Gemini limitation 2; missing pin 2.
- **Observed output or state**: dry-run JSON reported `mode=dry-run`, `cli=claude`, `model=fixture-model`, and one case. Claude argv contained `--setting-sources "" --model fixture-model`. Codex argv contained `--ignore-user-config --ignore-rules --ephemeral --model fixture-model`. Gemini stderr named the missing isolation contract without a traceback. Missing-model stderr required an explicit pin.
- **Comparison**: matches.
- **Environment**: Python 3 on Windows PowerShell in the isolated v4.10.1 worktree; no provider call or paid model invocation occurred.
- **Evidence paths**: this record, the tooling decision, `references/cli-adapter.md`, `scripts/optimize_skill_description.py`, and `catalog/hooks/tests/test_eval_loop.py`.
- **Delegates**: `[[functional-verification]]` procedure applied; `[[known-gaps-tracker]]` records unsupported runners.
- **NOT COVERED**: live provider responses were deliberately not invoked because this phase verifies command isolation, not model quality, and the plan forbids remote CI before the final phase.

## Mutation proofs

- Removed Claude's `--setting-sources` pair temporarily. `TestEvalLoopCLIAdapter::test_isolated_cli_has_required_flags_and_model_pin[claude]` failed on the missing flag while the Codex case passed. The exact flag pair was restored.

- Added a temporary `fifth-cli` runner marker to the reference. `TestEvalLoopCLIAdapter::test_reference_branch_set_matches_runtime` failed and named the extra branch. The marker was removed.

- After both restorations, the complete eval-loop module passed 55 tests.

## Verification

- `python -m pytest catalog/hooks/tests/test_eval_loop.py -q`: 55 passed.

- `python scripts/validate_decision_records.py`: 46 records valid.

- `python scripts/validate_skills.py --bundles-only`: 337 skills scanned, 0 errors.

- `python scripts/ci/run.py --profile fast`: 15 passed, 0 failed, 0 skipped, 0 advisory in 150.0 seconds.

- `python -m py_compile scripts/optimize_skill_description.py catalog/hooks/tests/test_eval_loop.py`: passed.

- Broad Ruff on the two legacy files reported existing line-length and style findings. The one new `subprocess.run` finding introduced by the phase was fixed with `check=False`; no adjacent pre-existing formatting was changed.

- `git diff --check`: passed after restoring both mutations.

## CI impact

This phase changes one existing runtime script and extends one existing pytest module. It adds no dependency, environment variable, new test path, workflow, or required-check context. The existing repository pytest collection covers the tests and the fast profile covers repository contracts, so no pipeline edit is required in this phase.

## Session history

The phase began from clean Phase 1 commit `6ef46b4c`. Primary-source research confirmed the planned Claude and Codex levers and disproved full isolation for Gemini and OpenCode. The initial test-first run produced 13 intended failures. Runtime and documentation implementation reduced that to one compatibility test that still omitted the newly required run pin; updating its fixture call produced a 54-test pass. A clean CLI error preflight and its regression test raised the total to 55. The functional exercise initially used `trigger-cases.json`, whose routing-eval schema is not an optimizer eval corpus; the input validator rejected it before the new boundary. A one-case compatible temporary fixture then exercised the intended paths and was deleted. No branch push, pull request, paid model call, or remote CI run occurred.
