# v4.10.1 Phase 4 - Eval safety and runtime entry points

This record covers T014-T016 of the v4.10.1 eval-isolation and adaptive-compaction plan. It records the eval-code safety contract, runtime debugging table, functional exercise, validation, and CI impact in the enclosing commit named `docs(evals): define safety and runtime entry points`.

## Completed work

- Added a six-part eval-code safety contract covering untrusted model output, production and unrelated files, externally visible actions, bounded provider cost, published-result provenance, and isolated unit tests.

- Delegated spending-cap mechanics to `[[ai-billing-safeguards]]` instead of duplicating its rules.

- Added an `AGENTS.md` table mapping all 17 registered runtime integrations to the implementation file a maintainer opens first.

- Added four validator tests that derive the expected runtime roster and paths from the live registry, confirm every path exists, cover every machine-readable read-contract key, and lock the safety clauses.

## Functional exercise - runtime debugging entry points

- **Revision**: the enclosing commit named `docs(evals): define safety and runtime entry points`.
- **Artifact and boundary**: the runtime debugging table in `AGENTS.md`, checked against the integration registry and `docs/policy/platform-read-contracts.json`.
- **Command or action**: parsed the Markdown table, resolved each documented path from the repository root, and compared its keys with the read-contract keys.
- **Input**: current `AGENTS.md`, `scripts/lib/integrations/`, and `docs/policy/platform-read-contracts.json`.
- **Expected contract**: 17 registered integration rows, zero missing paths, and zero uncovered `contract_checks` keys.
- **Exit code or measurement**: exit 0; `rows=17 missing_paths=0 contract_keys=14 uncovered_contract_keys=[]`.
- **Observed output or state**: every runtime row names its registered integration module; all named files exist; every runtime governed by `contract_checks` has a row.
- **Comparison**: matches.
- **Environment**: Python 3 on Windows PowerShell in the isolated v4.10.1 worktree.
- **Evidence paths**: this record, `AGENTS.md`, and `tests/validators/test_agents_entry_points.py`.
- **Delegates**: `[[functional-verification]]` procedure applied.
- **NOT COVERED**: the exercise does not assert vendor discovery behavior; `scripts/verify_platform_contracts.py` owns that separate contract.

## Verification

- `python -m pytest -q tests/validators/test_agents_entry_points.py`: 4 passed.

- `python scripts/check_base_template_parity.py`: 5 of 5 lockstep templates present.

- The first unmodified-shell `python scripts/ci/run.py --profile fast` run reported 14 passed and 1 failed because PATH resolved `bash` to an unusable shim; all content and workflow checks passed.

- `$env:PATH='C:\Program Files\Git\bin;' + $env:PATH; python scripts/check_interpreter_resolution.py`: passed and resolved Git Bash.

- The full fast-profile rerun with the same process-local PATH correction passed: 15 passed, 0 failed, 0 skipped, 0 advisory in 9.9 seconds.

- `git diff --check`: passed before evidence recording and is rerun at the commit gate.

## CI impact

This phase changes documentation and adds one pytest module under the already-collected `tests/validators/` path. It introduces no runtime command, dependency, environment variable, workflow, or required-check context. Existing pytest collection, documentation checks, and template parity validation cover the changes, so no pipeline edit is required.

## Session history

The phase began from clean Phase 3 commit `bbc0fb50`. The first focused test exposed and corrected one display-name mismatch for Gemini CLI. The first fast profile exposed only the host PATH ordering issue described above; the prescribed process-local Git Bash ordering made the interpreter probe and full profile pass. No branch push, pull request, provider call, paid model call, networked eval, or remote CI run occurred.
