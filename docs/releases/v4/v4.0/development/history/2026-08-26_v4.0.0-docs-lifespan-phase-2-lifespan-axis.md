# Session History - v4.0.0 Docs Lifespan, Phase 2

**Date**: 2026-08-26
**Plan**: [`docs/releases/v4/v4.0/plans/v4.0.0-docs-lifespan-tree-and-enforcement.md`](../../plans/v4.0.0-docs-lifespan-tree-and-enforcement.md)
**Phase**: 2 - Lifespan axis (non-breaking)
**Branch**: `feat/v4.0.0-docs-lifespan-tree-and-enforcement`

## Starting state

Phase 1 was committed locally as `09a400a2`. The worktree was clean and the branch remained unpushed, preserving the plan's phase boundary.

## What this phase delivered

1. Replaced name-first policy with the enforceable admission question, "when does this document stop being true?", and six explicit lifespan dispositions.
2. Demoted the recognized subtree list to a lookup fast path. Unknown names now return to the admission test, with Cat 3 reserved for a genuinely indeterminate answer.
3. Added executable Signal 9 detection to `audit-docs.py`, comparing tracked frozen-bucket files with matching release-tag dates and returning structured findings without automatic moves.
4. Defined Signal 9 once in `references/link-integrity.md` and linked it from both `docs-layout-refactor` and `documentation-consistency`.
5. Added the snapshot naming rule and its audit failure mode to the skill and archive reference.
6. Added trigger cases and focused regression tests, then bumped `docs-layout-refactor` to 1.4.0 and synchronized its catalog entry.

## Troubleshooting

The first focused pytest invocation yielded without its session identifier while a temporary Git fixture was active. The process finished independently, but its result was not used as evidence. The fixture was hardened against inherited signing policy with local `commit.gpgsign=false` and `tag.gpgSign=false`; the bounded rerun then completed normally.

The catalog rebuild command exposed pre-existing generator drift in thousands of unrelated catalog rows and in the templates catalog. Those generated side effects were restored to the Phase 1 checkpoint. Only the source-derived fields for `docs-layout-refactor` were synchronized manually, and `check_registry_entries.py` confirmed the catalog contract.

## Verification gate

| Check | Result |
|---|---|
| Lifespan and link focused tests | PASS - 11 passed |
| Routing runner tests plus lifespan/link tests | PASS - 54 passed |
| Whole-catalog comparative routing | PASS - 325 skills, 453 cases, 0 failures |
| Registry structure and reachability | PASS |
| Skill body size | PASS - 474 lines |
| Windows-equivalent `make validate` chain | PASS - every listed command green |
| ShellCheck | PASS - `scripts/installer.sh` and `install.sh` |
| Repository-native fast profile | PASS - 12 passed, 0 failed |
| Post-documentation focused suite | PASS - 54 passed |
| Documentation conventions and whitespace | PASS |

## CI/CD impact

The repository-native test group already executes `tests/skills/`, and the catalog-validation group already runs the trigger gate and bundled-resource checks. Phase 2 adds no new runtime, dependency, secret, artifact path, or workflow event, so no workflow edit is needed. The final post-documentation profile result is recorded before the phase commit.

## Known gaps

No new Phase 2 gaps. A lifespan contradiction remains a finding that requires human intent review by design, not an automated remediation gap.

## Documentation and cleanup

The audit report records the three new artifacts and the single-source rule. `docs/DEVLOG.md` remains unchanged because v4.0.0 has not been released.

## Next steps

Phase 3 writes the decision record first, then performs the breaking prescription rename to `docs/releases/` and `docs/archives/` with migration guidance and contract updates.
