# Queue Assessment for v4.13.2

Authored: 2026-09-24
Skill: `plan-queue-assessment`
Subject: `docs/releases/v4/v4.13/plans/v4.13.2-implement-full-by-default-with-completion-goal.md`

This records the placement of the implement-to-completion plan. The user chose v4.13.2 on 2026-09-24, directly after the data-loss fix `v4.13.1`; the ranking below confirms that order and records what each queued plan inherits.

## Inventory

`python scripts/enumerate_plan_queue.py --root . --json` exited 0 (no unreadable plan). Membership follows the `v4.13.1` assessment: every plan from `v4.13.1` to `v4.17.7` is authored and unreleased.

## Surface overlap with the subject

The subject's declared surface (revised 2026-09-24 for cross-platform enforcement): `catalog/commands/implement.md`, `catalog/commands/update.md`, `catalog/skills/workflow/implement-phase/` (skill, runbook, references), `mandatory-final-phase.md`, `project-refactor`, `docs-layout-refactor`, `cicd-architect`, `scripts/check_plan_completion.py` and `scripts/completion_gate.py` with installer registration, `catalog/hooks/completion-gate.{sh,ps1}`, hook and plugin registration in most files under `scripts/lib/integrations/`, `scripts/nexus_hub_cli.py` (`run-plan`), and `docs/policy/completion-levers.{json,md}`.

| Queued plan | Overlapping paths | Verdict |
|---|---|---|
| `v4.13.1` user-edit preservation | `catalog/hooks/settings.json` | ordering: it lands first; its approval-collection pattern (pre-approved hook registration) is the precedent for this plan's upfront round |
| `v4.14.0` push-size guard | `scripts/installer.sh`, `scripts/installer.ps1` | ordering |
| `v4.15.0` report-contract discipline | `scripts/installer.sh` | ordering |
| `v4.16.0` instruction-necessity review | `catalog/commands/update.md` | ordering and content: its update-command edits must preserve the pre-approved release path |
| `v4.16.2` agent tooling and subagent cost | installers | ordering |
| `v4.17.3` harness economics | `loop-engineering`, installers | ordering; content: its unattended-loop outcomes should reuse this plan's completion checker verdicts |
| `v4.17.4` process safety and review pipeline | `catalog/hooks/settings.json` | ordering |
| `v4.17.6` testing, headings, Opus 5.5 | invokes `/implement` in its prompts | content: executes under the new full-by-default driver |
| `v4.17.7` hooks and single-item install | `catalog/skills/workflow/implement-phase/SKILL.md`, `scripts/nexus_hub_cli.py`, `scripts/lib/integrations/` | ordering and content: both add CLI subcommands and integration registrations; its spec-scope hook reads this plan's run record instead of adding a second pointer |
| `v4.15.1`, `v4.16.1`, `v4.16.3`, `v4.17.0`, `v4.17.1`, `v4.17.2`, `v4.17.5` | none | no impact |

## Ranking

Impact on the harness (maintainer judgement): every later plan is executed through `/implement`, so a driver that runs to completion reduces supervision for the whole queue. It ranks second only to `v4.13.1`, which fixes data loss occurring now. The user confirmed this order.

## Placement

`v4.13.2` is free (no plan, no tag), sorts after `v4.13.1` and before `v4.14.0`, so version order equals execution order with no renumbering.

## Unknowns

- Eight queued plans lack a declared status line (named in the `v4.13.1` assessment).
- `touched` lists are a lower bound.
