# Queue Assessment for v4.13.1

Authored: 2026-09-23
Skill: `plan-queue-assessment`
Subject: `docs/releases/v4/v4.13/plans/v4.13.1-user-edit-preservation-and-attribution-ban.md`

This records the queue ranking that placed the user-edit preservation plan at v4.13.1. It is the written artifact the ranking rule requires; the plan's `## Queued predecessors` section summarizes it.

## Inventory

`python scripts/enumerate_plan_queue.py --root . --json` exited 0 (no unreadable plan). Membership was established by rule 3 of the skill (no merged release for the version): every plan from `v4.14.0` to `v4.17.7` is authored and unreleased. Eight carry no parseable `**Status**:` line (`v4.15.0`, `v4.16.0`, `v4.16.1`, `v4.16.2`, `v4.17.0`, `v4.17.1`, `v4.17.4`, `v4.17.5`); they are treated as queued on rule 3, and adding a declared status line to each is left to their own next session.

## Surface overlap with the subject

Computed by intersecting each plan's `touched` list with the subject's declared surface, collapsed to directory level for skills, templates, and test folders. A shared `tests/validators/` or `catalog/hooks/tests/` directory where each plan adds its own new file is not a real conflict and is omitted below.

| Queued plan | Overlapping paths | Verdict |
|---|---|---|
| `v4.14.0` push-size guard | `scripts/installer.sh`, `scripts/installer.ps1` | ordering |
| `v4.15.0` report-contract discipline | `scripts/installer.sh` | ordering |
| `v4.15.1` de-slop repair | none | no impact (disjoint skills; no stated dependency) |
| `v4.16.0` instruction-necessity review | `templates/ai-instructions/` | content and ordering: the subject adds two always-loaded rules before `v4.16.0` records its before-figure; `v4.16.0` must count them in its baseline |
| `v4.16.1` model-adaptive harness | none | no impact (profile layer and runtime prototype only) |
| `v4.16.2` agent tooling and subagent cost | `scripts/installer.sh`, `scripts/installer.ps1` | ordering |
| `v4.16.3` agent experience and harness economics | none | no impact |
| `v4.17.0` frontier agent practice | none | no impact |
| `v4.17.1` AiKA modes | none | no impact |
| `v4.17.2` proactive memory intervention | none | no impact |
| `v4.17.3` harness economics and portable engineering system | `templates/ai-instructions/`, installers | ordering; content: its platform-neutral `AGENTS.md` owner must carry the two new rules forward |
| `v4.17.4` process safety and review pipeline | `catalog/hooks/settings.json` | ordering (both register hooks) |
| `v4.17.5` agent working agreement | `## Autonomous Operation` region of the templates | content: its boundary rule must not restate the subject's user-edit rule |
| `v4.17.6` testing, headings, Opus 5.5 | `document-to-interactive-html`, `deep-research-compilation`, `check_base_template_parity.py` | ordering; content: its Phase 3 template pass inherits the attribution rule from the subject instead of adding it |
| `v4.17.7` hooks and single-item install | `catalog/hooks/settings.json`, provenance ledger | ordering; content: its spec-scope hook reuses the subject's ledger reader |

## Ranking

Parallel compatibility: the subject overlaps the installers and templates, so it cannot run concurrently with `v4.14.0`, `v4.16.0`, `v4.16.2`, or `v4.17.3`, and there is no reason to run it concurrently with anything.

Impact on the harness (maintainer judgement, recorded with reasoning): the subject fixes data loss that is occurring now (a user's manual slide edits overwritten twice on 2026-09-23, with a prior occurrence reported) and an attribution leak the user bans outright. Both are user-mandated, both are safety fixes, and every other queued plan is an improvement rather than a repair. It ranks first. `v4.14.0` (also a safety guard) keeps second place. The relative order of `v4.15.0` through `v4.17.7` is unchanged: nothing in the subject changes the reasons those plans were sequenced as they are.

Class rule: the subject is not a documentation or refactor-class plan.

## Placement

A free slot exists below every queued plan: the last release is `v4.13.0`, and no plan or tag exists for `v4.13.1`. The subject takes `v4.13.1`, so version order equals execution order with no renumbering of any existing plan or comparison. The renumber procedure was therefore not invoked, and no residual-reference check was needed.

## Unknowns

- Eight plans lack a declared status line (named above). Their membership rests on rule 3.
- `touched` lists are a lower bound: a plan that edits a file without naming it on a task line is under-reported.
