# Session History -- v4.13.3 Phase 6: final duties, deep pass, and publication

**Date**: 2026-09-25
**Plan**: [v4.13.3-adoption-agent-practice-and-harness-token-efficiency](../../plans/v4.13.3-adoption-agent-practice-and-harness-token-efficiency.md)
**Phase**: 6 of 6 (T036-T045)
**Branch**: `feat/v4.13.3-adoption-agent-practice-and-harness-token-efficiency`
**Worktree**: `../Nexus-Hub-worktrees/v4133-session-context`

## What this phase was for

This phase closed the plan: prove the Goal landed, reconcile gaps and the pipeline, exercise every feature once more against its real entry point, pass the complete local gate, and publish once.

## Steps taken

Every duty's quoted output is in [`v4.13.3-last-phase-evidence.md`](../v4.13.3-last-phase-evidence.md).

1. **Refactor scan (T036)**: no empty directories or stray files in the touched paths; three stale comments now name `merge_instruction`.
2. **Known gaps (T037)**: a `## v4.13.3` section records the plan's five handoffs and seven items found while implementing, and the other 44 registers were searched for items this plan changes (none closed).
3. **Living docs (T038)**: the `distribution` handbook was stale on four changed code inputs. The claim review found no false claim, the build reproduced the committed bytes, and the receipt was refreshed; `check_handbooks.py` passes.
4. **Git hygiene (T039)**: clean, report only.
5. **CI/CD (T040)**: one difference caused by this plan. The `validate` job's explicit `--only` list omitted the new `legacy-fingerprints` group, so the drift check would never have run in CI. It was added, with a regression test.
6. **Deep pass (T041)**: 14 features exercised; an independent adversarial pass and an independent code-vs-plan review ran over the whole diff. One fix cycle applied:
    - pointer eligibility requires a catalog skill in the tree, not any `SKILL.md`;
    - whole-string token checks in both installers, and `legacy-report` echoes only well-formed tokens;
    - honest report wording;
    - the pointer-mode second pass also runs for a single integration;
    - an order pin for Codex before Copilot.
7. **Goal review and manual suggestions (T042-T043)**: written.
8. **Full gate (T044)**: `--profile full` PASS 62/62, plus a re-run of the doc-reading groups on the final tree.
9. **Publication (T045)**: see the evidence file's last section.

## Troubleshooting

### The fingerprint check failed as soon as CI would run it

Adding `legacy-fingerprints` to CI exposed that the committed file recorded the newest commit touching the installers and `base.py`, so the Phase 4 and 5 installer edits made `--check` fail with an identical hash set. The revision now tracks only the template and skill-index sources; regenerating changed one line.

### The Phase 4 convention for the evidence file would have overwritten v4.13.0

`last-phase-evidence.md` already holds v4.13.0's released evidence, so this phase writes the version-scoped `v4.13.3-last-phase-evidence.md`.

### Pointer eligibility under the real installers

The code-vs-plan review asked whether the per-platform installer calls could leave Copilot on the full index. A simulation of the installers' exact order on a fresh home produced the same eligible set as a combined run, and the order is now pinned by a test.

## Test results

- `python scripts/ci/run.py --profile full`: `PASS: 62 passed, 0 failed, 0 skipped` (5499 s).
- Final-tree re-run of the docs, contracts, fingerprints, hygiene, and workflows groups: `PASS: 25 passed`.
- Deep-pass fix reruns: 65 passed, then 4 passed.

## CI impact record

- `.github/workflows/ci.yml`: `legacy-fingerprints` added to the `validate` job's `--only` list. This is the pipeline reconciliation this final phase owns.
- `configs/installer-parity.json` and `scripts/ci/profiles.py` changes were already recorded in Phases 4 and 5.
- No new dependency.

## Plan delta

**Disposition: Delta, recorded.**

- The evidence file name is version-scoped.
- The deep pass narrowed pointer eligibility to trees holding catalog skills.
- The fingerprint revision field tracks only line sources.

No Definition of Done item is omitted.

## Deviations

None beyond the delta.

## Next steps

After merge: hold `/update release` until v4.13.1 lands, so tags stay in version order, as the maintainer decided.
