# Known Gaps -- v3.9.0

**Status**: v3.9.0 (loop-design + cross-model egress-hygiene enrichment from the design-time loop-coach and cross-model agent-runtime comparisons) is feature-complete on `develop`. The `adoption-looper-and-deer-flow` plan ([docs/releases/v3/v3.9/plans/adoption-looper-and-deer-flow.md](plans/adoption-looper-and-deer-flow.md)) landed all three phases: Phase 1 (handoff egress hygiene + reviewer-vs-judge verdict-honesty rule + argv-array invocation in `cross-model-orchestrator`), Phase 2 (design-first loop section + typed-verification ordering + render-and-confirm note + egress cross-link in `loop-engineering`), Phase 3 (default-deny host-execution posture in `agent-access-policy`, optional typed-fact schema in `context-pack-builder`, three reverse-engineering-matrix declines + the convergent-validation finding). All four edited bodies stayed under the 500-line norm; no frontmatter changed, so no `data/` registry edit was required (decision recorded below).

**Last updated**: 2026-06-25 (v3.9.0 adoption-no-mistakes Phase 3)

This file tracks per-phase unfinished work, intentional deferrals, bugs, missing tests, warnings, and bypassed quality gates for v3.9.0. The next version's `/plan` ingests the open items here. Category prefixes: `NI` (not implemented / skipped subtask), `DF` (intentionally deferred), `BG` (bug), `MT` (missing test), `WN` (warning / suppressed rule), `QG` (quality gate bypassed).

## Summary

| Category | Open | Resolved |
|---|---|---|
| NI | 0 | 0 |
| DF | 7 | 0 |
| BG | 0 | 0 |
| MT | 0 | 0 |
| WN | 3 | 0 |
| QG | 0 | 0 |
| **Total** | **10** | **0** |

## Open Items

| ID | Category | Source | Reason | Suggested next step | Severity |
|---|---|---|---|---|---|
| WN-v33-1 | WN | carried from v3.3.0 / v3.8.0 | Local Windows verification is partially emulated: `make` is not on PATH, so `make validate` / `make lint` / `make test` are run by invoking the underlying validators and pytest directly. For v3.9.0 the direct validate chain is green (JSON catalogs OK, per-skill orphan-bundle audit PASS, unicode-safety exit 0, the four enriched skills' new wikilinks resolve). | Confirm CI `validate` + `shellcheck` + `tests` are green on the release run. No code change expected. | Low (direct validator equivalents all green; only ShellCheck relies on CI). |
| WN-v36-1 | WN | carried from v3.7.0 / v3.8.0 | bash cannot be fully run on the Windows dev host: the checkout path contains a space ("OneDrive - Supira") and `bash.EXE` mis-resolves it, so the bash-invoking installer test suites hang or return exit 127 on this checkout. v3.9.0 changed no installer or bash code (skill-doc + docs only), so this does not bite the v3.9.0 diff. | CI (ubuntu / macOS) remains the authoritative bash gate. No production-code change. | Low (environment-only; reproduces only under space-containing paths; green on CI and space-free checkouts). |
| WN-v37-1 | WN | carried from v3.7.0 / v3.8.0 | The live `curl \| bash` one-liner against GitHub cannot be exercised in CI or on the Windows dev host. v3.9.0 made no installer change, so the v3.7.0 smoke-test checklist remains the verification path. | Run the recorded macOS smoke-test checklist ([docs/releases/v3/v3.7/development/mac-smoke-test.md](../v3.7/development/mac-smoke-test.md)) on a real Mac if not yet done. | Low (the macOS bash path is auto-verified in CI; only the live-network one-liner is manual; unaffected by v3.9.0). |
| DF-v36-2 | DF | carried from v3.6.0 / v3.8.0 | A portable, agent-agnostic declarative orchestration / loop runtime remains deferred and policy-disfavored as a runtime. The v3.9.0 enrichment reinforced this again by declining a portable loop runner script and its spec-compile step (both `drop-outright` in the reverse-engineering matrix, citing the v3.8.0 host-driver precedent). | Revisit only if cross-agent declarative orchestration becomes a stated product goal; until then the declarative loop definition + host `/loop` / `/goal` driver + the harness's Dynamic Workflows are the answer. | Low (deferred + policy-disfavored; reinforced by the v3.9.0 decline). |
| DF-v39-presentify-1 | DF | presentify-interactive-html Phase 1 | Scanned / image-only PDF OCR is out of scope for v1: `extract_content.py` reads the PDF text layer only, so a scanned PDF yields little or no text. | RESOLVED in v3.12.0: the two-tier scanned-page path (local OCR via optional rapidocr-onnxruntime/pytesseract + an always-on full-page image block for agent-vision reading) ships in `extract_content.py`; see `docs/v3/v3.12/known-gaps.md`. | Low (documented in the extraction runbook; text-bearing PDFs unaffected). |
| DF-v39-presentify-2 | DF | presentify-interactive-html Phase 1 | PDF raster-image extraction is not implemented in v1; a PDF contributes text and tables only (PPTX/DOCX images are extracted as base64). | RESOLVED in v3.12.0: PDF embedded-raster extraction (pypdf bytes + pdfplumber bboxes, repeated-asset dedup, caption pairing) AND vector-figure region rasterization (optional pypdfium2) ship in `extract_content.py`. | Low (PPTX/DOCX image path is implemented and tested). |
| DF-v39-presentify-3 | DF | presentify-interactive-html Phase 1 | Native PowerPoint / Word embedded chart objects are not extracted; chartable data is sourced from `.xlsx` numeric ranges, which map cleanly to `chart` blocks. | RESOLVED in v3.12.0: native PPTX charts (python-pptx plots/series) and DOCX chart parts (stdlib OOXML parse) map to `chart` blocks with provenance `native-chart` and the source's real values. | Low (the primary chart path - spreadsheet ranges - is implemented and tested). |
| DF-v39-presentify-4 | DF | presentify-interactive-html Phase 1 | Video / audio media embedded in any source format is ignored (the output is a single self-contained offline HTML deck; embedded media would break the offline / size guarantee). | Revisit only if embedded media is a stated requirement; would need a size + offline strategy. | Low (out of scope by design for a self-contained deck). |
| DF-v39-presentify-5 | DF | presentify-interactive-html Phase 2 | Custom brand-font embedding is not implemented in the builder: a theme / brand override supplies CSS font stacks, but a non-system custom font would need a base64 `@font-face` to stay self-contained. The builder keeps fonts as system stacks so the output never fetches a font. | Add an opt-in base64 `@font-face` embed path (read the font file from the brand `assets_dir`, inline it) if a brand custom font becomes a requirement. | Low (documented in `references/interactive-features.md`; system-stack themes and brand palettes are fully supported). |
| DF-v39-nomistakes-1 | DF | adoption-no-mistakes Phase 3 (N7) | The optional reverse-engineerable enrichment (a diff-to-session matching heuristic for `session-query`: discover candidate local agent sessions in the commit time window, score each by file-overlap against the diff, then summarize the chosen session into intent) was deliberately not built. The source tool itself deprioritizes after-the-fact transcript inference in favor of intent passed verbatim from the live conversation -- which is exactly the intent-as-verbatim-oracle guidance delivered in Phase 1 -- so its value is low when the agent already knows the intent. It is a `re-partial` item (a local, read-only heuristic over session logs already in `session-query`'s scope) that would require a reverse-engineering-matrix row because it reads transcript content. | Build only if a concrete need for after-the-fact intent recovery appears (the agent lacked the live intent and must reconstruct it). If built: add the diff-overlap-plus-time-window matching section to `session-query/SKILL.md`, add the required RE-matrix row classifying it `re-partial`, and confirm the existing redaction discipline covers transcript content. | Low (optional; verbatim live intent is preferred and already delivered in Phase 1). |

## Notes

- **Registry-edit decision (adoption-looper-and-deer-flow Phase 3.4): no edit needed.** The final `summary_l0` / `overview_l1` of all four enriched skills (`cross-model-orchestrator`, `loop-engineering`, `agent-access-policy`, `context-pack-builder`) still accurately describe them at the one-line / paragraph level; the enrichments are doctrine refinements within each skill's existing scope, not new headline capabilities. So `data/SKILL_INDEX.md`, `data/skills.json`, and `data/marketplace.json` were left unchanged, and `cross-model-orchestrator`'s footer version stays at 1.0.0. This follows the v3.8.0 loop-engineering-enrichment precedent (comparable body enrichment, no frontmatter / registry change).
- **Registry-edit decision (adoption-no-mistakes Phase 3.4): no edit needed.** The final `summary_l0` / `overview_l1` of all six enriched skills (`intent-based-review`, `receiving-code-review`, `multi-agent-code-review`, `shipping-and-launch`, `loop-engineering`, `pr-description-writer`) still accurately describe them; each enrichment is a doctrine refinement within the skill's existing scope (the finding-action taxonomy sits under `intent-based-review`'s "verify acceptance criteria" core, the verbatim-escalation rule under `receiving-code-review`'s "no performative agreement" stance, round-history hygiene under `multi-agent-code-review`'s "confidence-gated findings pipeline", the canonical pre-merge gate under `shipping-and-launch`'s "pre-flight checks", the no-busy-poll note under `loop-engineering`'s "goal-terminated loops", and the optional PR-body-from-trail pattern under `pr-description-writer`'s "risk, and reviewer notes"), not a new headline capability. `session-query` was not edited (N7 deferred -- DF-v39-nomistakes-1). So `data/SKILL_INDEX.md`, `data/skills.json`, and `data/marketplace.json` were left unchanged. This follows the v3.8.0 / v3.9.0 precedent (doctrine refinements within existing scope do not require a registry edit). The two declines (a host-side verification-gate runtime and default-on telemetry) are recorded as durable rows in [docs/policy/mcp-reverse-engineering-matrix.md](../../../policy/mcp-reverse-engineering-matrix.md), referencing [comparison-no-mistakes.md](comparison-no-mistakes.md).
- **Declines are durable, not gaps.** The three v3.9.0 declines (loop runner script, spec-compile step, advisory-only cost caps) and the convergent-design-validation finding are recorded as authoritative rows in [docs/policy/mcp-reverse-engineering-matrix.md](../../../policy/mcp-reverse-engineering-matrix.md) so a future comparison recognizes them as already-adjudicated rather than re-surfacing them as fresh gaps.
- **Separate v3.9.0 plan: presentify-interactive-html is COMPLETE (all four phases landed).** [docs/releases/v3/v3.9/plans/presentify-interactive-html.md](plans/presentify-interactive-html.md) is an independent v3.9.0 plan, now fully implemented on `develop`. Phase 1 (local multi-format extraction): `references/content-model.md` (the normalized schema), `scripts/extract_content.py` (the lazy-import PDF/Word/Excel/PowerPoint extractor), `references/extraction-runbook.md`. Phase 2 (the self-contained interactive HTML template + builder): `assets/presentation-template.html` (the offline self-contained scaffold with nav/outline/progress/fullscreen/keyboard/transitions/reduced-motion/responsive + print path), `assets/theme.json` (default theme on the theme-tokens schema with a documented theme-tokens / brand-styling override path), `scripts/build_presentation.py` (content model -> one offline `.html` with inline base64 images and inline SVG bar/line/pie/doughnut charts, plus a post-write no-external-reference self-check), and `references/interactive-features.md` (feature catalog + theme override + the hallmark-design enrichment pass + the three input modes). Phase 3 (SKILL.md + command + registration): `SKILL.md` (pushy description with verbatim triggers + SKIP, quoted `summary_l0` 11 words / `overview_l1` 132 words, all required sections in order, body 128 lines, references every bundle file), `catalog/commands/presentify.md`, and the registration surface (`data/skills.json` + `data/SKILL_INDEX.md` + `data/marketplace.json` specialized-domains 13 -> 14, plus AGENTS.md / README.md headline counts 256 -> 257 skills and 15 -> 16 commands). Phase 4 (worked example + full validation + docs): a reproducible worked example under [docs/releases/v3/v3.9/development/worked-example/](development/worked-example/) converted a sample 5-slide PPTX (flow preserved) and a sample 4-section DOCX report (synthesized title + agenda) into two self-contained `.html` decks, each verified offline (zero external fetch constructs, builder `assert_no_external` passed), well-formed (lxml), ASCII-only, with inline charts (a grouped bar chart and a line chart from the enrichment pass) and clearing the `hallmark-design` anti-slop gates; the full validate chain is green (JSON integrity, orphan-bundle audit 0 errors, quality 0/0, personal-paths / unicode-0-errors / supply-chain / workflow-security / version-sync / base-parity / compression-gate all pass) and the Reverse-Engineering Attribution grep is clean. No new deferrals across any phase; the feature's deferrals are tracked above (DF-v39-presentify-1..5). The installer + integrations pytest subsuites do not complete on the Windows dev host (carried WN-v36-1: bash mis-resolves the space-containing checkout path); every suite that runs there is green (extensions, hooks, validators).
- **presentify Phase 1 heuristic limitations (not blocking).** Two extraction heuristics are intentionally approximate and documented in [`references/extraction-runbook.md`](../../../../catalog/skills/specialized-domains/document-to-interactive-html/references/extraction-runbook.md): a worksheet is interpreted as one contiguous grid (a sheet holding several separate tables is treated as one), and PDF section headings rely on a short-first-line heuristic (PDF has no reliable heading structure). Both are recorded as runbook gotchas with the recommended source-side workaround, not as open defects.

## Resolved

_None resolved in v3.9.0._

Post-release note (2026-07-11): DF-v39-presentify-1, -2, and -3 were resolved in v3.12.0 (the presentify-fidelity-and-variety plan, Phases 1-5); DF-v39-presentify-4 (video/audio) and -5 (brand-font embedding) carried into the v3.12 ledger as DF-4 / DF-5.
