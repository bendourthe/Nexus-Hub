# Known Gaps -- v3.2.0

**Status**: v3.2.0 has two sub-plans. (1) `adoption-teach` -- all 3 of 3 phases complete on `feat/adoption-teach`, merged into `develop` (merge `8523281`): core `session-teach-back` skill + modes/depth-levels/checklist + cross-links. (2) `adoption-headroom` -- all 7 of 7 phases complete on `feat/adoption-headroom` (branched off the post-teach `develop` merge `8523281`). **Phase 1** (Foundation + SmartCrusher): scaffolded `extensions/nexus-context-compressor` (no-op `compress` pipeline + `CompressResult` metrics + an offline-safe tiktoken/stdlib token counter), implemented the deterministic SmartCrusher JSON-array dedup (global-uniqueness + adjacent-change scoring, stable SHA-256 CCR markers `<<ccr:HASH N_rows>>`), and registered the package copy + editable-install block in both installers (no MCP registration yet -- deferred to Phase 4). **Phase 2** (CCR reversible store): added the `ccr/` subpackage -- a shared marker codec (`ccr/marker.py`, single source of truth for the `<<ccr:HASH N_rows>>` grammar so producer and consumer cannot drift), a local SQLite `CCRStore` (`ccr/store.py`, content-addressed, idempotent `put`, WAL, oldest-first `prune` eviction) wired into SmartCrusher via optional dependency injection (`smart_crush(records, store=...)` stays pure when `store=None`), and the `retrieve()` interface (`ccr/retrieve.py`) that resolves a marker/object/bare-hash back to the originals or a named `NOT_FOUND` sentinel, never raising. 68 package tests pass (19 Phase 1 + 49 Phase 2; 100% coverage on `ccr/`); the round-trip is provably lossless and survives a store restart; `make validate` emulated green; no shell surface this phase. Deferred SmartCrusher refinements are logged below as DF-v32hr-1..3; the Phase 2 eviction-scheduling deferral is DF-v32hr-4. **Phase 3** (Remaining deterministic strategies): added the three remaining transforms -- `transforms/cache_aligner.py` (KV-cache prefix stabilizer: regex-detects volatile tokens -- dates, times, UUIDs, long-hex hashes, semantic versions, epoch timestamps -- moves those lines to the tail and normalizes whitespace, leaving a byte-identical stable prefix; optional spaCy NER seam default-off and graceful), `transforms/code_compressor.py` (AST-aware function/method body elision that REUSES the `nexus-code-search` tree-sitter extractors via `parse_file` rather than re-vendoring grammars, keeps imports/decorators/signatures/class structure by eliding only innermost body interiors, emits reversible `<<ccr:HASH N_rows>>` markers for bodies >= `ccr_min_lines` and a plain note for smaller ones, with a dependency-free regex/indent/brace fallback for unsupported languages or when the AST infra is absent), and `transforms/content_router.py` (classifies JSON-array / JSON-object / code / log / text and dispatches arrays to SmartCrusher + code to CodeCompressor, splitting mixed content on fenced code blocks and threading the CCR store). Extended the marker codec (`ccr/marker.py`) with `find_marker` / `find_all_markers` for markers embedded in free text (the form CodeCompressor leaves inside code comments) and registered the package in the `make test` target. 130 package tests pass (62 added in Phase 3; 92% total coverage, `ccr/` still 100%, the three new transforms 88-97%); all four sibling extension suites (skill-server 43, code-search 201, web-fetch 29, skill-scanner 87) remain green (no regressions); `make validate` emulated green; `make lint` N/A (Python-only, no shell surface). New Phase 3 deferrals/limitations are logged below as DF-v32hr-5..8. **Phase 4** (Runtime integration + retire rtk): added the runtime seam `compress_output(text, persist=...)` (the single-blob entry the hook and MCP call) plus a CLI (`compress`/`retrieve`/`serve` subcommands), wired the top-level `compress(messages)` through ContentRouter (no longer a no-op; identity on prose, so the Phase 1 scaffold tests stay green), and added a runtime "never expand / never lose output" guard. Shipped the opt-in/default-off PreToolUse hook `catalog/hooks/compress-output.sh` (rewrites a Bash command so its stdout pipes through the engine, preserving the exit code via `PIPESTATUS`; jq-gated, fail-open; registered in `settings.json` and copied by both installers) with a pytest suite, and the internal MCP server (`server.py`, lazy-`mcp` import behind the optional `[mcp]` extra) exposing `context_compress` + `context_retrieve`, registered in `catalog/mcp-configs/mcp-servers.json` (re-full) with a matrix row, auto-registered + `[mcp]`-installed by both installers, and added to CI (the compressor extension suite was not previously CI-gated -- now fixed). Superseded the external rtk recommendation in `guides/RTK_CONTEXT_COMPRESSION.md` (+ the `base-claude.md` template line) with an honest parity note. 159 package tests pass (29 added in Phase 4); the full `catalog/hooks` suite (434, incl. 8 new hook tests), `tests/validators` (136), `tests/installer` (70), `tests/integrations` (197) all green; all `make validate` validators 0 errors; skill-security gate exit 0; ShellCheck clean (new hook + installer.sh); installer.ps1 parses clean; a throwaway-venv `[mcp]`-extra install + `serve` startup confirmed. DF-v32hr-7 is resolved here; new Phase 4 items are DF-v32hr-9..11 and WN-v32hr-1..2; DF-v32hr-4 (auto-prune) refined and kept open. **Phase 5** (Accuracy-regression harness): added the top-level `extensions/nexus-context-compressor/evals/` harness (sibling of `src/`/`tests/`, runnable as `python -m evals` after a self-bootstrap of `src/` onto `sys.path`, so no install is needed) that proves compression preserves answer quality via deterministic, offline structural-fidelity checks instead of a live-LLM benchmark: CCR round-trip completeness (reconstruct == original for JSON arrays; elided code bodies resolve back exactly), signature-preservation rate for code, and a tokenizer-free character-reduction effectiveness floor. Shipped a fixed local dataset (`fixtures/`: two JSON arrays + a Python and a TypeScript module with a `manifest.json` answer key), a committed `baseline.json` (fidelity pinned at 1.0; effectiveness floor 0.36, a 10-point margin below the measured 0.458), Markdown/JSON reports, and an intentional `--update-baseline` path. Wired the gate three ways: a `make compress-eval` target, a step inside `make validate`, and a step in the CI `validate` job alongside the skill-security gate. 12 new harness tests (incl. the load-bearing proof that an irreversible compressor -- a no-op CCR store -- is caught by the gate, plus synthetic below-floor / signature-loss detections); 171 package tests pass total; the gate command runs verbatim green (CCR 100%, signatures 100%, reduction 45.8%); AST and regex strategies produce identical fixture numbers so the floor is environment-independent. New Phase 5 items are DF-v32hr-12..13 and WN-v32hr-3. **Phase 6** (Optional ML token-dropper): added `transforms/ml_token_dropper.py` -- a DEFAULT-OFF, lossy, opt-in free-text compressor that ports headroom's Kompress ModernBERT importance-scoring drop. It scores each whitespace word (a pluggable `scorer` seam; the shipped default backend loads pre-placed public ONNX weights via the optional `[ml]` extra, now `onnxruntime`+`numpy`+`tokenizers`), keeps the top `target_ratio` by importance, and rebuilds readable text preserving inter-word spacing of surviving runs. It is offline-first to the strongest degree -- the module never downloads weights (it only *loads* pre-placed local ones, like tiktoken's vocab / the v3.0.0 OSV.dev DB) and sends no user data anywhere -- and degrades gracefully (onnxruntime/numpy/tokenizers or weights absent, or a failing scorer => original text + a precise install hint, never a raise or a fetch). It is a standalone transform (exported from `transforms/`), NOT wired into the default ContentRouter/`compress` pipeline, so the deterministic strategies remain the default. 35 new tests (206 package tests pass, 2 numpy-reduction tests skip via `importorskip` since the heavy deps are absent), covering off-by-default, drop-to-ratio with an injected scorer, every backend degradation branch via `sys.modules` stubs, no-network (socket poisoned), determinism, and the pure selection/reconstruction/pooling helpers; the Phase 5 accuracy gate re-runs green with the module present (it is off the deterministic pipeline by design). **Phase 6 reconciliation (2026-06-10)**: Phase 6 was implemented twice in parallel (a duplicate run on a second machine); the pushed implementation (`e6ddbc5`) was kept as the base and **CCR-reversibility was grafted onto it** per the maintainer's decision, so the dropper now upholds the engine's every-drop-reversible invariant: `drop_tokens(text, *, store=...)` persists the full original behind a `<<ccr:HASH N_rows>>` marker appended to the kept preview (resolvable via `retrieve`, exactly like SmartCrusher/CodeCompressor), with a never-expand guard so the marker's token cost never grows a small payload; with `store=None` (the default) it stays the pure lossy preview e6ddbc5 shipped, so all 35 original tests are unchanged. 7 reversibility tests added (full suite green: 215 pass on this host where numpy is present; the 2 numpy-reduction tests skip on a numpy-absent host). New Phase 6 items: DF-v32hr-10 resolved (the scheduled free-text mechanism shipped, opt-in/default-off); new DF-v32hr-14 (ML dropper not wired into the default runtime pipeline), DF-v32hr-15 (whitespace-word vs upstream sub-word granularity), MT-v32hr-1 (the live ONNX inference glue is unrunnable in CI without the `[ml]` deps + weights), WN-v32hr-4 (partial Windows local verification). **Phase 7** (Methodology cross-links + docs): cross-linked `nexus-context-compressor` as the programmatic counterpart in the three context skills (`catalog/skills/orchestration/context-compression`, `orchestration/prompt-token-optimization`, `developer-experience/context-optimization`) with a "when to reach for the engine" note + `[[ ]]` cross-links and no engine-internals duplication; replaced `context-optimization`'s verbatim `cargo install --git rtk` setup with the internal-engine setup (`NEXUS_CONTEXT_COMPRESS=1`) + an rtk-migration note (resolving DF-v32hr-11); surgically synced that skill's now-false rtk `description`/`overview_l1` in `data/skills.json` (a full `build-catalog` regen of the broader frontmatter-to-`skills.json` drift is left to the release-time bump, same disposition as WN-v32-3); expanded the engine README with an `Architecture` section (ContentRouter -> strategy pipeline, module map, CCR reversibility, local-first/zero-outbound, ML offline-first, rtk migration) and added a project-level `docs/v3/v3.2/context-compressor-architecture.md`. Documentation-only, no code/shell surface: the full validator suite (bundles/personal-paths/unicode/supply-chain/workflow/solution-frontmatter/version-sync) ran 0 errors, the 215-test compressor suite passed, the compression accuracy gate passed (CCR 100%, signatures 100%, reduction 45.8%), and the skill-security scan exited 0 (no HIGH/CRITICAL). No new Phase 7 deferrals; DF-v32hr-11 moves to Resolved. adoption-headroom is now complete end-to-end. The v3.2.0 version bump, CHANGELOG finalization, count-prose reconciliation, and tag are applied at the develop->main release (owned by `/update version` / `/update release`).
**Last updated**: 2026-06-10 (live-IDE smoke: WN-v32ag-3 resolved -- Antigravity IDE slash commands are workspace-scoped via `.agents/workflows/`, verified; global file-drop is not IDE-scanned)

This file tracks per-phase unfinished work, intentional deferrals, bugs, missing tests, warnings, and bypassed quality gates for v3.2.0. The next version's `/plan` ingests the open items here. Category prefixes: `NI` (not implemented / skipped subtask), `DF` (intentionally deferred), `BG` (bug), `MT` (missing test), `WN` (warning / suppressed rule), `QG` (quality gate bypassed).

## Summary

| Category | Open | Resolved |
|---|---|---|
| NI | 0 | 0 |
| DF | 13 | 3 |
| BG | 0 | 1 |
| MT | 1 | 0 |
| WN | 10 | 0 |
| QG | 0 | 0 |
| **Total** | **24** | **4** |

## Open Items

| ID | Category | Source phase | Plan reference | Reason | Suggested next step | Severity |
|---|---|---|---|---|---|---|
| WN-v32-1 | WN | Phase 1 (grown in Phase 2) | T001 (write the core skill); T002 (register); T004 (teaching-mode trigger phrases) | `scripts/validate_skills.py` run with no flags (its default mode) reports a description-length error (description > 250 characters) for `session-teach-back` (923 chars after Phase 2's T004 added 2 more verbatim teaching-mode trigger phrases; 828 chars at Phase 1), whose pushy `description` carries 7 verbatim trigger phrases + a 3-way SKIP clause as the plan's T001/T004 mandate. This is NOT a gate: `make validate` and the CI `validate` job run only `validate_skills.py --bundles-only` and `--quality` (both PASS), never the bare default; the strict `--allow-existing` path demotes the violation to a warning for files in `scripts/validate_skills.allowlist.json`, to which `session-teach-back/SKILL.md` was added (mirroring its sibling `session-query` and the two most-recent skills, `skill-security-scan` and `agent-orchestration-primitives`). The 250-char cap directly conflicts with the AGENTS.md mandatory pushy-description rule and the plan's explicit T001 requirement. The long description is intentional and norm-consistent. (Same root class as v3.1.0 WN-v31cr-1; note that a few other catalog skills -- e.g. `ai-attack-patterns`, `pentest-reporting`, `git-branching-workflow` -- carry pushy descriptions not yet in the allowlist on `develop`, a separate pre-existing item, not introduced here.) | Do not shorten the pushy description to chase the default-mode cap. If the divergence is undesirable catalog-wide, reconcile `validate_skills.py` default-mode behavior with the pushy-description rule (raise/remove the 250-char cap or scope it to a non-`description` field). Out of scope for this plan. | Low (not a CI/`make validate` gate; by-design per the authoring contract) |
| WN-v32-2 | WN | Phase 1 (re-confirmed Phases 2-3) | T003, T007, T010 (stabilization) | Local verification on the Windows dev host is partial: `make` is not on PATH, so `make validate` and `make scan` were emulated by invoking each validator and the scanner directly (all green at Phases 1, 2, and 3). `make lint` (ShellCheck) was not run -- all three phases added only Markdown + JSON, so there is no shell surface. (Same root class as v3.1.0 WN-v31cr-2 / WN-v31-1.) | Confirm the CI `validate` + scanner jobs are green for this commit on the ubuntu runner (it runs the same validators + the scanner gate); no code change expected. | Low (covered by CI; phase added no shell/Python surface) |
| WN-v32-3 | WN | Phase 1 | T002 (register); T003 (consistency) | Count-prose surfaces on `develop` predate the v3.1.0 release-time count reconciliation: on `origin/develop` the `data/SKILL_INDEX.md` Total label read 248 while `data/skills.json` already had 250 entries (the v3.1.0 reconciliation to 250 was applied on the release/main side). Adding `session-teach-back` bumped the label by 1 (248 -> 249) and the true skills.json/marketplace count to 251, so the machine-readable registries agree at 251 but the SKILL_INDEX Total label and other headline-total prose surfaces (README, AGENTS.md, plugin description) still read pre-reconciliation values on `develop`. | Reconcile every count-prose surface to the truthful total (251 after Phase 1) at the v3.2.0 develop->main release bump via `/update version` -- exactly as the resolved v3.1.0 WN-v31-3 did at that release. No per-phase action needed; do not hand-reconcile on the feature branch. | Low (machine-readable registries already consistent at 251; prose reconciliation is the release bump's job) |
| DF-v32hr-1 | DF | adoption-headroom Phase 1 | T003 (SmartCrusher) | SmartCrusher dedups on EXACT canonical content: a record whose only difference from a neighbour is a volatile field (timestamp, request id) is treated as novel, and is collapsed only if its adjacent field-change count falls below `variance_threshold`. Fuzzy near-duplicate fingerprinting (the SimHash/MinHash-style detection production engines use to collapse records that differ only in noise) is intentionally out of scope for the v1 deterministic, dependency-free port. The SmartCrusher docstring points here. | CacheAligner shipped in Phase 3 (T008) and now normalizes dynamic fields (dates/UUIDs/versions/hashes), removing most of the noise that defeats exact dedup; the pure-stdlib fingerprint near-dup pass itself is still deferred -- let the Phase 5 accuracy-regression harness (T016) measure whether it improves the ratio/accuracy trade-off before adding it. | Low (exact dedup is lossless and gate-passing; near-dup is an enhancement) |
| DF-v32hr-2 | DF | adoption-headroom Phase 1 | T003 (SmartCrusher) | The keep budget is a fixed `SmartCrusherConfig.max_items_after_crush` (default 15), per the plan's config contract. Information-theoretic auto-sizing (stop keeping items once additional ones add no new information, e.g. a knee/elbow on a coverage curve) -- the adaptive sizing production engines use -- is deferred; the plan deliberately scoped v1 to the fixed cap. | Optional enhancement after the Phase 5 eval harness exists: the harness (T016) can measure whether adaptive sizing beats the fixed cap on the ratio/accuracy trade-off before any added complexity ships. | Low (fixed cap meets the stability gate; auto-sizing is an optimization) |
| DF-v32hr-3 | DF | adoption-headroom Phase 1 | T003 (SmartCrusher) | Anomalous records are preserved implicitly today: a record differing from its predecessor in >= `variance_threshold` fields is kept, and a one-off error line survives as novel content. Explicit error/outlier preservation (always retain `level=ERROR` / `status>=4xx` / statistical numeric outliers regardless of variance, as production string/number-array specializations do) is deferred to avoid shipping an error-detection heuristic that could misfire (false-keep junk, false-drop a genuine error) without the Phase 5 harness to measure it. | Phase 3's ContentRouter (T009) was kept minimal per its prompt (log/text pass through untouched), so explicit error/outlier preservation was NOT added and remains deferred to Phase 5: fold type-specific preservation into per-content-type handling once the accuracy harness (T016) can measure it. | Low (distinct/novel records, including lone errors, already survive) |
| DF-v32hr-4 | DF | adoption-headroom Phase 2 | T005 (CCR store) | The CCR store ships an eviction *primitive* (`CCRStore.prune(max_entries=..., older_than_seconds=...)`, oldest-first by `created_at`) but no consumer invokes it automatically: `put` only inserts, so the default store at `~/.nexus-hub/cache/ccr-store.db` grows unbounded across sessions until something prunes it. This is deliberate -- auto-eviction on the hot path would add a clock/IO side effect to the per-drop write, and the *policy* (threshold, schedule, max size) belongs to the runtime that owns the store lifecycle, which does not exist until Phase 4 wires the PreToolUse hook + internal MCP. | Still open after Phase 4: the runtime (PreToolUse hook + MCP server) now exists, but neither invokes `store.prune(...)` -- the compress path only `put`s. Wire a documented prune policy into a runtime owner (e.g. an MCP-server-startup TTL sweep, or a periodic max-entry cap), since the server process is the natural lifecycle owner. A pruned span's marker already resolves to `NOT_FOUND`, which the consumer handles, so eviction is safe. Defer the policy choice to a follow-up rather than guess a threshold. | Low (eviction primitive exists and is tested; only the auto-scheduling is deferred, and a cold store simply never prunes) |
| DF-v32hr-5 | DF | adoption-headroom Phase 3 | T009 (ContentRouter) | ContentRouter splits mixed content on fenced code blocks and classifies each whole non-fenced chunk; a JSON array embedded mid-paragraph *inside* prose (neither the whole chunk nor a fenced block) is left untouched rather than isolated and crushed. Deliberate: paragraph-level splitting of prose risks cutting non-code text, while the high-value shapes (a tool dump that *is* a JSON array, and fenced ` ```json ` / code blocks) are handled exactly. | If real payloads show prose-embedded arrays worth compressing, add paragraph-level segmentation guarded by per-paragraph JSON validation; measure the ratio/accuracy effect with the Phase 5 harness (T016). | Low (common JSON shapes are routed; only prose-embedded arrays are skipped, never mangled) |
| DF-v32hr-6 | DF | adoption-headroom Phase 3 | T010 (CodeCompressor) | The dependency-free regex/structural fallback -- used only when the reused `nexus-code-search` AST infra is unavailable, or for a brace language it does not cover (e.g. `.js`/`.jsx`) -- is best-effort: it does not tokenize strings/comments while tracking brace depth, and it requires a brace function's body-opening `{` to share a line with the parameter list (multi-line signatures are not detected). The AST path -- the default whenever `nexus-code-search` is importable -- is string-aware via tree-sitter and has neither limitation. | Optional: harden the brace fallback (string/comment-aware depth, multi-line-signature detection) only if a meaningful share of real inputs hit the fallback; otherwise rely on the AST path. The 232-252 sibling-path-discovery branch in `_load_ast` is uncovered because `nexus-code-search` is editable-installed in the dev/CI env (the direct import wins); it is the air-gapped-checkout fallback. | Low (AST path is primary and unaffected; the fallback degrades to under-compression, never a mis-cut) |
| DF-v32hr-8 | DF | adoption-headroom Phase 3 | T008 (CacheAligner) | The optional spaCy NER pass for richer dynamic-entity detection is default-off and not bundled: with spaCy (or its model) absent, CacheAligner uses the regex detector only and degrades silently (covered by a fake-spaCy test). The regex detector's epoch (10-13 digit) and semantic-version patterns can in rare cases flag a stable line that merely contains such a number, moving it to the tail and shrinking the cacheable prefix -- a conservative miss, never data loss. | If prefix-stability regressions appear, ship the spaCy extra on a documented install path or tighten the epoch/version patterns with surrounding-context anchors; measure the prefix-hit rate. | Low (regex path is lossless and deterministic; NER is an opt-in enhancement) |
| DF-v32hr-9 | DF | adoption-headroom Phase 4 | T012/T013 (runtime) vs T008 (CacheAligner) | Phase 4 wired `compress(messages)` and `compress_output(text)` through ContentRouter, but CacheAligner (KV-cache system-prompt stabilization) is NOT applied anywhere in the runtime path: the hook compresses Bash *output* (not system prompts) and `compress(messages)` routes content without a CacheAligner pass over system messages. CacheAligner remains a standalone, individually-callable transform. | Apply CacheAligner to system/leading messages inside `compress(messages)` (or a dedicated `align_system_prompt` seam) when a runtime that controls the system prompt exists; the PreToolUse-hook path legitimately does not touch system prompts, so this is a messages-API enhancement, not a hook gap. | Low (CacheAligner is complete and tested in isolation; the cache-hit benefit is unrealized until wired) |
| WN-v32hr-1 | WN | adoption-headroom Phase 4 | T012 (hook) | The PreToolUse hook rewrites the Bash command via `hookSpecificOutput.updatedInput`, but the repo's authoritative `guides/CLAUDE_CODE_SETTINGS_REFERENCE.md` documents only the exit-code hook contract (0/2/other), not a command/input-rewrite mechanism. If a Claude Code version does not honor `updatedInput`, the hook is a safe no-op (the original command runs unmodified -- the emitted JSON is simply ignored). The hook is also opt-in/default-off (`NEXUS_CONTEXT_COMPRESS=1`) and jq-gated, so it never acts unless explicitly enabled. The pytest suite verifies the hook's transformation *logic* (emitted JSON shape), not live Claude honoring of it. | If Claude Code's `updatedInput` support is confirmed/denied, note it in the settings reference; if denied, switch the integration to whatever output-rewrite mechanism the installed version supports (or rely on the Windows-style CLAUDE.md-instruction path on all platforms). | Low (fail-safe: no-op when unsupported; opt-in; output never lost) |
| WN-v32hr-2 | WN | adoption-headroom Phase 4 | T012/T015 (local verification) | Local verification on the Windows dev host is partial (same root class as WN-v32-2): `make` is not on PATH (every target emulated by invoking the underlying tool directly -- all green), and `jq` is not installed by default, so the hook's command-rewrite tests were exercised by downloading a throwaway `jq.exe` onto PATH (the 4 rewrite-path tests skip gracefully without jq; the 4 inert-path tests run everywhere). CI (ubuntu) runs the same validators, the full `catalog/hooks` suite, and the new compressor-extension step. | Confirm the CI `validate` + hooks + compressor-extension jobs are green on the ubuntu runner (jq present there exercises the rewrite path); no code change expected. | Low (covered by CI; the rewrite-path tests skip safely without jq) |
| DF-v32hr-12 | DF | adoption-headroom Phase 5 | T016 (accuracy harness) | The harness proves "accuracy preserved" via deterministic STRUCTURAL fidelity (CCR round-trip completeness, code signature preservation) rather than a live-LLM semantic benchmark (headroom's `eval.yml` runs GSM8K / TruthfulQA / SQuAD / BFCL through a model). This is a deliberate scope choice: a live model call is non-deterministic, slow, and an outbound call this repo refuses to add, and the plan's T016 explicitly specifies deterministic structural-fidelity checks. The trade-off: the gate proves compression is lossless/reversible and structure-preserving, but does not measure whether a model *answers* as well on compressed context. | A true semantic accuracy benchmark would require a live (or local) model harness and a QA dataset; defer indefinitely unless an offline local-model eval becomes worthwhile. The structural gate is the mandated, sufficient guard for shipping aggressive ratios (comparison Section 13). | Low (structural fidelity is exact and is the mandated pre-aggressive-ratio guard; semantic benchmark is an optional future addition) |
| DF-v32hr-13 | DF | adoption-headroom Phase 5 | T016/T017 (effectiveness gate) | The effectiveness floor gates CHARACTER-length reduction, not token reduction, because token counts depend on whether `tiktoken` (and its cached vocab) is available offline -- a value that varies across environments and would make the floor flaky. The token ratio is computed and reported but never gated. Consequence: a hypothetical regression that preserves character reduction while worsening *token* savings would not trip the gate. | If a token-savings regression mode ever matters, add a separate token-reduction floor guarded behind `using_accurate_counter()` (skip the token assertion in fallback mode). Char reduction is a faithful, deterministic proxy for now. | Low (char reduction tracks token reduction closely for these content types; the headline token number is reported, just not gated) |
| WN-v32hr-3 | WN | adoption-headroom Phase 5 | T018 (local verification) | Same Windows local-verification class as WN-v32-2 / WN-v32hr-2: `make` is not on PATH, so `make validate` / `make compress-eval` were emulated by running the gate command verbatim (`cd extensions/nexus-context-compressor && python -m evals --check`, exit 0) and invoking the `make validate` validators directly (personal-paths / unicode-safety / version-sync all 0 errors). `ruff` is not installed locally, so lint was a `py_compile` syntax check (clean); the CI `validate` job's pre-commit run covers ruff. | Confirm the CI `validate` job (which now runs the compression gate) and the `tests` job (compressor suite incl. `test_evals.py`) are green on the ubuntu runner; no code change expected. | Low (covered by CI; the gate command itself ran green locally) |
| DF-v32hr-14 | DF | adoption-headroom Phase 6 | T019 (ML token-dropper) vs T009 (ContentRouter) | The ML token-dropper is a standalone, individually-callable transform (`transforms.drop_tokens`); it is NOT wired into the default `ContentRouter`/`compress`/`compress_output` runtime path, which still passes free text/prose through unchanged. So even a user who installs the `[ml]` extra + places the public weights + sets `enabled=True` gets no automatic free-text compression in the runtime today -- they must call `drop_tokens` directly. This deliberately mirrors the CacheAligner situation (DF-v32hr-9): a lossy default belongs off the auto pipeline until a runtime owner opts it in explicitly, and the Phase 5 gate guards structural fidelity, not free-text drops. | Add an opt-in seam in `compress_output`/`route` (behind an explicit config flag, default-off) that runs `drop_tokens` over `TEXT`-classified segments when the user has enabled the ML path; gate the default ratio against a free-text fidelity measure before any default-on consideration. | Low (the capability exists and is fully tested in isolation; only the runtime auto-wiring is deferred, by design) |
| DF-v32hr-15 | DF | adoption-headroom Phase 6 | T019 (ML token-dropper) | The port scores and drops at WHITESPACE-WORD granularity rather than the upstream Kompress sub-word-token granularity. This was chosen for clean, dependency-light reconstruction (a word rebuilds to readable text without a sub-word detokenizer, and surviving-run spacing is preserved) and so the drop unit is the unit a reader sees. The trade-off: a coarser drop unit than sub-word scoring, and the model's sub-word importance scores are mean-pooled up to their covering word (`_pool_subword_scores`). | If finer-grained dropping proves worthwhile, add a sub-word reconstruction path (needs the tokenizer's detokenizer and offset handling) and measure the ratio/fidelity effect with a free-text eval fixture before switching the default granularity. | Low (word-level drop is faithful to the importance signal and reconstructs cleanly; sub-word is an optional refinement) |
| MT-v32hr-1 | MT | adoption-headroom Phase 6 | T020 (tests) | The live ONNX inference glue cannot be exercised in CI: `onnxruntime`, `numpy`, `tokenizers`, and the public weights are all absent in the dev/CI environment, so `_onnx_word_scores` (real session.run + tokenizer encode) and the scorer returned by `build_onnx_scorer`'s all-present branch are never *called* under test. The two `_reduce_logits_to_importance` tests skip via `importorskip("numpy")`; `build_onnx_scorer`'s guard branches (each dep absent, weights absent, load failure, all-present-returns-callable) ARE covered with `sys.modules` stubs, and the entire drop/selection/reconstruction/pooling logic is covered through an injected scorer. So the untested surface is only the thin numpy/onnxruntime/tokenizers call-glue, which degrades safely (`_safe_scores` wraps it; any fault returns the original text). | Add an opt-in CI lane (or a documented local dev test) that installs the `[ml]` extra and a tiny fixture ONNX importance model + tokenizer to exercise the live inference path end-to-end. | Low (pure logic fully tested; the untested glue is thin, uses documented APIs, and fails safe to identity) |
| WN-v32hr-4 | WN | adoption-headroom Phase 6 | T020 (local verification) | Same Windows local-verification class as WN-v32-2 / WN-v32hr-2 / WN-v32hr-3: `make` is not on PATH, so `make validate` / `make test` were emulated by invoking the compressor pytest suite, the eval gate (`python -m evals --check`, exit 0), and each `make validate` validator directly (bundles audit, no-personal-paths, unicode-safety, supply-chain-iocs, workflow-security, solution-frontmatter, version-sync -- all 0 errors). `onnxruntime`/`numpy`/`tiktoken` are absent locally, so the engine ran in its stdlib-token-fallback mode and the ML path degraded to its install-hint branch -- exactly the CI posture. | Confirm the CI `validate` + `tests` jobs are green on the ubuntu runner; no code change expected. | Low (covered by CI; the absent-deps degradation is the intended default path and is directly tested) |
| WN-v32ag-1 | WN | post-release Antigravity-compat fix | `scripts/lib/integrations/antigravity.py` (hooks) | The Antigravity `hooks.json` uses the confirmed `run_command` matcher for shell hooks (git-guardrails, compress-output) but a match-all matcher (`""`) for the file-content guards (secret-scan, large-file-guard) because the exact Antigravity write/edit tool names (the equivalents of Claude's `Write`/`Edit`) are not yet verified against a live `agy` binary. Match-all is safe (the scripts self-filter and are fail-open) but runs the guards on more tool calls than necessary. | Once an `agy` binary is available for a live smoke, confirm the file-write/edit tool names and narrow the file-guard matchers from `""` to the precise tool list. | Low (match-all is safe + fail-open; only mildly over-broad) |
| WN-v32ag-2 | WN | post-release Antigravity-compat fix | `scripts/lib/integrations/antigravity.py` (hooks) | The curated hook scripts (`secret-scan.sh`, `large-file-guard.sh`, `git-guardrails.sh`, `compress-output.sh`) parse the Claude Code hook stdin JSON schema (field names like `tool_input`/`tool_name`). Antigravity documents the same allow/deny stdin/stdout contract, but the exact input field names were not verified against a live `agy`. If a field name differs, the affected script degrades to a no-op (fail-open) rather than a false block. | On a live `agy` smoke, diff Antigravity's hook stdin JSON against the Claude schema; if field names differ, add a small input-normalization shim shared by the ported scripts. | Low (scripts are fail-open; a schema mismatch is an ineffective hook, never a wrong block) |
| WN-v32ag-3 | WN | post-release Antigravity-compat fix | `scripts/lib/integrations/antigravity.py` (global paths) | RESOLVED EMPIRICALLY 2026-06-10 against the live Antigravity 2.0 IDE: workflows surface as `/<name>` slash commands ONLY from an OPEN workspace's `.agents/workflows/` (verified -- both a minimal probe and the real `plan.md`, with its long YAML `description`, registered correctly). The desktop IDE does NOT scan the global `~/.gemini/antigravity/{workflows,global_workflows}` mirror for slash commands: `plan.md` was present in both global dirs yet `/plan` did not appear on the welcome screen with no folder open. The IDE's global workflows are created through its own "+ Global" UI into an internal registry, not by file-drop. CONSEQUENCE: a global-only install does not give the IDE slash commands; the user must run a workspace/project install (`.agents/`) and open that folder. The global mirror is harmless and retained for the `agy` CLI (whose discovery was not separately smoke-tested). | Guidance updated: the `base-antigravity-20.md` template and installer now direct IDE users to a workspace install. Optionally, drop the IDE global workflow/skill mirror (keep only the CLI root) since the IDE ignores it -- deferred (harmless, and the CLI may use it). The maintainer chose not to chase a file-based global IDE mechanism (it is UI/registry-managed). | Low (workspace path verified working; global IDE slash via file-drop is not supported by Antigravity by design) |
| DF-v32cmd-1 | DF | v3.2.0 deprecated-command removal | shim cull (post-Phase-7) | After deleting the 40 deprecated command shims, the hard breakages were all fixed (the installer smoke test, every dangling `catalog/commands/<old>.md` file/link reference, the installer/base.py/new-feature old-command MENTIONS, and the `using-nexus-hub` onboarding command table). A residual remains: ~134 old-command slash *mentions* in ~25 catalog skill bodies + style-guides still name removed commands (e.g. `implementation-plan`, `known-gaps-tracker`, `project-refactor`, `spec-driven-development`, `cross-artifact-analyzer`, `tasks-to-issues`, `project-constitution`, `pr-description-writer`, `deep-research-compilation`, `idea-refine`, `business-logic-abuse`, `advanced-attack-patterns`, `test-strategy-doc`, `cross-project-comparison`, `docx-generation`, `mcp-builder`, `docs-layout-refactor`, plus the `commands-cheatsheet` / `markdown` style-guides and the `tasks-to-issues` helper scripts -- naming `/generate-plan`, `/refactor-project`, `/analyze-spec`, `/run-penetration-test`, etc.). These are stale prose, not broken commands: no test, slash menu, or file link is affected (all fixed), and many sit in the retained skills the consolidated commands delegate to. The slash-menu noise that motivated the removal is fully resolved. | Run a dedicated command-reference modernization sweep mapping each old `/name` to its consolidated form per `docs/v3/v3.0/command-migration.md`, handling retained-skill self-references and step-number pointers with care; ship as a non-breaking v3.2.1 patch or a develop commit. | Low (prose-only staleness; menu noise resolved, hard breakages fixed) |

## Resolved

| ID | Category | Source phase | Resolved in | Note |
|---|---|---|---|---|
| BG-v32ag-1 | BG | v3.2.0 release (and earlier) | post-release Antigravity-compat installer fix | Skills and commands never surfaced in the Antigravity 2.0 IDE. Two causes: (1) the installer copied `catalog/skills/` verbatim, so skills landed at `skills/<category>/<name>/SKILL.md` while Antigravity discovers a FLAT `skills/<name>/SKILL.md` -- the extra category layer made every skill invisible; (2) global content was written only to `~/.gemini/antigravity-cli/` (the `agy` CLI root), but the desktop IDE reads from `~/.gemini/antigravity/`. Fixed by rewriting `Antigravity20Integration` to flatten the skill tree, write to BOTH global roots, and (newly) install the curated hooks + a `hooks.json`; the redundant/buggy legacy `safe_folder_copy` blocks were removed from both installers. Commands (already flat) now surface as `/<name>` workflows under the correct roots. Verified by the extended `tests/integrations/test_antigravity.py` suite (flattening, hooks.json schema, dual-root global). Residuals tracked as WN-v32ag-1/2/3. |
| DF-v32hr-7 | DF | adoption-headroom Phase 3 | adoption-headroom Phase 4 (T012/T013) | The top-level `compress(messages)` no longer the Phase 1 no-op: it now routes each message's content through ContentRouter (identity on prose, so the scaffold tests stay green). The runtime seam `compress_output(text)` was added and is the single-blob entry the PreToolUse hook (via the CLI) and the internal MCP `context_compress`/`context_retrieve` tools call, threading the shared CCR store. The CacheAligner-on-system-prompts portion of the original deferral is carved out as DF-v32hr-9 (still open). |
| DF-v32hr-10 | DF | adoption-headroom Phase 4 | adoption-headroom Phase 6 (T019) | The scheduled free-text compression mechanism shipped: `transforms/ml_token_dropper.py` ports headroom's Kompress ModernBERT importance-scoring drop as an opt-in, default-off, lossy free-text compressor (public pre-trained ONNX weights via the `[ml]` extra, offline-first, zero-outbound, graceful degradation). The structured-vs-free-text scope the rtk guide documented now has its free-text path. The residual -- the dropper is default-off and NOT auto-wired into the default runtime pipeline (the deterministic strategies remain the default; free text still passes through unless the user opts in) -- is carved out as the narrower DF-v32hr-14 (still open), exactly as DF-v32hr-7 was resolved with DF-v32hr-9 carved out. |
| DF-v32hr-11 | DF | adoption-headroom Phase 4 | adoption-headroom Phase 7 (T021) | `catalog/skills/developer-experience/context-optimization/SKILL.md` no longer documents the verbatim `cargo install --git rtk` setup: Phase 7 (T021) replaced it with the internal-engine setup (`NEXUS_CONTEXT_COMPRESS=1` + Windows CLAUDE.md path) and an rtk-migration note, mirroring the already-updated `guides/RTK_CONTEXT_COMPRESSION.md`. The engine is cross-linked as the programmatic counterpart in all three context skills (`context-compression`, `prompt-token-optimization`, `context-optimization`) with a "when to reach for the engine" note + `[[ ]]` links and no engine-internals duplication. The transient guide-vs-skill inconsistency is closed. The skill's now-false rtk `description`/`overview_l1` were surgically synced in `data/skills.json`; a full `build-catalog` regen of the broader pre-existing frontmatter drift is deferred to the release-time bump (same disposition as WN-v32-3, count-prose reconciliation). |
