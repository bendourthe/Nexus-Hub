# Known Gaps -- v3.0.0

**Status**: finalized (Phases 1-10 of 10 closed; Phase 10 -- live verification + release readiness; v3.0.0 release-ready). Catalog green: all validators exit 0; ShellCheck 0.11.0 clean (installer.sh / install.sh / 28 catalog .sh); 1160 tests passed / 4 skipped / 0 failed; `check_version_sync.py` green at 3.0.0 across all six surfaces; catalog skill-scan clean (0 HIGH/CRITICAL). **Update 2026-06-10**: DF-v30-4 (the v3.x shim removal originally scheduled for v4.0.0) was resolved early in v3.2.0 -- see Resolved.
**Last updated**: 2026-06-10 (DF-v30-4 resolved early in v3.2.0)

This file tracks per-phase unfinished work, intentional deferrals, bugs, missing tests, warnings, and bypassed quality gates for the v3.0.0 `command-consolidation-skill-security` plan. The next version's `/generate-plan` ingests the open items here. Category prefixes: `NI` (not implemented / skipped subtask), `DF` (intentionally deferred), `BG` (bug), `MT` (missing test), `WN` (warning / suppressed rule), `QG` (quality gate bypassed).

## Summary

| Category | Open | Resolved |
|---|---|---|
| NI | 0 | 1 |
| DF | 8 | 2 |
| BG | 1 | 0 |
| MT | 0 | 0 |
| WN | 3 | 6 |
| QG | 0 | 0 |
| **Total** | **12** | **9** |

## Open Items

| ID | Category | Source phase | Plan reference | Reason | Suggested next step | Severity |
|---|---|---|---|---|---|---|
| WN-v30-2 | WN | Phase 2 | T008 (register both skills) | The T008 prompt prescribed regenerating `data/skills.json` + `data/SKILL_INDEX.md` via `infrastructure/tools/build_skills_catalog.py`. Running the generator produced a ~5,500-line diff that materially rewrote existing entries: it splits categories by raw frontmatter casing (e.g. `Developer Experience` vs `developer-experience`, `Research`, `Workflow`), recomputes `long_description`/`size` differently, and reorders entries -- indicating the committed `data/` files are maintained by a newer/different process than this generator. To keep the diff traceable, registration was done by hand (2 skill entries + recomputed `statistics` + 2 `SKILL_INDEX.md` rows + the `orchestration`/`security` `marketplace.json` counts), and the regeneration was reverted. Net effect: `make build-catalog` is currently unsafe to run -- it would clobber curated catalog data. | Reconcile `build_skills_catalog.py` with the committed `data/skills.json` (normalize category casing in source SKILL.md frontmatter, align `long_description`/`size`/ordering, and category counting) so `make build-catalog` reproduces the committed catalog with a no-op diff; OR document that `data/` is hand-maintained and retire/replace the generator. Verify with a no-op regen diff. | Medium (a future `make build-catalog` or `/update` run could corrupt the catalog) |
| DF-v30-1 | DF | Phase 6 | T027 (static analyzers) | The `nexus-skill-scanner` ships the highest-signal patterns for each of the 15 static classes (1-13, 15-16), not the full ~64-pattern surface described in the comparison. This is the deliberate "start with the highest-signal classes and grow the pattern set per release" approach from `comparison-skillspector.md` Section 13. Coverage is sufficient to score the planted-malicious fixture CRITICAL and pass the clean catalog, but individual classes (e.g. memory poisoning, tool misuse, output handling) have a thin pattern set. | Grow the per-class pattern set in subsequent releases, adding eval fixtures per new pattern; prioritize classes by observed catalog/third-party false-negative rate. Class 14 (YARA) and the live OSV.dev lookup are scheduled for Phase 7, not gaps. | Low (by-design incremental coverage; gate + semantic skill compensate) |
| DF-v30-2 | DF | Phase 6 | T027 (taint tracking) | Class 13 taint tracking is a module-scoped over-approximating heuristic: it marks names assigned from a tainted source (os.environ / os.getenv / input / sys.stdin) and flags them reaching a code-exec sink (HIGH) or process-exec sink (MEDIUM). It is flat (not intra-procedural / flow-sensitive), so it can over- or under-approximate across function boundaries. Severities are capped so the heuristic cannot trip the HIGH gate except on the genuinely dangerous input->exec/eval flow (which the catalog has none of). | If false positives/negatives emerge in practice, replace with a flow-sensitive intra-procedural dataflow pass (per-function def-use chains). Add adversarial taint fixtures. | Low (conservative severities; bounded blast radius) |
| DF-v30-3 | DF | Phase 7 | T033 / T034 (optional modules) | The two Phase 7 optional modules ship deliberately minimal starter content: the signature engine bundles 12 re-authored rules across 3 files (cryptominers, reverse-shell/exploit cradles, web shells), and the offline OSV advisory DB seeds 5 well-documented public advisories. Both are sufficient to demonstrate detection and graceful degradation, but neither is comprehensive coverage of its class. This mirrors the DF-v30-1 "highest-signal first, grow per release" philosophy. | Grow the signature rule set (add a generic-malware/loader file and per-language web-shell coverage) and expand the offline advisory seed per release, each with a paired test fixture; the live `--osv` lookup already supplements the offline DB when enabled. | Low (by-design incremental coverage; modules are opt-in and adjudicated by the skill) |
| WN-v30-4 | WN | Phase 7 | T035 (stabilization) | Local verification on the Windows dev host was partial: `make` is not on PATH, so `make validate`/`make test` were emulated by invoking each validator and the package test suite directly (all green: 72 package tests incl. the new 18 signature tests, version-sync, supply-chain, workflow-security, and the bundle audit at exit 0; `ruff check` ran clean on the changed files). Phase 7 made no shell/installer changes (Python modules + bundled data + README only), so there is no ShellCheck surface this phase. | Confirm the CI `validate` and `tests` jobs are green for this commit on the ubuntu runner (which runs the editable install + `pytest extensions/nexus-skill-scanner/tests/`); no code change expected. | Low (covered by CI) |
| DF-v30-5 | DF | Phase 9 | T040 (Swift/Kotlin extractors) | Phase 9 shipped the recommended mobile extractor batch (Swift + Kotlin), raising code-search language coverage 10 -> 12, each at 100% recall / 100% precision on its eval fixture. This carries forward the remaining DF-v24-7 scope: ~8 languages are still uncovered (Scala, Bash/shell, Lua, Perl, Objective-C, Dart, Haskell, etc.), and framework extractors (e.g. iOS/SwiftUI or Android/Jetpack route / component resolution) plus parameter-node parity were not in scope for the mobile batch. | Implement the next demand batch following the per-language extractor pattern under `extensions/nexus-code-search/.../extraction/languages/`, each with an eval fixture clearing the 80% recall gate and unit tests. Note for grammar pinning: newer tree-sitter grammars version their own package independently of the core's <0.26 ceiling (swift at 0.7.x, kotlin at 1.x), so verify ABI compatibility with the installed core before pinning rather than assuming a <0.26 marker. Pick up at the next version's /generate-plan. | Low (incremental coverage; 12 of ~20 languages shipped) |
| DF-v30-6 | DF | Phase 10 | T045 (live skill-eval-loop) | Re-deferred. A model CLI IS on PATH this version (`claude` + `codex`), so the prior "no CLI" reason no longer holds -- but a faithful with-skill-vs-baseline trigger run is still not achievable: the bundled harness (`scripts/optimize_skill_description.py`) builds `claude -p ... --skill <path>` / `codex exec ... --prompt <path>`, and both flags are rejected by the shipped CLIs (`error: unknown option '--skill'`; `codex exec` has no `--prompt`). After patching detection to the real CLI surface (install the skill into a temp `.claude/skills/`, run headless `claude -p --output-format stream-json`, parse the stream for a `Skill` tool_use), 3 live runs (haiku positive, sonnet positive, sonnet fenced-negative) for `agent-orchestration-primitives` showed the skill is discoverable and the `Skill` tool is available, yet neither model invoked it on a dead-on-topic prompt -- because in a Nexus-Hub environment the real trigger path is the `search_skills` MCP (per the global skill-discovery instruction), not the bare `Skill` tool, and a capable model answers an advisory prompt directly. Carries DF-v24-8 (subsumes DF-v24-1/2/3/4/6, carries DF-v23-7) for: `agent-orchestration-primitives`, `skill-security-scan`, `verification-before-completion`, `receiving-code-review`, `using-git-worktrees`, `solution-knowledge-base`, `solution-refresh`, `multi-agent-code-review`, `plan-review`, `product-strategy`, `session-query`, `product-pulse`, `demo-capture`. Static trigger-surface checks (verbatim trigger phrases + SKIP clauses + cross-fencing) already done at authoring for all. | A faithful live eval requires replicating the `search_skills` MCP discovery path: wire `nexus-skill-server` into each nested run (no `--strict-mcp-config`) and detect trigger via the `search_skills` call plus the subsequent `Skill` load -- tracked by BG-v30-1. Pick up once BG-v30-1 fixes the harness; author a minimal `evals.json` per skill and confirm 1.0 positive / 0.0 fenced-negative. | Low (static surface verified; live confirmation blocked on BG-v30-1) |
| DF-v30-7 | DF | Phase 10 | T045 (eval-harness trigger techniques) | Re-deferred for the same root cause as DF-v30-6. The three eval-harness trigger techniques (premature-action detection, multi-turn replay, cheap-model fragility) in `scripts/optimize_skill_description.py` cannot be exercised live because the harness's `build_cli_command` targets the non-existent `--skill` / `--prompt` flags. They remain validated at v2.3.0 via dry-run + pure-logic + fixture-stream tests (+23 pytest cases in `catalog/hooks/tests/test_eval_loop.py`, still green). The one live observation this phase: cheap-model fragility is real -- haiku did not load the skill on the positive prompt (consistent with what technique 3 is designed to surface). Carries DF-v24-9 (carries DF-v23-8). | Fold into the same live-eval pass as DF-v30-6 once BG-v30-1 fixes the harness invocation: confirm `premature_action` flips on a planted pre-`Skill` tool use, a multi-turn eval triggers at its `trigger_turn`, and a description's trigger rate is compared across default and a cheaper `--model`. | Low (logic-tested; live confirmation blocked on BG-v30-1) |
| BG-v30-1 | BG | Phase 10 | T045 (live evals; harness defect found) | Shipped catalog tooling constructs a CLI command the shipped CLI rejects. `scripts/optimize_skill_description.py::build_cli_command` builds `claude -p <prompt> --skill <path>`, `codex exec <prompt> --prompt <path>`, `gemini --workflow <prompt> --skill-file <path>`, and `opencode run <prompt> --skill <path>` -- but the installed Claude Code CLI returns `error: unknown option '--skill'` and `codex exec` has no `--prompt` flag (verified 2026-06-04). The harness was authored against an aspirational future "skill-load flag" surface (its own docstring says "Real CLIs will eventually surface a structured `skill_loaded` flag; until then this heuristic..."). The parity test (`catalog/hooks/tests/test_eval_loop.py::TestEvalLoopCLIAdapter`) only asserts no cross-CLI binary bleed, so it never caught that the flags do not exist. Discovered while running T045 live. | Patch `build_cli_command` (and `_detect_trigger`) to the real per-CLI surface: for `claude`, drop `--skill`, install the target skill into a temp project `.claude/skills/<name>/`, run from that cwd with `--output-format stream-json --verbose`, optionally wire `nexus-skill-server` via `--mcp-config` (no `--strict-mcp-config`) to exercise the real `search_skills` discovery path, and detect trigger from a `search_skills`/`Skill` tool_use in the stream rather than the description-phrase-echo heuristic. Update the parity test to assert each branch uses a flag the CLI actually accepts. Then DF-v30-6/-7 become runnable. | Medium (the bundled eval harness cannot perform a live run against any shipped CLI as written) |
| DF-v30-8 | DF | Phase 10 | T046 (cross-OS installer smoke + live --branch) | Re-deferred. The implementation host is Windows-only (no macOS / Linux host available, verified 2026-06-04), so the full macOS 14 installer smoke (sub-steps 1a-1l of `docs/archive/v2/v2.3/installer-smoke-post.txt`) and the Ubuntu installer-probe + `make eval` could not be run, and the live `--branch <pushed-branch>` clone+install (bash + PowerShell) was not exercised end-to-end. Windows is empirically green this version (full pytest 1160 passed / 4 skipped / 0 failed; ShellCheck 0.11.0 clean on installer.sh / install.sh; all validators + scan + version-sync green) and the Linux Python suite is green via CI (`.github/workflows/ci.yml` on ubuntu-latest). Carries DF-v24-10 (subsumes DF-v24-5; carries DF-v23-6). | Before any packaged-binary release, run sub-steps 1a-1l on a macOS 14 host and the installer-probe/eval portion on an Ubuntu host (record in `docs/<version>/installer-smoke.txt`); while on those hosts, run the live `--branch` clone+install into a throwaway target and confirm the cache dir populates, the re-exec carries `NEXUS_HUB_BRANCH_RESOLVED=1` without re-cloning, the install completes from the cache, and the working copy is untouched. | Low (Windows empirically green; Linux green via CI; deferral acceptable for a source release) |
| WN-v30-8 | WN | Phase 10 | T046 (Antigravity agy live probe) | Re-deferred. The `agy` binary is not installable on the Windows implementation host (verified 2026-06-04), so the four Antigravity CLI residuals (the `.agent/` vs `.agents/` codelab dissent, the exact global subpath, the `subagents/` / `rules/` subdirs, and whether `agy` requires a root `AGENTS.md`) could not be reconciled against a live `agy --help` / `agy init`. The conventions remain verified against Google's public Antigravity CLI docs + official codelabs (recorded in `docs/archive/v2/v2.2/antigravity-cli-probe.md` Section 11). Carries WN-v24-3 (carries WN-v23-5). | Once `agy` is installable, run the live probe and reconcile the four residuals; if a root `AGENTS.md` is required, add a per-integration marker scheme so codex + antigravity2 can co-manage it. Update `docs/archive/v2/v2.2/antigravity-cli-probe.md` and the next version's known-gaps. | Low (docs-verified; live confirmation pending binary availability) |
| DF-v30-9 | DF | Phase 10 | T047 (DF-v23-9 visual-brainstorming-server re-evaluation) | Re-evaluated and re-deferred. DF-v23-9 is a Superpowers-style visual-brainstorming server -- an intentional `re-full`, local-only deferral first recorded in v2.3.0 (binds 127.0.0.1, zero outbound, no new credential, so it would pass the MCP Registry Policy). The revisit trigger is "build only if a user-facing need for in-session visual collaboration emerges". No such concrete need has emerged through v3.0.0 (a command-consolidation + skill-security + orchestration-adoption release; nothing in it depends on or requests in-session visual collaboration). Per Nexus-Hub's catalog-content-first identity, a long-lived local Node websocket server is not built pre-emptively. Carries v2.3.0 DF-v23-9. | Build only if a concrete user-facing need for in-session visual collaboration emerges; when it does, scope it as an `extensions/` internal MCP under the reverse-engineer-first policy and add a `docs/policy/mcp-reverse-engineering-matrix.md` row before building. Otherwise keep re-deferring with the revisit trigger preserved. | Low (intentional, identity-grounded deferral; revisit trigger preserved) |

## Resolved

| ID | Category | Source phase | Resolved in | Note |
|---|---|---|---|---|
| NI-v24-1 | NI | Phase 9 (T042) | v3.0.0 Phase 9 (won't-do, by convention) | Confirmed the convention-based decision to keep `validate_solution_frontmatter.py` as a single cross-platform `.py` validator with no `.ps1` sibling. Verified all five peer top-level validators are likewise `.py`-only (`check_version_sync.py`, `scan_supply_chain_iocs.py`, `validate_no_personal_paths.py`, `validate_unicode_safety.py`, `validate_workflow_security.py`); a `.py` validator runs via `python` on every OS and is copied to `~/.nexus-hub/scripts/` by both installers, while the mandatory `.sh`->`.ps1` parity rule is scoped to bash scripts only. A PowerShell reimplementation would be pure duplication with a real sync / correctness risk. Resolved as won't-do, ingested from v2.4.0 NI-v24-1. |
| DF-v24-7 | DF | Phase 9 (T040) | v3.0.0 Phase 9 | Shipped the recommended mobile extractor batch (Swift + Kotlin), raising code-search language coverage 10 -> 12. Each ships an eval fixture clearing the 80% recall gate (both at 100% recall / 100% precision), unit tests, and a tree-sitter grammar dep (`tree-sitter-swift` 0.7.x, `tree-sitter-kotlin` 1.1.x -- these version independently of the core's <0.26 ceiling and were verified ABI-compatible with core 0.25.2). Registered in `LANGUAGE_EXTRACTORS` (.swift / .kt / .kts). Remaining languages + framework / parameter parity carried forward as DF-v30-5. Ingested from v2.4.0 DF-v24-7. |
| WN-v24-2 | WN | Phase 9 (T041) | v3.0.0 Phase 9 | Removed the redundant `## Quality Checklist` section from the 71 skills that carried BOTH it and `## Verification`, consolidating each to the single canonical `## Verification` binary checklist. The rigorous, observable-artifact Verification form authored in v2.4.0 supersedes the looser grandfathered Quality Checklist, whose design guidance already lives in each skill's Instructions / Best Practices body. Surgical removal: 911 deletions, 0 additions; `validate_skills.py --quality` stays at 0 warnings across all 247 skills. Ingested from v2.4.0 WN-v24-2. |
| WN-v30-1 | WN | Phase 1 (T004) | v3.0.0 Phase 10 (T044) | ShellCheck 0.11.0 is now installed on the dev host and was run at the Phase 10 release gate: `shellcheck --severity=warning scripts/installer.sh install.sh` exits 0 (clean), confirming the new `installer.sh` validator-copy block that Phase 1 had deferred to CI. The full validator set + `check_version_sync.py` were also re-run directly and are green. |
| WN-v30-3 | WN | Phase 6 (T032) | v3.0.0 Phase 10 (T044) | ShellCheck 0.11.0 now present and run clean at the Phase 10 gate across `scripts/installer.sh`, `install.sh`, and all 28 `catalog/**/*.sh` files, confirming the two Phase 6 installer copy blocks deferred to CI. The full test suite (1160 passed / 4 skipped) and all validators were run directly and are green; `ruff` remains absent but `py_compile` + the package's own gate cover the Python surface. |
| WN-v30-5 | WN | Phase 8 (T039) | v3.0.0 Phase 10 (T044) | The partial-local-verification caveat is discharged at the Phase 10 gate: every validator was run directly (all green), the full pytest suite ran (1160 passed / 4 skipped / 0 failed -- not emulated), ShellCheck ran clean, and `check_version_sync.py` is green across all six surfaces. Phase 8's edits (40 markdown shims + migration doc + count prose) carried no new shell/Python surface; nothing remained beyond the standard ubuntu CI run, which the gate's local equivalents now cover. |
| WN-v30-7 | WN | Phase 9 (T043) | v3.0.0 Phase 10 (T044) | The partial-local-verification caveat is discharged at the Phase 10 gate: all validators run directly and green, the full pytest suite run (1160 passed / 4 skipped / 0 failed, incl. the code-search 200/1 suite with the new swift/kotlin modules), ShellCheck clean, version-sync green. `ruff check` remains the package gate (not `ruff format`). Nothing remained beyond the standard ubuntu CI run, now covered by the gate's local equivalents. |
| WN-v30-6 | WN | Phase 8 (T038) | v3.0.0 Phase 10 (T048) | Reconciled the stale "245 curated skills" prose to "247" in both `.claude-plugin/plugin.json` and `data/marketplace.json` descriptions during the `/update release` version bump (AGENTS.md / README.md were already 247 from Phase 2). Both JSON files now agree with the on-disk catalog (247) and `data/skills.json`. The broader count-consistency check (assert skill / command / hook / agent counts agree across all prose surfaces) is left as a future enhancement to `check_version_sync.py`, noted for the next version. |
| DF-v30-4 | DF | Phase 8 (T036) | v3.2.0 (deprecated-command cleanup) | The 40 v3.x forwarding deprecation shims were deleted from `catalog/commands/` in v3.2.0, ahead of the originally-announced v4.0.0 schedule, to cut the slash-menu noise the consolidation set out to reduce (a maintainer decision; a breaking interface change shipped early -- documented as such in the v3.2.0 CHANGELOG `Removed` entry). Dropped the "+ 40 deprecated v3.x shims" count clause from `AGENTS.md` (2 places) and rewrote each consolidated command's "replaces deprecated /X" note to "(removed in v3.2.0)"; `docs/v3/v3.0/command-migration.md` was updated to past tense (shims removed in v3.2.0) and retained as the old-to-new mapping reference. The historical "41 -> 14" design-doc references (`command-consolidation-design.md`, `command-scope-mechanism.md`, the plan) were intentionally preserved as history. README / `marketplace.json` carried no shim clause, so neither needed an edit. |
