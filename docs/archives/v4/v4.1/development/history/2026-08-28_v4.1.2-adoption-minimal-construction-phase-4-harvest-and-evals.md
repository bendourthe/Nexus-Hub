# Development Log: v4.1.2 Phase 4 - Construction-Debt Harvest and Eval Proof

**Date**: 2026-08-28
**Operator**: maintainer
**Assisted by**: Cursor Grok 4.6
**Objective**: Track deliberate corners with a generic in-code marker, and prove the construction skill with evals that reward native reuse and refuse safety cuts.
**Outcome**: `references/construction-debt.md` documents a grep-only harvest. `evals/evals.json` has three cases: native date control, stdlib cache, refused safety cut. Unreleased changelog names the two new skills as +2.

---

## 1. Starting State

- **Branch**: `feat/v4.1.2-ponytail-planning`
- **Starting tag/commit**: `6a391462` (Phase 3)
- **Environment**: Windows 11, PowerShell, Python 3.12
- **Prior session reference**: Phase 3 history
- **Plan reference**: `docs/releases/v4/v4.1/plans/v4.1.2-adoption-minimal-construction.md`

---

## 2. Chronological Steps

### 2.1 Marker convention

Added `catalog/skills/code-cleanup/minimal-construction/references/construction-debt.md` with a generic `construction-debt:` marker, grep harvest skipping `node_modules` / `.git` / build output, and a read-only report shape. No top-level `scripts/*.py`. Linked from the skill body.

### 2.2 Eval cases

Added `evals/evals.json` as a bare list of three internal-schema entries. Assertions fail a skill that drops validation or accessibility to look smaller. Did not add promptfoo or a hosted judge. Did not run the eval loop against a CLI in this phase (that uses the user's configured CLI).

### 2.3 Changelog

Unreleased now names the always-on section, the two skills as +2, and the marker. No new slash command.

### 2.4 Testing

Bundle orphan audit PASS. Converter tests 18 passed. Fast profile next.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| evals.json parses as a list of 3+ entries | PASS |
| `python scripts/validate_skills.py --bundles-only` | PASS |
| `python -m pytest tests/validators/test_skill_eval_convert.py -q` | PASS (18) |

---

## 4. Known Issues

None identified during this session.

---

## 5. Plan Discrepancies

None; harvest is grep-only as the plan preferred over a new installer-copied script.

---

## 6. Assumptions Made

- **Authoring evals.json without running the CLI loop is enough for Phase 4.** The schema is the gate; live paired runs belong to a later eval session with the user's CLI.

---

## 7. Testing Summary

### Automated Tests

- Converter tests: 18 passed
- Bundle audit: PASS

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session

- [x] T016-T019

### Remaining

- [ ] Phase 5 last-phase duties and publication

---

## 9. Summary and Next Steps

Harvest docs and eval assertions are in the skill tree. Next is the fail-closed last phase.
