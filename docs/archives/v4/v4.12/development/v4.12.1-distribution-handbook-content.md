# Distribution handbook content review - v4.12.1

**Handbook**: `docs/handbooks/distribution.html` (id `distribution`)
**Reviewed on**: 2026-09-16
**Trigger**: `check_handbooks` reported `distribution: stale evidence -- inputs: scripts/installer.ps1, scripts/installer.sh`

## Why the receipt went stale

Both installers are declared `code_inputs` for this handbook, meaning the handbook explains claims about them and those claims were reviewed against the files. The v4.12.1 release commit changed both files, which invalidates the prior receipt by design.

## What actually changed

The complete diff of both code inputs between the last verified revision (`02e4e825`) and this candidate:

```
-$script:NexusHubVersion = "4.12.0"
+$script:NexusHubVersion = "4.12.1"
-NEXUS_HUB_VERSION="4.12.0"
+NEXUS_HUB_VERSION="4.12.1"
```

Two lines, both the version constant. No function, path, delivery call, platform branch, copy rule, or ordering changed.

## Claim review

The handbook's distribution claims concern which surfaces are delivered to which platform, the flatten behaviour, the copy rules, and the verification pass. Each was re-read against the current installers:

| Claim area | Affected by this change | Basis |
|---|---|---|
| Per-platform delivery paths | no | no path constant changed |
| Flatten / mirror behaviour | no | no adapter call changed |
| Copy rules and overwrite prompts | no | no copy logic changed |
| Install verification pass | no | no verify branch changed |
| Reported version string | yes, mechanically | the constant the installer prints, now 4.12.1 |

The handbook does not quote a version number in its prose, which the build result below confirms independently.

## Build result

Rebuilding the handbook from its retained sources produced **byte-identical output**:

```
$ python catalog/skills/specialized-domains/document-to-interactive-html/scripts/build_presentation.py \
    docs/handbooks/_sources/distribution/model.json --out docs/handbooks/distribution.html --root .
Built retained handbook: 8463c66d85d0d354374b89ceb2a10ddf8c9fe5138a866ad13606776d763cc557
```

The output hash is unchanged from the prior receipt, which is the objective evidence that no version metadata is embedded in this handbook and that its rendered evidence remains valid for these exact bytes.

## Disposition

- **content**: re-reviewed for v4.12.1; claims unaffected. This file is the evidence.
- **build**: re-run; reproduces the prior artifact byte for byte. Prior build evidence remains valid for these bytes and is retained.
- **rendered**: the prior rendered evidence records `output_sha256` equal to the current output, so it describes these exact bytes and is reused rather than re-captured. Per the refresh procedure, evidence is reusable when all relevant inputs and output hashes match; re-capturing a browser pass for identical bytes would attest nothing new.

Only the snapshot input hashes for the two installers required updating.
