# Attribution remote-boundary handbook content review

The distribution handbook's ownership section now states that a new-branch push checks the actual destination's advertised history instead of another remote or stale tracking refs. It retains the separate caveat for direct hosting API writes. The reading source and presentation model carry the same claim, and the generated reading page shows it in chapter 4 without clipping at a 1440 by 900 Chromium viewport. This review covers the changed explanation, not a new installer behavior claim.
