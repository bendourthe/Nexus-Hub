# Attribution hook line-ending handbook content review

The distribution handbook's ownership chapter now says that installed Git hooks come from a digest-checked copy, equivalent LF/CRLF source checkouts satisfy the freshness check, and changed source content requires reinstalling the guard. This matches the source comparison and unchanged installed-copy SHA-256 check in `scripts/nexus_git_attribution.py`, plus the two-way regression in `tests/test_git_attribution.py`. The retained Markdown source and presentation model carry the same paragraph. The prior new-branch destination-history and direct hosting API boundaries remain visible. The updated reading chapter at 1440 by 900 and ownership slide at 761 by 900 were visually inspected in Chromium; the text is legible and unclipped in both.
