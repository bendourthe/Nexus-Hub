# Session History - v4.2.3 Phase 3: Foundations project-generic content and diagram repair

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.3-guide-refinement.md`
**Phase**: 3 of 7

## Subtasks

- **3.1 Numbers and language** (T009): deleted all five `.fx-num` badges and their CSS rule - each consumed a full line and carried no information. Rewrote teaching copy across the guide to be project-generic: the page H1 became "What actually happens when an AI works on your project", Scene 2's diagram now reads YOUR PROJECT with `files / tools / whatever it runs on` chips, Scene 4's node is PROJECT, Scene 5 is "One job, two runs" about "the thing that keeps failing", and Home's closing heading, comparison lead, and install disclosure lost "repo" and "repository artifacts". Factual claims were deliberately left alone: the hero still names the AI coding assistants, "paste into Terminal" still says Terminal because you literally do, and every Cheatsheets description still describes what its command actually does.
- **3.2 Loop diagram repair** (T010): replaced the open two-line `.fx-arrow` chevrons - the "half a chevron" the maintainer saw - with filled closed-path arrowheads (`.fx-head`, with `--good` and `--bad` variants). Rewrote the two loop captions, which had been all-bold-accent and unreadable, into a two-line hierarchy: a dim uppercase role line (`THE MODEL ASKS` / `THE PLATFORM ANSWERS`) over a plain-weight detail line in normal ink.
- **3.3 Pulses behind nodes** (T011): SVG has no z-index - paint order is document order - so every `.fx-pulse` is now declared before the node groups it crosses. A test enforces the ordering, because a later edit that moves a pulse would silently restore the defect.
- **3.4 Ordering and colour** (T012): every comparison now reads without-then-with. Scene 3 was flipped so NOISY CONTEXT precedes FOCUSED CONTEXT, and Scene 5 already led with the unaided run. Differentiation is now carried by colour across the whole lane, not just the outcome badge: amber-tinted windows, chips, connectors and arrowheads on the unaided side, accent/green on the harnessed side.
- **3.5 Harness redraw** (T013): the paragraph was cut to the four guardrail names with one clause each, plus a short closing line. The four arcs now sweep into place in sequence (`.fx-guard` + `.fx-sweep`) rather than merely appearing, and the centre loop keeps its own small arrowheads. Arc endpoints stay on the r=132 circle about (260,156), the geometry v4.2.2 had to fix once already.

## Rendered QA findings, fixed in-phase

1. The page H1 still said "edits your code" after the first pass - caught by rendering, not by the vocabulary test, which was scoped to the section body.
2. Scene 5's "a command" chip was 58 px wide against a label needing ~80 px, so the text spilled. Re-laid the whole harnessed lane with correct widths and spacing.
3. The outcome card's subtitle ran to the SVG's right edge. Widened the viewBox to 700 and shortened the text to "saved, not lost", which now parallels "gone at tab close" on the unaided lane.
4. The "a guardrail stops it once" and "gate" captions collided. Separated onto one baseline with clear horizontal spacing.

## Deviations

- None.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **65 passed, 1 skipped**.
- New Phase 3 tests: no `.fx-num` markup; unaided-state-first ordering in both comparisons; arrowheads are closed filled paths and `fx-arrow` is gone; loop labels use the dim-role plus plain-detail pair and the old caption strings are absent; every pulse is declared before its node groups; and a vocabulary guard asserting Foundations teaching copy contains none of "repo", "repository", "terminal", "git", or "codebase".
- The vocabulary guard immediately caught the bridge line "a simulated terminal answers", which was rewritten.
- Browser checks: reduced motion holds guardrails opaque, sweeps complete, pulses hidden; only 2 of 5 scenes carry `.live` at rest; no horizontal overflow.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, environment variables, test paths, or artifacts. Render PNGs only.

## Known gaps

- None new.

## Next steps

- Phase 4: Training present mode, loop-stage progress, and bottom-right icon controls.
