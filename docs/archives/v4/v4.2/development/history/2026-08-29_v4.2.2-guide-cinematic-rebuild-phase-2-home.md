# Session History - v4.2.2 Phase 2: Home page rebuild

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.2-guide-cinematic-rebuild.md`
**Phase**: 2 of 7

## Subtasks

- **2.1 Hero and page rhythm** (T005): hero and shared 700px measure landed in Phase 1's live placeholder; this phase completed the page body on the compact rhythm - "Same model. Different working method." comparison table (now in an `overflow-x` scroll container with a 560px floor so narrow viewports scroll instead of crushing three columns), a new "One governed loop" band with a six-step `/describe -> /update` loop strip deep-linking into Cheatsheets stops, and the closing "Learn it, then run it on your repo" band with Training/Cheatsheets CTAs, the glow-booth.zip download, and a labeled GitHub button reusing the corrected octocat path.
- **2.2 Install section** (T006): Windows-first default tab, slim copy buttons, and full warning-box/origin-check removal all landed in Phase 1's shell; this phase added the green verify callout with `/skills list` and `/commands` as `cmd-cell` copy cells (payload equals visible text) and restored the "Need another option?" details (wget, --workspace, --platforms, --yes, nexus-hub upgrade).

## Rendered QA findings

1. Scroll-gated `.reveal` sections rendered transparent in full-page screenshots (they only animate in view). Harness now forces the end state before capture - the page behavior itself was correct.
2. The sticky, backdrop-filtered header stitches as a dark band in Chromium full-page screenshots even in light theme. Verified live computed styles (light `#f3f1ea` header, dark ink) and a viewport-only capture (`header-light-420-viewport.png`); the harness now pins the header static during capture so future evidence is not misleading.

## Deviations

- None. Sub-task 2.1/2.2 items that Phase 1's richer placeholder already delivered are recorded there.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **41 passed, 1 skipped, 1 xfailed** (remaining strict xfail owned by Phase 4).
- `test_home_verify_commands_are_copy_cells` un-xfailed and passing.
- Rendered QA: phase-2 set (home x 2 themes x 3 widths) reviewed at 1440 dark and 420 light.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, env vars, test paths, or artifacts beyond the phase-2 render PNGs. Existing pipeline coverage unchanged.

## Next steps

- Phase 3: Foundations rebuild (scene script, then implementation).
