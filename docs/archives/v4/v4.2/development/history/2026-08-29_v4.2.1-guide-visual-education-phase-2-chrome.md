# Development Log: v4.2.1 Phase 2 - Chrome, theme, and install terminals

**Date**: 2026-08-29
**Operator**: maintainer
**Assisted by**: Cursor Grok 4.6
**Objective**: Clear screenshot defects 1-6 so the rest of the redesign is not judged through a broken shell.
**Outcome**: Header is opaque; GitHub is icon-only with an accessible name; theme is a sun/moon toggle defaulting to dark; light wordmark uses `--ink`; copy buttons attach to `.cmd-line`, not `code[data-copy]`; light-theme terminals use light tokens.

---

## 1. Starting State

- **Branch**: `feat/v4.2.1-guide-visual-education` (ahead of `develop` by Phase 1)
- **Starting tag/commit**: `4f1219fe`
- **Environment**: Windows 11, PowerShell, Python 3, pytest. Cursor picker-only; session remains `cursor-grok-4.6` (frontier / high).
- **Prior session reference**: `docs/releases/v4/v4.2/development/history/2026-08-29_v4.2.1-guide-visual-education-phase-1-contract.md`
- **Plan reference**: `docs/releases/v4/v4.2/plans/v4.2.1-guide-visual-education.md`

---

## 2. Chronological Steps

### 2.1 Opaque nav, wordmark, and icon controls

**Plan specification**: T005.

**What happened**: `--nav-bg` is solid in both themes. `.wordmark b` uses `var(--ink)`. GitHub visible text removed; `aria-label="Nexus-Hub on GitHub"`. Theme control is sun (dark, action: light) / moon (light, action: dark). Boot script defaults to dark when storage is empty, not `prefers-color-scheme`. Hit targets for GitHub, theme, and primary nav links are 44px.

**Key files changed**: `guides/website/nexus-hub-guide.html`

**Troubleshooting**: N/A

---

### 2.2 Terminal copy layout and light-theme tokens

**Plan specification**: T006.

**What happened**: `initCopyButtons` appends to `.cmd-line` via `host.appendChild`. Light `--term-bg` is `#fffdf8`. Term bar and copy chip use tokens. `.t-bold` uses `var(--term-ink)` so light terminals are not white-on-beige.

**Key files changed**: `guides/website/nexus-hub-guide.html`

---

### 2.3 Testing

**Plan specification**: T007.

**What happened**: Added tests for icon-only GitHub, sun/moon default-dark, wordmark ink, copy host, opaque nav, and light terminal background. Focused suite: 35 passed, 1 skipped.

**Key files changed**: `tests/guides/test_nexus_hub_guide.py`

---

## 3. Verification Gate

| Check | Result |
|---|---|
| `python -m pytest -q tests/guides/test_nexus_hub_guide.py` | PASS (35 passed, 1 skipped) |
| Human / browser screenshots | NOT RUN; last phase only |

---

## 4. Known Issues

None identified during this session.

---

## 5. Plan Discrepancies

None; all work followed the implementation plan.

---

## 6. Assumptions Made

- **Workbench `data-copy` on the prompt** still gets a copy host via `parentNode` if it is not inside `.cmd-line`. Phase 5 may replace that UI.

---

## 7. Testing Summary

### Automated Tests

- `tests/guides/test_nexus_hub_guide.py`: 35 passed, 1 skipped, 0 failed

### Manual Testing Performed

None; human QA is deferred to the last phase.

### Manual Testing Still Needed

human QA is deferred to the last phase

---

## 8. TODO Tracker

### Completed This Session

- [x] T005 Fix opaque header, wordmark contrast, GitHub icon, and sun/moon toggle
- [x] T006 Pin copy buttons and light-theme terminal tokens
- [x] T007 Phase 2 tests, session history, and local commit

### Remaining (Not Started or Partially Done)

- Phase 3 through Phase 7

### Out of Scope (Deferred)

- Human screenshot re-check of defects 1-6 (Phase 7)

---

## 9. Summary and Next Steps

Chrome defects 1-6 are addressed in markup and CSS. Next: Phase 3 modernizes the Home six-stop loop graphic.

**CI IMPACT**: no new commands, dependencies, env vars, test paths, or artifacts. Existing `pytest tests` job covers the updated `tests/guides/test_nexus_hub_guide.py`. No workflow edit. No remote CI run.
