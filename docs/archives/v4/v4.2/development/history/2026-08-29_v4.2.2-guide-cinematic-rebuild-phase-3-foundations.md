# Session History - v4.2.2 Phase 3: Foundations rebuild

**Date**: 2026-08-29
**Plan**: `docs/releases/v4/v4.2/plans/v4.2.2-guide-cinematic-rebuild.md`
**Phase**: 3 of 7

## Subtasks

- **3.1 Scene script** (T008): `development/guide-rebuild/foundations-script.md` - five scenes plus bridge, each with teaching copy, diagram spec, animation spec, and reduced-motion static state. Binding constraints restated: no fixed/sticky overlays, no raw-vs-harnessed toggle (both always visible), no chart libraries, themed via tokens.
- **3.2 Implementation** (T009): `#page-foundations` rebuilt as five alternating scrollytelling scenes with hand-authored inline SVGs: (01) model text-in/text-out, (02) the agent loop with a traveling pulse, (03) focused vs noisy context side by side under one "same model" chip, (04) the harness ring - Skills/Commands/Hooks/Gates arcs snapping around the loop, (05) "One task, two runs" twin-lane chart (raw lane fades to a chat bubble "gone at tab close"; harnessed lane runs /test -> skill -> loop -> hook (red-to-green dot) -> gate diamond -> tested-commit card), plus a bridge band into Training. Entry animations (pop/draw/grow/fade) ride the shared reveal observer via `pathLength="1"` dash draws; continuous pulses ride CSS `offset-path` and run only while a scene is on screen (`.live` observer). Complete static equivalents under `prefers-reduced-motion`.

## Rendered QA findings (fixed in-phase)

1. Harness-ring arcs rendered broken: endpoints were not on the r=132 circle, so the arc flags produced wrong geometry. Recomputed all four arcs to exact points on the circle (center 260,156).
2. Scene 5 was missing its gate station; added the gate diamond with a check between the hook dot and the artifact card (viewBox widened to 690).
3. Scene 1 caption clipped the viewBox; shortened.
4. Screenshot harness waited 600ms while staggered transitions run to ~2.4s; raised the settle wait to 2600ms.

## Deviations

- None against the scene script after the in-phase fixes.

## Test results

- `python -m pytest -q tests/guides/test_nexus_hub_guide.py`: **45 passed, 1 skipped, 1 xfailed**.
- New Phase 3 tests: five scenes with one SVG each, side-by-side (never toggled) comparisons with no `aria-pressed`/range/toggle remnants, exactly one `position: fixed` (constellation) and one `position: sticky` (header) in CSS, reduced-motion coverage for every fx animation class, `offset-path` pulses present.
- Rendered QA: phase-3 set reviewed at dark 1440 and light 900; both themes correct from one markup.

## CI impact (recorded, no pipeline change)

- No new commands, dependencies, env vars, or test paths; phase-3 render PNGs only.

## Next steps

- Phase 4: Training rebuild (scene data redesign, Glow Booth mockup, simulated terminal, slide mode).
