# Session History -- v4.4.2 Phase 2: Home Hero and Restored Sections

**Date**: 2026-09-02
**Plan**: [v4.4.2-guide-production-ready-rebuild.md](../../plans/v4.4.2-guide-production-ready-rebuild.md)
**Phase**: 2 of 8
**Branch**: `feat/v4.4.2-guide-production-ready-rebuild`
**Driver mode**: `full` (on `claude-fable-5-1`, the plan's recommended frontier tier)
**Outcome**: Phase 2 complete. Home reads like the v4.1.2 landing page again: the hero subtitle and lead are back in their original style, five explanatory sections are restored at two-thirds of their original length in a logical order, the two comparisons are merged into one, a new illustrated guardrails section grounds every safety claim in a shipped hook, and the licence attribution moved to a site footer. Gate: 259 passed, 1 skipped. One local commit; nothing pushed.

## Sub-task 2.1 -- decision: attribution location

The GitHub Copilot mark is CC BY 4.0, so deleting the credits outright would break a licence term. [`2026-09-02-platform-mark-attribution-in-footer.md`](../../../../../decisions/implemented/policy/2026-09-02-platform-mark-attribution-in-footer.md) records the choice: a shared `site-footer` below `main`, present on every page, carrying the trademark sentence and the CC BY line with its link; the Home disclosure is deleted. The plan named a `product/` decision class that does not exist in `docs/decisions/implemented/`; the record lives under `policy/`, the closest existing class, and this note is the trace.

## Sub-task 2.2 -- Hero

`.hero-tagline` is gone. Beneath the floating wordmark sit the exact v4.1.2 statement, `Upgrade your agentic AI platforms with an autonomous team of world experts`, with the gradient span on the closing phrase, and the lead paragraph naming Claude, ChatGPT, Gemini, and Cursor. Both are centred on the platform rail's axis. The gradient is painted through `-webkit-text-fill-color: transparent` while `color` stays `var(--ink)`, so the contrast sweep measures a real colour rather than `transparent`; the v4.1.2 file used plain `color: transparent`, which the detector would now reject.

## Sub-tasks 2.3 and 2.4 -- Restored and new sections

Recovered from `git show v4.1.2` and condensed against a fixture of the original word counts (`tests/guides/fixtures/v412-home-copy.json`):

| Section | v4.1.2 words | Ceiling (2/3) | Shipped |
|---|---:|---:|---:|
| Why it matters (four cards) | 85 | 56 | 56 |
| How it works (Three things) | 175 | 116 | under |
| Raw prompting vs Nexus Hub (merged with v4.4.1's `Keep your platform`) | 256 | 170 | under |
| Your favorite commands (seven-row migration table) | 399 | 266 | under |

The order is hero, Why it matters, Installation, How it works, Guardrails and safety, Raw prompting vs Nexus Hub, Your favorite commands, the loop, Learn it. The merged comparison keeps v4.4.1's five animated rows, gains the `It does not change the model` callout, and its `Without` / `With` labels render at 26 px, twice their v4.4.1 size. The migration table reflows into labelled cards below 720 px.

`Guardrails and safety` is new. Three paragraphs make three checkable claims (the platform's permission prompt is advisory to the model; Nexus Hub's hooks fire on the platform's own tool events and block regardless; the tool is local-first), and a layered SVG shows a `Model` inside a `Platform permissions - asks first` ring inside a `Nexus Hub hooks - blocks regardless` ring, with four ports naming real hooks. A five-step `NexusSeq` timeline walks a `commit + key` chip past the platform ring (`asked, allowed`) to a red block at `secret-scan`, then a `rewrite history` chip to a block at `git-guardrails`. A test reads the four port names off the page and asserts each exists as `catalog/hooks/<name>.sh` with a `.ps1` sibling and is registered in `settings.json`, so the page can never advertise a guardrail that no longer ships. The section's wording is enforcement (`blocks`, `before an action runs`), and a test forbids the phrase `makes AI safe`.

Every count on the page is now a stamped marker: two `skills` (329), two `hooks` (30), one `pretooluse` (15). `stamp_guide_counts.py --check` is green, and the v4.4.0 stale-count guard was rewritten per the register to assert that every count phrase in Home EQUALS the current catalog value instead of forbidding numbers outright.

## Two word-budget misses, caught before commit

The new two-thirds test failed twice on the first integrated run: `Why it matters` at 62 words against 56, and the merged comparison at 196 against 170 (its `innerText` counts the eyebrow, the title, the lead, the five rows, and the callout). Both were trimmed in copy, not by loosening the test: the four card sentences lost one to two words each, and the comparison's lead and callout were cut to the sentence that carries the claim. Recorded here rather than as a defect, because the test did exactly what it was written to do on the day it was written.

## Verification

```text
NEXUS_REQUIRE_RENDER=1 pytest -q tests/guides/ tests/verification/test_visual_defect_detector.py
259 passed, 1 skipped in 178.61s

python scripts/stamp_guide_counts.py --check
OK -- 5 marker(s) match the catalog (skills=329, hooks=30, pretooluse=15, commands=18)

Byte ledger
page bytes: 314,571 | Phase 2 delta from 300,047: +14,524 of 36,000 | headroom 185,429
```

Eight browser tests were appended to `tests/guides/test_v441_phase2_home.py`: exact hero text and centring, section order, the two-thirds ceilings, the 26 px labels, footer attribution visible on all four pages with no Home disclosure, the stacked table at 420 px, hook-name grounding with the `pretooluse` marker equal to the registry, and the guardrails end state under reduced motion. Register rows consumed: the tagline test (replaced by the subtitle-and-lead test), the comparison title, the attribution carrier, and the stale-count guard.

## CI impact

One new fixture under `tests/guides/fixtures/` and one new decision record; no new command, dependency, environment variable, or workflow edit. `guide-render` covers the appended tests.

- [x] Hero subtitle and lead exact, centred, gradient contrast-measurable
- [x] Nine Home headings in the agreed order; 24 section titles on one scale
- [x] Every restored section within its two-thirds ceiling
- [x] Guardrails section grounded in shipped, registered hooks
- [x] Attribution in the footer on all four pages; no Home disclosure
- [x] One local commit; no push, pull request, or remote CI
