# v4.4 Linux browser qualification

This archived record qualifies v4.4 MT-446-3 for maintainers closing the historical Linux-browser coverage gap. It identifies the tested guide and browser environment, the retained result, and the separate guide gates that remain open.

GitHub Actions [run 35933463823](https://github.com/bendourthe/Nexus-Hub/actions/runs/35933463823), job `guide-render`, checked out pull request #267's merge result `55e73c41e45c9ee8737b34e7c6526726f5394e14` on `ubuntu-latest`. The runner reported Linux kernel `6.17.0-1022-azure` and Python 3.11.16. The job installed Playwright 1.63.0 and Chrome for Testing 153.0.8010.12 (Playwright Chromium revision 1243), then ran `python scripts/ci/run.py --profile full --only guide-browser --reports-dir reports`. The `guide-browser` profile invokes pytest on `tests/guides/` and `tests/verification/test_visual_defect_detector.py` with `NEXUS_REQUIRE_RENDER=1`, so a missing or unlaunchable browser cannot become a quiet rendering skip.

The retained `ci-guide-render-report` artifact's `junit/guide-render.xml` records 385 cases: 384 passed, zero failures, zero errors, and one skip. The skipped `test_optional_portfolio_copy_when_env_set` requires an optional `NEXUS_HUB_PORTFOLIO_ROOT` sibling path and does not exercise guide rendering. The command exited 0 after 441.6 seconds. The JUnit file's SHA-256 is `dec0b12554574da201ed4ae9e87871a98068507146eb57178bea2882b21b8aa3`.

The tested merge commit and `origin/develop` at `66a2ea3299089903720c3ed3ab97753e044f82c3` both resolve `guides/website/nexus-hub-guide.html` to Git blob `532dec2f3d78cfd27b3db10a93848c883740e033`. `git diff --name-status 55e73c41e45c9ee8737b34e7c6526726f5394e14 origin/develop -- guides/website/nexus-hub-guide.html tests/guides tests/verification/test_visual_defect_detector.py scripts/ci/profiles.py` returned no changed paths. The hosted browser result therefore covers the current guide and the same test/profile sources, not merely a former guide revision.

This result resolves Linux browser verification, not human comprehension or visual approval (MT-446-1), native 200% zoom and OS occlusion (MT-446-2), or the separate historical local full-profile gate (QG-446-1). The original failed evidence and the superseded v4.4.6 design remain unchanged. Publication of this documentation closure is a separate protected-branch gate.
