# v4.0-v4.13 worktree closeout inventory

**Scope**: Read-only and recoverable cleanup evidence for the v4.0-v4.13 backlog reconciliation on 2026-09-23. This record distinguishes integrated work from local user-owned changes and backups; it does not declare a clean slate while those remain.

The completed v4.10.1 and v4.11.1 plans, the later cross-version repairs through PR #249, and their hosted/post-merge receipts are recorded in the version ledgers and `docs/todos.md`. Closed implementation evidence belongs under `docs/archives/v4/`; the current retention check does not identify a whole release directory due for archival. The original interrupted v4.2 full-profile report is preserved as `docs/archives/v4/v4.2/development/unicode-worktree-interrupted-full-profile.zip` rather than rewritten as a successful run.

The registered `v4-gap-reconciliation` and `v4-model-source-recheck` implementation worktrees were removed after their branches merged and the latter's shared attribution guard was verified from a surviving checkout. Two unregistered cache/report residue folders, `v4-agent-span-contract` and `v4-html-unicode`, were moved to the Windows Recycle Bin after exact-path and content checks; the latter's 15 reports matched the archived v4.2 ZIP entries byte-for-byte. They can be recovered from the Recycle Bin. At this record's creation, the worktree parent contains only the active `v4-attribution-line-endings` and `v4-mt10-connector-evidence` publication worktrees. Remove each only after its merge and post-merge verification.

**Publication update**: PR #250 merged at `4753784d`; post-merge run 35841622725 passed. The clean `v4-attribution-line-endings` worktree was removed, and the installed guard verified and rejected a forbidden attribution trailer from the surviving `v4-mt10-connector-evidence` checkout. That docs publication worktree remains until its own merge and post-merge checks complete.

The main `Nexus-Hub` checkout remains at local `develop` with 47 pre-existing status entries, mostly staged historical archive, lifecycle, and model-routing work, plus two unstaged edits. These are user-owned and have not been reset, stashed, committed, or moved by this closeout. Three local future-plan branches (`docs/v4.13.1-guide-training-rebuild-plan`, `docs/v4.15.1-mods-comparison-and-plan`, and `docs/v4.16.0-agent-tooling-and-subagent-cost`) retain distinct work; the latter two also have open pull requests. They are not stale merged branches and remain preserved.

`Nexus-Hub-backups` is not a deletion candidate. Its `2026-09-14-pre-v4.12-cleanup/all-refs.bundle` is a 199,012,448-byte complete Git bundle; 145 of its 162 distinct ref tips are absent from the current checkout's object store. The `2026-09-14-v4.12-attribution/pre-rewrite.git` directory has 129 of 133 distinct ref tips absent from that store. Deleting either backup would discard recovery history not retained by the active checkout. No backup directory was moved or deleted.

The v4.0 CI plan has 62 unticked historical task boxes despite an implemented deliverable. The plan header says 47 of 57 named artifacts exist and ten missing paths are stale v3.16 documentation locations; that spot-check does not prove each phase's historical gate, so the boxes remain unticked. Other open entries, including GitHub branch protection, paid trigger-pilot rerun, contributor display, external-host proof, and manual visual evidence, remain in their version ledgers. None is silently converted into verified completion here.
