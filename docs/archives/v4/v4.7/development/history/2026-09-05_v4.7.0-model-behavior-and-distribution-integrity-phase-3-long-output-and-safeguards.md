# Session History - v4.7.0 Phase 3: Long-Output and Safeguard Guidance in the Emitting Skills

**Date**: 2026-09-05
**Plan**: main plan Phase 3 of 7 (no amendment sub-tasks in this phase)
**Driver**: `/implement ... in-full`, worktree `nh-v45`, branch `feat/v4.7.0-model-behavior-and-distribution-integrity`
**Model routing**: plan asked frontier / high; session ran `claude-fable-5-1` (frontier) at high. Agreed, no change.

## Outcome

The two surfaces that emit the catalog's largest single-reply deliverable now state the single-limit output-budget rule and reconcile it with `## Output Minimization`. The base64 question is settled with evidence: the scripts are write-only, but the agent's own reads (of the model while authoring, of the generated page while verifying) are the two paths that return payloads into context, so the skill now forbids both with a projection recipe and script-based verification, leaving the offline guarantee untouched. Both security skills carry the two safeguard phrasings and state that a refusal on a code-review request is a false positive to rephrase around.

## Sub-tasks

| Task | What was done | Where |
|---|---|---|
| T011 | Step 6 of the skill opens with a BINARY output-budget bullet (single limit across reasoning and reply; reasoning for understanding, inputs, structure, hard decisions; output for writing once; start at the documented default effort; explicit reconciliation with `## Output Minimization`). The command gains the same rule as a Notes bullet, shorter. | `catalog/skills/specialized-domains/document-to-interactive-html/SKILL.md`, `catalog/commands/presentify.md` |
| T012 | Asset path traced: `extract_content.py` encodes (lines 275, 2148) into `model.json` `data_uri` fields; `build_presentation.py` copies them into HTML (lines 707, 722); `visual_qa_score.py` reads the page as a script (line 1678); Step 9 reads screenshots. Two agent-side paths do return payloads to context: authoring from a wholesale read of `model.json` (Step 6, the primary path) and a wholesale read of the generated `.html` at Step 8. **Option (b)**, closed by guidance. | `development/base64-context-decision.md` |
| T013 | Step 3 gains a BINARY payload rule: inspect the model through a one-line projection that replaces each `data_uri` with MIME type and length, refer to images by section and block index, copy URIs with a script, never read the page back wholesale (Step 8 uses `grep` and scripts, Step 9 screenshots). Two Verification items pin it. Both security skills gain a short subsection "Asking the model about code without tripping safety classifiers" (bugs-not-compiles phrasing, supply the lesser-known language's documentation, refusal is a false positive to rephrase around). | the skill; `security-review/SKILL.md` (before Coverage), `security-patch-advisor/SKILL.md` (before Best Practices) |
| T014 | `tests/skills/test_presentify_output_budget.py` (6 tests on stable phrases: both surfaces state the rule and name Output Minimization; the skill states the payload rule and pins it; both security skills carry the phrasings; the decision note names option (b)). No asset path changed, so no new self-containment test was needed; the existing offline assertions stand. | `tests/skills/` |

## Plan delta

**Disposition: Incomplete (non-blocking, resolved inside the phase).**

Evidence: the plan framed the base64 question as whether a re-read of the generated HTML turns the pipeline into the trigger, and named that path specifically. The trace found that path and a second one the plan did not name: the authoring step itself, where the agent reads `model.json` (which carries every payload) to write the page. Both are agent reads, not script behavior, so the fix is the same kind (guidance) and the offline guarantee is untouched. The plan's option (b) still holds; its description of the path was incomplete.

Consequence for remaining phases: Phase 7's Goal review should read the base64 decision from the note, not from the plan's framing; nothing else depends on it.

## Verification

- `validate` target (35 commands): 0 failed.
- `tests/skills` tier: 989 passed (1m05s), including the 6 new tests. `check_registry_entries.py --check --strict`: agree (no Tier 1 field changed).
- `validate_skills.py --bundles-only`: PASS. Body sizes: the skill 361 to 366 lines, `security-review` 445 to 449, `security-patch-advisor` 603 to 607 (grandfathered above the 500 target, well under the 800 cap; the plan asked for short rules, and each addition is one paragraph).
- Verification expectation (functional-verification, real boundary): a multi-section fixture (a Markdown review with eight sections, lists, and tables, plus a four-row CSV) was extracted with `scripts/extract_content.py` (13 sections, 27 blocks) and built with `scripts/build_presentation.py`. Observed artifact: 39,400 bytes, ends with `</html>`, 13 `<section>` elements, zero external `src`/`href` references, no truncation. `scripts/visual_qa_score.py` reported the render-only-defects check PASS and a structural-only FAIL with two high-severity design findings, which is the expected grade for the deterministic plain baseline (the skill states that output "does not by itself meet the interactive-website bar"); the expectation under test was completeness without truncation, which held. The output-budget rule is guidance for the agent-authored PRIMARY path, so its application cannot be observed in a script build; the honest record is that the deterministic path produces a complete artifact and the rule now governs the path that could truncate.
- `ruff check` and `ruff format --check`: clean on the new test. Unicode strict on the four touched surfaces and the v4.7 docs: exit 0. Detector defect class: 0 on all four surfaces and the decision note.

## CI impact record (8.3)

No pipeline file changed. One new test module under `tests/skills/`, a tree the `tests` job runs whole. Four distributed skill and command bodies changed; `validate_skills.py --bundles-only` in the `validate` job already covers them.

## Deviations

None marked `# DEVIATION:`.

## Next

Phase 4: worked quoting examples in the two research skills, the progress-update half and the formatting rule in `## Communication Contract` across all twelve templates and in `agent-communication`. Plan asks strong / high; the session's frontier tier meets it.
