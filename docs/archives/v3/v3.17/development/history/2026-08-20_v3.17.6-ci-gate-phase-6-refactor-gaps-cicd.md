# Session History - v3.17.6 Phase 6: Architecture Refactor, Known-Gaps Reconciliation, and CI/CD

**Date**: 2026-08-20
**Plan**: [docs/releases/v3/v3.17/plans/v3.17.6-ci-gate-and-branch-hygiene.md](../../plans/v3.17.6-ci-gate-and-branch-hygiene.md)
**Phase**: 6 of 6 (final)
**Branch**: `develop`
**Status**: Complete. The terminal-phase gate found a real CI regression that the plan's own Phase 3 had predicted might exist, and fixing it is the substance of this phase.

## Goal

Leave the project well-organized, its gaps reconciled, and CI/CD complete and measured.

## The Terminal-Phase Gate Earned Its Place

Phase 3 recorded `DF-1`: the `tests-windows` PyYAML fix had never run in CI, because that job is gated off pull requests, so its first execution would be the post-merge push to `develop`. Phase 6 checked that run.

**It failed.** `tests-windows` failed on the `develop` push for `43f144ca`, and `ci-required` failed with it. The PyYAML fix was correct and insufficient.

The root cause was not PyYAML. `test_ci_changes_classifier.py` invoked bare `bash`, resolved via `shutil.which("bash")`. On a GitHub Windows runner that resolves to the **System32 WSL launcher stub**, which precedes Git Bash on PATH and, with no distribution installed, prints a UTF-16 "no installed distributions ... to install" message and exits 1. The suite passed locally (Git Bash won the PATH race there) and on ubuntu, and failed only on the Windows runner. The UTF-16 stdout in the failure output is what identified it.

**This repository already documented that hazard.** The `bootstrap` job comment in `ci.yml` records the identical PATH shadowing from v3.15.6 Phase 4, where it had been misdiagnosed for four minor versions as "the dev host cannot run bash". That is the finding worth keeping: the knowledge existed, in this file, and was not reachable from where the new code was written.

### The fix, and the fail-open inside it

`tests/validators/bash_helper.py` resolves bash **empirically**: it probes each candidate with `printf ok` and takes the first that answers, rather than filtering by path. A stub is rejected because it does not work, not because its location was guessed. It sits outside `conftest.py` deliberately, because `from conftest import ...` resolves to `tests/conftest.py` and shadows this directory's own conftest.

The resolver then becomes load-bearing in its own right: returning `None` would make both bash-driving suites SKIP, reporting green while asserting nothing. `test_bash_helper.py` closes that with four tests, asserting a working bash is found on this host, that it can execute a FILE rather than only `-c`, that a non-working stub is rejected, and that nothing usable yields `None` rather than a broken path.

One incidental validation: `ci-required` failed **because a needed job failed**. The aggregate gate's entire purpose was exercised by a real failure rather than only by its 12 unit tests.

## A Defect I Introduced And Reverted

`ruff check --fix` plus `ruff format` run on `tests/validators/` rewrote **32 unrelated test files** alongside the three intended ones. `AGENTS.md` states that every changed line must trace to the request and that adjacent style issues outside scope are not to be cleaned up; a 32-file formatting diff inside a release commit also buries the reviewable change. All 32 reverted. Final scope: two modified files (+21/-5) and two new ones. The 30 pre-existing lint findings in that directory remain untouched and are not this version's to fix.

The lesson is narrow and worth keeping: lint the FILES a phase touched, never the directory containing them.

## Architecture Refactor (6.1)

Little to do, which is the expected outcome for a version that added guards rather than restructuring anything. One directory removed as an empty untracked leftover (`docs/v3/v3.20/comparisons`). **That characterisation was wrong**: it was being populated by a concurrent session through OneDrive sync and returned with a 27 KB comparison in it. Corrected as `QG-8` in the known gaps. No duplicates, no orphans, no stray artifacts, no reference repair needed because nothing moved.

Terminal-gate verification checks all clean: installer parity, platform read-contract alignment (10 platforms), platform defaults sync (13 platforms), base-template lockstep parity, model-prompting profile structure, and registry entries.

## CI/CD, Measured (6.3)

Billed minutes for the same PR shapes before and after the migration, from the check-runs API rather than estimated:

| PR shape | Before | After | Delta | Mergeable before? |
|---|---|---|---|---|
| docs-only | 0.30 (`#54`) | 1.38 (`#60`) | **+1.08** | No |
| code-only | 15.47 (`#52`) | 16.15 (`#61`) | **+0.68** | No |

**Accepted, no follow-up.** About one extra billed minute on a docs-only pull request buys the removal of a defect that made six pull requests unmergeable in a single day.

This also corrects a figure quoted earlier in the version. The docs-only delta was given as +1.38 on the basis that the shape "previously ran no `ci.yml` jobs at all". That is true of `ci.yml`, but 0.30 billed minutes were already being spent by `doc-colocation.yml` and `presentify-extractor.yml`, so the honest delta is **+1.08**.

The Phase 1 validator runs as one step in the pre-existing `validate` job: no new job, no new matrix leg. `ci.yml` went from 8 jobs to 10, and both additions (`changes` 0.12, `ci-required` 0.05 billed min) belong to Phase 2. Matrix shapes for `bootstrap`, `install-smoke`, and `installer-smoke` are unchanged, so no expensive leg was added.

## Known-Gaps Reconciliation (6.2)

All three items the plan named explicitly are recorded:

- **The OneDrive hazard** (`DF-3`) is not fixable in this repository. Nothing in a catalog can stop a sync client holding a directory handle; moving the working copy off the synced drive is the only real remedy and belongs to the operator. What this version did instead is make the consequence detectable rather than the cause preventable, via the `--pre-tag` assertion.
- **The Actions-minute delta** (`QG-6`) is accepted with the measured table above.
- **The two clean CI skills** (`NI-6`, detailed at `NI-3` and `NI-4`) are restated so a future reader knows they were checked rather than skipped.

## Test Results

- Phase 6 affected suites: 95 passed.
- `tests/validators` + `tests/workflows`: 966 passed, 11 skipped.
- New: `test_bash_helper.py` 4 tests; both bash-driving suites 34 passed.

## Outstanding Operator Items

Deliberately not actioned inside this phase, because each is an outward-facing act on project state rather than a code change:

- The stale repository description (`NI-2`): reports 256 skills / 15 commands / 22 hooks against a declared 273 / 18 / 31.
- Four `test/ci-proof-*` branches whose pull requests were closed unmerged (`QG-2`). `delete_branch_on_merge` does not cover a closed-unmerged PR.
- The platform read-contract freshness stamp reads `verified_for_version: 3.17.5`. Bumping to 3.17.6 will fail that hard gate unless the contract is re-verified or knowingly re-stamped. v3.17.6 changed no platform integration and no read path, and the last full ten-platform pass was 2026-08-18.

## Next

`/update release` owns the version bump, changelog finalization, tag, push, and GitHub Release, behind its own confirmation gates. This phase creates no tag and pushes no tag.
