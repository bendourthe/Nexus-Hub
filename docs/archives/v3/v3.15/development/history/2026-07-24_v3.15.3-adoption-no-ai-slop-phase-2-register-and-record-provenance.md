# Session History - v3.15.3 adoption-no-ai-slop Phase 2: register and record provenance

**Date**: 2026-07-24
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.3-adoption-no-ai-slop.md](../../plans/v3.15.3-adoption-no-ai-slop.md)
**Phase**: 2 of 3 - Register and record provenance
**Branch**: `feat/adoption-no-ai-slop`
**Status**: IMPLEMENTATION COMPLETE; not yet committed (awaiting the 8.10 commit-and-push decision)

## Goal

Make the Phase 1 `anti-slop-editing` skill discoverable across the catalog (the three `data/` registry files) and record its reverse-engineering provenance (the matrix row + a CHANGELOG entry), keeping JSON integrity green. Registry + docs only; no skill-body or code change.

## Model-routing pre-flight

Phase 2's recorded recommendation is the mid tier at medium effort (Sonnet 5). The session is on Opus 4.8 (a stronger tier). Per the no-degradation guarantee the pre-flight never auto-downshifts mid-plan, and a stronger model handles mechanical registry edits without risk, so proceeded on Opus 4.8 without a switch (a cost-motivated downshift would be a manual `/model` choice, not a pre-flight action).

## Final-phase detection

`is_final_phase = false`. Phase 3 ("Architecture Refactor, Known-Gaps Reconciliation, and CI/CD") is the terminal phase; Phase 1 is complete (session-history file + committed on this branch). The standard 10-step post-phase sequence ran; the release-readiness workflow did not.

## Sub-tasks completed

### 2.1 - Register in the three data/ files (by hand)

- **`data/SKILL_INDEX.md`**: one row added under the developer-experience rows, matching the file's actual `| name | developer-experience | "summary" | path |` format (lowercase category, as the file uses, not the title-case the AGENTS.md example shows).
- **`data/skills.json`**: a full entry inserted after the sibling `hallmark-design` object, matching its schema and the file's real 6-space indentation. Two file conventions were confirmed by inspection before writing: `summary_l0` (267/267 entries) and `overview_l1` (264/267) are stored wrapped in literal `\"..\"`; `description` is stored UNwrapped (266/267). Computed the real `size` (234 lines, 16020 chars, ~2480 tokens at the sibling's chars/token ratio). `security` defaults 100/100/95 + validated true; `status` production; `priority` MEDIUM.
- **`data/marketplace.json`**: developer-experience `skill_count` bumped 31 -> 32.
- Did NOT run `build_skills_catalog.py` (it rewrites the whole tree into a ~6000-line diff); all three edits are surgical, minimal-diff hand edits.
- Consistency confirmed: `skills.json` = 268 entries, marketplace category-sum = 268, `SKILL_INDEX.md` contains the row.

### 2.2 - Reverse-engineering-matrix row

- Added a `skill-native` row to the "Skill-native adoptions (no MCP, no outbound calls)" section of `docs/policy/mcp-reverse-engineering-matrix.md`.
- Records the MIT external source (petergyang/no-ai-slop) in the Rationale column ONLY; the source repo, product, author, and course are named nowhere in the shipped `SKILL.md` or its reference files (Reverse-Engineering Attribution Rule).
- Confirms zero outbound call, API key, runtime dependency, third-party data processor, and no new commercial relationship. Lists the NOT-adopted items (the `agents/openai.yaml` cross-harness stub + the paste-a-URL install model, both superseded by Nexus-Hub's installer + integration registry; the `behindthecraft.com` marketing links) and the em-dash-ceiling tightening.

### 2.3 - CHANGELOG entry

- Added a `## [Unreleased]` section (none existed since the v3.15.2 section was finalized) with an `### Added` bullet describing the skill generically (no external name), the dual Edit/Detect mode, the two reference files + routing eval, and the 268-skill catalog count. No version-carrying surface was bumped (that is `/update release`'s job).

### 2.4 - Testing and stabilization

- JSON integrity: `skills.json` (268), `bundles.json` (15), `workflows.json` (18), `marketplace.json`, `templates.json` all parse.
- `python scripts/check_version_sync.py`: PASS - every surface still matches 3.15.2. The `[Unreleased]` heading has no version number, so the extractor correctly reads 3.15.2 as the latest released version; no version numbers were touched.
- `python scripts/run_trigger_evals.py --gate`: PASS (0 un-allowlisted collisions, 0 routing failures across 7 skills with cases). The routing gate reads `SKILL.md`, not the `skills.json` description mirror, so adding the mirror entry is inert to it.
- `python scripts/validate_skills.py` on the skill: PASS (0 errors, 0 warnings), unchanged from Phase 1.
- CI/CD: no edit needed. `data/**` is outside `docs/`, so a registry change already triggers the full `validate` job, which parses the catalog JSON and runs `validate_skills.py --bundles-only` + `run_trigger_evals.py --gate`. Confirmed no-op; the final CI create/update/optimize pass is Phase 3.3.

## Quality gate (GO / NO-GO)

- Tests: no code changed; the validation surface is JSON integrity + the two validators, all green. GO.
- Coverage: not applicable (no code). GO.
- Build: all 5 catalog JSON files parse; counts consistent at 268. GO.
- Version sync: green (no version numbers touched). GO.

## Deviations

- **`statistics.total_skills` does not exist (advisory, resolved)**: sub-task 2.1 instructed incrementing `statistics.total_skills`, but the current `marketplace.json` schema has an empty `statistics: {}` and derives the total from the sum of `categories[].skill_count`. Only the category count was bumped (31 -> 32), which keeps the derived total (268) consistent; no non-existent field was invented. Logged as a v3.15.3 advisory.
- **Pre-existing stale `plugin.description` count (advisory)**: `marketplace.json` `plugin.description` reads "265 curated skills" - pre-existing drift (already stale at 267), not introduced or worsened here. A marketing-prose count, not a `check_version_sync` surface; `/update release` reconciles it. Logged as a v3.15.3 advisory. The README / AGENTS.md "267 skills" prose is likewise left for `/update release`.

## Files touched

- Modified: `data/SKILL_INDEX.md`, `data/skills.json`, `data/marketplace.json`, `docs/policy/mcp-reverse-engineering-matrix.md`, `CHANGELOG.md`, `docs/v3/v3.15/known-gaps.md`, `docs/DEVLOG.md`.
- Added: `docs/archive/v3/v3.15/development/history/2026-07-24_v3.15.3-adoption-no-ai-slop-phase-2-register-and-record-provenance.md` (this file).

## Next steps

1. Resolve the 8.10 commit-and-push decision (commit Phase 2 on `feat/adoption-no-ai-slop`).
2. Phase 3 (final): terminal architecture-refactor + known-gaps reconciliation + CI/CD pass (a near-no-op for this skill-only plan), then hand off to `/update release`, which bumps every version surface (incl. the README / AGENTS.md / `plugin.description` skill counts to 268 via `check_version_sync`), finalizes the changelog, merges develop -> main, tags, and publishes the GitHub Release.
