# Development Log: v3.15.9 Phase 3 Cursor Usage Contracts, Auth Probe, and Assets

**Date**: 2026-08-04
**Operator**: Repository maintainer
**Assisted by**: GPT-5.6 Sol in Cursor, frontier tier
**Objective**: Complete Phase 3 of `v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md` by locking personal usage, authentication/scrape, fixture, and visual contracts before runtime extension work.
**Outcome**: Tasks T013-T019 are complete on `feat/v3.15.9-cross-provider-routing`, including the maintainer-approved local phase commit. No push, pull request, release, installer, runtime provider, or billing mutation is included.

---

## 1. Starting State

- **Branch**: `feat/v3.15.9-cross-provider-routing`
- **Starting commit**: `3c65abb9` (`feat(routing): add cross-provider model maps`)
- **Working tree**: clean before editing
- **Environment**: Windows 10, PowerShell 5.1, Python 3.12, pytest, Ruff, Cursor
- **Prior session reference**: `docs/archive/v3/v3.15/development/history/2026-08-03_v3.15.9-cross-provider-routing-phase-2-model-routing-skill-command-wiring.md`
- **Plan reference**: `docs/v3/v3.15/plans/v3.15.9-cross-provider-routing-and-cursor-usage-monitor.md`

Phase 2 had completed the portable routing runtime. Phase 3 began with no Cursor usage extension, contracts, fixtures, workflow, or committed artwork. The plan and deterministic reassessment both selected `frontier` / `max`; the GPT-5.6 Sol session already met that tier.

---

## 2. Chronological Steps

### 2.1 T013-T014 - Data Contract and Sanitized Fixtures

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Define personal Cursor Models, Other Models, on-demand, reset, freshness, and source semantics; add healthy, on-demand, error, empty, unknown-denominator, and scrape fixtures.

**What happened**: Created a normalized personal-usage model with same-unit percentage rules, independent pool ownership, personal currency spend, Teams shared-pool context, source/freshness, reset/cache constraints, dashboard semantic anchors, typed failure behavior, and a bounded probe. Added seven JSON and two HTML fixtures without credentials or personal records.

**Key files changed**: `docs/v3/v3.15/development/cursor-usage-data-contract.md`, `tests/fixtures/cursor-usage/`

**Troubleshooting**:

- **Problem**: The first focused run failed the exact `$limit / member_count` assertion even though the contract described the same prohibition in prose.
- **Resolution**: Added the literal formula so the plan invariant is mechanically visible; the rerun passed 25/25.

**Verification**: JSON and HTML syntax parse cleanly; tests verify fixture inventory, secret patterns, source enums, percentage math, unknown denominators, on-demand ownership, errors, and semantic scrape anchors.

---

### 2.2 T015 - Authentication and Scrape Probe

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Record read-only Windows/macOS/Linux credential candidates, dashboard routes, field mapping, failure modes, and SecretStorage/manual fallback without logging tokens or cookies.

**What happened**: Distinguished official Cursor CLI/configuration behavior from empirical IDE-state candidates and undocumented dashboard endpoints. The Windows path probe confirmed `state.vscdb` and Cursor.exe exist while `~/.cursor/cli-config.json` does not; no file or value was opened. Documented one-key/one-request authorization, redaction, no-cookie-scan, and fail-soft rules.

**Key files changed**: `docs/v3/v3.15/development/cursor-usage-auth-probe.md`

**Troubleshooting**: The initial PowerShell presence-check expression used a pipeline directly after `foreach`, which PowerShell 5.1 rejected. Capturing the loop output first produced the bounded boolean-only result.

**Verification**: Contract tests require all three OS candidates, the documented configuration path, the no-open statement, no browser-cookie access, no personal absolute path, and the private-API disclaimer.

---

### 2.3 T016-T017 - Visual Contract and Artwork Ingest

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Ingest the maintainer's Cursor artwork, lock `#4682B4`, define status/warning/package use, and record theme/accessibility and icon-size requirements.

**What happened**: The maintainer supplied 480px and 48px transparent PNGs. Their virtual attachment paths were not filesystem-readable, but the filenames identified Icons8. The official Icons8 CDN returned matching art under icon ID `DiGZkjCzyZXn`. Committed those auditable bytes, hashes, dimensions, source links, and free-use attribution requirement. Defined a Phase 5 monochrome 20x20 `currentColor` vector/font pipeline and 256x256 package downscale.

**Key files changed**: `extensions/cursor-usage-monitor/icons/cursor-ai-480.png`, `extensions/cursor-usage-monitor/icons/cursor-ai-48.png`, `extensions/cursor-usage-monitor/icons/README.md`, `docs/v3/v3.15/development/cursor-usage-visual-contract.md`

**Troubleshooting**:

- **Problem**: An attempted generated reproduction changed the art by adding gradients and a checkerboard.
- **Resolution**: Deleted it immediately and used only byte-exact official CDN assets matching the supplied references.

**Verification**: Tests assert both dimensions, RGBA color type, SHA-256 values, `#4682B4`, attribution, icon ID, transparent 256px package plan, and 20x20 status-vector plan.

---

### 2.4 T018 - Contract Tests and Stability Gate

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Reject false per-member caps, secretful fixtures, missing brand color/assets, and undocumented auth claims.

**What happened**: Added 25 semantic tests across data, auth, fixtures, assets, licensing, and ASCII safety. Simplified one dense source-extraction comprehension after the first green pass without changing assertions.

**Key files changed**: `tests/plans/test_v3_15_9_cursor_usage_contracts.py`

**Troubleshooting**: None after the exact contract-text correction in T013.

**Verification**: Focused suite 25 passed; complete `tests/plans` suite 79 passed; full clean-scaffold repository suite 1,508 passed with 20 declared skips.

---

### 2.5 T019 - Lifecycle Records and Commit Preparation

**Branch**: `feat/v3.15.9-cross-provider-routing` | **PR**: none | **Merged to**: not merged

**Plan specification**: Run contract tests and secret scans, generate session history, and create one local commit without pushing.

**What happened**: Ran focused and repository tests plus every direct Windows validation equivalent relevant to this phase. Existing CI already collects `tests/plans`, triggers on the extension/fixture paths, cancels superseded runs, caches dependencies, and bounds jobs with timeouts, so no workflow edit was justified. Updated the plan, known-gaps checkpoint, docs audit, DEVLOG, and this history.

**Key files changed**: Phase 3 plan checklist, `docs/v3/v3.15/known-gaps.md`, `docs/v3/v3.15/docs-cleanup-report.md`, `docs/DEVLOG.md`, and this history

**Troubleshooting**: The full repository suite would copy eight documented untracked empty skill scaffolds into flattened integrations. They were temporarily isolated and restored inside a `try/finally` test harness, yielding a clean-checkout result without modifying tracked content.

**Verification**: See the gate and testing tables below.

---

## 3. Verification Gate

| Check | Result |
|---|---|
| Branch and clean-start precondition | PASS |
| Focused Cursor contract tests | PASS - 25 passed |
| Complete plan-contract suite | PASS - 79 passed |
| Complete repository suite | PASS - 1,508 passed, 20 skipped |
| Ruff lint and format | PASS |
| IDE diagnostics | PASS - none |
| Fixture secret/personal-record checks | PASS |
| Asset dimensions, alpha, and hashes | PASS |
| Skill bundle/quality/routing gates | PASS |
| Personal-path and Unicode validators | PASS |
| Supply-chain/workflow/security scans | PASS - no HIGH or CRITICAL findings |
| Version/template/profile/platform checks | PASS |
| Compression accuracy gate | PASS - 100% CCR, 100% signatures, 45.8% reduction |
| `git diff --check` | PASS before lifecycle docs; repeated before commit |
| CI/CD readiness and optimization | PASS - no workflow change required |

No runtime source module was added in Phase 3, so the 80% code-coverage gate is not applicable; every new executable assertion lives in the fully passing contract test module.

---

## 4. Known Issues

| Issue | Severity | Decision |
|---|---|---|
| HO-5: no authorized live Cursor session/dashboard request yet | Non-blocking hand-off | Phase 4 must obtain explicit authorization before claiming automatic session reuse; otherwise use SecretStorage plus HTML/cache/manual fallback |

No Phase 3 implementation defect, regression, suppressed warning, missing-test gap, or bypassed quality gate remains.

---

## 5. Plan Discrepancies

- The plan described supplied PNG/SVG artwork, but the maintainer provided two PNGs and no SVG. Phase 3 preserves both PNG sources and defines the planned Phase 5 vector normalization instead of inventing an SVG or modifying the artwork.
- The virtual chat attachment paths were not accessible to filesystem tools. Official Icons8 CDN files with matching geometry, sizes, icon identity, and source provenance were used.

---

## 6. Assumptions Made

- **Source ownership**: The supplied filenames and matching official icon ID establish Icons8 provenance. Free-use attribution remains required unless a paid license is later documented.
- **Session state**: Presence of `state.vscdb` proves only a candidate path, not a usable credential, schema, or endpoint.
- **Dashboard integration**: Private JSON endpoint names are discovery leads, not public API contracts.
- **Unit safety**: A source-provided percentage may stand alone, but derived percentages require same-unit quantities.
- **Phase boundary**: Final SVG/font/package icon generation belongs to Phase 5; installer host isolation remains Phase 6.

---

## 7. Testing Summary

### Automated Tests

- Cursor Phase 3 contract suite: 25 passed.
- Complete plan-contract suite: 79 passed.
- Complete repository suite: 1,508 passed, 20 skipped.
- Validation and security gates: all pass.

### Manual Testing Performed

- Compared the supplied large and compact Cursor marks with the official CDN renderings.
- Verified official CDN headers expose icon ID, size, format, and successful image responses.
- Confirmed the Windows Cursor state database path and executable path exist without opening content.
- Re-read current Cursor billing, usage, spend-limit, CLI auth/configuration, API, and Admin API documentation.

### Manual Testing Still Needed

- [ ] With separate explicit authorization, run the one-key/one-request session/dashboard probe before selecting automatic credential reuse.
- [ ] Phase 5 Extension Development Host screenshots for light, dark, and high-contrast themes.

---

## 8. TODO Tracker

### Completed This Session

- [x] T013 Create the Cursor usage data contract.
- [x] T014 Add sanitized Cursor usage fixtures.
- [x] T015 Record auth candidates and scrape procedure.
- [x] T016 Create the visual contract.
- [x] T017 Ingest supplied Cursor artwork with source hashes and licensing gate.
- [x] T018 Add and pass contract tests.
- [x] T019 Generate Phase 3 lifecycle records.

### Remaining

- [ ] Begin Phase 4 from the completed local phase boundary.

### Out of Scope

- [ ] Typed provider, authentication adapter, scrape normalizer, and cache/store - Phase 4.
- [ ] Status bar, dashboard, settings, warning view, vector/font/package assets - Phase 5.
- [ ] Installer host isolation, packaging, and CI E2E - Phase 6.

---

## 9. Summary and Next Steps

Phase 3 converts ambiguous billing, session, scrape, and branding assumptions into fixture-backed contracts. The two personal pools, on-demand spend, team shared context, source provenance, least-privilege auth boundary, visual color, source artwork, licensing, and derivative pipeline are now explicit and mechanically enforced. HO-5 is the only open hand-off and does not block fixture-driven Phase 4 implementation.

**Next session should**:

1. Resolve or honor HO-5 while implementing the Phase 4 auth/provider boundary.
2. Keep user-interface derivatives and host-isolated installer work in Phases 5-6.
