# Session History - v3.15.5 model-prompting-research Phase 6 (final): architecture refactor, known-gaps reconciliation, and CI/CD

**Date**: 2026-07-28
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.5-model-prompting-research.md](../../plans/v3.15.5-model-prompting-research.md)
**Phase**: 6 of 6 - Architecture Refactor, Known-Gaps Reconciliation, and CI/CD (TERMINAL)
**Branch**: `feat/model-prompting-research` (Phases 1-5 at `fb6a33e7`, `38b020cc`, `58ccadb3`, `f0ef2dbe`, `ca9c23d0`)
**Status**: IMPLEMENTATION COMPLETE; v3.15.5 RELEASE-READY. Not yet committed (awaiting the 8.10 decision); `/update release` NOT run.

## Goal

Leave the repo clean, correctly documented, gap-reconciled, and covered by optimized CI, and prepare the release handoff.

## Final-phase detection

`is_final_phase = **true**`, from all five signals and not from any claim:

1. **Phase ordering**: Phase 6 is the numerically highest and the last `## Phase N:` by document order.
2. **Title heuristics**: it carries the v3.11.0 terminal title "Architecture Refactor, Known-Gaps Reconciliation, and CI/CD".
3. **Prior-phase completion**: Phases 1-5 each have a session-history file under `<version_dir>/development/history/` and a commit.
4. **Plan metadata**: no `Final-Phase:` marker overrides the ordering.
5. **Adjacent plans**: four sibling plans share the version dir, which is why signal 1 alone was not treated as sufficient.

So the mandatory 9.0 gate and the release-readiness workflow both ran, and 9C-9E were handed to `/update release` rather than executed.

## Model-routing pre-flight

Strong reasoning tier at high effort per the plan (repo-wide refactor plus doc correctness ahead of a release). Enumeration returned the picker sentinel; the session is Opus-class, matching.

## Sub-tasks completed

### 6.1 - Architecture refactor

`project-refactor` + `docs-layout-refactor` over the release's footprint. The v3.15.5 diff introduced:

- no duplicates;
- no orphans (the bundle audit finds all eight bundled resources referenced from SKILL.md, 0 unreferenced);
- no committed empty directories;
- no moved or renamed committed file, so no reference repair was needed.

The docs tree stays canonical (`comparisons/`, `development/`, `plans/`, `known-gaps.md`).

**A deliberate departure from precedent.** The detectors surfaced **8 UNTRACKED empty directories** elsewhere under `catalog/skills/`: `code-cleanup/lint-repair-loop`, `developer-experience/helper-script-authoring`, `developer-experience/code-optimizer/scripts`, `testing/visual-regression-testing/scripts`, `tests-generation/false-confidence-test-audit`, `tests-generation/performance-regression-gate/scripts`, `workflow/commit-sweep`, `workflow/end-of-shift-validation`. Each carries zero tracked files (confirmed with `git ls-files`), so they have zero repo impact. v3.15.4 Phase 7 removed two such directories for working-tree tidiness; this phase **reported and left them in place**, because several are shaped like skill scaffolds (a skill directory with no `SKILL.md` yet) and a parallel session is actively writing in this worktree. Deleting another session's in-progress scaffolding is not a tidiness win.

**NI-4 resolved.** All five prose count surfaces corrected to 269 skills / 17 commands:

| Surface | Line |
|---|---|
| `AGENTS.md` Repository Overview | 11 |
| `AGENTS.md` project-structure comment | 27 |
| `README.md` opening description | 9 |
| `README.md` "what this repo is" bullet | 33 |
| `README.md` global-install bullet | 181 |

`docs/CATALOG-COVERAGE.md` carries no counts. No platform-coverage table needed a row, because the new skill and command reuse existing distribution channels (the recursive skill-folder copy and the `catalog/commands/` copy) rather than adding a surface. A prose-count guard was considered and deliberately NOT added: the five sentences phrase the counts too differently to assert safely, and the machine-readable counts are already guarded by Phase 4's `test_registry_consistency.py`. The README "What's New in v3.15.5" section is left to `/update release`, matching the v3.15.4 precedent.

### 6.2 - Known-gaps reconciliation

Every item now has a disposition. **Four RESOLVED** across the release: NI-1 (Phase 2.1), NI-3 (Phase 4.3), NI-4 (Phase 6.1), and **MT-1, which Phase 5.3 closed as a side effect** rather than by design, since its paired gate tests assert the schema validator IS wired into both the Makefile and `ci.yml` while the advisory checker is NOT. **Six carried** as documented, non-blocking deferrals: NI-2, DF-1, DF-2, MT-2, MT-3, MT-4.

The TODO/FIXME/XXX/HACK/DEVIATION sweep over the release's files found two hits, both benign, and corrected one: the seed claim's note read `TODO(phase-2)`, a reference to a phase that has now passed without a real research run ever happening. It was re-pointed at `TODO(first-verified-run)` and expanded to state plainly that the entry is a hand-seeded contract example rather than research output, which is also why the planner correctly still lists that model as needing research.

**DF-2 got the decision the maintainer deferred to this phase: ACCEPTED as a documented residual**, rather than building the catalog-wide leakage gate now. Three reasons, recorded in full in the known-gaps entry:

1. The DoD's *intent* is met. The autonomous path (the only one this release creates) provably cannot write model-specific content into a shared body, proven by a paired test rather than asserted.
2. The gate's blocking cost lands on pre-existing content, not on this feature: 6 matches across the `base-*.md` set and 10 SKILL.md files would fail on day one, several legitimately, so shipping it means a triage pass over unrelated skills or an allowlist large enough to blunt the guard.
3. The residual is bounded and visible: a human hand-edit already goes through review, and the corrected mechanism is documented in three places (`references/edit-routing.md` with the three-case evidence, the SKILL.md hard-rail section, and the command's safety posture) so no future reader inherits the false premise.

A repo-internal `check_model_specific_leakage.py` remains the right long-term fix and is recorded for the next `/plan` ingest, not as a v3.15.5 blocker.

### 6.3 - CI/CD

Complete with no edit, verified surface by surface rather than assumed:

| Required coverage | Where it runs |
|---|---|
| The two new repo scripts' pytests | `pytest tests/validators` |
| The profile schema validator | `validate` job, alongside `make validate` |
| The research-engine deterministic tests | `pytest tests/skills` |
| The auto-apply guard-loop tests | `pytest tests/skills` |
| The release-step tests | `pytest tests/skills` |
| The registry-consistency guard | `pytest tests/validators` |

The workflow already carries the optimizations the plan asks for: workflow-level `paths-ignore`, `concurrency` cancel-in-progress, pip caching keyed on the extension manifests, and cost-gated matrix legs (PRs run ubuntu-only; macOS and Windows legs run on pushes).

**The freshness checker is confirmed NOT a blocking gate**, and that confirmation is machine-enforced rather than prose: Phase 5's `test_freshness_checker_is_not_wired_into_any_gate` fails the moment it appears as an invocation in the Makefile or `ci.yml`.

### 6.4 - Release readiness

The Definition of Done is confirmed item by item in a table in the known-gaps section. Five of six items are met outright. The sixth is recorded as **met in substance with its stated mechanism corrected**: the DoD credits `check_base_template_parity.py` with proving that a model-specific finding cannot reach a shared body, which Phase 3 disproved with three fixture cases. The accurate claim, now recorded, is that this feature cannot autonomously write model-specific content into a shared body.

## Full verification sweep

| Check | Result |
|---|---|
| JSON integrity | 269 skills, 15 bundles, 18 workflows, templates OK |
| The 10 single-shot validate gates | all exit 0 |
| `validate_skills.py --bundles-only` | PASS, 0 errors, 6 warnings (baseline) |
| `run_trigger_evals.py --gate` | PASS, 0 collisions, 0 routing failures, 269 descriptions |
| `scan_skill_security.py --fail-on high` | PASS |
| Compression accuracy gate | PASSED (CCR 100.0%, signatures 100.0%) |
| ShellCheck (`installer.sh`, `install.sh`) | 0 |
| `tests/skills` | 209 passed, 3 skipped |
| `tests/validators` | 372 passed, 1 failed (pre-existing `test_discover_obsidian_vault_marker`) |

## Release-readiness workflow (9A / 9B done; 9C-9E handed off, NOT run)

- **9A**: re-read the open items and re-ran the marker sweep. No release blocker. One stale TODO reference corrected; every remaining item carries an accurate reason and suggested next step for the next `/plan` ingest.
- **9B**: the test surface is comprehensive. Every new module has unit coverage; the deterministic ends of the agent pipeline sit at 83-97% branch coverage (apply engine 83%, profile writer 91%, schema validator 94%, freshness checker 97%); CI covers every change; no secret is introduced (the catalog security scan is clean).
- **9C-9E**: the version bump (3.15.4 to 3.15.5), changelog stamp, count reconciliation, commit, `develop` merge, `v3.15.5` tag, push, and GitHub Release are **HANDED to `/update release`** and were not run. The branch is unpushed and not merged to `develop`, and the maintainer has held every phase at commit-only.

**This phase performed no version bump, tag, merge, or push.**

## Files changed

**Added (1)**

- `docs/archive/v3/v3.15/development/history/2026-07-28_v3.15.5-model-prompting-research-phase-6-refactor-knowngaps-cicd.md` (this file)

**Modified (5)**

- `AGENTS.md` (two count surfaces)
- `README.md` (three count surfaces)
- `catalog/skills/ai-development/model-prompting-research/assets/profiles-index.json` (stale TODO reference corrected)
- `CHANGELOG.md` (`[Unreleased]` summary finalized to all-6-phases / release-ready)
- `docs/DEVLOG.md`, `docs/v3/v3.15/known-gaps.md` (Phase 6 entry, DoD confirmation table, reconciliation summary, dispositions)

## Next steps

Run `/update release` when ready. It owns the version bump 3.15.4 to 3.15.5 across every version-carrying surface (with `check_version_sync.py` enforcing consistency), the `[Unreleased]` to `## [3.15.5]` stamp, the README "What's New" section, the platform read-contract freshness re-stamp, `MANIFEST.sha256` regeneration, then the commit, the `develop` merge, the `v3.15.5` tag, the push, and the GitHub Release, keeping its own confirmation gates throughout.

One hold condition to be aware of, not a blocker: a parallel session is active in this worktree, so confirm the tree state before the release merge.
