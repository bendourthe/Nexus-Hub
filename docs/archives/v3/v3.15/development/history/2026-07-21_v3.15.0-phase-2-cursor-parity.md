# Session History - v3.15.0 Phase 2: Cursor parity

**Date**: 2026-07-21
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.0-platform-parity-all-gaps.md](../../plans/v3.15.0-platform-parity-all-gaps.md)
**Phase**: 2 of 7 - Cursor parity (skills, agents, hooks.json, project commands)
**Branch**: `feat/platform-parity-all-gaps` (cut off `develop`)
**Status**: Complete; per-phase gates green (1 pre-existing failure, deferred to Phase 5.2).

## Overview

Phase 2 delivers the full Cursor surface set. The load-bearing surprise: the Cursor integration code was already implemented in the branch-untangle commit `a56cdffa` (labeled "wip") - `cursor.py` already flattens skills, copies subagents, writes a `version:1` `hooks.json`, and mirrors project commands, with a comprehensive `test_cursor.py`. So the `implement-phase` plan-vs-codebase review re-scoped the phase away from re-writing code toward closing the verification gate `a56cdffa` deferred (known-gap DF-1), finishing sub-task 2.3's literal acceptance, and reconciling one stale cross-platform test that `a56cdffa` had left contradicting the new behavior. Two user decisions were taken up front (pre-flight): run the outbound DF-1 web re-verification, and seed project commands on `nexus-hub init`.

## Sub-tasks completed

- **2.1 Cursor skills + agents surfaces.** Already present from `a56cdffa` and verified: `cursor.py` flattens `catalog/skills` to `.cursor/skills` (native path only - the shared `~/.agents/skills` is deliberately NOT written, to avoid a teardown conflict with the codex integration that owns it) and copies `catalog/agents/*.md` verbatim to `.cursor/agents`. No code change needed.
- **2.2 Cursor hooks.json writer.** Already present from `a56cdffa`: a `version:1` schema writing `beforeShellExecution -> git-guardrails.sh`, gated on `hooks_supported`. Phase 2 CONFIRMED the schema against official docs (DF-1(b), below) and made no code change - the minimal writer is valid as-is.
- **2.3 Project-scoped Cursor commands.** Already mirrored by the workspace install; Phase 2 completed the literal acceptance ("`nexus-hub init` seeds `.cursor/commands/`") by extending `wire_project_surfaces` to seed project `.cursor/commands/` in addition to the rules stub (refactored the rules write into `_write_project_rules_stub` to remove the early-return tangle). The global `~/.cursor/commands/` mirror stays in `install_global`.
- **2.4 Testing and stabilization.** Added a `wire_project_surfaces` command-seeding test to `test_cursor.py` (now 9/9); reconciled the stale `test_cross_platform_flatten.py` (below); ran the contract validators + full integrations suite; updated the contract JSON + doc; re-scoped DF-1.

## DF-1 resolution (2026-07-21 direct doc re-read)

Phase 1 flagged DF-1: the Cursor code was written on a partially-unverified contract. Phase 2 did the direct re-read the gap asked for.

- **DF-1(b) `hooks.json` schema - RESOLVED, no code change.** [cursor.com/docs/hooks](https://cursor.com/docs/hooks) confirms the top-level shape `{"version": 1, "hooks": {<event>: [{...}]}}`; each entry's fields `type` / `timeout` / `loop_limit` / `failClosed` / `matcher` are all OPTIONAL (defaults `type="command"`, `failClosed=false`, `loop_limit=5`), so the entry may be the minimal `{"command": "..."}`. `beforeShellExecution` is a documented event; exit code `2` (or `{"permission":"deny"}`) blocks, other non-zero codes fail-open. Cursor reads both `~/.cursor/hooks.json` and `<project>/.cursor/hooks.json`. The integration's minimal git-guardrails writer is therefore schema-valid as-is.
- **DF-1(a) global `~/.cursor/commands/` - RESIDUAL, still UNVERIFIED.** Project `.cursor/commands/<name>.md` is officially documented (Cursor 1.6+) and confirmed. The user-global `~/.cursor/commands/` dir has no reachable official doc (the commands page 404s / redirects to Skills); the Cursor forum reports it as an open feature-request, not a built-in. Per plan 2.3 ("keep the global mirror unchanged") and the contract's negative-only-evidence caution, the global write is RETAINED (harmless if unread; removing a possibly-live path on negative evidence could break delivery) and recorded as the DF-1 residual for a future direct-confirmation cycle.

Source docs read: [Hooks | Cursor Docs](https://cursor.com/docs/hooks), [Slash commands | Cursor Docs](https://cursor.com/docs/cli/reference/slash-commands), [Rules | Cursor Docs](https://cursor.com/docs/rules), [Cursor community: personal custom slash commands](https://forum.cursor.com/t/personal-custom-slash-commands/133386).

## Stale test reconciled (in-scope)

`tests/integrations/test_cross_platform_flatten.py::test_cursor_has_no_skills_surface` was a v3.12.0 Phase-4 regression guard asserting Cursor has NO skills surface. `a56cdffa` added the skills surface without updating it, so it had been failing on the branch tip. Because Phase 2 IS Cursor skills parity, this is a Phase-2-owned reconciliation (not out-of-scope cleanup): promoted Cursor into the `_FLATTENED` parametrized sweep as a first-class flattened platform and replaced the inverted guard with `test_cursor_flattens_skills_and_keeps_rules` (skills + `.mdc` rules coexist). The module docstring was updated to match.

## Files changed

- `scripts/lib/integrations/cursor.py` - `wire_project_surfaces` now seeds project `.cursor/commands/`; extracted `_write_project_rules_stub`; docstring updated with the DF-1(a) UNVERIFIED note.
- `tests/integrations/test_cursor.py` - added `test_cursor_wire_project_surfaces_seeds_rules_and_commands` (9 tests total).
- `tests/integrations/test_cross_platform_flatten.py` - added Cursor to `_FLATTENED`; replaced `test_cursor_has_no_skills_surface` with `test_cursor_flattens_skills_and_keeps_rules`; updated the docstring.
- `docs/policy/platform-read-contracts.json` - `parity_verification_v3_15_0.cursor`: hooks `phase2_resolved` note; commands `phase2_note` (global UNVERIFIED, project confirmed, init seeding).
- `docs/policy/platform-read-contracts.md` - added the 2026-07-21 Phase 2 Cursor DF-1 resolution entry to the Re-verification log; updated the "Last verified" line.
- `docs/v3/v3.15/known-gaps.md` - Phase 2 status paragraph; DF-1 re-scoped to the global-commands residual (DF-1(b) resolved).
- `CHANGELOG.md` - Phase 2 entry under `[Unreleased]`.
- `docs/DEVLOG.md` - Phase 2 entry.

## Test results

- `tests/integrations/test_cursor.py`: 9 passed.
- `tests/integrations/test_cross_platform_flatten.py`: 13 passed (Cursor now in the flatten sweep).
- Full `tests/integrations`: green (the pre-fix run's sole failure was the stale flatten test, now reconciled).
- Real-repo guards: `verify_platform_contracts.py` OK (8 platforms), `check_platform_contract_freshness.py` OK (v3.14.7, last_verified 2026-07-20), `validate_unicode_safety.py` 0 errors on the touched docs.
- `ruff check` on the changed Python files: clean.
- Workspace + `nexus-hub init` dry-runs: every Cursor surface lands at the verified paths; the shared `~/.agents/skills` is correctly NOT written.

## Deviations

- **`ruff format` not applied** (same rationale as Phase 1): `ruff check` passes; the repo does not enforce `ruff format`, so reformatting would be out-of-scope churn.
- **CHANGELOG entry folded into `[Unreleased]`** rather than a new version section: v3.15.0 is not yet released; the version section is cut at `/update release` (Phase 7).

## Deferred / carried forward

- **DF-1(a) residual** - global `~/.cursor/commands/` UNVERIFIED; kept and tracked for a future direct-confirmation cycle.
- **Advisory (pre-existing)** - `test_init_subcommand.py::test_default_wire_project_surfaces_returns_none` still fails on the copilot override; owned by Phase 5.2. Cursor is already in that test's `overrides` set and still returns a `WriteResult`, so Phase 2 adds no new failure.

## Next

Phase 3 (OpenCode parity - agents folder; plugins/hooks decision per DF-4).
