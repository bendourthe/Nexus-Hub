# Session History - v3.15.2 adoption-awesome-llm-apps Phase 6 (final): refactor + known-gaps + CI/CD

**Date**: 2026-07-23
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.2-adoption-awesome-llm-apps.md](../../plans/v3.15.2-adoption-awesome-llm-apps.md)
**Phase**: 6 of 6 (FINAL) - Architecture Refactor, Known-Gaps Reconciliation, and CI/CD
**Branch**: `feat/adoption-awesome-llm-apps` (cut off `develop`)
**Status**: IMPLEMENTATION COMPLETE and verified; RELEASE HELD (parallel re-stamp + version flux + unpushed branch)

## Goal

Leave the project well-organized, its known gaps reconciled, and its CI/CD complete and optimized (including promoting the trigger-and-routing eval to a hard gate), then run release readiness and hand off to `/update release`.

## Model-routing pre-flight

Phase 6's recommendation is the strong reasoning tier at high effort (Opus 4.8) - a repo-wide refactor + reference repair. The session was on Opus 4.8; proceeded without a switch.

## Sub-tasks completed

### 6.1 - Architecture refactor

- Audited this plan's cumulative diff (2584 insertions across 33 files). All additions are cohesive and correctly placed; no empty dirs, duplicates, orphans, or overcomplicated structure - a no-op cleanup.
- Confirmed each new artifact is referenced from the right place: `run_trigger_evals*` + `skill_eval_convert.py` in both installers + Makefile + CI; `hermes` registered in `_register_builtins()`; per-skill `evals/trigger-cases.json` consumed by the runner; tests under the right trees.
- DEFERRED docs-tree refactoring (`docs-layout-refactor`): a parallel task is actively re-stamping `docs/v3/v3.16` -> `docs/v3/v3.15`; running it now would collide (a release-hold condition).

### 6.2 - Known-gaps reconciliation

- Reconciled the v3.15.2 section of the shared per-minor `docs/v3/v3.15/known-gaps.md` (left the released v3.15.0 / v3.15.1 sections untouched).
- DF-1 (CI per-path-filter granularity) RESOLVED by the Phase 6.3 CI-review decision. DF-2 (Hermes registry-only), WN-1 (Windows dry-run), MT-1 (trigger-cases WARN-path coverage) carried as documented non-blocking deferrals; MT-1 explicitly flagged for the next release.

### 6.3 - CI/CD + gate promotion

- Promoted the trigger-and-routing eval from warning-only to a HARD `--gate` in `.github/workflows/ci.yml` and the Makefile `validate` step (catalog clean: 0 un-allowlisted collisions, 0 routing failures; 261/267 skills stay on the never-failing WARN path).
- Confirmed CI covers every plan change with no new/redundant job (A1/A2 `--gate` step; A3 inside `validate_skills.py --bundles-only`; A4 `pytest tests/validators`; A5 `pytest tests/integrations`); existing concurrency + caching + gated-matrix optimizations intact.

### 6.4 - Verification

- `--gate` PASS; `validate_skills --bundles-only` PASS; `verify_platform_contracts` OK (10); `check_version_sync` / `check_base_template_parity` / `check_platform_contract_freshness` green.
- `tests/validators` 302 passed; `tests/integrations` 398 passed (Phase 5 run; no Python changed in Phase 6).
- All 3 new scripts + allowlist referenced in both installers; `hermes` registered; shellcheck clean; Hermes runner dry-run lands `.hermes/skills/`.

## Release readiness (9.0 / 9A / 9B done; 9C-9E HELD)

- 9.0 mandatory refactor + known-gaps + CI/CD gate: done (6.1-6.3).
- 9A known gaps: reconciled (6.2). 9B tests + CI: green / covered (6.4).
- 9C-9E (`/update release`): NOT run. Hold conditions:
  1. **Active parallel re-stamp** - a concurrent task is renumbering `docs/v3/v3.16/*` into the `docs/v3/v3.15/` line (v3.15.3-.6 plans + comparisons appearing); uncommitted and NOT part of this plan. A release must not run over a half-reorganized docs tree.
  2. **In-flux version numbering** - whether v3.15.2 is still the correct release stamp for this plan needs user confirmation (comparison-versioning-flaw pattern).
  3. **Unpushed branch** - `feat/adoption-awesome-llm-apps` (Phases 1-6) is local-only.
- No version bump, changelog finalize, develop->main merge, tag, or GitHub Release performed.

## Deviations

- **Docs-layout-refactor deferred** (6.1) - the active parallel docs re-stamp makes it unsafe to run now.
- **Release step intentionally not run** - per the runbook, an active re-stamp + unresolved version choice + unpushed branch are hold conditions; Phase 6 completed the refactor / gaps / CI-gate work and stopped before the `/update release` hand-off.
- **Surgical commit** - the Phase 6 commit (like Phases 1-5) stages only this plan's files, excluding the parallel re-stamp.

## Files touched (Phase 6)

- Modified: `.github/workflows/ci.yml` (eval -> `--gate`), `Makefile` (validate step -> `--gate`), `docs/v3/v3.15/known-gaps.md` (Phase 6 reconciliation + release hold), `docs/DEVLOG.md`.
- Added: this session-history file.

## Next steps (user-owned)

1. Reconcile the parallel v3.16->v3.15 re-stamp in the working tree and confirm the release version stamp for this plan.
2. Push `feat/adoption-awesome-llm-apps` (Phases 1-6, 6 commits local-only).
3. Run `/update release` for whichever version this plan ships as (platform-contract re-verify + freshness re-stamp, version bump via `check_version_sync`, changelog finalize, develop->main merge, tag, GitHub Release).
4. Follow-ons: DF-2 (promote Hermes to default-installer-wired), MT-1 (further trigger-cases tranches + `lexical: false` command-dispatcher cases).
