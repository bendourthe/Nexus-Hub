# Session History - v3.15.0 Phase 7 (final): architecture refactor, known-gaps reconciliation, CI/CD

**Date**: 2026-07-21
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.0-platform-parity-all-gaps.md](../../plans/v3.15.0-platform-parity-all-gaps.md)
**Phase**: 7 of 7 (final) - architecture refactor + known-gaps reconciliation + CI/CD
**Branch**: `feat/platform-parity-all-gaps` (cut off `develop`)
**Status**: Complete; v3.15.0 RELEASE-READY. Hands off to `/update release`.

## Overview

The terminal phase: the mandatory refactor + known-gaps + CI/CD gate before the release handoff. The load-bearing work was correcting the AGENTS.md platform-coverage prose that the six preceding phases made stale - Cursor, OpenCode, Qwen, and Kimi are no longer the "behavioral-guardrails only" / commands-only platforms the doc described. No file refactor was needed (the release introduced no cleanup targets), and CI already covers every change.

## Sub-tasks completed

- **7.1 Architecture refactor + doc correction.** Refactor detectors: the v3.15.0 cumulative diff (33 files) introduced no empty dirs / duplicates / orphans, so nothing to clean (pre-existing empty `catalog/skills/` dirs are out of scope). AGENTS.md corrections:
    - **Cursor** rewritten to full-surface parity (skills + subagents + `hooks.json` + project commands, beyond the global commands + rules it already had).
    - **OpenCode + Qwen Code + Kimi Code CLI** moved out of "Behavioral-guardrails only" into a new "Skills-bearing integrations (v3.15.0 platform parity)" bullet; guardrails-only is now correctly Aider / Windsurf / OpenClaw.
    - Distribution-channels **skills row** (Cursor/Qwen/Kimi flattened; Copilot opt-in selector) and **commands row** (Cursor project commands, Qwen Markdown commands, Kimi `/skill:<name>`) updated; slash-surface line gained Cursor's project `.cursor/commands/`.
    - The stale Kimi `.kimi/system.md` + `agent.yaml` / `~/.kimi/` framing replaced with `~/.kimi-code/`.
    - CLAUDE.md carries no platform-coverage classification (verified), so no change.
- **7.2 Known-gaps reconciliation.** `known-gaps.md` marked ALL 7 phases COMPLETE / RELEASE-READY; deliverable gaps resolved (DF-2/3/4/5); a Release-readiness residuals block records the non-blocking open items (DF-1(a), WN-1, DF-5 residual, OpenCode `rules_subdir` note).
- **7.3 CI/CD.** Confirmed `.github/workflows/ci.yml` already covers the release: `pytest tests/integrations tests/installer` (all new integration/verify tests + the WN-1 bootstrap test), `pytest tests/validators`, `check_platform_contract_freshness.py`, and `verify_platform_contracts` (10 platforms). Already optimized (concurrency, path filters, caching); no workflow edit needed.
- **7.4 Validation + handoff.** Full guard sweep green; hands to `/update release`.

## Validation

- `verify_platform_contracts.py`: OK (10 platforms).
- `check_platform_contract_freshness.py`: OK (v3.14.7; bumps to 3.15.0 at release).
- `check_version_sync.py`: consistent at 3.14.7.
- `check_base_template_parity.py`: 5/5 present.
- `validate_unicode_safety.py`: 0 errors (warnings only; the AGENTS.md table em-dashes are pre-existing).
- `validate_no_personal_paths.py`: exit 0.
- Tests: unchanged from Phase 6 (Phase 7 touched only docs) - `tests/integrations` 386 passed; `tests/installer` 120 passed / 15 skipped / 1 environmental WN-1.

## Release-readiness residuals (open, non-blocking)

- **DF-1(a)** - Cursor global `~/.cursor/commands/` UNVERIFIED (kept; the confirmed project path is load-bearing).
- **WN-1** - `test_bootstrap.py` fails in this Windows Git-Bash dev env (environmental `tar`); confirm green in CI at release.
- **DF-5 residual** - Copilot custom-agents + Preview hooks surfaces not yet populated (future release).
- **OpenCode `rules_subdir`** - pre-existing "no rules folder" vs the integration writing rules; future rules-surface review.

## Files changed

- `AGENTS.md` - platform-coverage prose correction (Cursor parity; skills-bearing OpenCode/Qwen/Kimi; distribution-channels skills + commands rows; slash-surface line; Kimi `~/.kimi-code/`).
- `docs/v3/v3.15/known-gaps.md` - all-7-phases-complete status; Phase 7 paragraph; Release-readiness residuals block.
- `docs/DEVLOG.md` - Phase 7 (final) entry.
- `CHANGELOG.md` - Phase 7 clause in the `[Unreleased]` intro (no production bullet - docs/CI only).

## Deviations

- **No file refactor** - the release introduced no cleanup targets; the phase's refactor gate was a no-op on files, with the real work in doc correction. Documented so the empty refactor diff is not mistaken for a skipped step.
- **No CI workflow edit** - CI already covers the release (the new tests live in dirs CI already runs); confirmed rather than changed.
- **`ruff format` not applied** (same rationale as Phases 1-6; no Python changed this phase anyway).

## Next

Hand off to `/update release` (user-triggered): 3.14.7 -> 3.15.0 version bump across all surfaces, CHANGELOG finalize (cut the `[Unreleased]` block to `[3.15.0]`), `develop` -> `main` merge, tag `v3.15.0`, push, GitHub Release - all behind `/update release`'s own confirmation gates. Governance step 4 there re-verifies the platform contract and re-stamps the freshness marker to 3.15.0.
