# Session History - v3.15.1 adoption-codesight Phase 7: terminal refactor + known-gaps + matrix + CI/CD

**Date**: 2026-07-22
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.1-adoption-codesight.md](../../plans/v3.15.1-adoption-codesight.md)
**Phase**: 7 of 7 (FINAL) - Architecture Refactor, Known-Gaps Reconciliation, and CI/CD
**Branch**: `feat/adoption-codesight` (cut off `develop`)
**Status**: IMPLEMENTATION COMPLETE; RELEASE HELD (QG-2 version collision + branch not pushed)

## Goal

Leave the project well-organized, its known gaps reconciled, the reverse-engineering matrix updated, and its CI/CD complete and optimized (including the context-map GitHub Action recipe), then hand off to release readiness.

## Pre-flight note (branch state)

The shared working tree had been switched to `feat/usage-monitor-warning-layout` (a parallel task) between the Phase 6 commit and this phase. Verified all six phase commits were intact on `feat/adoption-codesight` (nothing lost); surfaced the situation and the release blocker to the user, who chose to switch back to `feat/adoption-codesight` and run Phase 7 with the release step held. No destructive git operation was performed.

## Sub-tasks completed

### 7.1 - Architecture refactor

- Audited the extension additions: no empty dirs, duplicates, orphans, or overcomplicated structure. The feature landed cohesively under `contextmap/` (16 modules, clear boundaries). No files moved.
- Bumped `extensions/nexus-code-search/pyproject.toml` `2.0.0 -> 2.1.0` and refreshed the description (resolves DF-2; the extension's own version, not a `check_version_sync` catalog surface).

### 7.2 - Known-gaps + reverse-engineering matrix

- Reconciled `docs/v3/v3.15/known-gaps.md`: DF-1 / DF-2 / QG-1 -> Resolved; DF-3 finalized as explicit deferred detector coverage; WN-1 / WN-2 / MT-1 / MT-2 carried as documented non-blocking deferrals; QG-2 marked the active release hold.
- Updated the `nexus-code-search` row in `docs/policy/mcp-reverse-engineering-matrix.md` to record the context-map / extraction capability + reverse-engineering provenance (generic names in the shipped artifact; attribution only in the matrix per the Attribution Rule; still `already-local`).

### 7.3 - CI/CD + context-map Action recipe

- Confirmed CI covers the full surface (extension suite incl. benchmark / lint / knowledge in the path-filtered `code-search.yml` + the always-on `ci.yml` tests job).
- Resolved QG-1 by decision (retain both workflows: path-filtered fast signal + always-on cross-cutting gate).
- Added a documented, copy-pasteable context-map GitHub Action recipe (H-ci) to the extension README.

### 7.4 - Validation + dry-run install

- Extension suite: 294 passed, 1 skipped. `check_version_sync` exit 0 (catalog version untouched). no-personal-paths + unicode-safety exit 0. `make` unavailable on this Windows host -> CI is the authoritative repo-level gate.
- Dry-run install / HO-1: zero new catalog skills shipped -> no flat/nested skill-name collision possible; the extension `src/` auto-copies recursively with no installer edit; CI's `install-smoke` job is the authoritative installer gate.

## Release readiness (9A / 9B done; 9C-9E HELD)

- 9A known gaps: reconciled (above). 9B tests + CI: green / covered.
- 9C-9E (`/update release`): NOT run. Hold conditions: (1) QG-2 - three plans stamped v3.15.0, only one can ship; (2) the branch is not pushed. No version bump of the catalog, no changelog finalize, no `develop -> main` merge, no tag, no GitHub Release. `/update release` will run only after the user reconciles QG-2.

## Deviations

- **Branch switch out from under the session** (documented in the pre-flight note) - handled by verifying commit safety and getting user confirmation before switching back; not a code deviation.
- **Release step intentionally not run** - per the runbook, an unresolved version choice (QG-2) is a hold condition; Phase 7 completed the refactor / gaps / matrix / CI work and stopped before the release handoff.

## Next steps (owned by the user + `/update release`)

1. Reconcile QG-2: decide which of the three v3.15.0-stamped plans is the real v3.15.0 and re-stamp the others (comparison-versioning-flaw pattern).
2. Push `feat/adoption-codesight` (currently local-only, 7 commits).
3. Run `/update release` for whichever version this plan ships as (catalog version bump via `check_version_sync`, changelog finalize, `develop -> main` merge, tag, GitHub Release).
4. Optional cleanup (non-blocking): WN-1 / WN-2 pre-existing ruff findings; the deferred DF-3 detectors (additional frameworks / ORMs / component libs / event patterns) in a follow-on.
