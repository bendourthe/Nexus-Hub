# Session History - v3.15.3 adoption-no-ai-slop Phase 1: author the anti-slop-editing skill and bundle

**Date**: 2026-07-24
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.3-adoption-no-ai-slop.md](../../plans/v3.15.3-adoption-no-ai-slop.md)
**Phase**: 1 of 3 - Author the anti-slop-editing skill and bundle
**Branch**: `develop` (feature-branch decision deferred to the commit prompt)
**Status**: IMPLEMENTATION COMPLETE; not yet committed (awaiting the 8.10 commit-and-push decision)

## Goal

Author a conformant, triggerable `anti-slop-editing` skill (a 20+ named-pattern prose de-slop editor with a dual Edit/Detect mode) plus its reference bundle and routing evals, passing every bundle-level and routing gate. This is a pure `skill-native` addition (MCP Registry Policy tier 2): no new code, outbound call, dependency, or credential.

## Model-routing pre-flight

Phase 1's recorded recommendation is the strong reasoning tier at high effort (Opus 4.8). The session was already on Opus 4.8, so no switch was made; proceeded on the plan's recommendation.

## Final-phase detection

`is_final_phase = false`. Phase 1 is not the numerically highest phase (Phase 3 is the terminal "Architecture Refactor, Known-Gaps Reconciliation, and CI/CD" phase), its title is an authoring task, and there is no `Final-Phase:` marker. The standard 10-step post-phase sequence ran; the release-readiness workflow did not.

## Sub-tasks completed

### 1.1 - SKILL.md

- `catalog/skills/developer-experience/anti-slop-editing/SKILL.md`. Quoted single-line `description` (241 chars, under the 250 ceiling), `summary_l0` (13 words), `overview_l1` (~120 words).
- Body sections in the required order: title + intro; When to Use (with When-NOT-to-use routing generic writing to `writing-editing`, from-scratch docs to `technical-writer`/`user-documentation`, and UI slop to `hallmark-design`); a Two-Modes table (Edit default, Detect); Instructions; the named-pattern catalog; Voice-Preservation Discipline; Self-Check Loop; Common Rationalizations; a binary Verification checklist; Related Skills.
- 17-entry named-pattern catalog, each with a quoted smell and a concrete before/after fix: binary contrasts, throat-clearing openers, faux-insight setups, colon reveals, superficial analysis (trailing -ing clauses), importance puffery, weasel attribution, fake-strong verbs, synonym cycling, negative listing, dramatic fragmentation, robotic rhythm, rhetorical setups, fake-profound kickers, summary-recap endings, formatting slop, and em-dash discipline.
- Em-dash guidance reconciled to the project ceiling: no em-dashes AND no clause-joining spaced hyphens (stronger than the source's "one or two are fine"), ASCII-only punctuation. Stated in the body.
- Detect mode's hard rule encoded: no rewrite, no numeric AI-probability score, no AI-authorship verdict (named, checkable evidence only).
- No external repository / product / author / course named anywhere (reverse-engineering attribution rule); provenance is recorded only in the Phase 2 matrix row.

### 1.2 - references/slop-wordlist.md

- Banned words (delve, foster, leverage, utilize, facilitate, empower, streamline, robust, cutting-edge, and more), often-empty adverbs (just, literally, honestly, simply, actually, ...), and often-empty phrases (it's worth noting, at the end of the day, when it comes to, ...).
- Framed as judgment guidance, not a hard lint, with explicit keep-it / cut-it examples so legitimate voice is not flattened. Cross-linked to the project Communication Style rules. ASCII-only.
- Referenced from SKILL.md by basename, so the orphan-bundle audit passes.

### 1.3 - references/self-check.md and the self-check loop

- A pass/fail content-quality rubric: shared checks (point preserved, voice preserved, proportional cutting, ASCII punctuation) plus mode-specific checks (Edit: patterns removed, empty words removed, What-changed list present; Detect: each finding named + quoted + short-fixed, no rewrite/score/authorship verdict).
- SKILL.md's Workflow wires the loop: after editing, grade against the rubric, fix any failing check, re-grade until all pass, all in the single agent (no separate evaluator). The body distinguishes this runtime self-check from the artifact-level Verification section and the routing-level `evals/trigger-cases.json`.
- Referenced from SKILL.md by basename (orphan-bundle audit passes).

### 1.4 - evals/trigger-cases.json

- v3.15.2 schema (lowercase keys: `skill`, `purpose`, `cases`). 4 positives (`de-slop this and keep the writer's voice`, `make this read less like AI slop`, `detect the AI slop patterns here`, `remove the robotic filler and AI-slop from this`) each asserting `anti-slop-editing` routes first, and 4 near-miss negatives drawn from the SKIP clause (README, structure/technical doc, proofread/grammar, blog-post flow) each asserting `writing-editing`/`technical-writer`/`user-documentation`, not here.
- Lexically co-engineered with the description: every positive's surviving tokens exist in the description (so each scores ~1.0 and strictly outranks the look-alikes), and negatives avoid the description's vocabulary (so the strongest near-miss stays near zero and the default 1.15x margin clears). This directly addresses the comparison's trigger-overlap conflict note.

### 1.5 - Testing and stabilization

- `python scripts/validate_skills.py --path catalog/skills/developer-experience/anti-slop-editing --verbose`: PASS (0 errors, 0 warnings). Confirms quoted `description` parses under strict YAML, the unfilled-placeholder lint is clean, `name` matches the directory, and both reference files are linked (no orphans). All optional frontmatter fields present (no optional-field warnings).
- `python scripts/validate_skills.py --bundles-only` (whole tree, the `make validate` command): PASS (268 skills, 0 errors, 1 warning). The one warning is a pre-existing `demo-capture` `.pyc` orphan, not this skill (logged as a v3.15.3 Advisory).
- `python scripts/run_trigger_evals.py --gate --verbose`: PASS (0 un-allowlisted collisions, 0 routing failures across 7 skills with cases). The new skill's 4 positives route first and the near-miss margin clears; its description collides with no sibling.
- CI/CD: `.github/workflows/ci.yml` already covers this skill's validation with no edit - the bundle flows through the existing `validate_skills.py --bundles-only`, `run_trigger_evals.py --gate`, and `scan_skill_security.py` steps under the `validate` job, which already has the requested optimizations (path filter via `paths-ignore: docs/**`, `concurrency` cancel-in-progress, pip caching). Confirmed no-op; the final CI create/update/optimize pass is Phase 3.3.

## Quality gate (GO / NO-GO)

- Tests: no code changed (prose skill); the validation surface is the trigger-cases + the two validators, all green. GO.
- Coverage: not applicable (no code). GO.
- Lint / strict-YAML / placeholder / orphan-bundle: all PASS. GO.
- Build: JSON parses (`trigger-cases.json` valid; the routing runner loads it). GO.

## Deviations

- **NI-1 - description carries 3 of the 5 plan-listed trigger phrases verbatim**: the 250-char single-line `description` cap fits the three highest-routing-signal phrases verbatim ("de-slop this", "make it less AI-sounding", "does this read as AI"); the other two ("remove AI patterns", "audit this draft for slop") are covered by `overview_l1` and the When-to-Use section. Routing is proven by the passing `--gate`, so discoverability is not degraded. Logged as v3.15.3 NI-1 (non-blocking).

## Files touched

- Added: `catalog/skills/developer-experience/anti-slop-editing/SKILL.md`, `.../references/slop-wordlist.md`, `.../references/self-check.md`, `.../evals/trigger-cases.json`.
- Modified: `docs/v3/v3.15/known-gaps.md` (v3.15.3 section + header), `docs/DEVLOG.md` (Phase 1 entry), `docs/archive/v3/v3.15/development/history/2026-07-24_v3.15.3-adoption-no-ai-slop-phase-1-author-skill-and-bundle.md` (this file).

## Next steps

1. Resolve the 8.10 commit-and-push decision (including whether to move to a `feat/adoption-no-ai-slop` branch, matching the v3.15.x pattern, or commit on `develop`).
2. Phase 2: register the skill by hand in the three `data/` files, add the reverse-engineering-matrix row (records the MIT external source; not named in the skill body), and add the CHANGELOG `[Unreleased]` entry (268 skills).
3. Phase 3: terminal architecture-refactor + known-gaps reconciliation + CI/CD pass, then `/update release`.
