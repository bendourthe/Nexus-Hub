# Session History - v3.15.4 presentify-visual-fidelity Phase 4: reliable stock/mix imagery integration

**Date**: 2026-07-24
**Plan**: [docs/releases/v3/v3.15/plans/v3.15.4-presentify-visual-fidelity.md](../../plans/v3.15.4-presentify-visual-fidelity.md)
**Phase**: 4 of 7 - Reliable stock/mix imagery integration
**Branch**: `feat/presentify-visual-fidelity`
**Status**: IMPLEMENTATION COMPLETE; not yet committed (awaiting the 8.10 commit-and-push decision)

## Goal

Make a stock/mix imagery choice reliably put relevant, license-verified images into image-starved sections (consent folded into the opening round, image-starved-section detection, keyless Openverse/Wikimedia fetch with Pexels when configured, and an integration gate) so "mix" never silently produces zero images.

## Model-routing pre-flight

Phase 4's recorded recommendation is the strong reasoning tier at medium-high effort (Claude Opus class). The session was already on Opus 4.8; proceeded on the plan's recommendation.

## Final-phase detection

`is_final_phase = false` (Phase 7 is the terminal gate). Standard post-phase sequence ran; no release-readiness. Prerequisites: none (independent; graded by Phase 5). Phases 1-3 committed on this branch.

## Sub-tasks completed

### 4.1 - Fold build-time-network consent into the opening round

- `catalog/commands/presentify.md` and SKILL.md Step 2: the build-time-network / generation CONSENT for `stock` / `mix` / `auto` / `ai` is now captured as part of the up-front batched design round, not a separate mid-run prompt. A consented run is EXPECTED to attempt integration (silence is a bug). Invariants preserved: default `procedural` and non-interactive runs stay offline; `fetch_stock_media.py` performs no network without `--consent`; a recalled preference never pre-answers consent.

### 4.2 - Image-starved-section detection and per-section fetch

- SKILL.md Step 6 (concise) + `references/interactive-features.md` Tier 2 (full): after the structure is drafted, identify each section with NO source visual that would be materially clearer with one; derive SHORT topic keywords (never the raw source text, keeping the compiling-content trap closed); fetch/license-verify/base64-embed ONE relevant asset per starved section (Openverse-first, then Wikimedia, Pexels when configured), placed per the Phase 2 sizing rules with relevance-and-restraint.

### 4.3 - Integration verification gate

- SKILL.md Verification item + a Tier 2 rubric criterion (feeding Phase 5): a consented `stock` / `mix` / `ai` run MUST integrate at least one relevant asset into each image-starved section OR record a per-section reason; a consented run integrating ZERO assets with no reason FAILS. Offline + license-safety gates unchanged.

### 4.4 - Verify the Pexels setup path (VERIFIED, no change)

- Confirmed `nexus-hub setup-media` already exists and stores the Pexels key at `~/.nexus-hub/config/media.env` at mode 0600: wired in `scripts/nexus_hub_cli.py` (`cmd_setup_media` -> `scripts/setup_media_keys.py`), listed in `--help`, present in both installers, tested by `tests/skills/test_media_key_setup.py`. The first-time-video note in the command/skill already points at it. No installer edit, no "Ask first" trigger.

### 4.5 - Testing and stabilization

- `tests/skills/test_presentify_stock_fetch.py` (14 cases), network monkeypatched (no real request, no `requests` dependency): the consent gate (no `--consent` = no network + degrade code 3, with a boom-if-called guard), a consented Openverse run integrating + crediting a base64 asset (offline), the free-for-commercial-use allow-list rejecting NC/ND (parametrized `is_commercial_cc` + `accept_candidate`), and degrade-always-carries-a-reason.
- CI: no edit (the fetcher is under the bundle path filter and the new test under `tests/skills/**`, both wired in Phase 2; the setup-media 0600 assertion is posix-guarded).

## Quality gate (GO / NO-GO)

- Tests: full `tests/skills` 55 pass / 3 skip (the 3 skips are the headless-render checks). GO.
- Coverage: the fetcher's enforcement layer (consent, license, degrade-reason) is unit-tested; no production code changed (the fetcher is complete). GO.
- Lint: ruff clean on the new test. GO.
- Build / validate: orphan audit 0 errors, unicode 0 errors, SKILL.md within the size norm (217 lines). GO.

## Deviations

- None. `fetch_stock_media.py` was unchanged (complete); `nexus-hub setup-media` was verified present (no installer edit). This phase is command + skill + reference prose plus tests.

## Known gaps recorded (docs/v3/v3.15/known-gaps.md, v3.15.4 section)

- **MT-2 (new, open)**: the agent-side per-section detection + asset placement is LLM-native, not deterministically unit-testable. The fetcher enforcement (consent/license/degrade-reason) is unit-tested; the end-to-end integration is graded by the Phase 5 visual-QA loop. Non-blocking.
- DF-1, DF-2, DF-3, BG-1 carry from prior phases, untouched.

## Files touched

- Modified: `catalog/commands/presentify.md` (4.1), `catalog/skills/specialized-domains/document-to-interactive-html/SKILL.md` (4.1 Step 2 + 4.2 Step 6 + 4.3 Verification), `catalog/skills/specialized-domains/document-to-interactive-html/references/interactive-features.md` (4.2 detection + 4.3 integration gate).
- Added: `tests/skills/test_presentify_stock_fetch.py` (4.5).
- NOT modified: `scripts/fetch_stock_media.py` (complete since v3.13/v3.14), `nexus-hub setup-media` / `scripts/setup_media_keys.py` (verified present).
- Docs: `docs/v3/v3.15/known-gaps.md` (Phase 4 entry + MT-2 + summary), `docs/DEVLOG.md`, `CHANGELOG.md` (`[Unreleased]`), and this session-history file.

## Next steps

1. Resolve the 8.10 commit-and-push decision (feature branch, commit only, per the Phase 1-3 pattern).
2. Phase 5: the iterative multi-agent visual-QA self-critique loop (render -> per-segment grade vs source + the Phase 1-4 rubric -> adversarially verify -> synthesize fixes -> re-render), which grades the imagery-integration criterion (MT-2) end-to-end.
3. Phases 6-7: command/skill/registry polish and the terminal refactor + known-gaps + CI/CD gate.
