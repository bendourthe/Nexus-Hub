# Session History - v3.14.4 Phase 4 (final): Refactor, Docs, and Release Readiness

**Date**: 2026-07-18
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.4-usage-monitor-split.md](../../plans/v3.14.4-usage-monitor-split.md)
**Phase**: 4 of 4 (FINAL) - Refactor, Known-Gaps Reconciliation, Docs, Version, and CI/CD
**Branch**: `feat/usage-monitor-split`
**Status**: COMPLETE - RELEASE-READY (hands off to `/update release`)

## Goal

Make the repository consistent end-to-end for the split, reconcile the v3.14 known gaps, sync the top-level docs, and confirm release readiness. Per the develop+main model and the implement-phase runbook, the version bump / tag / push are handed to `/update release`.

## Sub-tasks completed

### 4.1 - Repo-wide consistency and refactor pass

- Swept for stale "unified extension" references ("Claude & Codex", "unified multi-provider", "usageMonitor.provider", "switch provider"): all 7 matches are historical change-records (Phase 1-3 CHANGELOG/DEVLOG/known-gaps/session-histories) or v3.14.0 records (the codex-lb plan + its Phase-1 history), correctly frozen. No live artifact mis-describes the current state.
- No empty dirs / orphaned bundle files; both CI workflows and both dependabot entries present.

### 4.2 / 9A - Known-gaps reconciliation

- v3.14.4 status updated to Phases 1-4 COMPLETE / RELEASE-READY. Four carried, non-blocking items: DF-1 (icon reconstructed), DF-2 (Codex credential path unverified), MT-1 (UI unit-test-light), HO-1 (runtime side-by-side unverified here).
- No new TODO/FIXME/DEVIATION markers introduced this version.

### 4.3 - Documentation sync

- Root `README.md`: rewrote the "VS Code Extension" section to describe both separate extensions; added a v3.14.4 "What's New".
- `llms.txt`: added a `codex-usage-monitor` line.
- `SECURITY.md`: the "OAuth token access" section now documents both extensions' data flows (Claude -> `api.anthropic.com`, Codex -> `chatgpt.com/backend-api/wham/usage`).
- Both extension READMEs were already correct from Phases 1-2.

### 4.4 - Version (deferred to /update release)

- Per the implement-phase runbook, the version bump / changelog finalization / merge / tag / push are owned by `/update release`. The CHANGELOG stays under `[Unreleased]` and `check_version_sync` stays consistent at 3.14.3 until release.

### 9.0 / 9B - Mandatory gate + verification

- `validate_skills --bundles-only` PASS (267 skills, 0/0); `check_version_sync` consistent; `validate_workflow_security` + `check_base_template_parity` PASS.
- Both extension suites green (Claude compile + 3 tests; Codex compile + 34 tests); hooks/installer pytest 460 passed / 36 skipped.

## Release readiness (4.5)

RELEASE-READY. Hands off to `/update release` for: version bump 3.14.3 -> 3.14.4, CHANGELOG `[Unreleased]` -> `[3.14.4]`, `feat/usage-monitor-split` -> `develop` -> `main`, tag `v3.14.4`, push, GitHub Release - all under `/update release`'s own gates, driven by the user. No tag or push performed here.

**Suggested manual pre-release check (HO-1)**: install both VSIX in a live VS Code and confirm both status-bar items render (`$(claude-icon) Claude Usage: ...` and `$(codex-icon) Codex Usage: ...`).

## Next steps

- Run `/update release` to cut v3.14.4.
- Optionally swap the reconstructed Codex `icon.png` for the exact `codex-2048x2048.png` (DF-1).
