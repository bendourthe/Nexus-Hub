# Session History - v3.14.1 Phase 3: refactor, known-gaps reconciliation, and CI/CD (final phase)

**Date**: 2026-07-16
**Branch**: `fix/installer-hotfix` (off the released `develop` / v3.14.0)
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.1-installer-hotfix.md](../../plans/v3.14.1-installer-hotfix.md), Phase 3 of 3 (**final phase**)
**Scope**: `docs/v3/v3.14/known-gaps.md`, this session history, `docs/DEVLOG.md`. No code change (the two-file hotfix landed in Phases 1-2).

## Goal

The mandatory terminal-phase gate for a two-file hotfix: confirm a clean layout, reconcile the v3.14 known gaps, confirm CI covers the new tests and is optimized, run full validation, and hand off to `/update release` for the v3.14.1 bump / merge / tag / push / GitHub Release.

## Sub-tasks completed

1. **3.1 - Architecture refactor (near-no-op).** The branch changed exactly the expected files: `scripts/lib/integrations/{runner.py,legacy.py}`, `tests/integrations/{test_runner_target_root.py,test_legacy_cleanups.py}`, `CHANGELOG.md`, the plan, and two session histories. No stray, deprecated, redundant, or misplaced files; no empty dirs; no orphaned bundle files; consistent session-history naming (`v3.14.1-phase-1`, `-phase-2`). No `project-refactor` / `docs-layout-refactor` action needed. The untracked forward-planning docs (`v3.14.2-comparison-versioning-fix.md`, `v3.14.3-presentify-upfront-questions.md`, a `v3.15/` tree, `v3.14.0-comparison-awesome-llm-apps.md`) are separate forward planning, not hotfix artifacts, and were deliberately left untouched.
2. **3.2 - Known-gaps reconciliation.** The v3.14.1 subsection records both resolved defects (BG resolved 2). Added the residual-latent-installer advisory: both installers still pass `--target` only for workspace scope, which is now correct-by-construction because the runner resolves global scope to the user home centrally (Phase 1), so the platform installers need no change and no `base-*.md` lockstep edit. Open Items: none.
3. **3.3 - CI/CD (confirmed, no change).** The existing `tests` job in `.github/workflows/ci.yml` already runs `pytest tests/integrations tests/installer` on every non-docs change; workflow-level `concurrency: cancel-in-progress` is set; the `tests` job caches pip. The branch changed only `.py` + `.md` files (no shell scripts), so the `shellcheck` job is unaffected. A dedicated path-filtered integration job would only duplicate the run and raise action minutes, so none was added.
4. **3.4 - Full validation.** See below.

## Validation results (make unavailable on this Windows host; ran the commands directly)

- ruff: clean on both changed source files (Phases 1-2); no code changed in Phase 3.
- Full `tests/integrations/` suite: **328 passed, 0 failed** on the Phase 2 commit (`1b36508`), whose code is byte-identical to the release candidate (Phase 3 changed only docs). Re-confirmed as the release gate.
- Both defects' Fixed entries are present under `CHANGELOG.md` `## [Unreleased]` (manifest path, graceful degradation, orphaned auth-monitor).
- `check_version_sync.py`: consistent at 3.14.0 (the 3.14.0 -> 3.14.1 bump is `/update release`'s job).

## Deviations

- None. The terminal-phase gate (refactor + known-gaps + CI/CD) ran and was a near-no-op appropriate to a two-file hotfix, as the plan predicted.

## Release-readiness handoff

Hands off to `/update release` for v3.14.1: merge `fix/installer-hotfix` -> `develop`, bump 3.14.0 -> 3.14.1 across all surfaces (`check_version_sync.py`-guarded), finalize `CHANGELOG.md` `## [Unreleased]` -> `## [3.14.1]`, regenerate `MANIFEST.sha256`, commit, merge `develop` -> `main`, tag `v3.14.1`, push, and publish the GitHub Release (with the manual `Unregister-ScheduledTask` remediation in the notes). Every outward-facing step keeps its confirmation gate; nothing is tagged or pushed automatically.
