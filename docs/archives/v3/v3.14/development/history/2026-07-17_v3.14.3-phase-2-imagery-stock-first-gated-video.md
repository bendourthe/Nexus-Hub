# Session History - v3.14.3 Phase 2: imagery stock-first priority + gated stock video (video-scope reconciled)

**Date**: 2026-07-17
**Branch**: `feat/presentify-upfront-questions` (off `develop`)
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.3-presentify-upfront-questions.md](../../plans/v3.14.3-presentify-upfront-questions.md), Phase 2 of 4
**Scope**: `catalog/skills/specialized-domains/document-to-interactive-html/SKILL.md` (Step 2 imagery choice, Step 6 authoring bullet, "When NOT to use" video bullet), `references/interactive-features.md` (stock-first priority rule + new "Stock video (Pexels-only, gated)" material), `references/extraction-runbook.md` (out-of-scope line), `catalog/commands/presentify.md` (`--images` bullet + merged design section), plus CHANGELOG + known-gaps + DEVLOG + this history. INSTRUCTION-ONLY: `fetch_stock_media.py` was read and confirmed but NOT modified; no code, installer, `base-*.md`, or `data/` registry change.

## Context

The imagery question needed a content upgrade per the user's request: when the stock tier is chosen, prioritize real, license-free, commercial-use stock media and minimize AI-generated images, while still offering local AI. Stock VIDEO was inconsistent across the docs - the command already advertised stock "images / video" and `fetch_stock_media.py` already had a Pexels video path, but the skill's "When NOT to use" and the extraction runbook flatly declared video embedding out of scope.

## Goal

The imagery choice prefers real license-free stock over AI (AI last resort), still offers local AI, and offers gated stock video (embedded only with a Pexels key + explicit consent, degrading to images-only otherwise); the "video out of scope" wording distinguishes source-embedded media (ignored) from license-free stock video in the output (supported, gated). No code change.

## Sub-tasks completed

1. **2.3-prep - Read `fetch_stock_media.py`, confirm the gate.** The consent gate is enforced by construction (`main()` returns `degrade()` with exit 3 before any network import when `--consent` is absent). For video: `run_source` raises for openverse / wikimedia ("use --source pexels for video") and requires `PEXELS_API_KEY` from the environment for pexels; a missing key / consent / result degrades to an empty manifest (Tier 1), never raising, never hotlinking (assets are base64 `data:` URIs, and `is_safe_https` rejects non-https). The code and the intended gate agree - so Phase 2 is wording-only.
2. **2.1 - Stock-first / minimize-AI priority.** Added the priority rule to the reference's imagery-tiers intro (prefer real license-free stock derived from the content; Tier 3 local AI is the last resort, only when relevant stock cannot be found or the user asks for AI; a change to the priority ORDER among the non-default tiers, not to the Tier 1 default). Applied the same rule in SKILL.md Step 2 (the imagery choice), Step 6 (the authoring bullet), and the command's `--images` bullet + merged design section; the "mix" / `auto` option now reads procedural base + real stock accents first, local AI only where stock cannot serve.
3. **2.2 - Reconcile "video out of scope".** Rewrote the SKILL.md "When NOT to use" video bullet and the extraction-runbook "Out of scope" line to distinguish (1) media EMBEDDED IN A SOURCE document (still out of scope - the extractor never carries it into the output, per the offline / size guarantee) from (2) license-free stock VIDEO in the OUTPUT (supported through the opt-in, consent-gated stock tier, Pexels-only, base64-embedded). The two statements are cross-referenced and mutually consistent, with the size caution stated.
4. **2.3 - Describe the video gate accurately.** Added a "Stock video (Pexels-only, gated)" paragraph to the reference's Tier 2 material and the precise gate to the command's `--images` bullet + merged section: video is offered under stock / mix; the fetch runs only with `--consent`; video specifically requires `--source pexels` + `PEXELS_API_KEY` (Openverse / Wikimedia have no license-clean keyless video path); missing key / consent / result degrades to images-only or Tier 1 with a one-line note; never blocks, never hotlinks; base64-embedded within the media budget, reserved for a few high-value placements. Confirmed the description matches the code; did NOT modify the script.

## Validation results (make unavailable on this Windows host; ran the scripts directly)

- `python scripts/validate_skills.py --bundles-only`: **PASS, 0 errors, 0 warnings**.
- `grep -rInE "video.*out of scope|out of scope.*video"` over the skill dir: the ONLY surviving match is `fetch_stock_media.py:475` (`"Wikimedia video is out of scope; use --source pexels for video"`) - a per-source routing message that directs to Pexels, NOT a blanket ban, and it lives in the script (correctly not modified). No doc carries an unqualified "video out of scope" ban.
- `git diff --name-only HEAD`: exactly four Markdown files (SKILL.md, presentify.md, interactive-features.md, extraction-runbook.md) - **no Python changed**.
- Non-ASCII scan of the four edited files: **0 non-ASCII lines** each.

## Decisions and deviations

- **No script change (instruction-only).** The plan authorized editing only wording; `fetch_stock_media.py` already enforces the Pexels-key + `--consent` gate and degrades to Tier 1 on any missing key / consent / result, so the code and the intended gate did not diverge - nothing to fix in Python.
- **"mix" / `auto` reconciliation.** The interactive menu's 4th option is "A mix" while the `--images` flag value is `auto`; SKILL.md Step 2 now labels it "mix (the `auto` flag)" so both the menu label and the flag value stay discoverable.
- **The `fetch_stock_media.py:475` string is correct as-is.** It is a Wikimedia-source routing message pointing at Pexels for video, consistent with the reconciled "output video is supported via Pexels" wording; not a blanket out-of-scope ban.

## Next steps

- **Phase 3**: bring-your-own-key media setup - a config-file key-resolution fallback in `fetch_stock_media.py` (`_resolve_pexels_key`), a guided `scripts/setup_media_keys.py` helper (no-echo capture, 0600, upsert), a `nexus-hub setup-media` subcommand wired into BOTH installers in parity, skill-side first-time guidance, and a pytest module with secret-hygiene assertions. This is a code-bearing, installer-aware phase (the second scoped installer change the plan authorizes).
