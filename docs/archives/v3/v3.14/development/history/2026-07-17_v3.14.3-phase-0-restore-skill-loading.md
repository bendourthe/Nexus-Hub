# Session History - v3.14.3 Phase 0: restore skill loading (fix invalid YAML frontmatter + Claude flatten)

**Date**: 2026-07-17
**Branch**: `feat/presentify-upfront-questions` (off `develop`)
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.3-presentify-upfront-questions.md](../../plans/v3.14.3-presentify-upfront-questions.md), Phase 0 of 4 (BLOCKING prerequisite for Phase 1)
**Scope**: 47 `catalog/skills/**/SKILL.md` (quote the `description:` scalar), `scripts/validate_skills.py` (strict-YAML gate), `scripts/installer.sh` + `scripts/installer.ps1` (Claude skill flatten), removed 3 stray `scripts/__pycache__/` dirs, plus `CHANGELOG.md` + known-gaps + DEVLOG + this history + the plan file. No `data/` registry edit (no parse-truncation found), no `base-*.md` change.

## Context

A real v3.14.0 install made `/presentify` fail at delegation with "Unknown skill: document-to-interactive-html". The plan diagnosed two independent root causes; Phase 0 fixes both so the reorder Phases 1-4 introduce is actually observable. The user (in this session) chose to do Phase 0 first, then Phase 1, and authorized the scoped installer change.

## Goal

`document-to-interactive-html` (and every other skill) parses under a strict YAML parser and lands in Claude's discoverable one-level skills space after a normal install; the defect cannot regress.

## Sub-tasks completed

1. **0.1 / 0.2 - Quote the `description` frontmatter across 47 skills.** A catalog-wide strict-parse scan confirmed exactly 47 of 267 skills failed `yaml.safe_load` (an unquoted `description:` scalar containing a `: ` sequence, usually from a `SKIP:` clause). A self-verifying fix script wrapped each value in double quotes and asserted a byte-identical round-trip before writing. Re-scan: 0 failures / 267. Diff: exactly 47 files, 1 line each (47 insertions / 47 deletions), staged diff confirms no line-ending churn (`*.md text=auto` stores LF).
2. **0.3 - Strict-YAML validator gate.** Added `validate_frontmatter_strict_yaml` to `scripts/validate_skills.py` (PyYAML-guarded, with an unquoted-`: `-scalar heuristic fallback), wired as a hard error in both the full validator and `--bundles-only` (the CI mode), leaving `--quality`'s always-exit-0 contract intact. Verified: fires on a broken fixture (both modes, exit 1); catalog stays green (0 new "not valid YAML" errors).
3. **0.4 - Registry re-verify + `__pycache__` removal.** The mandatory 0.4 check found NO parse-truncation: the registry generator uses a tolerant parser and stored full descriptions, so the syntax-only quoting requires no registry edit (parsed values byte-identical). Pre-existing `data/skills.json` content drift (17 of 47 fixed skills store older/shorter descriptions) logged as DF-1 (same root as v3.14.2 WN-1 / Advisory), not fixed (out of scope; full rebuild forbidden). Removed the 3 gitignored `scripts/__pycache__/` dirs; bundle audit now 0/0.
4. **0.5 - Flatten Claude skills in BOTH installers.** Chose Mechanism B (surgical flatten) over Mechanism A (drop `--instruction-only`) after a read-only recon showed A disturbs the non-skill mirror (it would dump the whole `catalog/hooks` tree, including tests / `__pycache__` / a settings.json template, over the curated guardrails/settings install, and lose install-time stale-skill pruning). Added `flatten_skills_into` (bash) / `Flatten-SkillsInto` (PowerShell): stage a flattened copy in a temp dir, hand it to the existing `safe_folder_copy` / `Safe-Folder-Copy` (reusing refresh-prune / merge), plus an explicit category-dir cleanup for merge mode. Applied at all four sites (global + workspace, both installers). Kept `--instruction-only` and the commands/agents/rules/mcp-configs/context/memory copies + Claude helpers unchanged.
5. **0.6 - Testing + docs.** Ran the full stability gate (all green, below), wrote the two CHANGELOG `### Fixed` entries, updated known-gaps, wrote this history.

## Validation results (make unavailable on this Windows host; ran the underlying scripts directly)

- Strict-parse scan (Stability Gate command): **0 `ScannerError` / 267 skills**.
- `python scripts/validate_skills.py --bundles-only` (the CI command): **PASS, 0 errors, 0 warnings**.
- Strict-YAML gate on a deliberately-broken fixture: **FAIL (exit 1)** in both the full validator and `--bundles-only`; **PASS** once quoted.
- Full validator on the catalog: 165 pre-existing errors (the 250-char single-line `description` rule etc., grandfathered / WN-1), **0 of them "not valid YAML"** - my gate adds nothing to the fixed catalog.
- ShellCheck on `installer.sh`: **GREEN**; `installer.ps1` AST parse: **0 errors**.
- Installer flatten unit tests (real functions extracted verbatim, both shells): refresh mode prunes stale flat + category dirs; merge mode preserves user-added skills and still removes the category dir; target skill at exactly one level; bundled `scripts/` preserved; **275-dir set byte-identical to the Python `flatten_skills` adapter** (parity confirmed).

## Decisions and deviations

- **DEVIATION (plan assumption corrected)**: the plan said the target skill's `description` had "no embedded double-quotes" and a length of 1685. It actually contains several (trigger phrases like `"presentify this"`) - 26 of the 47 needed `"`->`\"` escaping - and the real plain-scalar length is 1672. The invariant that matters (byte-identical round-trip) is verified directly, so this is a benign staleness in the plan, not a fix change.
- **Mechanism B over A for the installer flatten** (justified above): lower-risk, behavior-preserving, and the plan's own tie-breaker ("prefer A unless it disturbs the non-skill mirror") points to B because A disturbs it. Parity risk of two hand-written helpers is mitigated by both delegating to the shared, tested copy helper and by the verbatim-extraction unit tests.
- **No `data/` registry edit** (0.4): no parse-truncation, syntax-only change, parsed values byte-identical. The pre-existing drift is DF-1, deferred.
- **Plugin manifest not modified** (DF-2): `.claude-plugin/plugin.json` declares a category-nested `"skills"` path; whether PLUGIN discovery reads it recursively is unverified here, so it is flagged (not changed) per the plan's "do not over-engineer" guidance.

## Next steps

- **Phase 1**: hoist and batch the four `/presentify` design questions up front (before extraction), forbid memory/saved-preference pre-answering, preserve the non-interactive fallback. Target files already mapped: the command's three "Choosing..." sections + delegation arrow, the skill's Step 4 (split menus from the token brainstorm), and `references/interactive-features.md` lines 59/96.
- Phase 0 is the BLOCKING prerequisite and is now complete; the reorder in Phases 1-4 will be observable.
