# Session History - v3.14.4 Phase 3: Distribution and Coexistence Wiring

**Date**: 2026-07-18
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.4-usage-monitor-split.md](../../plans/v3.14.4-usage-monitor-split.md)
**Phase**: 3 of 4 - Distribution and Coexistence Wiring
**Branch**: `feat/usage-monitor-split`
**Status**: COMPLETE (non-final phase)

## Goal

Both installers build + package + install both extensions; dependabot and CI cover both; the installer smoke test asserts both; static coexistence is confirmed.

## Sub-tasks completed

### 3.1 - Generalize the bash installer

- Refactored `install_vscode_extensions` (in `scripts/installer.sh`) to extract a shared `build_and_install_one_extension` helper (clean / npm install / compile / vsce package / uninstall-then-install per extension). The Node.js + npm check and the VS Code CLI detection run once and are shared; the function calls the helper for `nexus-hub.claude-usage-monitor` and `nexus-hub.codex-usage-monitor`. Each extension is independent (one failure does not block the other). Banner is now "> Usage Monitors".

### 3.2 - Mirror in the PowerShell installer

- Same structure via a `Build-And-Install-One-Extension` function; the `$ErrorActionPreference = "Continue"` suspend/restore wraps both builds once; the `$vsceOutput` capture pattern is preserved.

### 3.3 - Dependabot + smoke test

- Added an npm dependabot entry for `/extensions/codex-usage-monitor`.
- Added `test_installers_build_both_usage_monitor_extensions` (both extension dirs, both ids, both display names, in both installers).

### 3.4 - Coexistence verification

- Both extensions build + package to distinct VSIX; the Phase-2 static scan confirmed zero shared identifiers; runtime side-by-side recorded as HO-1 (needs a live editor).

## Test / build results

- `bash -n scripts/installer.sh`: OK.
- ShellCheck `scripts/installer.sh`: exit 0 (only pre-existing SC2012/SC2015 info notes).
- `installer.ps1`: parses clean (`[Parser]::ParseFile`, 0 errors). PSUseApprovedVerbs warnings are the repo's descriptive-verb convention (non-blocking).
- Installer smoke tests: 29 passed.
- Both VSIX present: `claude-usage-monitor-0.8.0.vsix`, `codex-usage-monitor-0.1.0.vsix`.

## Deviations

None.

## Known gaps

- **HO-1**: runtime side-by-side install (both status-bar items in a live VS Code) not verified here - needs an interactive editor. Static guarantees hold; a manual smoke check before release is sufficient.
- DF-1 / DF-2 / MT-1 carry from Phases 1-2.

## Next steps

- **Phase 4** (final): terminal architecture-refactor + known-gaps reconciliation + CI/CD, root `README.md` / `llms.txt` sync, repo version bump to 3.14.4, then hand off to `/update release`.
