# Session History - v3.14.3 Phase 4 (final): refactor, known-gaps reconciliation, and CI/CD

**Date**: 2026-07-17
**Branch**: `feat/presentify-upfront-questions` (off `develop`)
**Plan**: [docs/releases/v3/v3.14/plans/v3.14.3-presentify-upfront-questions.md](../../plans/v3.14.3-presentify-upfront-questions.md), Phase 4 of 4 (terminal gate + release readiness)
**Scope**: `.github/workflows/ci.yml` (new `tests/skills` step, 4.3), `docs/v3/v3.14/known-gaps.md` (final reconciliation, 4.2), plus DEVLOG and this history. An audit-and-CI phase - no catalog / skill / installer code changed here beyond the one CI step.

## Context

Phases 0-3 delivered the fix (restore skill loading), the reorder (four batched design questions up front + no memory pre-answer), the imagery upgrade (stock-first + gated stock video), and the bring-your-own-key media setup. Phase 4 is the terminal gate: confirm the diff is limited and parity holds, reconcile the v3.14 known gaps, complete CI, then hand off to `/update release`. The final-phase gate runs because this plan HAS an explicit Phase 4.

## Sub-tasks completed

1. **4.1 - Architecture refactor + diff-scope + parity audit.** The cumulative `develop..HEAD` diff is exactly 66 files, all traced: 47 SKILL.md (Phase 0 `description` quote) + 4 session histories + 15 code/doc files (`.gitignore`, `CHANGELOG.md`, `presentify.md`, the skill body + two references + `fetch_stock_media.py`, `DEVLOG.md`, `known-gaps.md`, the plan, both installers, `nexus_hub_cli.py`, `setup_media_keys.py`, `validate_skills.py`, `test_media_key_setup.py`). NO `data/` registry change (Phase 0.4 found no parse-truncation); NO `base-*.md` change (`check_base_template_parity.py` exit 0). Installer parity confirmed: the Phase 0 flatten helper (`flatten_skills_into` / `Flatten-SkillsInto`) and the Phase 3 `setup_media_keys.py` copy each appear symmetrically in both installers. Orphan-bundle audit PASS 0/0. No empty dirs or orphans were introduced by this version (the pre-existing empty catalog dirs are the DF-3 SKILL.md-less dirs, untracked by git, out of scope); no `project-refactor` / `docs-layout-refactor` action needed.
2. **4.3 - CI/CD.** Added a `pytest tests/skills -v` step to the CI `tests` job (after the validator-tests step), resolving QG-1 and also gating the pre-existing `tests/skills/test_audit_docs_version_topic.py`. The workflow's `paths-ignore: docs/**` means the step runs on `scripts/`, `tests/`, and `catalog/skills/` changes; `concurrency: cancel-in-progress` is already set. Confirmed the `validate` job already runs `python scripts/validate_skills.py --bundles-only`, which now carries the Phase 0.3 strict-YAML frontmatter gate (an unquoted `description` with a `: ` sequence fails CI). `ci.yml` re-parses clean; ShellCheck / lint jobs unaffected (only a pytest step was added). NOTE: my first draft of the step name contained `(v3.14.3: ...)` - the exact unquoted-`: ` YAML defect Phase 0 fixed - caught by the YAML parse check and corrected (dropped the colon).
3. **4.2 - Known-gaps reconciliation.** Recorded the release-readiness wrap-up: (0) the "Unknown skill" load failure is FULLY resolved (both root causes); (1) the presentify late-and-partial + memory-pre-answer defects are resolved; (2) stock-video setup is a guided one-paste flow. Flipped QG-1 to RESOLVED, added DF-6 (headless aspect uses `deck_like` only after extraction - by design), and set the version status to RELEASE-READY. Open items are all by-design deferrals (DF-1..DF-6) plus one low-priority coverage nice-to-have (MT-1); none is a release-blocker.
4. **4.4 - Testing + stabilization + release readiness (9A / 9B).** Full validation green (below). 9A: no new `TODO`/`FIXME`/`# DEVIATION:` code markers in the diff (deviations are documented in prose in the session histories / known-gaps / DEVLOG, not as code markers). 9B: the substantive logic is tested (22 `tests/skills` tests - key resolution, video degrade, upsert, masking, sanity, 0600, secret hygiene) and CI is wired; the thin `setup-media` dispatch is smoke-verified (`nexus-hub setup-media --help` dispatches to the helper).

## Validation results (make unavailable on this Windows host; ran the scripts directly)

- `python -m pytest tests/skills -q`: **22 passed** (9 media-key + 13 docs-audit).
- `python scripts/validate_skills.py --bundles-only`: **PASS 0/0** (Phase 0.3 strict-YAML gate active).
- Strict-parse scan of all 267 SKILL.md frontmatters: **0 failures**.
- `shellcheck -S warning scripts/installer.sh`: GREEN; `python scripts/check_base_template_parity.py`: exit 0; `ci.yml` YAML parses.
- `tests/installer/test_upgrade_cli.py`: 24/24 (from Phase 3 - no CLI regression).
- CHANGELOG `[Unreleased]` carries all entry types: `### Added` (Phase 3), `### Changed` (Phases 1 + 2), `### Fixed` (Phase 0 x2).
- Cumulative diff: 66 files, all traced; no `data/` or `base-*.md` drift.

## /presentify end-to-end dry-run walkthrough (documented read-through)

- **Four questions up front**: the FIRST user-facing step is the single batched round asking style / aspect / interactivity / imagery, before any document is read (skill Step 2, command's merged "Choosing the design" section). PASS.
- **Flags bind and drop**: `--style` / `--theme` / `--layout` / `--interactivity` / `--images` each bind and remove their question; a fully-specified run asks nothing. PASS.
- **Headless fallback never blocks**: content-aware auto-picks (surprise-me / full-width-or-standard / balanced-or-restrained / procedural Tier 1), recorded with notes. PASS.
- **No memory pre-answer**: a recalled `presentation-style-preference` is ignored for the four choices; the batch is still asked. PASS.
- **Imagery stock-first + gated video**: the choice prefers real license-free stock, minimizes AI, and offers gated Pexels-only stock video (key + consent, degrade to images-only otherwise). PASS.
- **Stock-video first-time guidance**: with no key, the skill points the user at `nexus-hub setup-media` in the terminal (never the chat) and proceeds without blocking. PASS.

## Decisions and deviations

- **CI: one step in the existing `tests` job, not a new path-filtered job.** The workflow already scopes to non-docs changes; a per-step addition matches the established structure (e.g. the `tests/validators` step) and avoids duplicating a job. This satisfies the plan's "path filter" intent via the workflow-level `paths-ignore`.
- **Pre-existing failures left unfixed (documented in the Advisory).** The two `tests/installer` failures (copilot `wire_project_surfaces` test drift; a Windows MSYS-tar bootstrap env issue) were verified in Phase 3 to fail on the baseline; they are unrelated to presentify and out of scope. The copilot one is a one-line test fix for a separate patch.
- **No `data/` registry or `base-*.md` change, all four phases** - the frontmatter was byte-stable after Phase 0's syntax-only quoting, and no instruction-template text changed.

## Release readiness / hand-off

RELEASE-READY. Hands off to `/update release` for the v3.14.3 bump, CHANGELOG `[Unreleased]` -> `[3.14.3]`, `feat/presentify-upfront-questions` -> `develop` -> `main` merge, tag `v3.14.3`, push, and GitHub Release - all under `/update release`'s own confirmation gates, and NOT performed here (no tag or push created automatically).

**HOLD CONDITION (sequencing)**: per the plan, v3.14.3 must be numbered and released only AFTER v3.14.0 (codex-lb, committed-not-pushed), v3.14.1 (installer hotfix), and v3.14.2 (comparison-versioning-fix) land, and the release step must confirm the `check_version_sync.py` guard is green against the then-current baseline. v3.14.1 and v3.14.2 are already merged to `main`; v3.14.0 (codex-lb) is held by the user. So the build is complete and release-ready, but the actual `/update release` run should wait until the version ordering is settled.
