# Known Gaps - v2.4.0

This file tracks per-version unfinished work, deferred items, deviations from plan, and bugs discovered during phase implementation. The next phase plan and the version-bump checklist read this file to decide what carries forward.

**Plan**: [docs/archives/v2/v2.4/plans/adoption-compound-engineering-plugin.md](plans/adoption-compound-engineering-plugin.md)
**Status**: complete (Phases 1-8 of 8 closed; Phase 8 - live verification + release readiness; v2.4.0 release-ready). All 13 A-items shipped; all 15 ingested v2.3.0 gaps resolved (11) or dated-deferred (4). Catalog green: make validate exit 0; 1056 tests passed / 4 skipped / 0 failed; code-search eval recall 100% / precision 100%; zero new outbound verified.
**Last updated**: 2026-06-02 (Phase 8 close / v2.4.0 release readiness)
**Transferred**: 2026-06-02 -- all open items (NI-v24-1, DF-v24-7, WN-v24-2, DF-v24-8, DF-v24-9, DF-v24-10, WN-v24-3) ingested into the v3.0.0 plan [command-consolidation-skill-security](../v3.0.0/plans/command-consolidation-skill-security.md) and moved to the Resolved table below with `Resolved in: transferred to v3.0.0 plan`. They are not yet fixed -- only re-homed to the v3.0.0 tracking surface; resolution happens when the corresponding v3.0.0 sub-tasks (Phases 6, 9, 10) are implemented. DF-v24-8 still subsumes DF-v24-1/2/3/4/6 and carries DF-v23-7; DF-v24-9 carries DF-v23-8; DF-v24-10 subsumes DF-v24-5 and carries DF-v23-6; WN-v24-3 carries WN-v23-5.

## Summary

| Category | Open | Resolved this version |
|---|---|---|
| NI -- Not implemented (skipped subtask) | 0 | 0 |
| DF -- Deferred (intentionally) | 0 | 5 |
| BG -- Bug or unresolved test failure | 0 | 2 |
| MT -- Missing tests / coverage gap | 0 | 0 |
| WN -- Warning or suppressed lint rule | 0 | 5 |
| QG -- Quality gate bypassed | 0 | 1 |
| **Total (all open items transferred to v3.0.0)** | **0** | **13** |

_Phase 8 (final / release readiness) closed the 4 remaining ingested v2.3.0 gaps as dated re-deferrals (no model CLI / no `agy` binary / Windows-only host - all acceptable for a source release): DF-v24-8 (carries DF-v23-7, subsumes DF-v24-1/2/3/4/6 live skill-eval-loop runs), DF-v24-9 (carries DF-v23-8 eval-harness live run), DF-v24-10 (carries DF-v23-6 cross-OS smoke, subsumes DF-v24-5 live --branch), WN-v24-3 (carries WN-v23-5 Antigravity CLI live probe). T040 ran the full catalog-green gate: all suites pass, registries reconciled at 245/21, zero new outbound. WN-v24-1 (doc-count prose) is addressed at the version bump (Phase 9D/9E)._

_Phase 1 resolved 2 of the 15 ingested v2.3.0 gaps (WN-v23-1 count drift, BG-v23-1 secret-scan false positives). The remaining 13 ingested gaps are tracked as sub-tasks in the plan (Phases 2-8) and are not duplicated here until discovered/closed during their phase. Phase 1 added 3 new gaps; Phase 2 added 1 (DF-v24-2) and extended WN-v24-1 with the agent-count delta. Phase 4 added 1 (DF-v24-3) and extended WN-v24-1 to the 242-skill truth. Phase 3 (run after Phase 4, since Phase 4's prerequisite was "None beyond Phase 1") added 1 (DF-v24-4) and extended WN-v24-1 to the 244-skill truth (added `product-strategy` + `session-query`). Phase 5 added 1 (DF-v24-5, live --branch clone+install deferred); it ships docs-only + installer changes, no new skills, so the registries are unchanged at 244. Phase 6 added 1 new skill (`demo-capture`, workflow), bringing the on-disk truth to **245 skills across 21 categories** (workflow 36); it also added the local release/changelog helper scripts (no new skill). Phase 6 added 2 gaps (DF-v24-6 live eval deferred for `demo-capture`; BG-v24-1 a pre-existing installer.ps1 stale-test failure discovered while running the full suite, unrelated to Phase 6 work) and extended WN-v24-1 to the 245-skill truth._

## Open Items

> **TRANSFERRED to the v3.0.0 plan on 2026-06-02.** All items in this section were ingested into [command-consolidation-skill-security](../v3.0.0/plans/command-consolidation-skill-security.md) (Phases 6, 9, 10) and re-homed to the Resolved table below. Open count is now 0. The detailed entries are retained here for their reasons and next-steps (also restated in the v3.0.0 plan task prompts). Do not treat these as open against v2.4.0.

### NI-v24-1 -- validate_solution_frontmatter.ps1 PowerShell sibling intentionally not created

- **Source phase**: Phase 1 - Foundation (sub-task T003)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 1.3 / T003)
- **Category**: NI -- Not implemented (deliberate convention-based decision)
- **Reason**: T003's prompt asked for a `scripts/validate_solution_frontmatter.ps1` sibling under the "cross-platform parity rule". That rule (AGENTS.md "Per-skill Bundled Resources" + "Installer-Aware Changes") scopes mandatory `.ps1` siblings to `.sh` scripts (bash is not guaranteed on Windows). A `.py` validator is already cross-platform - it runs via `python` on every OS and is copied to `~/.nexus-hub/scripts/` by BOTH installers. All four existing top-level validators (`validate_no_personal_paths.py`, `validate_unicode_safety.py`, `scan_supply_chain_iocs.py`, `validate_workflow_security.py`) are `.py`-only with no `.ps1` sibling. A hand-maintained PowerShell reimplementation of a stdlib YAML-parser-safety checker would be pure duplication with a real sync/correctness risk and would diverge from the established convention.
- **Suggested next step**: No action required. If a maintainer disagrees, the `.py` logic can be ported to `.ps1`; but the recommendation is to keep the single cross-platform `.py` validator. Confirm at version-bump review.

### DF-v24-1 -- Live skill-eval-loop trigger run deferred for the two new Phase-1 skills

- **Source phase**: Phase 1 - Foundation (sub-task T007)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 1.7 / T007)
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T007 asks for a live `skill-eval-loop` trigger run (1.0 on a positive prompt, 0.0 on a fenced negative) for `solution-knowledge-base` and `solution-refresh`. The harness (`scripts/optimize_skill_description.py`) invokes a model CLI (`claude`/`codex`/`gemini`/`opencode`) as a subprocess; none is available on PATH in the implementation environment, and a live run is token-intensive. A static trigger-surface check was performed instead: both descriptions carry explicit verbatim trigger phrases plus a `SKIP` clause, and the two skills cross-fence each other (capture vs audit) to prevent over-trigger. This mirrors the v2.3.0 DF-v23-7 precedent (live run deferred, static check substituted).
- **Suggested next step**: Run the live `skill-eval-loop` for both skills when a model CLI is available - Phase 8 sub-task T037 already targets a Phase-1 knowledge-base skill for a live run; fold these two in there. Author a minimal `evals.json` per skill (one should-trigger, one should-not-trigger prompt) and confirm 1.0 positive / 0.0 negative; tighten the description per `skill-eval-loop/references/improvement-heuristics.md` if under-triggering.

### DF-v24-2 -- Live skill-eval-loop trigger run + full-pipeline benchmark deferred for the two new Phase-2 review skills

- **Source phase**: Phase 2 - Persona review pipeline (sub-task T012)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 2.5 / T012)
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T012 asks for a live `skill-eval-loop` trigger check (1.0 positive / 0.0 fenced-negative) for `multi-agent-code-review` and `plan-review`, and a review-pipeline benchmark. No model CLI (`claude`/`codex`/`gemini`/`opencode`) is on PATH in the implementation environment (same constraint as DF-v24-1). Two substitutes were performed and recorded: (1) a static trigger-surface check confirming both descriptions carry verbatim trigger phrases plus a SKIP clause and cross-fence each other (code-diff vs plan-doc), and (2) a real persona-dispatch benchmark over a seeded fixture - the correctness and security personas each surfaced their planted defect, stayed in lane, emitted the findings-schema JSON contract, and left the clean control untouched (see `docs/archive/v2/v2.4/development/phase-2-benchmark/README.md`). The full end-to-end pipeline run (all conditional personas, the independent validation pass, model tiering, cross-reviewer promotion) was not exercised, because the dedicated `*-reviewer` persona agents ship as catalog templates and are not registered as dispatchable subagent types in the authoring harness; the two reused dispatchable agents (`code-reviewer`, `security-reviewer`) validated the contract and lens discipline.
- **Suggested next step**: When a model CLI is available (fold into Phase 8 T037, which already targets a Phase-2 review skill for a live run), author a minimal `evals.json` per skill and confirm 1.0 positive / 0.0 negative; tighten descriptions per `skill-eval-loop/references/improvement-heuristics.md` if under-triggering. Optionally run the full pipeline with the dedicated persona agents once they are registered as dispatchable subagents in a consuming environment, and confirm cross-reviewer promotion on an overlapping seeded finding.

### DF-v24-3 -- Live skill-eval-loop trigger run deferred for the new Phase-4 product-pulse skill

- **Source phase**: Phase 4 - Remaining skill-native (sub-task T019)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 4.3 / T019)
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T019 asks for a live `skill-eval-loop` trigger check for `product-pulse`. No model CLI (`claude`/`codex`/`gemini`/`opencode`) is on PATH in the implementation environment (same constraint as DF-v24-1 / DF-v24-2). A static trigger-surface check was performed instead and recorded: the description carries 7/7 verbatim positive trigger phrases ("product pulse", "usage report", "how is the product doing", "monthly product report", "error trends", "performance over time", "product metrics summary"), an explicit `SKIP` clause fencing dashboards / real-time monitoring / external-analytics routing / one-off greps, and the zero-outbound assertion ("no outbound call and no new data processor"). All six required body sections are present, the frontmatter parses as YAML with all required keys, and the MCP skill-server suite (43 passed) consumes the new entry. The T017 persistence-discipline section was dry-run-verified separately (write -> verify-read -> done-marker -> resume-skips-completed).
- **Suggested next step**: Run the live `skill-eval-loop` for `product-pulse` when a model CLI is available - fold into Phase 8 T037 alongside DF-v24-1 / DF-v24-2. Author a minimal `evals.json` (one should-trigger usage-report prompt, one should-not-trigger dashboard prompt) and confirm 1.0 positive / 0.0 negative; tighten the description per `skill-eval-loop/references/improvement-heuristics.md` if under-triggering.

### DF-v24-4 -- Live skill-eval-loop trigger run deferred for the two new Phase-3 skills

- **Source phase**: Phase 3 - Close the compound loop (sub-task T016)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 3.4 / T016)
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T016 asks for a live `skill-eval-loop` trigger check for `product-strategy` and `session-query`. No model CLI (`claude`/`codex`/`gemini`/`opencode`) is on PATH in the implementation environment (same constraint as DF-v24-1 / DF-v24-2 / DF-v24-3). A static trigger-surface check was performed instead and recorded: `product-strategy` carries verbatim positive trigger phrases ("write a strategy", "what is our product strategy", "define the target problem", "who is this for", "what metrics matter", "set the product direction", "what are our bets", "what tracks are we working on") plus a `SKIP` clause fencing governance MUST/SHOULD (project-constitution), single-idea refinement (idea-refine), known-gaps logging, and single-feature specs; `session-query` carries verbatim positive trigger phrases ("did we look at this before", "what did we try last time", "find the session where we debugged X", "search my past sessions", "pull up prior context on this branch", "have we hit this error before") plus a `SKIP` clause fencing session-history generation, solution capture, known-gaps logging, and any external upload. Both `summary_l0` are <=15 words and both `overview_l1` are <=150 words; the MCP skill-server suite (43 passed) consumes both new entries. The session-query extractor was additionally proven via 13 pytest cases and an empirical PowerShell-parity smoke (topic / time-window / no-match all match the Python digest).
- **Suggested next step**: Run the live `skill-eval-loop` for `product-strategy` and `session-query` when a model CLI is available - fold into Phase 8 T037 alongside DF-v24-1 / DF-v24-2 / DF-v24-3. Author a minimal `evals.json` per skill (one should-trigger, one should-not-trigger prompt) and confirm 1.0 positive / 0.0 negative; tighten the description per `skill-eval-loop/references/improvement-heuristics.md` if under-triggering.

### DF-v24-5 -- Live --branch clone+install path verified only via the dry-run probe

- **Source phase**: Phase 5 - Internal RE builds re-full (sub-task T022)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 5.2 / T021, 5.3 / T022)
- **Category**: DF -- Deferred (intentionally; environment / scope constraint)
- **Reason**: T021 added the `--branch <name>` / `-Branch <name>` installer flag, which shallow-clones a pushed branch into `~/.nexus-hub/branches/<sanitized>/` and re-execs the install from that checkout. T022's acceptance was satisfied via the clone-free dry-run probe (`--branch <name> --check` / `-Branch <name> -Check`): the probe resolves the cache path, neutralizes path-traversal branch names (`../../etc` -> `---etc`), reads the clone source from `git remote.origin.url`, and exits 0 without cloning, on both bash and PowerShell (7 pytest probes in `tests/installer/test_branch_flag.py`). The actual end-to-end path -- shallow clone of a real pushed branch, `NEXUS_HUB_BRANCH_RESOLVED` re-exec into the cached installer, and a full install from the cached checkout -- was not run, because it requires a real pushed branch and a full (non-dry-run) install into a throwaway target, which is heavier than the phase's probe-level acceptance bar.
- **Suggested next step**: Once a test branch is pushed, run `bash scripts/installer.sh --branch <that-branch>` (and the PowerShell `-Branch` equivalent) into a throwaway global/workspace target and confirm: (1) the cache dir is created and populated from the branch, (2) the re-exec runs with `NEXUS_HUB_BRANCH_RESOLVED=1` and does not re-clone, (3) the install completes from the cached checkout, and (4) the user's working copy is untouched. Fold into the Phase-8 cross-OS smoke (T039) or a maintainer pre-release check.

### DF-v24-6 -- Live skill-eval-loop trigger run deferred for the new Phase-6 demo-capture skill

- **Source phase**: Phase 6 - Internal RE builds re-partial (sub-task T025)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 6.3 / T025)
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T025 asks for a live `skill-eval-loop` trigger check for `demo-capture`. No model CLI (`claude`/`codex`/`gemini`/`opencode`) is on PATH in the implementation environment (same constraint as DF-v24-1 through DF-v24-5). A static trigger-surface check was performed instead and recorded: the description carries verbatim positive trigger phrases ("record a demo", "capture a demo", "make a GIF of this", "screenshot the app for the PR", "show this working in the PR", "attach visual evidence", "demo reel", "record the terminal") plus an explicit `SKIP` clause fencing prompt-driven image generation (creative-generation / ui-component-generation), PR-description authoring (pr-description-writer), behavior verification (run / verify), and any external upload/host/share. All six required body sections are present, the frontmatter parses as YAML with all required keys, the orphan-bundle audit is clean (both scripts referenced), and the MCP skill-server suite (43 passed) consumes the new entry. The "degrades gracefully when a capture tool is missing" requirement was verified directly (not deferred): `capture-demo.py` and `capture-demo.ps1` both exit 0 and report the install hint when the browser/recorder binary is absent (covered by `tests/validators/test_capture_demo.py` plus a PowerShell parity smoke).
- **Suggested next step**: Run the live `skill-eval-loop` for `demo-capture` when a model CLI is available - fold into Phase 8 sub-task T037 alongside DF-v24-1 through DF-v24-4. Author a minimal `evals.json` (one should-trigger "record a demo for the PR" prompt, one should-not-trigger "generate a logo image" prompt) and confirm 1.0 positive / 0.0 negative; tighten the description per `skill-eval-loop/references/improvement-heuristics.md` if under-triggering.

### DF-v24-7 -- Remaining code-search language extractors + framework/parameter parity still deferred

- **Source phase**: Phase 7 - Catalog-quality + hygiene remediation (sub-task T033)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 7.8 / T033); carries forward the unfinished scope of v2.3.0 DF-v23-4
- **Category**: DF -- Deferred (intentionally; scope)
- **Reason**: T033's plan floor was "at least one new extractor (Ruby) end-to-end". This implementation shipped the full recommended demand batch -- Ruby, PHP, C, and C++ -- each with an eval fixture clearing the 80% recall gate (all four land at 100% recall / 100% precision) and unit tests, raising code-search language coverage from 6 to 10. The original DF-v23-4 noted ~14 languages remained; ~10 are still uncovered (Swift, Kotlin, Scala, Bash/shell, Lua, Perl, Objective-C, etc.). Framework extractors for the four new languages (e.g. Rails route resolution for Ruby, Laravel for PHP) and parameter-node parity were not in scope and remain deferred.
- **Suggested next step**: Implement the next demand batch (recommend Swift + Kotlin for mobile coverage) following the established per-language extractor pattern under `extensions/nexus-code-search/.../extraction/languages/`, each with an eval fixture clearing the 80% recall gate and unit tests, and the matching tree-sitter grammar dep under the shared `<0.26` ceiling. Pick up at the next version's `/generate-plan`.

### WN-v24-2 -- Some grandfathered skills carry both `## Quality Checklist` and `## Verification`

- **Source phase**: Phase 7 - Catalog-quality + hygiene remediation (sub-task T026)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 7.1 / T026)
- **Category**: WN -- Cosmetic content redundancy (validator-clean)
- **Reason**: The T026 full sweep drove the `--quality` warning count from 576 to 0 across all 245 skills by adding the missing canonical sections (`## Common Rationalizations`, `## Verification` binary checklist, `## Related Skills` with `[[cross-links]]`). The validator only recognizes the exact heading `## Verification`, so where a grandfathered skill already had a near-miss `## Quality Checklist` with binary items, some remediation passes RENAMED it to `## Verification` while others ADDED a distinct `## Verification` alongside the existing `## Quality Checklist`. The result is correct and validator-clean, but a subset of skills now carry both headings (cosmetic redundancy) rather than a single canonical Verification section. No functional impact.
- **Suggested next step**: Optional consistency pass: for skills carrying both `## Quality Checklist` and `## Verification`, merge the two into the single canonical `## Verification` checklist (or rename `## Quality Checklist` to a clearly-distinct purpose). Low priority; can be folded into a future `skill-stocktake` remediation batch.

### WN-v24-1 -- AGENTS.md catalog-count prose is stale after the registry reconciliation [RESOLVED at the v2.4.0 release, 2026-06-02; detail retained below for the count-history record]

- **STATUS: RESOLVED (v2.4.0 release / Phase 8-9 bump, 2026-06-02)**. Updated the count prose to the on-disk truth (245 skills across 21 categories, 41 commands, 22 hooks, 23 agents): `AGENTS.md` "Current catalog" line + the project-structure tree comments (agents 10->23, commands 33->41, skills 230->245 / 23->21 categories); `README.md` lines 7 / 23 / 115; `.claude-plugin/plugin.json` description (206+ -> 245) and version (2.3.0 -> 2.4.0); `data/marketplace.json` `plugin.description` (208+ -> 245) and version. The embedded SKILL INDEX needs no manual regeneration - it is installer-generated from `data/SKILL_INDEX.md` (already at 245) via the `{{SKILL_INDEX}}` placeholder. The seven v2.4.0 pushy descriptions were reconciled: all six previously-unallowlisted skills (`product-pulse`, `multi-agent-code-review`, `plan-review`, `demo-capture`, `product-strategy`, `session-query`) were added to `scripts/validate_skills.allowlist.json`, so `validate_skills.py --allow-existing` now passes with 0 errors (the two Phase-1 skills were already allowlisted). See the Resolved table.
- **Source phase**: Phase 1 - Foundation (sub-task T005)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 1.5 / T005)
- **Category**: WN -- Stale documentation count
- **Reason**: The T005 reconciliation revealed the registries had drifted far beyond the planned "1-skill" estimate: 6 conformant on-disk skills were unregistered in `data/skills.json` (`advanced-attack-patterns`, `business-logic-abuse`, `dev-progress-tracker`, `hallmark-design`, `html-output-conventions`, `implementation-plan`), 3 skills carried mis-cased category values, and `data/marketplace.json` omitted the `research` category. All three registries are now reconciled to the on-disk truth: **239 skills across 21 categories**. AGENTS.md prose still reads "230 skills across 23 categories" (the "23" was inflated by the 3 mis-cased duplicate category keys; the true directory count is 21). The Repository Overview line and the embedded SKILL INDEX in AGENTS.md / the platform instruction templates were not updated in Phase 1 (out of scope for T005, which is data-file-only).
- **Suggested next step**: At the v2.4.0 version bump (Phase 9 / `/update-version`), update the AGENTS.md "Current catalog" line and any platform-template count prose, and regenerate the embedded SKILL INDEX from `data/SKILL_INDEX.md`. As of Phase 4 close the on-disk truth is **242 skills across 21 categories** (Phase 2 added `multi-agent-code-review` + `plan-review`; Phase 4 added `product-pulse`) and the agent count is **23** (AGENTS.md prose still reads "230 skills across 23 categories" and "10 agents"; `README.md` line 113 still reads "208 skills"). The `data/SKILL_INDEX.md` Total line was corrected from a stale "239" to the true **242** during the Phase 4 registration (the 239 was a Phase-2 oversight - rows were added without updating the footer). As of Phase 3 close the on-disk truth is **244 skills across 21 categories** (Phase 3 added `product-strategy` + `session-query`, both workflow); all three registries (`data/skills.json` total + per-category, `data/marketplace.json` workflow `skill_count`, `data/SKILL_INDEX.md` rows + Total line) agree at 244 / workflow 35. AGENTS.md prose still reads "230 skills across 23 categories" and `README.md` still reads "208 skills". As of Phase 6 close the on-disk truth is **245 skills across 21 categories** (Phase 6 added `demo-capture`, workflow); all three registries agree at 245 / workflow 36. The `data/marketplace.json` `plugin.description` prose ("208+ curated skills", version "2.3.0") is likewise stale and rolls forward at bump time. Roll in all the v2.4.0 additions at bump time. Separately, the v2.4.0 new skills carry deliberately "pushy" descriptions (per the AGENTS.md combat-undertriggering guidance) that exceed the 250-char ceiling enforced only under `scripts/validate_skills.py --allow-existing`: `solution-knowledge-base` and `solution-refresh` (Phase 1) were added to `scripts/validate_skills.allowlist.json`, but `product-strategy`, `session-query` (Phase 3), `product-pulse`, `multi-agent-code-review`, `plan-review` (Phases 2/4), and `demo-capture` (Phase 6) were not. This is benign for the CI gate (`make validate` runs only `--bundles-only` and `--quality`, never the description-length check), but the `--allow-existing` reconciliation across the seven v2.4.0 pushy descriptions should be done once at the version bump (either allowlist all seven for consistency or accept the documented divergence).

### DF-v24-8 -- Live skill-eval-loop trigger runs re-deferred (carries DF-v23-7; subsumes DF-v24-1/2/3/4/6)

- **Source phase**: Phase 8 - Live verification + release readiness (sub-task T036)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 8.1 / T036); carries forward v2.3.0 DF-v23-7
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T036 asks for a live `skill-eval-loop` run (1.0 on a positive prompt, 0.0 on a fenced negative) for the three v2.3.0 discipline skills (`verification-before-completion`, `receiving-code-review`, `using-git-worktrees`). The harness (`scripts/optimize_skill_description.py`) shells out to a model CLI (`claude`/`codex`/`gemini`/`opencode`); none is on PATH in the implementation environment (verified 2026-06-02 - all four absent). This is the umbrella live-eval task that the plan designated to also fold in the v2.4.0 per-skill live-eval deferrals (DF-v24-1 `solution-knowledge-base`/`solution-refresh`, DF-v24-2 `multi-agent-code-review`/`plan-review`, DF-v24-3 `product-pulse`, DF-v24-4 `product-strategy`/`session-query`, DF-v24-6 `demo-capture`). Static trigger-surface checks were performed and recorded for every one of these skills (verbatim positive trigger phrases + explicit `SKIP` clauses, cross-fenced look-alikes, all six required body sections, YAML-parseable frontmatter, MCP skill-server suite consuming each new entry). Re-deferral is acceptable for a source release: the descriptions ship the combat-undertriggering surface the AGENTS.md guidance mandates, and the live run is a token-intensive confirmation, not a correctness gate.
- **Suggested next step**: When a model CLI is available, run the live `skill-eval-loop` across all of: `verification-before-completion`, `receiving-code-review`, `using-git-worktrees`, `solution-knowledge-base`, `solution-refresh`, `multi-agent-code-review`, `plan-review`, `product-strategy`, `session-query`, `product-pulse`, `demo-capture`. Author a minimal `evals.json` per skill (one should-trigger, one should-not-trigger prompt), confirm 1.0 positive / 0.0 negative, and tighten any under-triggering description per `skill-eval-loop/references/improvement-heuristics.md`. Pick up at the next version's `/generate-plan`.

### DF-v24-9 -- Live eval-harness trigger-techniques run re-deferred (carries DF-v23-8)

- **Source phase**: Phase 8 - Live verification + release readiness (sub-task T037)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 8.2 / T037); carries forward v2.3.0 DF-v23-8
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T037 asks for a live end-to-end run of the three v2.3.0 eval-harness trigger techniques added to `scripts/optimize_skill_description.py` - premature-action detection, multi-turn replay, and cheap-model fragility comparison - against a real model CLI. Same environment constraint as DF-v24-8 (no `claude`/`codex`/`gemini`/`opencode` on PATH, verified 2026-06-02). The three modes were validated at v2.3.0 via dry-run + pure-logic + fixture-stream tests (+23 pytest cases in `catalog/hooks/tests/test_eval_loop.py`); only the live model-CLI confirmation is outstanding. Re-deferred with a dated reason; acceptable for a source release.
- **Suggested next step**: When a model CLI is available, exercise each mode live against a representative skill (e.g. one of the Phase-2 review skills or a Phase-1 knowledge-base skill): confirm `premature_action` flips on a planted pre-`Skill` tool use, a multi-turn eval triggers at its `trigger_turn`, and a description's trigger rate is compared across the default and a cheaper `--model`. Fold into the same next-version live-eval pass as DF-v24-8.

### DF-v24-10 -- macOS + Linux installer smoke + live --branch path re-deferred (carries DF-v23-6; subsumes DF-v24-5)

- **Source phase**: Phase 8 - Live verification + release readiness (sub-task T039)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 8.4 / T039); carries forward v2.3.0 DF-v23-6, subsumes v2.4.0 DF-v24-5
- **Category**: DF -- Deferred (intentionally; environment constraint)
- **Reason**: T039 asks for the full macOS 14 installer smoke (sub-steps 1a-1l of `docs/archive/v2/v2.3/installer-smoke-post.txt`) and the Linux `installer.sh --print-config`/`--help` probes + `make eval`. The implementation host is Windows-only (MINGW64_NT, verified 2026-06-02); no macOS or Linux host is available and these are not in CI's scope. Windows is empirically green this version (full pytest suite 1056 passed / 4 skipped / 0 failed, eval recall 100% / precision 100%, installer probes pass) and the Linux Python suite is green via CI (`.github/workflows/ci.yml` on ubuntu-latest). This entry also subsumes DF-v24-5 (the live `--branch` clone+install path, verified at Phase 5 only via the clone-free dry-run probe) - the natural place to exercise a real branch clone+install is the cross-OS smoke. v2.4.0 is a source release, so this deferral is acceptable per the plan's own acceptance language.
- **Suggested next step**: Before any packaged-binary release, run sub-steps 1a-1l on a macOS 14 host and the installer-probe/eval portion on an Ubuntu host, recording results in `docs/<version>/installer-smoke.txt`. While on those hosts, also run the live `--branch <pushed-branch>` clone+install (bash + PowerShell) into a throwaway target and confirm the cache dir is populated, the re-exec carries `NEXUS_HUB_BRANCH_RESOLVED=1` without re-cloning, the install completes from the cache, and the working copy is untouched (closes DF-v24-5).

### WN-v24-3 -- Antigravity CLI live-VM probe re-deferred (carries WN-v23-5)

- **Source phase**: Phase 8 - Live verification + release readiness (sub-task T038)
- **Plan reference**: `docs/archive/v2/v2.4/plans/adoption-compound-engineering-plugin.md` (sub-task 8.3 / T038); carries forward v2.3.0 WN-v23-5
- **Category**: WN -- Docs-verified-but-not-live-verified residual
- **Reason**: T038 asks to reconcile the four Antigravity CLI residuals (the `.agent/` vs `.agents/` codelab dissent, the exact global subpath, the `subagents/`/`rules/` subdirs, and whether `agy` requires a root `AGENTS.md`) against a live `agy --help` / `agy init` probe. The `agy` binary is not installable on the implementation host (absent on PATH, verified 2026-06-02). The conventions remain verified against Google's public Antigravity CLI docs + official codelabs (recorded in `docs/archive/v2/v2.2/antigravity-cli-probe.md` Section 11); only the live-binary confirmation of the four residuals is outstanding. Re-deferred with a dated reason; acceptable for a source release.
- **Suggested next step**: Once `agy` is installed, run the live probe and reconcile the four residuals; if a root `AGENTS.md` is required, add a per-integration marker scheme so codex + antigravity2 can co-manage it. Update `docs/archive/v2/v2.2/antigravity-cli-probe.md` and the next version's known-gaps with the result.

## Resolved

| ID | Title | Category | Resolved in | Detail |
|---|---|---|---|---|
| NI-v24-1 | validate_solution_frontmatter.ps1 PowerShell sibling intentionally not created | NI | transferred to v3.0.0 plan | Carried into [command-consolidation-skill-security](../v3.0.0/plans/command-consolidation-skill-security.md) Phase 9, sub-task T042 (confirm/close the convention-based no-action decision; recommend keeping the single cross-platform .py validator). |
| DF-v24-7 | Remaining code-search language extractors + framework/parameter parity | DF | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 9, sub-task T040 (Swift + Kotlin extractor batch next, each clearing the 80% recall gate). |
| WN-v24-2 | Some grandfathered skills carry both `## Quality Checklist` and `## Verification` | WN | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 9, sub-task T041 (merge duplicate headings into the single canonical Verification checklist). |
| DF-v24-8 | Live skill-eval-loop trigger runs (subsumes DF-v24-1/2/3/4/6; carries DF-v23-7) | DF | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 10, sub-task T045 (run if a model CLI is available, else re-defer with a dated reason). |
| DF-v24-9 | Live eval-harness trigger-techniques run (carries DF-v23-8) | DF | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 10, sub-task T045. |
| DF-v24-10 | macOS + Linux installer smoke + live --branch (subsumes DF-v24-5; carries DF-v23-6) | DF | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 10, sub-task T046 (run if non-Windows hosts available, else re-defer). |
| WN-v24-3 | Antigravity CLI live-VM probe (carries WN-v23-5) | WN | transferred to v3.0.0 plan | Carried into command-consolidation-skill-security Phase 10, sub-task T046 (run if the agy binary is installable, else re-defer). |
| WN-v23-1 | `data/skills.json` skill entry count drifted from `statistics.total_skills` | WN | v2.4.0 Phase 1 (T005) | Root-caused as a three-way registry-vs-disk drift, not a simple statistic lag. Reconciled all three registries to the on-disk catalog: registered the 6 pre-existing unregistered skills + the 2 new Phase-1 skills (skills.json 231 -> 239), normalized 3 mis-cased category values to their lowercase directory ids, recomputed `statistics.total_skills` / `categories` / `priorities` / aggregate size stats, rebuilt `data/marketplace.json` per-category `skill_count` and added the missing `research` category, and appended the 6 missing `data/SKILL_INDEX.md` rows with a corrected Total line ("239 skills across 21 categories"). All three files now agree at 239; `make validate` passes; `validate_skills.py --allow-existing` is clean (0 errors). The 2 new skills were added to `scripts/validate_skills.allowlist.json` for consistency with the 137 already-grandfathered pushy descriptions. Residual doc-count staleness recorded as WN-v24-1. |
| BG-v23-1 | `scripts/validate_skills.py` reports 7 pre-existing "Generic secret assignment" false positives | BG | v2.4.0 Phase 1 (T006) | Made the secret scanner fenced-code-aware for Markdown: `scan_text_for_secrets` tracks fenced code blocks with CommonMark semantics (an opening fence may carry an info string; a closing fence must be the same char, at least as long, and carry no info string), so nested examples like a Markdown code block that itself shows a nested shell snippet no longer invert the fence state. Inside a Markdown fence the low-confidence "Generic secret assignment" pattern is suppressed (documentation examples), while high-confidence credential patterns (real API-key / token formats) are still flagged everywhere. Result: 0 generic-secret false positives across the catalog (was 7); the unfenced case still fails; high-confidence keys still flagged even inside fences. Covered by 5 new pytest cases in `tests/validators/test_validate_skills.py` (fenced-ignored, unfenced-flagged, high-confidence-in-fence-flagged, non-markdown-flagged, nested-fence-no-state-inversion). |
| WN-v23-4 | 574 quality-heuristics warnings across grandfathered skills | WN | v2.4.0 Phase 7 (T026) | Full sweep to zero: `python scripts/validate_skills.py --quality` went from 576 (re-measured) to 0 warnings across all 245 skills. Added skill-appropriate `## Common Rationalizations` tables, converted prose / `## Quality Checklist` sections to binary `## Verification` checklists, and wired real `[[skill-name]]` cross-links into `## Related Skills`; trimmed over-long `summary_l0` / `overview_l1` fields. 218 skills edited across 20 categories. Residual cosmetic redundancy (some skills now carry both `## Quality Checklist` and `## Verification`) tracked as WN-v24-2. |
| DF-v23-2 | security-operations skill bodies lacked platform-specific query examples | DF | v2.4.0 Phase 7 (T027) | Added `references/query-examples.md` to the three highest-traffic defensive skills (`siem-detection-engineering`: Sigma + Splunk SPL + Sentinel KQL + Elastic EQL for one behavior; `endpoint-edr-detection`: process injection / suspicious trees / LOLBin abuse as Sigma + MDE KQL + Splunk SPL; `cloud-audit-log-detection`: AWS CloudTrail + Azure Activity/Entra KQL + GCP audit-log detections). Each is linked from its SKILL.md (orphan-bundle clean), framework-mapping frontmatter untouched, ASCII-only. |
| WN-v23-2 | 15 instruction templates carry a leading UTF-8 BOM | WN | v2.4.0 Phase 7 (T028) | Stripped the leading BOM from all 15 `templates/ai-instructions/**/*.md` files (content otherwise unchanged) and removed the `--exclude templates/ai-instructions` from the `make validate` and CI unicode-safety calls. `validate_unicode_safety.py` now passes on the subtree with no exclusion (0 BOM errors; remaining punctuation findings are non-fatal warnings, out of scope per the task). |
| WN-v23-3 | ~1034 non-ASCII punctuation warnings in compliance-review templates | WN | v2.4.0 Phase 7 (T029) | Replaced em-dash/en-dash with `--`/`-`, curly quotes with straight, ellipsis with `...`, and NBSP with space across the flagged English-Markdown files under `templates/development/compliance-review/` (3 files changed). `validate_unicode_safety.py --strict` on that subtree now reports 0 findings (was 19 strict errors); no semantic content change. |
| DF-v23-1 | 8 `/Users/<user>` personal-path leaks in a hook test fixture | DF | v2.4.0 Phase 7 (T030) | Redacted the 8 real-username occurrences in `catalog/hooks/tests/test_classification_audit.py` to the placeholder `user` (an allowed placeholder that keeps the command strings shell-clean, avoiding the `<user>` angle-bracket parsing risk), and removed the `--exclude catalog/hooks/tests` from the `make validate` and CI no-personal-paths calls. The classification audit still passes 172/172 with the redacted paths; the validator passes the subtree with no exclusion. |
| DF-v23-3 | orphaned `base-gemini-ide.md` instruction template | DF | v2.4.0 Phase 7 (T031) | Confirmed no integration selects it (grep), deleted `templates/ai-instructions/base-gemini-ide.md`, and updated the two code-comment references (`scripts/lib/integrations/gemini.py`, `scripts/lib/installer/instruction_merge.py`) to no longer name the removed file. Integration suite green. |
| QG-v23-1 | CI shellcheck did not cover per-skill helper scripts | QG | v2.4.0 Phase 7 (T032) | Broadened the `.github/workflows/ci.yml` "Lint hook scripts" step from `find catalog/hooks -name '*.sh'` to `find catalog -name '*.sh'`, so the 8 per-skill `catalog/skills/**/scripts/*.sh` helpers are now gated. `bash -n` is clean across all 8 locally (shellcheck not on the host; full shellcheck runs in CI). |
| DF-v23-4 | only 6 of ~20 languages had code-search extractors | DF | v2.4.0 Phase 7 (T033) | Shipped the full recommended demand batch -- Ruby, PHP, C, C++ extractors -- raising coverage 6 -> 10. Each has an eval fixture clearing the 80% recall gate (all four at 100% recall / 100% precision), unit tests, and a tree-sitter grammar dep under the shared `<0.26` ceiling (ruby 0.23, php 0.24, c 0.24, cpp 0.23). Registered in `LANGUAGE_EXTRACTORS`. Remaining languages + framework/parameter parity carried forward as DF-v24-7. |
| DF-v23-5 | `python_app` code-search precision stuck at 70% | DF | v2.4.0 Phase 7 (T034) | Demoted IMPORT/EXPORT-kind nodes from the default `search_fts` result set (they are references, not definitions, so a name query like `make_user` matched both the def and every import site), keeping them reachable via the existing `all_fields=true` opt-out. `python_app` precision rose 70% -> 100% with recall held at 100%; aggregate eval precision is now 100%. Two new `test_traverser.py` cases (default demotes import nodes; `all_columns=True` surfaces them). |
| WN-v24-1 | AGENTS.md / README / plugin.json / marketplace.json catalog-count prose stale after the registry reconciliation | WN | v2.4.0 Phase 8-9 (release bump) | Updated all count prose to the on-disk truth (245 skills across 21 categories, 41 commands, 22 hooks, 23 agents) across AGENTS.md (headline + structure-tree comments), README.md (3 lines), plugin.json (description + version), and marketplace.json (description + version). Embedded SKILL INDEX is installer-generated from `data/SKILL_INDEX.md` (already 245). Reconciled the allowlist for the six remaining v2.4.0 pushy descriptions so `validate_skills.py --allow-existing` passes 0 errors. Full detail retained in the (marked-resolved) Open Items entry above for the count-history record. |
| BG-v24-1 | installer.ps1 stale-test failure (effortLevel xhigh literal) | BG | v2.4.0 Phase 7 (T035) | Closed the pre-existing failure surfaced in Phase 6. `installer.ps1` seeds core defaults dynamically from the template (`$coreKeys = @("effortLevel", "model")`) and is canonical; updated `test_installer_ps1_fallback_literal_matches_template` to assert the current fallback hint ("Manually copy effortLevel/model/env from ...") instead of the removed hardcoded `xhigh` literal. The full hook suite is green with a real bash on PATH (the 72 other local failures were the Windows App-Execution-Alias `bash.EXE` stub, not regressions; CI runs on Linux). |

---

**File lifecycle**: This file is appended by `/implement-phase` Phase 8 step 8.4 (per-phase append), swept by `/wrap-up-session` Phase 4 (catch-all from live conversation), and finalized by `/update-version` at the v2.4.0 -> next-version bump. After finalization, the next plan run by `/generate-plan` will read this file to decide which items carry forward.
