# Known Gaps -- v2.3.0

This file tracks per-version unfinished work, deferred items, deviations from plan, and bugs discovered during phase implementation. The next phase plan and the version-bump checklist read this file to decide what carries forward.

**Plan**: [docs/archives/v2/v2.3/plans/adoption-ecc-cybersec-skills.md](plans/adoption-ecc-cybersec-skills.md)
**Status**: Phases 1-9 of 9 closed (skill-native foundations + CI validators + runtime learning + installer lifecycle & selective install + skill-quality tooling + framework coverage & defensive security content + installer instruction-file parity + code-graph quality & extractor expansion + live-environment verification); v2.3.0 release-readiness workflow running -- 15 open items transferred to the v2.4.0 adoption-compound-engineering-plugin plan (2026-05-30). The sibling adoption-superpowers plan also closed all 6 of 6 phases on 2026-05-30 (process-discipline skills) and recorded one new deferral, DF-v23-9 (visual brainstorming server, not built).
**Last updated**: 2026-05-30 (adoption-superpowers Phase 6 close -- deferral record + polish: recorded DF-v23-9 (visual brainstorming server deferral, not built; re-full and policy-compliant but deferred on catalog-content-first identity grounds), bumped the AGENTS.md catalog count to 230 skills / 23 categories, added the CHANGELOG [Unreleased] adoption block, and ran the final validation sweep. Summary DF and Total open counts 0 -> 1. Previously: 15 open items transferred to v2.4.0 adoption-compound-engineering-plugin plan) (adoption-superpowers Phase 5 close -- flaky-test tooling cluster (re-full). Shipped two per-skill bundled resources under `catalog/skills/tests-generation/flaky-test-detector/` (auto-copied by both installers; no installer copy-step edit, no `data/` registry edit). (1) `scripts/find-polluter.sh` + parity sibling `scripts/find-polluter.ps1`: a project-agnostic test-pollution bisector that runs each test file in isolation, removing a watched artifact before each run, and reports the first file whose isolated run re-creates it; `--watch`/`--tests`/`--`-test-command interface (bash) and `-Watch`/`-Tests`/`-TestCommand` (PowerShell), with a `{}` per-file placeholder (appended if absent), an unsafe-path guard on the watched pattern, and exit 0 on a successful scan / 2 on a usage-IO error. (2) `assets/condition-based-waiting-example.ts`: a copy-in `waitFor(probe, description, timeoutMs, intervalMs)` polling helper plus `waitForEvent`/`waitForCount`/`waitForState`, zero runtime deps, heavily commented on WHY. Both new scripts and the asset are referenced from `flaky-test-detector/SKILL.md` (new bisector block added under Step 3) so the orphan-bundle audit stays clean. Validators green (`make` unavailable on the Windows host so invoked directly): orphan-bundle audit PASS 0/0 across 237 skills, JSON catalogs OK (skills.json 231, untouched), no-personal-paths / unicode-safety / supply-chain-iocs / workflow-security exit 0, all four new/edited files ASCII-clean. `.sh`/`.ps1` parity CONFIRMED by running both live against a throwaway fixture (identical results across 5 cases each: polluter detection, no-polluter, append-mode, unsafe-watch guard, no-match guard). One bug was found via the smoke test and FIXED in-phase: a literal (metacharacter-free) `--watch` pattern always matched under `nullglob` in the `.sh`, so `artifact_exists` / `remove_artifact` / the test-glob expansion now test on-disk existence with `-e` (the `.ps1` was unaffected -- it uses existence-based `Test-Path`). No new open gap: ShellCheck is unavailable on the host so `find-polluter.sh` was verified via `bash -n` plus the live cross-OS smoke instead, and per-skill-script ShellCheck coverage is already tracked under the transferred QG-v23-1 (CI shellcheck job does not lint per-skill helper scripts); the `.ts` is a by-design copy-in example (not runtime code requiring tests). Zero new runtime dependencies, zero outbound calls, zero new credentials. Summary counts unchanged.) Previously: (adoption-superpowers Phase 4 close -- eval-harness trigger techniques (re-full). Added the three trigger-testing techniques superpowers' harness had that Nexus-Hub's `skill-eval-loop` lacked, all reusing the existing CLI dispatcher (no new dependency, no new outbound path, no new credential, no `data/` registry edits). (1) `scripts/optimize_skill_description.py`: extracted `build_cli_command()` from `invoke_cli()` so the no-cross-CLI-bleed parity invariant and the new `--model` threading are both testable without spawning a subprocess; added `extract_tool_invocations()` + `detect_premature_action()` (T014: flags a with_skill run that invoked a non-Skill/non-TodoWrite tool before the first Skill load); added `is_multi_turn()`/`first_trigger_turn()`/`multi_turn_passes()`/`evaluate_multi_turn()` (T015 multi-turn: assert the skill first triggers at the designated `trigger_turn`); and threaded a `--model` flag through `estimate_trigger_rate`/`run_iteration`/`_passes`/`render_dry_run` (T015 cheap-model fragility; a per-eval `model` field overrides it). (2) `scripts/aggregate_benchmark.py`: surfaces `premature_action` per-eval in `benchmark.json` as a pass-through from the with_skill `grading.json` (default False for the without_skill baseline). (3) `skill-eval-loop/references/schemas.md`: documented the optional `turns`/`trigger_turn`/`model` evals.json fields and the `premature_action` grading/benchmark field; `agents/grader.md`: records `premature_action`; new `references/trigger-testing.md` (linked from SKILL.md, orphan-bundle clean) documents all three techniques. (4) `catalog/hooks/tests/test_eval_loop.py`: +23 cases (premature-action detection incl. stream-json parsing + ordering + single-array fallback; multi-turn pure-logic; cheap-model `--model` argv threading across all four CLIs; dry-run model surfacing; benchmark premature_action pass-through); suite 14 -> 37 passed. Existing optimizer behavior preserved (dry-run exit 0, train/test split + `test_trigger_rate` selection metric unchanged); the CLI-parity test stays green after the `build_cli_command` extraction. `make` unavailable on the Windows host so pytest + validators were invoked directly: orphan-bundle audit PASS 0/0 across 237 skills, unicode-safety `--strict` 0 findings on the 6 changed files, no-personal-paths 0. CI already runs `pytest catalog/hooks/tests/` (`.github/workflows/ci.yml` line 94), so the new cases are gated with no CI edit. The 73 failures elsewhere in `catalog/hooks/tests/` are pre-existing bash-hook-on-Windows environmental failures in 5 unrelated test files (test_diff_review_hooks 44, test_session_digest 11, test_learning_capture 10, test_old_version_docs_guard 6, test_installer_smoke 2) -- same cross-OS class as DF-v23-6, green on Linux CI -- not regressions from this phase. One new low-priority gap recorded below (DF-v23-8: live end-to-end run of the three new modes deferred; only dry-run + pure-logic + fixture-stream tests done this phase). Previously: 2026-05-29 (adoption-superpowers Phase 3 close -- discipline framing + operational enhancements. Pure-markdown, zero-dependency enhancements to four existing skills (no new top-level skills, no `data/` registry edits). (1) `bug-fixing/regression-root-cause-analyzer/SKILL.md`: added an "Iron Law: No Fixes Without Root Cause Investigation First" gate, the "after 3 failed fixes, question the architecture" circuit-breaker, the multi-component-boundary evidence-gathering pattern, and a Common Rationalizations table; to satisfy the T013 <800-line gate (the file was a grandfathered 952 lines), the JavaScript + Java code examples were moved to a new `references/multi-language-examples.md` (Python stays inline as the primary examples), landing the body at 725 lines. (2) `orchestration/multi-agent-coordinator/SKILL.md`: added a "Step 4.5: Subagent-Driven Development" subsection (two-stage review ordering -- spec compliance THEN code quality; the 4-status implementer protocol DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED; per-role model tiering) plus three bundled prompt templates under `assets/` (`implementer-prompt.md`, `spec-reviewer-prompt.md`, `code-quality-reviewer-prompt.md`); 672 lines. (3) `tests-generation/flaky-test-detector/SKILL.md`: linked a new `references/condition-based-waiting.md` (wait-for-the-condition pattern, before/after, quick-patterns table, when a fixed timeout IS correct, common mistakes; forward-references the Phase-5 `waitFor` helper); 760 lines. (4) `developer-experience/spec-driven-development/SKILL.md`: added a "Hard Gate: No Implementation Before an Approved Design" section + one-question-at-a-time convention + two Common Rationalizations rows, with a reciprocal cross-link added to `idea-refine/SKILL.md`; 284 lines. Validators green: orphan-bundle audit 0 errors / 0 warnings (all 5 new bundled files referenced), skills.json OK (231, untouched), no-personal-paths / unicode-safety / supply-chain / workflow-security exit 0. NO new open gaps: all authored content is ASCII-clean (the only unicode-safety warnings on the touched files are pre-existing em-dashes already covered by the WN-v23-3 umbrella, left in place as out-of-scope); the regression refactor-to-references was an intentional plan-directed move, not a gap. `make` unavailable on the Windows host so validators were invoked directly; `make lint` n/a -- no shell scripts added this phase (find-polluter is Phase 5). Summary counts unchanged.) Previously: 2026-05-29 (adoption-superpowers Phase 2 close -- skill-authoring methodology upgrade. Pure-markdown, zero-dependency: three new bundled references under `catalog/skills/workflow/create-custom-command/references/` (`tdd-for-skills.md` 57 lines, `pressure-testing.md` 53 lines, `persuasion-principles.md` 53 lines), all linked from `create-custom-command/SKILL.md` via a new "Skill-Authoring Methodology (bundled references)" section, and a reciprocal cross-link from `skill-eval-loop/SKILL.md` plus a `[[skill-eval-loop]]` Related-Skills link. `make`-validate-equivalent green: full-catalog orphan-bundle audit 0 errors / 0 warnings across 237 skills, skills.json OK (231 entries, untouched), unicode-safety produced 0 findings in the new files. NO new open gaps: the only warnings observed are 2 pre-existing em-dashes in `create-custom-command/SKILL.md` (lines 396/402, in the original "Description Style" section, NOT introduced this phase and left in place as out-of-scope) -- already covered by the existing WN-v23-3 umbrella ("em-dashes across English Markdown ... and elsewhere"), so not re-recorded. No new dependency, no new standalone script (per-skill references auto-copy via the installer, no copy-step edit), zero outbound calls. Summary counts unchanged. Previously: 2026-05-29 (Phase 9 close -- live-environment verification (WN-2 / WN-3 / WN-4 / WN-8 from v2.2.0 carryover, all closed). The four Phase 9 items were live-environment verifications that the plan's T036 explicitly allowed resolving via documentation or re-deferring with a dated reason. (1) WN-2 / T032: the Antigravity CLI binary name was verified against Google's now-public Antigravity CLI documentation (the binary ships on a verifiable channel ahead of the 2026-06-18 Gemini CLI sunset) and CORRECTED from the inferred `antigravity` to `agy` (installs to `~/.local/bin/agy`); applied across `catalog/hooks/antigravity-cli-diff-review.sh`/`.ps1` (binary detection + invocation), `scripts/lib/integrations/antigravity.py`, both installers, and the base-antigravity templates. (2) WN-3 / T033: the workflow file format was CONFIRMED as Markdown under `.agents/workflows/` (the inferred value was correct), reconciled in `test_antigravity_commands.py`. (3) WN-4 / T034: YAML frontmatter is honored and the workflow name derives from the filename -- DOCUMENTED. (4) Bonus path corrections surfaced during verification: the per-project dir is `.agents/` (plural, not `.agent/`), the instruction file is `AGENTS.md` (not `AGENT.md`), and the global CLI footprint is `~/.gemini/antigravity-cli/` (not `~/.agent/`); applied to the integration config (`workspace_dir=.agents`, `global_dir=~/.gemini/antigravity-cli`, `instruction_file=AGENTS.md`, written to `.agents/AGENTS.md` to avoid clobbering the codex-managed root `AGENTS.md` shared marker block), both installers' legacy mirror paths in lockstep, and the three antigravity integration test files (20 tests pass). (5) WN-8 / T035: the cross-OS smoke was re-run -- Windows is empirically green (936 pytest cases across catalog/hooks 392+3skip, integrations+installer 260, validators 44, skill-server 43, code-search 168+1skip, web-fetch 29; eval recall 100% / precision 96.2% reproducing docs/archive/v2/v2.3/eval-baseline.md; installer.ps1 -Help/-PrintConfig probes for claude/antigravity2/gemini-cli exit 0 with antigravity2 now showing `.agents/`; negative case exits 1), the Linux test suite is empirically green via CI (`.github/workflows/ci.yml` runs on ubuntu-latest), and macOS plus the Linux installer-probe/eval portion are re-deferred with a dated reason (no macOS host; source release). Recorded in `docs/archive/v2/v2.3/installer-smoke-post.txt` and `docs/archive/v2/v2.2/antigravity-cli-probe.md` Section 11. Two new low-priority gaps recorded below: WN-v23-5 (Antigravity CLI live-VM residuals: `.agent/` vs `.agents/` codelab dissent, exact global subpath, subagents/rules subdirs, root-vs-`.agents/` instruction file) and DF-v23-6 (macOS full smoke + Linux installer-probe/eval deferral, required before a packaged-binary release). `make validate` equivalent green (skills.json 227, bundle audit 0/0, 4 CI validators rc=0); shellcheck + bash -n + PowerShell parse clean on all edited scripts. Zero new runtime dependencies; no new standalone scripts (so no installer copy-step registration needed); zero outbound calls. Previously: 2026-05-29 (Phase 8 close -- code-graph quality & extractor expansion (WN-1 / WN-5 / WN-6 / WN-7 / DF-002 from v2.2.0 carryover, all closed). Scope was entirely within `extensions/nexus-code-search/` plus the Makefile `eval` target path and docs. (1) WN-1 / T026: `_load_ignore_spec` in `indexer.py` switched the pathspec factory from the deprecated `gitwildmatch` to `gitignore`; the 52 `GitWildMatchPattern` DeprecationWarnings are gone (full suite now passes under `-W error::DeprecationWarning`). (2) WN-5 / T027: the `tree-sitter>=0.24,<0.26` + per-grammar `<0.26` pins were re-verified on 2026-05-29 -- no 0.26-compatible release of core (latest 0.25.2), tree-sitter-python (0.25.0), or tree-sitter-typescript (0.23.2) has shipped, so the ceiling stays; a dated comment records the check. (3) WN-6 / T028: `python.py` and `typescript.py` now emit `instantiates` (a call/`new` whose resolved target is a class) and `overrides` (a method whose enclosing class extends, in-file and transitively, a parent defining a same-named method); `INSTANTIATES` was added to `traverser._IMPACT_KINDS` so re-typing a constructor call from `calls` does not drop it from `code_impact`. (4) WN-7 / T029: `traverser.search_fts` now scopes the default FTS match to the `name` column via a `name : (<query>)` wrapper (new `_scope_to_name` helper), with an `all_columns` opt-out threaded through `GraphQueryManager.search` and the `code_search` MCP tool as `all_fields`; aggregate eval precision rose 63.3% -> 96.2% with recall held at 100%. (5) DF-002 / T030: four new extractors (`go.py`, `rust.py`, `java.py`, `csharp.py`) registered in `LANGUAGE_EXTRACTORS` for `.go`/`.rs`/`.java`/`.cs`/`.csx`, each emitting the language's node kinds + `contains`/`calls`/`instantiates` (+ Java/C# `extends`/`implements`/`overrides`, Rust `implements`); four matching tree-sitter grammar deps (go 0.25, rust 0.24, java/c-sharp 0.23) added to `pyproject.toml` under the shared `<0.26` ceiling; four eval fixtures (`go_app`/`rust_app`/`java_app`/`csharp_app`) added, each clearing the 80% per-fixture recall gate (all 100%). The new deps auto-distribute via both installers' `pip install -e` of the copied package -- no installer copy-step edit needed (T030 added no standalone `scripts/*.py`). (6) `docs/archive/v2/v2.3/eval-baseline.md` created and the Makefile `eval` target repointed there. Tests: nexus-code-search 168 passed / 1 skipped (+32 new: 6 Go, 6 Rust, 6 Java, 6 C#, 3 Python-edge, 2 TS-edge, 2 name-scope, 1 recall-gate), zero DeprecationWarnings; eval 8/8 fixtures 100% recall; make-validate equivalent green (skills.json 227 OK, bundle audit 0/0, 4 CI validators rc=0). Two new low-priority gaps recorded below (DF-v23-4 remaining extractors + param-node parity; DF-v23-5 import-node search-precision residual).) Previously: 2026-05-29 (Phase 7 close -- installer instruction-file parity (DF-001 / MT-1 / MT-2 from v2.2.0 carryover, fully closed). (1) T022: the Python registry runner reached body parity with the legacy bash `render_template`. `MarkdownIntegration._render` now merges a built-in default placeholder map (mirroring the bash constant sed substitutions), auto-loads `{{SKILL_INDEX}}` from `data/SKILL_INDEX.md`, leaves unknown tokens literal (matching the bash sed list), and appends per-language coding snippets; `scripts/lib/integrations/runner.py` gained `--var KEY=VALUE` (repeatable), `--languages`, and `--instruction-only` on `install` (and `--var`/`--languages` on `print-config`); `InstallContext` gained `languages` and `instruction_only`. (2) Path/template reconciliation: claude and codex now render their workspace instruction file (CLAUDE.md / AGENTS.md) at the PROJECT ROOT via a new `instruction_workspace_dir=""` config key (skills/ still mirror under .claude/ / .codex/), matching where the tools actually read them and the legacy bash output; the gemini integration was repointed from the orphan stub `base-gemini-ide.md` to the canonical `base-gemini.md` (one of the five lock-step base templates), closing the template-divergence half of DF-001. (3) T024 / MT-1: `CopilotIntegration.install_workspace` was refactored from its bespoke append-after-heading flow onto the canonical `merge_marker_section` primitive with `legacy_header="## Nexus-Hub Harness"`, so re-installs settle to `unchanged` and teardown removes only the marker block (track_shared). (4) Removal: all six `render_template` (installer.sh) and `Render-Template` (installer.ps1) instruction-file calls were replaced by `invoke_registry_platform ... --instruction-only` / `Invoke-RegistryPlatform ... -InstructionOnly`, and both dead render functions were deleted; the registry runner is now the single instruction-file renderer shared by both installers (eliminating the prior bash-vs-PowerShell snippet-whitespace drift). `invoke_registry_platform` / `Invoke-RegistryPlatform` thread the detected globals (PROJECT_NAME, PRIMARY_LANGUAGE, BUILD_CMD, OS_CONTEXT, ...) to the runner, which also fixed a latent literal-placeholder bug for the already-registry-driven platforms (cursor/opencode/antigravity/nexus-ai). (5) T023 / MT-2: `tests/integrations/test_parity_with_legacy_installer.py` gained `test_instruction_body_parity_with_legacy_render` (claude/codex/gemini x global+workspace) asserting the registry's marker-body equals an INDEPENDENT naive-str.replace reference render (the parity oracle) plus a no-literal-placeholder completeness check; `test_instruction_file_is_produced`, `test_install_workspace.py`, `test_markdown_integration.py`, `test_contract.py`, and the Copilot `test_base_writeresult.py` case were updated for the new root paths and `unchanged` settle behavior. Tests: tests/integrations + tests/installer + tests/validators 304 passed / 0 failed; catalog/hooks/tests 392 passed + 3 skipped; make-validate equivalent (JSON + bundle audit 0 orphans + quality pass 0 new warnings + 4 CI validators rc=0) green; shellcheck `--severity=warning` on installer.sh/install.sh clean; bash -n + PowerShell parse OK. Zero new runtime dependencies; no new standalone scripts (so no installer copy-step registration needed); zero outbound calls. One new low-priority gap recorded below (DF-v23-3 orphaned base-gemini-ide.md template).) Previously: 2026-05-29 (Phase 6 close -- framework coverage + defensive security content shipped. (1) New `scripts/build_framework_coverage.py` reads the optional framework-mapping frontmatter (mitre_attack / atlas_techniques / d3fend_techniques / nist_csf / nist_ai_rmf) across `catalog/skills/` and emits a Markdown or JSON coverage matrix (now spanning 34 ATT&CK techniques + 6 D3FEND countermeasures + 10 NIST CSF categories from the new content), registered in both installers and covered by 6 pytest cases in `tests/validators/test_build_framework_coverage.py`. (2) New `security-operations` skill category (maintainer-approved 2026-05-29) holding 15 re-authored defensive skills -- batch 1 (DFIR / hunting / IR): memory-forensics, hunting-credential-dumping, disk-artifact-forensics, siem-detection-engineering, log-threat-hunting, lateral-movement-detection, ransomware-incident-response, persistence-mechanism-hunting, endpoint-edr-detection; batch 2 (cloud / endpoint / phishing): cloud-security-posture-detection, cloud-audit-log-detection, container-runtime-detection, phishing-analysis-and-defense, identity-threat-detection, malware-triage-analysis. Each carries a pushy description with trigger phrases and a SKIP clause, ATT&CK / D3FEND / NIST CSF framework-mapping frontmatter, a `references/standards.md` companion, a Common Rationalizations table, and a binary Verification checklist; all re-authored from public MITRE / NIST framework knowledge (no third-party SKILL.md text, no source attribution in the artifact per the Reverse-Engineering Attribution Rule), and all defensive-only per the mandate filter. (3) Two deterministic helper scripts with `.sh` + `.ps1` parity: `memory-forensics/scripts/volatility-runner.sh|.ps1` (read-only Volatility 3 triage wrapper, requires a local `vol`, fetches no symbol packs) and `log-threat-hunting/scripts/ioc-log-scan.sh|.ps1` (local read-only IOC sweep), both shellcheck-clean and functionally verified, each referenced from its parent SKILL.md so the orphan-bundle audit passes. (4) Registered all 15 skills in the three data files (`data/skills.json` 212 -> 227 entries, `statistics.total_skills` 208 -> 223, new `categories.security-operations` = 15; `data/marketplace.json` new "Security Operations" category, skill_count 15; `data/SKILL_INDEX.md` +15 rows, 211 -> 226 skills / 22 -> 23 categories) and documented the new category in `AGENTS.md`. Tests: tests/ 298 passed (was 291), tests/validators 44 passed (+6 new), 4 CI validators rc=0, bundle audit 0 orphans, quality-heuristics pass 0 new warnings (the new skills clear it cleanly; the pre-existing 574-warning grandfathered debt is unchanged). Zero new runtime dependencies; everything stdlib / shell; zero outbound calls anywhere. Two new gaps recorded below (QG-v23-1 CI lint coverage; DF-v23-2 optional skill-body enrichment).) Previously: 2026-05-28 (Phase 5 close -- skill-quality tooling shipped. (1) New `catalog/skills/workflow/skill-stocktake/SKILL.md`: a two-mode (Quick Scan / Full Stocktake) holistic skill-quality audit that caches per-skill results under `.nexus/skill-stocktake/results.json` and delegates the deterministic checklist to the validator's new `--quality` pass. (2) New `catalog/skills/workflow/skill-create/SKILL.md`: a local-analysis-only (git log/diff, zero outbound) skill that drafts a conformant SKILL.md from recurring git-history patterns and surfaces it for maintainer review (skill-only form per user decision; no scripts/skill_create.py). (3) `scripts/validate_skills.py` gained a non-blocking `--quality` heuristics pass (missing Common Rationalizations, prose-only Verification, over-long Tier-1 fields, missing Related Skills links) wired into `make validate`; 6 new pytest cases in `tests/validators/test_validate_skills_quality.py`. (4) Both new skills registered in the three data files (skills.json 210 -> 212; workflow `skill_count` 25 -> 27; SKILL_INDEX total 209 -> 211). (5) Three pre-existing personal-path false positives (prose in `docs/DEVLOG.md`, `docs/archive/v2/v2.3/known-gaps.md`, and the Phase 4 session history that literally wrote the real `/Users/<user>/...` form while describing redacting it) were redacted to the `<user>` placeholder so `make validate` is green on the v2.3.0 tree. Tests: tests/ 291 passed, catalog/hooks/tests 392 passed + 3 skipped, tests/validators 37 passed, nexus-skill-server 43 passed. Zero new runtime dependencies; zero outbound calls; no new standalone scripts (so no installer registration needed). Previously: 2026-05-28 Phase 4 close -- installer lifecycle & selective install shipped. (1) Install-state manifest: `scripts/lib/integrations/manifest.py` extended additively with `record_actions(integration_key, file_actions)` capturing `{path, action, sha256, mtime}` per write; the legacy `_tracked` / `_shared` / `_logs` fields are untouched so the 50-case integration contract suite still passes (191/191 in tests/integrations green). The runner auto-records actions after each integration install. (2) Lifecycle subcommands: new `scripts/lib/integrations/lifecycle.py` ships `doctor` (diagnose drift / missing managed files vs. recorded SHA-256), `repair` (re-run install for drifted/missing integrations through the regular install pipeline so `merge_marker_section` semantics still apply), and `list_installed` (enumerate the manifest). All three exposed as new subcommands on `scripts/lib/integrations/runner.py` (`doctor`, `repair`, `list-installed`) with `--json` / `--quiet` flags. (3) Selective install: `data/bundles.json` schema bumped to 1.4.0 with two new top-level keys, `profiles` (minimal / core / full) and `modules` (testing / code-review / security-ops / ai-engineering / infrastructure / documentation). The existing `bundles` array is untouched. (4) Consult advisor: new `scripts/nexus_hub_consult.py` is a local, read-only natural-language matcher over skills / bundles / profiles / modules with a token-overlap + id-exact + tag-boost baseline scorer (the actual ranking heuristic is marked as a user-contribution slot for future tuning). (5) Harness audit: new `scripts/harness_audit.py` is a deterministic scorer over the manifest, scoring presence / integrity / coverage / marker_integrity per integration and emitting an aggregate 0-100 score (Markdown by default; `--json` available; `--min-score N` exits non-zero below threshold). (6) Installer wiring: `scripts/installer.sh` and `scripts/installer.ps1` register the two new scripts in lockstep with the existing v2.3.0 CI-validator block. (7) Test coverage: 33 new pytest cases across `tests/integrations/test_lifecycle.py` (13), `tests/integrations/test_consult.py` (12), `tests/integrations/test_harness_audit.py` (8). Full integration suite: 191 passed (was 158, +33 new). Pre-existing `/Users/<user>/...` findings in `docs/DEVLOG.md` (1) and `docs/archive/v2/v2.3/development/history/2026-05-28_phase-3-runtime-learning.md` (3) redacted to `<user>` so `make validate` stays clean on the v2.3.0 tree. Zero new runtime dependencies; everything stdlib; no outbound network calls anywhere; the additive-manifest guarantee in the plan is honored.) Previously: 2026-05-28 Phase 3 close -- runtime-learning subset shipped. (1) Memory-persistence hooks: `catalog/hooks/session-summary.sh` now also writes a compact project-scoped digest at `.nexus/context/last-session.md` (atomic via mktemp + rename) capturing branch, working-tree status, last five oneline commits, and up to 30 files-touched names; `catalog/hooks/session-start.sh` reads it back on `SessionStart` capped at `NEXUS_SESSION_START_MAX_CHARS` (default 8000). PowerShell siblings `session-start.ps1` / `session-summary.ps1` ship in lockstep. `catalog/hooks/settings.json` adds `PreCompact` and `SessionEnd` registrations for `session-summary.sh` alongside the existing `Stop` registration. (2) Continuous-learning skill + capture hooks: new `catalog/hooks/learning-capture.sh` / `.ps1` reads a Claude Code payload from stdin and appends one JSONL record (`ts`, `event`, `tool`, `prompt_sample`) to `.nexus/observations.jsonl` (auto-truncated at `NEXUS_LEARNING_MAX_BYTES`, default 1 MiB), wired to `UserPromptSubmit` and `Stop`. The new `catalog/skills/workflow/continuous-learning/SKILL.md` is the analysis half: read the JSONL on demand, surface candidate patterns, mint each as `.nexus/instincts/<slug>.yaml`, regenerate `.nexus/instincts/_index.md`, and draft new SKILL.md / commands for maintainer review when a high-confidence cluster forms. Hard constraint, stated in the body and the Common Rationalizations table: no background observer, no upload, no cross-project sharing -- the only observer is the agent itself, in-session. Skill count: 209 -> 210; workflow `skill_count`: 24 -> 25. (3) Test coverage: 22 new pytest cases under `catalog/hooks/tests/` (`test_session_digest.py` 11 cases + `test_learning_capture.py` 11 cases) exercising syntax, round-trip, runtime controls, off-switches, size-caps, and a static-analysis no-network-token check. Pre-existing personal-path findings in two Phase 2 docs (the v23-1 DF item description) redacted to use the `<user>` placeholder so `make validate` is clean on the v2.3.0 tree. Full hook sweep: 392 passed, 3 skipped (was 370 + 3 skipped at Phase 2). Zero new runtime dependencies; all reads/writes are local; no outbound network calls anywhere.)

## Summary

| Category | Open | Resolved this version |
|---|---|---|
| NI -- Not implemented (skipped subtask) | 0 | 0 |
| DF -- Deferred (intentionally) | 0 | 2 |
| BG -- Bug or unresolved test failure | 0 | 0 |
| MT -- Missing tests / coverage gap | 0 | 2 |
| WN -- Warning or suppressed lint rule | 0 | 8 |
| QG -- Quality gate bypassed | 0 | 0 |
| **Total (DF-v23-9 transferred to v3.0.0)** | **0** | **12** |

_(Phase 9 closed the final four v2.2.0 carryover items (WN-2, WN-3, WN-4 via Google's now-public Antigravity CLI docs; WN-8 via a Windows smoke + CI-Linux test-suite evidence -- see Resolved) and added two new low-priority gaps (WN-v23-5 Antigravity live-VM residuals; DF-v23-6 macOS full-smoke + Linux installer-probe deferral). Phase 8 previously closed WN-1/WN-5/WN-6/WN-7/DF-002; Phase 7 closed DF-001/MT-1/MT-2. All 12 ingested v2.2.0 known-gaps are now resolved or re-deferred with evidence. The remaining open items are pre-existing catalog-quality debt plus the superpowers-adoption deferrals (BG-v23-1, WN-v23-1..5, DF-v23-1..8, QG-v23-1) -- none is a release blocker. adoption-superpowers Phase 4 added DF-v23-8 (live end-to-end run of the new eval-harness trigger techniques deferred to the Phase 6 final validation, alongside the DF-v23-7 live `skill-eval-loop` run); both are low-priority verification-method deferrals, not defects.) All 15 remaining open items were transferred to the v2.4.0 adoption-compound-engineering-plugin plan on 2026-05-30 (ingested as known-gaps); their tracking surface is now that plan, and they are recorded in the Resolved table above as transferred._

## Open Items

_The 15 prior open items were transferred to the v2.4.0 plan [adoption-compound-engineering-plugin](../v2.4.0/plans/adoption-compound-engineering-plugin.md) on 2026-05-30 (see the Resolved table below). The single item below is a new adoption-superpowers Phase 6 deferral._

> **TRANSFERRED to the v3.0.0 plan on 2026-06-02.** DF-v23-9 was ingested into [command-consolidation-skill-security](../v3.0.0/plans/command-consolidation-skill-security.md) Phase 10, sub-task T047, and re-homed to the Resolved table below. Open count is now 0. The detailed entry is retained here for reference; it remains an intentional deferral with a revisit trigger (re-evaluated, not auto-built, in v3.0.0).

### DF-v23-9 -- Superpowers visual brainstorming server (deferred, not built)

- **Source phase**: Phase 6 - Deferral record + polish (adoption-superpowers)
- **Plan reference**: `docs/archive/v2/v2.3/plans/adoption-superpowers.md` (sub-task 6.1 / T021); comparison `docs/archive/v2/v2.3/comparison-superpowers.md` Section 11 (P3) / Section 13 (N3)
- **Category**: DF -- Deferred (intentionally)
- **Reason**: The superpowers visual brainstorming server is `re-full` and local-only (binds 127.0.0.1, makes zero outbound calls, requires no new credential), so it would pass the MCP Registry Policy decision tree. It is deferred on identity and maintenance grounds: Nexus-Hub is catalog-content-first, and a long-lived local Node websocket server belongs under `extensions/` only when a concrete user-facing need exists. Shipping it now would add a runtime surface with no demand behind it.
- **Suggested next step**: Do not build pre-emptively. If a user-facing need for in-session visual collaboration emerges, scope it as an `extensions/` internal MCP under the reverse-engineer-first policy and add a `docs/policy/mcp-reverse-engineering-matrix.md` row before building.
- **Revisit trigger**: build only if a user-facing need for in-session visual collaboration emerges.

## Resolved

| ID | Title | Category | Resolved in | Detail |
|---|---|---|---|---|
| DF-v23-9 | Superpowers-style visual brainstorming server (deferred, not built) | DF | transferred to v3.0.0 plan | Carried into [command-consolidation-skill-security](../v3.0.0/plans/command-consolidation-skill-security.md) Phase 10, sub-task T047 (re-evaluate the deferral; build only if a user-facing need for in-session visual collaboration has emerged, else re-defer into docs/v3/v3.0/known-gaps.md preserving the revisit trigger). Intentional re-full, local-only, policy-compliant deferral on catalog-content-first identity grounds. |
| DF-001 | Installer instruction-file byte parity (registry runner vs legacy bash `render_template`) | DF | v2.3.0 Phase 7 (sub-tasks T022 + removal) | Carried forward from v2.2.0 known-gaps DF-001 part 2. The Python `MarkdownIntegration._render` previously substituted only `{{PROJECT_NAME}}`, leaving 14+ placeholders literal; it now threads the full bash placeholder set (defaults + installer-detected `--var` values + auto-loaded `{{SKILL_INDEX}}` + per-language coding-snippet append) and leaves unknown tokens literal exactly as the bash `sed` list did. claude/codex render to the project root (`instruction_workspace_dir=""`) and gemini uses the canonical `base-gemini.md`, eliminating the path and template divergences. The six legacy `render_template` / `Render-Template` calls were replaced by `invoke_registry_platform ... --instruction-only`, the dead render functions were deleted, and the registry runner is now the single shared instruction-file renderer across both installers. Gated by the T023 body-parity test (below). |
| MT-1 | Copilot `install_workspace` does not use the canonical `merge_marker_section` primitive | MT | v2.3.0 Phase 7 (sub-task T024) | Carried forward from v2.2.0 known-gaps MT-1. `CopilotIntegration.install_workspace` was refactored from its bespoke append-after-heading flow onto `merge_marker_section(..., legacy_header="## Nexus-Hub Harness")`, matching Cursor. The v2.1 `## Nexus-Hub Harness` legacy header now migrates inline into the marker block, re-installs settle to `unchanged`, and teardown removes only the marker block (`track_shared`). The 50-case contract suite (idempotency + sibling preservation + partial-state recovery) passes for copilot. |
| MT-2 | `test_parity_with_legacy_installer.py` did not assert instruction-file byte parity | MT | v2.3.0 Phase 7 (sub-task T023) | Carried forward from v2.2.0 known-gaps MT-2. Added `test_instruction_body_parity_with_legacy_render` (claude/codex/gemini x global+workspace): it installs each integration in isolation with the full placeholder set, extracts the marker-delimited body, and asserts byte equality against an INDEPENDENT naive-`str.replace` reference render (the bash-semantics oracle), plus a no-literal-placeholder completeness check. This is the precondition that authorized the legacy-block removal under DF-001 above. |
| WN-1 | 52 `GitWildMatchPattern` ('gitwildmatch') deprecation warnings from pathspec | WN | v2.3.0 Phase 8 (sub-task T026) | Carried forward from v2.2.0 known-gaps WN-1. `_load_ignore_spec` in `extensions/nexus-code-search/src/nexus_code_search/indexer.py` switched the pathspec pattern factory from the deprecated `gitwildmatch` to `gitignore` (verified to match identically on the indexer fixtures). The 52 warnings are gone -- the full nexus-code-search suite (168 passed / 1 skipped) now runs clean under `pytest -W error::DeprecationWarning`. |
| WN-5 | tree-sitter dependency pin needs periodic re-verification for a 0.26-compatible release | WN | v2.3.0 Phase 8 (sub-task T027) | Carried forward from v2.2.0 known-gaps WN-5. Re-verified 2026-05-29: no 0.26-compatible release of `tree-sitter` (latest 0.25.2), `tree-sitter-python` (0.25.0), or `tree-sitter-typescript` (0.23.2) has shipped, so the `>=0.24,<0.26` / `>=0.23,<0.26` ceiling stays. A dated comment in `pyproject.toml` records the check; the four new Phase-8 grammar deps were added under the same shared `<0.26` ceiling. |
| WN-6 | `_collect_calls` did not emit `instantiates` / `overrides` edges despite the taxonomy supporting them | WN | v2.3.0 Phase 8 (sub-task T028) | Carried forward from v2.2.0 known-gaps WN-6. `python.py` and `typescript.py` now emit `instantiates` (a call / `new` expression whose resolved target is a class) and `overrides` (a method whose enclosing class extends, in-file and transitively, a parent class defining a same-named method, via a new `_resolve_overrides` pass). `INSTANTIATES` was added to `traverser._IMPACT_KINDS` so re-typing a constructor call from `calls` does not silently drop it from `code_impact`. Covered by new unit tests in `test_python_extra.py` / `test_typescript_extra.py` and the Java/C# `code_impact` eval questions. |
| WN-7 | `code_search` aggregate precision was 63.3% due to FTS matching qualified_name ancestor segments | WN | v2.3.0 Phase 8 (sub-task T029) | Carried forward from v2.2.0 known-gaps WN-7. `traverser.search_fts` now scopes the default FTS match to the `name` column (`name : (<query>)` via a new `_scope_to_name` helper), with an `all_columns` opt-out threaded through `GraphQueryManager.search` and the `code_search` MCP tool as `all_fields`. Aggregate eval precision rose 63.3% -> 96.2% with recall held at 100%. Two new `test_traverser.py` cases assert name-scoping excludes method/parameter ancestor matches and that `all_columns=True` restores the wide match. |
| DF-002 | only Python + TypeScript extractors shipped; 18 languages + 13 framework extractors deferred | DF | v2.3.0 Phase 8 (sub-task T030) | Carried forward from v2.2.0 known-gaps DF-002. Shipped the maintainer-confirmed priority batch: Go (`go.py`), Rust (`rust.py`), Java (`java.py`), and C# (`csharp.py`) extractors registered for `.go`/`.rs`/`.java`/`.cs`/`.csx`, each emitting the language's node kinds + `contains`/`calls`/`instantiates` (+ Java/C# `extends`/`implements`/`overrides`, Rust `implements`). Four eval fixtures (`go_app`/`rust_app`/`java_app`/`csharp_app`) each clear the 80% per-fixture recall gate (all 100%); 24 new extractor unit tests added. The remaining languages, framework extractors, and parameter-node parity are re-deferred as DF-v23-4 (above). |
| WN-2 | Antigravity CLI binary name unverified on a live VM | WN | v2.3.0 Phase 9 (sub-task T032) | Carried forward from v2.2.0 known-gaps WN-2. RESOLVED via Google's now-public Antigravity CLI documentation (the binary is on a verifiable channel ahead of the 2026-06-18 Gemini CLI sunset): the binary is `agy` (installs to `~/.local/bin/agy`), CORRECTING the v2.2.0 inferred `antigravity`. The `command -v agy` / `Get-Command agy` detection and the `agy -p` invocation now ship in `catalog/hooks/antigravity-cli-diff-review.sh`/`.ps1`; the `base-antigravity-20.md`/`base-antigravity-cli.md` templates and the `Antigravity20Integration` docstring were updated. Docs-verified, not live-VM-verified; the live-smoke residual is tracked as WN-v23-5. See docs/archive/v2/v2.2/antigravity-cli-probe.md Section 11.1. |
| WN-3 | Antigravity CLI workflow file format unverified on a live VM | WN | v2.3.0 Phase 9 (sub-task T033) | Carried forward from v2.2.0 known-gaps WN-3. RESOLVED (inferred value confirmed): the Antigravity CLI reads Markdown workflows under `.agents/workflows/*.md` (per Google's public codelab), NOT Gemini CLI's TOML schema. `test_antigravity_commands.py` was repointed to `.agents/workflows/` and the Markdown-not-TOML assertions pass; the conclusion in docs/archive/v2/v2.2/antigravity-cli-commands-schema.md is validated. The per-project dir was corrected from `.agent/` to `.agents/` (plural) across the integration and both installers. See docs/archive/v2/v2.2/antigravity-cli-probe.md Section 11.2. |
| WN-4 | Antigravity CLI workflow front-matter / name-derivation schema unverified | WN | v2.3.0 Phase 9 (sub-task T034) | Carried forward from v2.2.0 known-gaps WN-4. RESOLVED (documented): the Antigravity CLI honors YAML frontmatter in workflow/skill files and derives a workflow's name from its filename (`lint.md` -> `/lint`), so the existing verbatim `catalog/commands/*.md` mirror is compatible -- no `_write_antigravity_commands` variant needed. The instruction file was corrected from `AGENT.md` to `AGENTS.md`. See docs/archive/v2/v2.2/antigravity-cli-probe.md Section 11.3. |
| WN-8 | Phase 6 cross-OS installer smoke ran only on Windows; macOS and Linux not yet re-verified | WN | v2.3.0 Phase 9 (sub-task T035) | Carried forward from v2.2.0 known-gaps WN-8. RESOLVED with mixed evidence: Windows is empirically green (936 pytest cases + eval 100%/96.2% + installer -Help/-PrintConfig probes, with antigravity2 now showing `.agents/`); the Linux Python test suite is empirically green via CI (`.github/workflows/ci.yml` on ubuntu-latest), replacing the v2.2.0 PASS-by-parity inference for the test suite. The macOS full smoke and the Linux installer-probe/eval portion are re-deferred (no macOS host; not in CI) and tracked as DF-v23-6 for a pre-packaged-binary release. Recorded in docs/archive/v2/v2.3/installer-smoke-post.txt. |
| BG-v23-1 | `scripts/validate_skills.py` reports 7 pre-existing "Generic secret assignment" false positives in unrelated skills | BG | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: BG-v23-1]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| WN-v23-1 | `data/skills.json` skill entry count drifted from `statistics.total_skills` before Phase 1 | WN | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: WN-v23-1]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| WN-v23-2 | `templates/ai-instructions/**/*.md` ship with a UTF-8 BOM | WN | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: WN-v23-2]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| WN-v23-3 | `templates/development/compliance-review/**/*.md` ship with non-ASCII punctuation (em-dashes) | WN | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: WN-v23-3]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-1 | `catalog/hooks/tests/test_classification_audit.py` contains real personal paths in test fixtures | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-1]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| WN-v23-4 | New skill-quality heuristics pass surfaces 574 warnings across grandfathered skills | WN | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: WN-v23-4]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| QG-v23-1 | CI shellcheck job does not lint per-skill helper scripts | QG | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: QG-v23-1]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-2 | security-operations skill bodies are concise; optional enrichment deferred | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-2]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-3 | `templates/ai-instructions/base-gemini-ide.md` is orphaned as an instruction template | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-3]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-4 | nexus-code-search extractor coverage is partial: 4 of ~18 languages shipped | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-4]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-5 | `code_search` precision residual: import-statement nodes surface under a name-scoped query | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-5]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| WN-v23-5 | Antigravity CLI on-disk conventions are docs-verified, not live-VM-verified; four residual items remain | WN | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: WN-v23-5]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-6 | macOS full installer smoke + the Linux installer-probe/eval smoke portion are deferred to a packaged-binary release | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-6]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-8 | Live end-to-end run of the three new eval-harness trigger techniques deferred; validated via dry-run + pure-logic + fixture-stream tests only | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-8]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |
| DF-v23-7 | Live `skill-eval-loop` trigger run deferred for the three Phase 1 discipline skills; static trigger-surface check done instead | DF | v2.4.0 plan (transferred) | Transferred to the v2.4.0 adoption-compound-engineering-plugin plan as an ingested known-gap (see its sub-task tagged [from v2.3.0 known-gaps: DF-v23-7]). Not yet fixed -- tracking surface moved to the new plan; resolution happens when that sub-task is implemented. |

---

**File lifecycle**: This file is appended by `/implement-phase` Phase 8 step 8.4 (per-phase append), swept by `/wrap-up-session` Phase 6 (catch-all from live conversation), and finalized by `/update-version` at the v2.3.0 -> next-version bump. After finalization, the next plan run by `/generate-plan` will read this file to decide which items carry forward.
