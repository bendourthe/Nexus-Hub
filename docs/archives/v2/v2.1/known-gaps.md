# Known Gaps -- v2.1.0

This file tracks per-version unfinished work, deferred items, deviations from plan, and bugs discovered during phase implementation. The next phase plan and the version-bump checklist read this file to decide what carries forward.

**Plan**: [docs/archives/v2/v2.1/plans/adoption-spec-kit.md](plans/adoption-spec-kit.md)
**Status**: ready-to-ship (v2.1.0 base + Phase 9 polish + Phase 10 integration-registry pulled forward from v2.2.0)
**Last updated**: 2026-05-20 (Phase 10 close -- Integration Registry shipped as part of v2.1.0 rather than v2.2.0 per user request. The original plan targeted Phase 10 at v2.2.0 with the rationale that the refactor is "substantially larger in scope" and the byte-identical parity contract was non-trivial. The user revised that scope mid-implementation: keep the legacy installer copy paths for the original 4 platforms (Claude / Gemini / Codex / Copilot) as canonical -- skipping the byte-identical parity work and treating the new registry as ADDITIVE for the extended-platform set (Windsurf, Antigravity 2.0, Gemini CLI, Nexus-AI). This trades a strict-parity v2.2.0 release for an immediate cross-platform expansion in v2.1.0. The deferred parity migration is logged as DF-001 below for a future v2.2.0 release. Phase 9 prior history preserved below.)

**Prior status (Phase 9 close, 2026-05-20)**: ready-to-ship (v2.1.0 base release stable; Phase 9 P3-polish landed against v2.1.0 baseline rather than splitting to v2.1.1 -- four P3-polish items shipped: methodology essay, devcontainer, markdownlint-cli2.jsonc, installer path-traversal test. One DEVIATION recorded against the Phase 9.4 prompt: the plan asserted "no Makefile change should be needed; pytest's `tests/installer/` is auto-discovered", which is incorrect for this repo because `make test` runs three explicit `cd extensions/...` invocations rather than auto-discovering `tests/`. Minimal Makefile change made: appended a conditional `if [ -d tests ]; then python -m pytest -q tests; fi` line so the new suite runs alongside the extension tests; backwards-compatible for any future repo state without `tests/`. Phase 9 verification: `python scripts/validate_skills.py --bundles-only` 0 errors / 0 warnings across 210 skill bundles; `python -m pytest tests/installer` 19/19 passed; extension suites unchanged (37 + 36 + 23 = 96 passed); `bash -n .devcontainer/post-create.sh` clean; `python -c "import json; json.load(open('.devcontainer/devcontainer.json'))"` parses; JSONC config validates after comment-strip. No data-registry deltas (Phase 9 added no skills, no commands, no templates -- only docs/essay, devcontainer scaffolding, style-guide config, and a test suite). Release decision: per the Phase 9.5 prompt's "default: ship as v2.1.1 patch" guidance, the maintainer kept Phase 9 against the v2.1.0 baseline rather than cutting a separate tag, on grounds that the four items are non-functional polish that complements rather than alters v2.1.0 behavior.

**Prior status (Phase 8 close, 2026-05-20)**: WN-1 resolved; 1 BG resolved during the same phase (the `spec-kit-task` -> `spec-driven-task` rename); CHANGELOG.md `[2.1.0]` block authored; `docs/archive/v2/v2.1/RELEASE_NOTES.md` shipped; `data/marketplace.json` `plugin.version` bumped 2.0.0 -> 2.1.0; `data/skills.json` statistics rebaselined from `total_skills: 197` to `total_skills: 206`. Data-registry deltas at Phase 8 close: SKILL_INDEX.md 205 -> 208 rows, skills.json 203 -> 206 entries, marketplace.json 200 -> 203 category sum. Cross-OS installer-smoke results recorded in `docs/archive/v2/v2.1/installer-smoke-post.txt` with the Linux/macOS coverage carry-over from v1.1.5 DF-003 / DF-005 / DF-006 explicitly noted as out of scope. `git tag v2.1.0` was cut locally during Phase 8; push deferred per the CLAUDE.md destructive-git rule.

## Summary

| Category | Open | Resolved this version |
|---|---|---|
| NI -- Not implemented (skipped subtask) | 0 | 0 |
| DF -- Deferred (intentionally) | 1 | 0 |
| BG -- Bug or unresolved test failure | 0 | 1 |
| MT -- Missing tests / coverage gap | 0 | 0 |
| WN -- Warning or suppressed lint rule | 0 | 1 |
| QG -- Quality gate bypassed | 0 | 0 |
| **Total** | **1** | **2** |

## Open Items

| ID | Title | Category | Source phase | Plan reference | Reason | Suggested next step |
|---|---|---|---|---|---|---|
| DF-001 | Byte-identical parity migration of the original 4 platforms (Claude / Gemini / Codex / Copilot) into the integration registry **(part 1 resolved in v2.2.0 Phase 3; part 2 carried forward)** | DF | Phase 10 | [docs/archives/v2/v2.1/plans/adoption-spec-kit.md](plans/adoption-spec-kit.md) sub-tasks 10.2 and 10.3 | Phase 10 in v2.1.0 ships the integration registry as ADDITIVE -- the four new platforms (Windsurf, Antigravity 2.0, Gemini CLI, Nexus-AI) install through the new Python runner, while the original 4 platforms continue through the legacy `installer.sh` / `installer.ps1` copy blocks. The plan's original Phase 10.2 spec required byte-identical parity tests covering all 5 original platforms before any installer change; this was the heaviest sub-task in scope and was intentionally deferred so that v2.1.0 could ship the cross-platform expansion immediately. ADR-001 documents the additive choice. **Update (v2.2.0 Phase 3, T020):** part 1 -- tree-mirror byte parity (`catalog/skills`, `catalog/commands`, `catalog/agents`, `catalog/rules` across claude / codex / cursor / gemini / opencode) -- is now closed by `tests/integrations/test_parity_with_legacy_installer.py` (SHA-256-identical legacy vs. registry trees). Part 2 -- instruction-file byte parity and legacy-block removal -- remains open. | **Part 1 done.** Part 2 (v2.3.0): bring the registry runner to full instruction-file parity (thread the bash placeholder set `{{PRIMARY_LANGUAGE}}` / `{{BUILD_CMD}}` / `{{SKILL_INDEX}}` / ... and append per-language coding snippets in `MarkdownIntegration._write_instruction`), add the instruction-file byte-parity assertion to `tests/integrations/test_parity_with_legacy_installer.py`, then refactor `installer.{sh,ps1}` to delegate claude / codex / gemini to the runner and remove the duplicated copy blocks. Tracked as `DF-001` in [docs/archives/v2/v2.2/known-gaps.md](../v2.2.0/known-gaps.md). |

## Resolved

| ID | Title | Resolved in | Notes |
|---|---|---|---|
| WN-1 | `data/skills.json` statistics block out of sync with the actual skills array | Phase 8.1 | Rebaselined `statistics.total_skills` from `197` to `206` and `statistics.categories.*` to match the actual skills array. Per-category counts mirror the array categorization (kebab-case primary keys plus 3 historical Title-Case duplicates for `Developer Experience`, `Research`, `Workflow` carried over from before v2.0.0 -- left as-is because they are pre-existing data-quality artifacts unrelated to v2.1.0 scope and renaming them would break downstream consumers that already index by the legacy keys). |
| BG-1 | `spec-kit-task` GitHub-issues label leaks upstream attribution | Phase 8.1 step 6 | The Phase 7 prompt prescribed the label `spec-kit-task` for `/tasks-to-issues`, which violates the Reverse-Engineering Attribution Rule in `AGENTS.md` (cross-cutting constraint #1 of the plan also forbids `spec-kit` in user-facing artifacts). The label was renamed to `spec-driven-task` across the `/tasks-to-issues` command file, the `tasks-to-issues` SKILL.md (description + body + examples), both helper scripts (`tasks-to-issues.sh` + `tasks-to-issues.ps1`), the `gh-cli-auth-runbook.md` reference (including the `gh label create` recipe + the audit query), and the `data/skills.json` description / overview_l1 fields. Note: the historical plan file `docs/archive/v2/v2.1/plans/adoption-spec-kit.md` still mentions `spec-kit-task` in the Phase 7 prompt block -- left as-is because the plan is a frozen historical record. |

---

**File lifecycle**: This file is appended by `/implement-phase` Phase 8 step 2 (per-phase append), swept by `/wrap-up-session` Phase 4 step 4b (catch-all from live conversation), and finalized by `/update-version` at the v2.1.0 -> next-version bump. After finalization, the next plan run by `/generate-plan` will read this file to decide which items carry forward.
