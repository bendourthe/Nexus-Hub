# MCP Reverse-Engineering Matrix

**Version**: v1.0.0
**Source policy**: [AGENTS.md](../../AGENTS.md) `## MCP Registry Policy`
**Status**: authoritative

This matrix classifies every MCP server referenced by Nexus-Hub (both those kept in `catalog/mcp-configs/mcp-servers.json` and those dropped or reverted) under the MCP Registry Policy decision tree. The matrix is the permanent record of why each entry sits where it does; future registry additions must add a row here with upstream evidence. (The internal server keys were renamed from `devai-*` to `nexus-*` at v2.0.0 alongside the project rename; the original rows below preserve the new key names while the Rationale columns note the v2.0.0 rename.)

The v1.0.0 release was driven off this matrix: drops (Phase 3) and new internal MCP builds (Phases 6 and 7) flow from the `v1.0.0 action` column; deferred rebuilds flow from the `v1.1.0+ action` column into the backlog.

---

## Classification Legend

Listed in decision-tree order (from the policy in `AGENTS.md`). An entry gets the highest-precedence classification that applies:

| Classification | Meaning |
|---|---|
| `already-local` | No outbound calls. Internal Nexus-Hub or zero-outbound Anthropic-official. No action needed. |
| `skill-native` | Capability is achievable by instructing the agent's own LLM; the correct replacement is a skill, not an MCP. |
| `re-full` | Fully reverse-engineerable into a local internal MCP with no loss of function. |
| `re-partial` | Partially reverse-engineerable; some capability depends on a third-party data source; ship what's local, document the gap. |
| `vendor-intrinsic` | The third party IS the data destination (the user is already a customer). Rebuild-as-internal-MCP improves audit and supply-chain posture but does not reduce data flow. Defer to v1.1.0+ unless the audit argument is urgent. |
| `drop-outright` | Capability requires a third-party service that cannot be recreated (for example: "the web"). No local equivalent possible; not worth the trust cost. |

---

## Row Schema

Each row answers:

| Column | What it captures |
|---|---|
| MCP key | The key used in `mcp-servers.json`. |
| Current source | Who maintains the upstream code; package or repo. |
| What it does | One sentence on the capability. |
| Outbound-call surface | Where data goes at runtime, cited with evidence. |
| Classification | Per the legend above. |
| Effort if RE'd | Small / Medium / Large, for the `re-*` rows. |
| v1.0.0 action | What this release does. |
| v1.1.0+ action | What later releases should consider. |
| Rationale / citation | The upstream evidence that supports the classification. External source names appear only in this column (per the policy's Reverse-Engineering Attribution Rule). |

---

## Matrix

### Internal and Local-Only (Always Allowed)

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v1.0.0 action | v1.1.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `nexus-skill-server` | Nexus-Hub internal (`extensions/nexus-skill-server/`, Python 3.10+) | Skill catalog retrieval: `search_skills`, `get_skill`, `list_categories`, `list_bundles`, `get_bundle` | None - reads `data/skills.json` from local disk | `already-local` | n/a | Keep | Expand benchmark coverage in Phase 10; continue keyword-only search | Maintained in-repo; direct filesystem access only. Renamed from `devai-skill-server` at v2.0.0. |
| `filesystem` | Anthropic official (`@modelcontextprotocol/server-filesystem`) | Scoped read/write of files in a user-specified directory | None at the MCP layer; file contents flow to Claude as tool results (standard Claude Code behavior, not MCP-specific) | `already-local` | n/a | Keep | Keep | Anthropic-maintained reference server; no network activity. |
| `memory` | Anthropic official (`@modelcontextprotocol/server-memory`) | Persistent per-session memory store over a local file | None | `already-local` | n/a | Keep | Keep | Anthropic-maintained. Local JSON store. |
| `sequential-thinking` | Anthropic official (`@modelcontextprotocol/server-sequential-thinking`) | Structured step-by-step reasoning scaffold | None | `already-local` | n/a | Keep | Keep | Anthropic-maintained. No I/O beyond the tool call. |
| `sqlite` | Anthropic official (`@modelcontextprotocol/server-sqlite`) | Query a SQLite database file | None at the MCP layer; query results flow to Claude | `already-local` | n/a | Keep | Keep | Anthropic-maintained. SQLite is a local file. |

---

### Skill-native adoptions (no MCP, no outbound calls)

These rows record catalog content adopted from an external project under the `skill-native` classification: Markdown skills with zero runtime dependencies added to Nexus-Hub. One row (the v3.16.5 cinematic scroll-scrub adoption) also ships a zero-dependency browser asset reverse-engineered from the upstream engine and is therefore classified `re-full` for that half; it still adds no runtime dependency, no credential, and no outbound call. Per the Reverse-Engineering Attribution Rule, the upstream repo is named only here, never in the distributed artifact. The action columns are version-scoped to the adopting release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v2.2.0 action | v2.3.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (skill, not MCP) | external SDK skill at google-antigravity/antigravity-sdk-python skills/google-antigravity-sdk/ (pinned to v0.1.1 commit observed on 2026-05-21) | Builds autonomous AI agents on the Google Antigravity backend with async agent loop, hooks, policies, MCP integration, multimodal ingestion, triggers, subagents, structured output | None at the Nexus-Hub catalog layer. The SDK itself reaches the Gemini API at user runtime, but Nexus-Hub does not execute the SDK -- the skill teaches the user to install it in their own project | `skill-native` | n/a (already skill-native) | Adopted as catalog/skills/ai-development/google-antigravity-sdk/ in this plan (sub-tasks 1.1-1.3). Source content rewritten to Nexus-Hub tone; upstream repo stripped from every user-facing line per the Reverse-Engineering Attribution Rule | Track upstream SDK releases for material changes (default-model bump, new lifecycle hooks, breaking API changes); refresh the references and examples when meaningful | Source skill at github.com/google-antigravity/antigravity-sdk-python (skills/google-antigravity-sdk/SKILL.md + 7 reference docs + 12 example docs). Apache-2.0 licensed. Classified skill-native because the entire artifact is Markdown content in Anthropic's Agent Skill format -- zero code, zero runtime dependencies in Nexus-Hub. Hard-no items (google-genai runtime dep, bundled Go local-harness binary, Vercel/Context7 skills CLI distribution) explicitly NOT adopted; see docs/archive/v2/v2.2/comparison-antigravity-sdk-python.md Section 13 N1-N4 |
| n/a (skill, not MCP) | external prose anti-slop-editing skill at petergyang/no-ai-slop (MIT-licensed single-skill SKILL.md catalog) | Removes 20+ named AI-slop prose patterns (each with a quoted smell and a before/after fix) while preserving the writer's voice, with a dual Edit/Detect mode and a self-check loop | None at the Nexus-Hub catalog layer, and none at user runtime -- pure Markdown instructions to the agent's own LLM (no code, no dependency, no API key, no outbound call) | `skill-native` | n/a (already skill-native) | Adopted as `catalog/skills/developer-experience/anti-slop-editing/` in the v3.15.3 plan (Phase 1: `SKILL.md` + `references/slop-wordlist.md` + `references/self-check.md` + `evals/trigger-cases.json`). Source content rewritten to Nexus-Hub tone; the upstream repo, product, author, and course name are stripped from every user-facing line per the Reverse-Engineering Attribution Rule | Refresh the named-pattern catalog and word lists if the prose-slop failure modes shift; there is no runtime to track | Source skill at github.com/petergyang/no-ai-slop (`SKILL.md` + `eval.md` + a 4-line `agents/openai.yaml` interface stub). MIT-licensed. Classified `skill-native` because the entire artifact is Markdown content in the Agent Skill format -- zero code, zero runtime dependency, zero outbound call, zero credential, no new commercial relationship. Hard-no / NOT-adopted items: the `agents/openai.yaml` cross-harness stub and the paste-a-URL install model (both superseded by Nexus-Hub's installer + integration registry), and the `behindthecraft.com` marketing links. The em-dash guidance was tightened to the project ceiling (no em-dashes, no clause-joining spaced hyphens) rather than the source's softer "one or two are fine". Full analysis: [docs/releases/v3/v3.15/comparisons/v3.15.3-comparison-no-ai-slop.md](../releases/v3/v3.15/comparisons/v3.15.3-comparison-no-ai-slop.md) |
| n/a (skill + asset, not MCP) | external cinematic scroll-scrub skill at oso95/scroll-world (MIT-licensed single-skill plugin catalog, v0.8.0; `skills/scroll-world/SKILL.md` ~50 KB, `references/scrub-engine.js` ~29 KB, `references/pipeline.md`, `references/prompts.md`, `references/knockout.py`, `references/index-template.html`) | Builds a scroll-scrubbed landing page in which page scroll drives a pre-rendered camera flight continuously through connected scenes: blob-seek video scrubbing, dive + connector segment chaining, frame-accurate seam crossfade, per-section scroll / linger remapping, mobile seek-coalescing, iOS muted priming, and a `prefers-reduced-motion` stills-only fallback | **None as adopted.** The adopted engine is vanilla DOM/CSS with no `fetch`, no `XMLHttpRequest`, no WebSocket, no `eval`, no cookie access and no off-host URL (enforced by `tests/skills/test_presentify_cinematic.py`, which scans the engine's CODE with comments stripped). Clips load from `data:` URIs or Blob object URLs so the output stays one offline file. The UPSTREAM pack's outbound surface - paid Monid Seedance / Higgsfield generation CLIs, `curl` of result URLs, optional Codex `image_gen`, ~$27 for a 6-scene 1080p chain - was NOT adopted | `re-full` (the engine) + `skill-native` (the protocol) | Medium (the engine is ~340 lines of vanilla JS rebuilt from the described capabilities; the protocol is prose) | Adopted in v3.16.5 Phase 6 as `catalog/skills/specialized-domains/document-to-interactive-html/assets/scroll-scrub-engine.js` (engine, `re-full`) plus `references/scroll-scrub.md` (protocol, `skill-native`), wired as the opt-in `cinematic` interactivity level of `/presentify`. Generic naming throughout (`scroll-scrub`, `ss-` CSS prefix); no upstream product, repository, vendor, or author name appears in any distributed artifact, per the Reverse-Engineering Attribution Rule | Track nothing upstream: there is no runtime dependency and no API to follow. Revisit only if the single-file offline output contract changes, which would be the one thing that could make a sibling-asset layout acceptable | Source at github.com/oso95/scroll-world, MIT. Security scan (v3.16.5 comparison, Step 1.5): PROCEED-WITH-CAUTION - no prompt injection, no destructive code, no supply-chain risk, but the SKILL.md / `pipeline.md` / README heavily direct the adopting agent to run paid generation CLIs. That generation pipeline is the **generation-as-service Hard-No** and was dropped in full; only the local, zero-dependency engine mechanics and the presentation protocol were reverse-engineered. Also NOT adopted: the competing `/scroll-world` slash command (cinematic is a level of `/presentify`, not a fork), the multi-file sibling `.mp4` asset layout (breaks the offline single-file guarantee), the blank brand-industry interview (presentify presents existing documents rather than authoring new brand narratives), the paid native 9:16 portrait chain, and `references/knockout.py` (a local PIL background flood-fill for diorama stills - deferred with no active call site; see the v3.16.5 known-gaps DF-3). Full analysis: [docs/releases/v3/v3.16/comparisons/v3.16.5-comparison-presentify-scroll-scrub.md](../releases/v3/v3.16/comparisons/v3.16.5-comparison-presentify-scroll-scrub.md) |

### Skill-native adoptions in v3.20.2 (interface-craft cluster)

These rows record the v3.20.2 interface-craft cluster. Every item is original Markdown: no copied prose, tables, examples, or `better-*` names from the comparison source. The source is named only in this Rationale column (Reverse-Engineering Attribution Rule). No outbound call, API key, third-party processor, or runtime dependency is introduced. `npx skills add` distribution was declined (decision-tree tier 5); Nexus-Hub's installer already distributes the catalog.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.20.2 action | Follow-up | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (skill, not MCP) | Interface-craft comparison cluster (accessibility candidate A1) | Semantic names, keyboard and focus, forms, hit areas, reduced motion, zoom/reflow; decides contrast severity and heading outline | None. Pure Markdown instructions to the agent's own LLM | `skill-native` | n/a (already skill-native) | **Adopted** as `catalog/skills/developer-experience/accessibility-engineering/` with a `references/` bundle and `evals/trigger-cases.json`. Written originally; source phrasing not reused | Remaining cluster skills land in later phases of the same plan | Source identified in [docs/releases/v3/v3.20/comparisons/v3.20.2-comparison-interface-craft-skills.md](../releases/v3/v3.20/comparisons/v3.20.2-comparison-interface-craft-skills.md) (GitHub `jakubkrehel/skills`, MIT). Classified `skill-native` because the capability is instructing the agent. Hard-no / NOT adopted: the source's `npx skills add` registry channel |
| n/a (skill, not MCP) | Interface-craft comparison cluster (layout candidate A5) | Spatial grouping, spacing scales, breakpoints, logical properties / RTL mirroring | None. Pure Markdown | `skill-native` | n/a | **Adopted** as `catalog/skills/developer-experience/layout-and-spacing/` | Coordinator in Phase 6 | Same comparison. Original writing. Focus order stays on `accessibility-engineering` |
| n/a (skill, not MCP) | Interface-craft comparison cluster (copy candidate A4) | In-product microcopy: actions, errors, empty states, settings, confirmations | None. Pure Markdown | `skill-native` | n/a | **Adopted** as `catalog/skills/developer-experience/interface-copy/` | Coordinator in Phase 6 | Same comparison. Original writing. Docs and anti-slop stay on their existing skills |
| n/a (skill, not MCP) | Interface-craft comparison cluster (typography candidate A2) | Font loading, type scale, wrapping, truncation; visual heading size only | None. Pure Markdown | `skill-native` | n/a | **Adopted** as `catalog/skills/developer-experience/web-typography/` with a `references/` bundle | Coordinator in Phase 6 | Same comparison. Original writing. Heading ranks stay on `accessibility-engineering`; wording stays on `interface-copy` |
| n/a (skill, not MCP) | Interface-craft comparison cluster (color candidate A3) | OKLCH palettes, measure rendered contrast pairs, gamut fallbacks, semantic roles | None. Pure Markdown | `skill-native` | n/a | **Adopted** as `catalog/skills/developer-experience/color-systems/` with a `references/` bundle | Coordinator in Phase 6 | Same comparison. Original writing. Contrast severity stays on `accessibility-engineering`; applying supplied tokens stays on `theme-tokens` / `brand-styling` |
| n/a (skill, not MCP) | Interface-craft comparison cluster (coordinator candidate A8) | Orchestrate delegates in a fixed order; one consolidated verdict; missing-delegate honesty; finding cap | None. Pure Markdown | `skill-native` | n/a | **Adopted** as `catalog/skills/developer-experience/interface-review/` | Cluster complete | Same comparison. Original writing. Owns orchestration only; does not restate delegate rules |

---

### Re-full platform-integration adoptions (no MCP, no outbound calls)

These rows record platform-distribution reach adopted from an external project under the `re-full` classification: local file-transform logic rebuilt as `IntegrationBase` subclasses under `scripts/lib/integrations/`, adding no MCP, no outbound call, no dependency, and no credential. Per the Reverse-Engineering Attribution Rule, the upstream repo is named only here, never in the distributed artifact (the subclasses, templates, installer blocks, and AGENTS.md use only generic platform names). The action columns are version-scoped to the adopting release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.4.0 action | v3.5.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (integration subclasses, not MCP) | external multi-platform converter at msitarzewski/agency-agents (`convert.sh` single-canonical-source -> per-platform transform; MIT) | Extends Nexus-Hub's platform reach to Aider (project-root `CONVENTIONS.md`) and Windsurf (project-root `.windsurfrules` + global `~/.codeium/windsurf/memories/global_rules.md`) - behavioral-guidance surfaces carrying the Nexus-Hub instruction content + `{{SKILL_INDEX}}` block | None. Pure local file emission via the existing `MarkdownIntegration` base (stdlib `pathlib`/`shutil`, shared marker-merge). No network, no credential | `re-full` | Medium | Adopted as `scripts/lib/integrations/aider.py` + `windsurf.py` (Phase 2, sub-tasks 2.2-2.3), registered in `_register_builtins()`, wired into both installers' global + workspace blocks, with new `templates/ai-instructions/base-aider.md` / `base-windsurf.md`. Implemented generically; upstream repo stripped from every user-facing artifact per the Reverse-Engineering Attribution Rule | The C2 canonical-source -> declarative per-platform transform refactor remains deferred to backlog | Source converter at github.com/msitarzewski/agency-agents (`convert.sh` + per-platform target templates). MIT-licensed. Classified `re-full` because the conversion is local file-transform logic with no third-party data destination - reverse-engineering it into native `IntegrationBase` subclasses is a strict reach gain with zero new outbound surface. The platform file conventions themselves (Aider's `CONVENTIONS.md`, Windsurf's `.windsurfrules` / `global_rules.md`) are public, vendor-published conventions. Hard-no items from the same source explicitly NOT adopted: personality/vibe theater + business-division breadth (identity conflict) and multilingual catalog (ASCII-only style-guide conflict); see docs/v3/v3.4/comparisons/v3.4.0-comparison-nessie-and-agency-agents.md |
| n/a (integration subclasses, not MCP) | same external multi-platform converter at msitarzewski/agency-agents (`convert.sh` per-platform transform; MIT) | A3-ext: extends platform reach to Kimi (project-local `.kimi/system.md` + `.kimi/agent.yaml`), Qwen (project-root `QWEN.md`), and OpenClaw (project-local `.openclaw/` SOUL + AGENTS + IDENTITY split) - behavioral-guidance surfaces carrying the Nexus-Hub instruction content + `{{SKILL_INDEX}}` block in the primary file | None. Pure local file emission via the existing `MarkdownIntegration` base + a shared `_write_generated` companion helper (stdlib `pathlib`, shared marker-merge for the primary file, dedicated deterministic writes for companions). No network, no credential | `re-full` | Medium | Adopted as `scripts/lib/integrations/kimi.py` + `qwen.py` + `openclaw.py` (Phase 4, sub-tasks 4.2), registered in `_register_builtins()`, wired into both installers' global + workspace blocks, with new `templates/ai-instructions/base-kimi.md` / `base-qwen.md` / `base-openclaw.md`. Reuses the Phase 2 Aider/Windsurf pattern; global scope skips-with-note unless the platform config root (`~/.kimi`, `~/.qwen`, `~/.openclaw`) is detected. Implemented generically; upstream repo stripped from every user-facing artifact per the Reverse-Engineering Attribution Rule | The C2 canonical-source -> declarative per-platform transform refactor remains deferred to backlog | Source converter at github.com/msitarzewski/agency-agents (`convert.sh` + per-platform target templates). MIT-licensed. Classified `re-full` for the same reason as the Aider/Windsurf row: local file-transform logic, no third-party data destination, strict reach gain with zero new outbound surface. The platform file conventions (Kimi's `agent.yaml` + `system.md`, Qwen's `QWEN.md`, OpenClaw's SOUL/AGENTS/IDENTITY split) are public, vendor-published conventions. See docs/v3/v3.4/comparisons/v3.4.0-comparison-nessie-and-agency-agents.md |

---

### Dropped in v1.0.0 (Reverse-Engineered or Drop-Outright)

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v1.0.0 action | v1.1.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `context7` | Upstash (`@upstash/context7-mcp`) | Up-to-date library documentation lookup | Every query goes to Upstash servers; library names and search queries leave the local machine | `re-partial` | Medium | **Drop.** Ship the `local-docs-lookup` skill (Phase 7.3) covering local pydoc / go doc / vendored README patterns. Continuously-updated library index capability is documented as a gap. | Consider a local documentation cache MCP only if demand materializes; accept the staleness tradeoff explicitly. | Upstash runs a hosted indexed library corpus; the freshness aspect cannot be fully replicated locally without re-crawling and re-indexing upstream sources. Query text (often generated by the agent from local context) is the leak vector. |
| `exa-web-search` | Exa (`exa-mcp-server`) | Neural web search | Every query goes to exa.ai; full search queries (often agent-composed from local context) leave the machine | `drop-outright` | n/a | **Drop.** No replacement ships in v1.0.0; users who need web search can add the entry back to their own settings, aware of the cost. | None planned. Web search by definition requires a third-party service; the trust cost exceeds the benefit for the target regulated-industry / high-trust profile. | The web is not reverse-engineerable. Query-to-third-party is the entire capability. |
| `firecrawl` | Firecrawl (`firecrawl-mcp`) | Web scraping / site crawl with HTML extraction | URLs and optional auth sent to firecrawl.dev; scraped content returned via that intermediary | `re-full` | Medium | **Drop and replace** with the internal `nexus-web-fetch` MCP (Phase 7.1; renamed from `devai-web-fetch` at v2.0.0). HTTPS fetch goes directly to the target URL; no third-party intermediary. RFC 1918 blocked by default for SSRF safety. | Add optional Playwright-based JS rendering (currently raises `NotImplementedError`). | Scraping is fundamentally HTTP fetch + HTML extraction. Both are standard stdlib + well-known open-source libraries (`httpx`, `beautifulsoup4`, `readability-lxml`). Eliminating the Firecrawl intermediary is a strict improvement. |
| `magic-ui` | 21st.dev (`@21st-dev/magic@latest`) | UI component generation via an external LLM call | Component specs and design intent sent to 21st.dev's generation service | `skill-native` | Small | **Drop and replace** with the `ui-component-generation` skill (Phase 7.2). Zero code - the skill instructs the agent to generate components directly using its own LLM. | None planned. | The capability is "ask an LLM to generate UI code." The agent is already an LLM. The correct replacement is skill documentation, not an MCP. |

---

### Kept as Vendor-Intrinsic Wrappers (Your-Own-Account)

For each row below, the v1.0.0 `_comment` field (Phase 3) must inline the 5-question audit from the **Rationale** column.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v1.0.0 action | v1.1.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `github` | Anthropic-maintained GitHub MCP (`@modelcontextprotocol/server-github`) | Interact with GitHub repositories, issues, PRs | GitHub API with a user-supplied PAT; PAT scope gates reach | `vendor-intrinsic` | Medium | Keep as vendor wrapper. `_comment` audit: (1) user's agent spawns `npx` subprocess; (2) calls github.com/api with user's PAT; (3) requires GITHUB_TOKEN / PAT; (4) transmits whatever the agent supplies (issue bodies, queries) which may include repo context; (5) user already has a GitHub account and repo. Vendor-intrinsic: GitHub is the intended destination. | Build `nexus-github` internal wrapper using `gh` CLI or `PyGithub` for audit/supply-chain improvement. Data flow unchanged. | GitHub is where the user's code already lives. The MCP gives a typed tool surface over an API the user is already paying for / bound to. Reverse-engineering changes the implementer but not the data destination. |
| `postgres` | Anthropic-maintained (`@modelcontextprotocol/server-postgres`) | Query a Postgres database via user-supplied connection string | Postgres wire protocol to the user's own database | `vendor-intrinsic` | Small | Keep. `_comment` audit: (1) `npx` subprocess; (2) Postgres connection to the user's DB at DATABASE_URL; (3) requires DATABASE_URL; (4) query results flow to Claude (not to a new third party); (5) user owns the DB. Vendor-intrinsic: the DB IS the destination. | Build `nexus-postgres` internal wrapper over `psycopg` if audit-posture demand emerges. | The DB belongs to the user. No third-party data processor is introduced. Wrapping is a convenience over the wire protocol. |
| `supabase` | Supabase official (`@supabase/mcp-server-supabase`) | Interact with a user's Supabase project | Supabase API with the user's access token | `vendor-intrinsic` | Medium | Keep. `_comment` audit: (1) `npx`; (2) supabase.com/api with user's access token; (3) requires SUPABASE_ACCESS_TOKEN; (4) transmits user-provided queries / rows; (5) user is a Supabase customer. | Build `nexus-supabase` internal wrapper over `supabase-py`. Data flow unchanged. | Supabase is the user's backend. Vendor-intrinsic. |
| `railway` | Railway official (`@railway/mcp-server`) | Manage Railway deployments | Railway API with RAILWAY_API_TOKEN | `vendor-intrinsic` | Medium | Keep. `_comment` audit: (1) `npx`; (2) railway.app/api with token; (3) RAILWAY_API_TOKEN; (4) deployment metadata and user commands; (5) user is a Railway customer. | Build `nexus-railway` internal wrapper if demand. | Vendor-intrinsic per the decision tree. |
| `vercel` | Vercel official (`@vercel/mcp-adapter`) | Interact with Vercel deployments | Vercel API with VERCEL_TOKEN | `vendor-intrinsic` | Medium | Keep. `_comment` audit: (1) `npx`; (2) vercel.com/api with token; (3) VERCEL_TOKEN; (4) deployment metadata and user commands; (5) user is a Vercel customer. | Build `nexus-vercel` internal wrapper if demand. | Vendor-intrinsic. |
| `cloudflare` | Cloudflare official (`@cloudflare/mcp-server-cloudflare`) | Manage Cloudflare resources | Cloudflare API with CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID | `vendor-intrinsic` | Medium | Keep. `_comment` audit: (1) `npx`; (2) api.cloudflare.com with token; (3) CLOUDFLARE_API_TOKEN + CLOUDFLARE_ACCOUNT_ID; (4) resource queries and user commands; (5) user is a Cloudflare customer. | Build `nexus-cloudflare` internal wrapper if demand. | Vendor-intrinsic. |

---

### New in v1.0.0 (Reverse-Engineered Internal MCPs)

These land via Phases 6 and 7 of the plan. Registry entries are added by Phase 3 (for the `_comment` audit template structure) and Phase 6/7 (for the entry body itself).

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v1.0.0 action | v1.1.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `nexus-code-search` | Nexus-Hub internal (new; `extensions/nexus-code-search/`, Python 3.10+) | Local code search over a repo: `index_codebase`, `search_code`, `clear_index`, `get_indexing_status`. Keyword-only in v1.0.0 (inverted index + rapidfuzz). Content-hash incremental re-indexing. | None. Indexes are persisted under `<repo>/.nexus/code-index/`. No network. | `already-local` | n/a (this IS the reverse-engineering target) | Build and ship as keyword-only MVP. | Add dense retrieval (ONNX embeddings via `fastembed` + `sqlite-vec` vector store) + RRF hybrid mode; add tree-sitter AST chunking; upgrade the flat manifest to a directory-keyed Merkle tree. | Reverse-engineers one common "semantic code search" pattern into fully local code. No external attribution in the shipped artifact per the policy. Renamed from `devai-code-search` at v2.0.0 (index path also moved from `.devai/code-index/` to `.nexus/code-index/`). v2.1.0 (`adoption-codesight`) extends the extension into a compiled context-map capability -- framework route/env/middleware extraction, ORM schema (fields/keys/relations), component + event detection, file-level hot-file ranking, a git-scoped `--since` change map, a deterministic token-savings benchmark, a map-health lint, and a knowledge-map extractor -- emitted to a neutral `<repo>/.nexus/CONTEXT-MAP.md` + `.nexus/context/` article set (never CLAUDE.md / AGENTS.md). This reverse-engineers one external "compile the codebase into an AI-context map" tool (MIT, fully local); a direct dependency on that tool was rejected under the policy (tier-3 RE beats a tier-4 wrapper -- the logic is reimplementable locally over the extension's existing tree-sitter graph), and its WASM-plugin download-and-run distribution was rejected as an unnecessary supply-chain surface. Shipped code, tool names (`generate_context_map`, `map_health`, `generate_knowledge_map`, `nexus-hub map`), and the extension README use generic descriptive names with no external attribution; provenance is recorded only in this column per the Reverse-Engineering Attribution Rule. Adds no outbound call, API key, or third-party data processor -- still `already-local`. |
| `nexus-web-fetch` | Nexus-Hub internal (new; `extensions/nexus-web-fetch/`, Python 3.10+) | Local web fetch: `fetch_url(url, render_js, extract_mode)`. `httpx` for fetch; `beautifulsoup4` + `readability-lxml` for extraction. SSRF guard blocks RFC 1918 / loopback / link-local / `file://` by default. | HTTPS only, to user-specified URLs. No third-party intermediary. | `already-local` (as reverse-engineered target; see also: `firecrawl` row above) | n/a (this IS the reverse-engineering target) | Build and ship. `render_js=True` reserved for v1.1.0 (raises `NotImplementedError`). | Add optional Playwright-based JS rendering. Consider adding per-domain rate limiting. | Scraping-as-service is replaced with fetch-to-target-URL. Data destination is the URL itself, not a third-party processor. Renamed from `devai-web-fetch` at v2.0.0 (config path moved from `~/.devai/web-fetch.yaml` to `~/.nexus/web-fetch.yaml`). |

---

### New in v3.2.0 (Reverse-Engineered Internal MCP)

Added by the v3.2.0 `adoption-headroom` plan (Phase 4). Registry entry lives in `catalog/mcp-configs/mcp-servers.json` with the 5-question audit inlined in its `_comment`.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.2.0 action | v3.3.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `nexus-context-compressor` | Nexus-Hub internal (new; `extensions/nexus-context-compressor/`, Python 3.10+) | Local-first, reversible context compression: `context_compress(payload, persist)` compresses structured tool output (JSON arrays, code) and leaves logs/prose untouched; `context_retrieve(marker)` resolves a `<<ccr:HASH N_rows>>` marker back to the exact dropped records. Also a PreToolUse hook (`catalog/hooks/compress-output.sh`) and a CLI (`python -m nexus_context_compressor compress`). | None. Deterministic stdlib strategies; payloads are compressed in-process and dropped spans persist to a local SQLite CCR store under `~/.nexus-hub/cache/`. No network. | `re-full` | Medium | Build and ship as the owned replacement for the external `rtk` context-compression recommendation. Deterministic strategies (SmartCrusher / CacheAligner / ContentRouter / CodeCompressor) + reversible CCR retrieval. The optional ML token-dropper (default-off) lands in Phase 6. | Free-text/log compression via the optional Phase 6 ML token-dropper; an accuracy-regression gate (Phase 5). | Replaces a "trust a third-party GitHub binary installed via `cargo install --git`" posture (the external `rtk` Rust proxy) with an owned, audited, fully-local engine. rtk is command-output-only and lossy; this engine is reversible (CCR store) and routes by content type. The compression logic is pure Python + stdlib, so reverse-engineering it locally is a strict supply-chain improvement with no data-flow change (there was never an outbound call to remove -- rtk is local too -- but the trust surface of an unaudited external binary is eliminated). Upstream name appears only in this column per the Reverse-Engineering Attribution Rule. |

---

### New in v3.19.1 (Local persistent agent-memory store)

Added by the v3.19.1 agent-memory substrate plan (Phase 5). This is a local CLI package, not an MCP server. The row exists so the registry decision is recorded next to the other internal extensions.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.19.1 action | Later action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `nexus-memory` | Nexus-Hub internal (new; `extensions/nexus-memory/`, Python 3.10+) | Append-only local store of lasting agent-memory entries with an age-decaying summary tree. CLI: `read`, `record`, `merge`, `search`, `zoom`, `drop`. Compression is performed by the calling agent; the package never calls a model. | None. Stdlib file I/O only. Default root is `~/.nexus-hub/memory/`. No network module is imported on any path. | `already-local` | n/a (this IS the reverse-engineering target) | Build and ship the local store plus the `agent-memory` skill. Do not register an MCP server. | None planned. | The capability was reverse-engineered into a local package rather than adopted as an external runtime. Shipped names are generic (`nexus-memory`, `agent-memory`). Upstream names appear only in this column if a future maintainer needs provenance; the distributed artifact names none. Zero outbound, zero API keys, zero model downloads. |

---

### Reverted in v1.0.0

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v1.0.0 action | v1.1.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| `claude-context` | Zilliz (`@zilliz/claude-context-mcp`) | Semantic code search over a user's codebase | Ships code chunks to OpenAI's embedding API; vectors stored in Milvus (local) or Zilliz Cloud (managed) | `re-partial` | Large | **Reverted** (was added in an aborted v0.9.8 Phase 2, now undone). The knowledge pattern is reverse-engineered into `nexus-code-search` (Phase 6; renamed from `devai-code-search` at v2.0.0) + the `code-semantic-search` skill (Phase 8). | v1.1.0 adds dense/hybrid retrieval to `nexus-code-search` using a fully-local embedding backend (`fastembed` ONNX), closing the capability gap without the upstream's third-party data flow. | This entry is the entire reason the v1.0.0 plan exists. The upstream is an 8.4k-star MIT project whose default flow ships source code to OpenAI for embedding; that is the hard-no case. The knowledge is valuable and can be reverse-engineered; the external MCP cannot be shipped. |

---

### Declined in v3.6.0 (Drop-Outright under the MCP Registry Policy)

These rows record two GitHub Spec Kit extensibility systems that the v3.6.0 re-comparison evaluated and **deliberately declined** under the policy decision tree. They are not MCPs; they are recorded here so the decline is durable -- a future comparison reads these rows and recognizes the candidates as already-adjudicated rather than re-surfacing them as fresh gaps (the same discipline that closed the v2.0.0 G1-G12 series). Both are `drop-outright` because each crosses the trust boundary the policy exists to govern (third-party code or plaintext credentials), and neither can be reverse-engineered into a local-first equivalent without re-introducing exactly the surface that triggers the decline. The action columns are version-scoped to the declining release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.6.0 action | v3.7.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (external auth framework, not MCP) | GitHub Spec Kit authentication framework (`src/specify_cli/authentication/{base,github,azure_devops,http,config}.py`); a pluggable provider registry (GitHub Bearer, Azure DevOps Basic-PAT) injecting auth headers for private remote template / extension / workflow catalog fetches, opt-in via `~/.specify/auth.json` | Stores GitHub / Azure DevOps PAT credentials to authenticate fetches of private remote catalogs | Authenticated HTTPS requests to GitHub / Azure DevOps; the PAT is stored in **plaintext** in a user dotfile, reachable by any co-resident extension or shell step | `drop-outright` | n/a (no local-first equivalent is possible without re-introducing the credential surface) | **Dropped.** Not adopted in any form -- no auth provider, no credential store, no remote-catalog fetch added. v3.6.0 introduces zero new credential surface. | None planned. Reconsider only in the architecturally-absent event that Nexus-Hub ever ships a private *remote* catalog (today the catalog IS the repo, installed locally), and even then only with a secrets-manager / OS-keychain backend, never plaintext-dotfile storage. | Plaintext PAT storage directly contradicts the secret-handling rules in `catalog/rules/*/security.md` (no plaintext long-lived credentials in committed or dotfile config), and remote credentialed catalog fetch is N/A for a local-first catalog that ships in the repo and installs locally. Upstream evidence: `src/specify_cli/authentication/{github,azure_devops}.py`. Full analysis: [docs/releases/v3/v3.6/comparisons/v3.6.0-comparison-spec-kit.md](../v3.6.0/comparison-spec-kit.md) Section 6.3 + Bucket D (candidate N5). |
| n/a (external extension-install channel, not MCP) | GitHub Spec Kit third-party extension system (`extensions/RFC-EXTENSION-SYSTEM.md`, `extensions/EXTENSION-API-REFERENCE.md`, `src/specify_cli/extensions.py`); installs unsandboxed community-catalog plugin code (commands / hooks / templates) into `.specify/extensions/`, layered at runtime with agent privileges | A code-distribution channel that fetches and runs third-party plugin code from a dual curated / community catalog | Remote catalog fetch of third-party code; the installed code runs with agent privileges (no sandbox), can read env / config, and is an exfiltration surface | `drop-outright` (the *capability* is already reverse-engineered into the local skill catalog) | n/a -- the local-first equivalent already exists and is safer | **Dropped.** No unsandboxed third-party-code install path is added. The capability (extend the agent with new commands / skills) is already met by Nexus-Hub's skill catalog (`data/marketplace.json` + `data/bundles.json` + `nexus-skill-server` + `/skills import`) WITH a pre-install scanner (`skill-security-scan` skill + `nexus-skill-scanner` MCP) that the upstream lacks. The v3.6.0 N6 work (Phase 4) further hardened that import path (HTTPS-only + discovery-only flag + hash-on-import). | None planned. The scanned local skill mechanism is the reverse-engineered answer; adopting an unsandboxed community-catalog code-install would be a net **regression** in trust posture, not a gain. | Code-distribution-as-service is on the MCP Registry Policy hard-no spectrum. The upstream trust model is catalog curation + an `install_allowed` flag with **no** pre-install scanner; Nexus-Hub already exceeds it (Section 8 of the comparison). Upstream evidence: `extensions/RFC-EXTENSION-SYSTEM.md`, `src/specify_cli/extensions.py`. Full analysis: [docs/releases/v3/v3.6/comparisons/v3.6.0-comparison-spec-kit.md](../v3.6.0/comparison-spec-kit.md) Section 6.3 + Bucket D (candidate N1b). |

---

### Adopted in v3.7.0 (previously deferred -- N4 self-upgrade)

This row records N4 (a self-upgrade CLI), deferred in v3.6.0 as low-ROI (DF-v36-1) and reprioritized by the maintainer as part of the v3.7.0 install-UX overhaul. It is policy-clean because the only outbound call it adds is to the project's OWN GitHub (the same posture the installer/bootstrap already has) -- no third-party data processor, credential, or new dependency. The action columns are version-scoped to the adopting release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.7.0 action | v3.8.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (local self-upgrade CLI, not MCP) | GitHub Spec Kit `specify self upgrade` (a packaged-CLI in-place updater) | Ships a local `nexus-hub` CLI on PATH (`~/.nexus-hub/bin/nexus-hub` POSIX + `nexus-hub.cmd` Windows, thin shims over the stdlib-only `scripts/nexus_hub_cli.py` core). `nexus-hub --version` reads the installer-written `~/.nexus-hub/VERSION`; `nexus-hub upgrade` compares it to the latest `.claude-plugin/plugin.json` version on the project's own GitHub, prints the matching CHANGELOG block as a what's-new summary, and on confirmation re-runs the Phase 1 install bootstrap to upgrade in place | One version-check fetch to the project's OWN GitHub (`raw.githubusercontent.com` / `github.com`), preferring `curl`, falling back to `wget`, then stdlib `urllib`; the upgrade itself re-runs the existing bootstrap (also project-GitHub-only). No third-party processor, credential, or new dependency | `re-full` | Medium | **Adopted** in the install-ux-overhaul plan Phase 3 (sub-tasks 3.1-3.3): the `nexus-hub` launcher + version marker are installed by both installers (`install_cli_launcher` / `Install-CliLauncher`); offline / fetch-failure is handled with a clear message and a non-zero exit (no partial state). Implemented generically; the upstream is named only in this column per the Reverse-Engineering Attribution Rule | None planned. Revisit only if a richer self-management surface (channel pinning, rollback) becomes a stated goal | Reverse-engineers the *intent* of `specify self upgrade`, not its packaging: Nexus-Hub is a template catalog consumed from the repo (not a `uv`/`pip`-installed CLI), so the local equivalent is a launcher over the existing bootstrap, and the version check reuses the installer's existing project-GitHub posture rather than introducing a new data destination. Deferred as DF-v36-1 in v3.6.0; reprioritized for v3.7.0. Full analysis: [docs/releases/v3/v3.6/comparisons/v3.6.0-comparison-spec-kit.md](../v3.6.0/comparison-spec-kit.md) Bucket C (candidate N4) + [docs/releases/v3/v3.7/plans/install-ux-overhaul.md](../v3.7.0/plans/install-ux-overhaul.md) Phase 3. |

---

### Declined in the loop-engineering enrichment (Drop-Outright under the MCP Registry Policy)

These rows record three capabilities the loop-engineering enrichment evaluated against an executable loop runtime and **deliberately declined** under the policy decision tree. They are not MCPs; they are recorded here so the decline is durable -- a future comparison reads these rows and recognizes the candidates as already-adjudicated rather than re-surfacing them as fresh gaps. Each is `drop-outright` because it crosses a trust or scope boundary the policy governs, and each already has a local-first equivalent in the catalog. The upstream reference lives only in the comparison report per the Reverse-Engineering Attribution Rule.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | Action | Later action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (external cloud sandbox, not MCP) | A cloud code-execution sandbox that runs the writable agent iteration in a remote container behind a paid account | Executes each writable loop iteration in third-party cloud compute | Uploads project source and prompt content to a third-party compute API; requires a paid-account credential | `drop-outright` | n/a -- the local container is the zero-egress equivalent | **Dropped.** Not adopted. The local-only "Sandboxing an Unattended Loop" subsection in `loop-engineering/SKILL.md` (composing `containerization` + `agent-access-policy` + `using-git-worktrees`) is the reverse-engineered, zero-egress equivalent. | None planned. Reconsider only if a remote-compute requirement becomes a stated goal, and even then prefer the local container. | Compute / data-egress-as-service is on the policy hard-no spectrum: it egresses source code and prompts off the machine and adds a credential plus a commercial relationship. Full analysis: [docs/releases/v3/v3.8/comparisons/v3.8.0-comparison-ralph-claude-code.md](../v3.8.0/comparison-ralph-claude-code.md). |
| n/a (external task-queue runtime, not MCP) | A dependency-DAG task queue that orders and schedules multi-item loop work by dependency | Maintains a directed-acyclic task graph for loop runs | None inherent, but presupposes a bespoke queue runtime layered into the loop | `drop-outright` | n/a -- the local-first equivalent already exists | **Dropped.** Redundant with GitHub issues + the `tasks-to-issues` skill + the `dev-progress-tracker` / `known-gaps-tracker` memory layer, which already persist and order loop work items locally. | None planned. | A new queue runtime would duplicate the issue-tracker + memory-layer skills and introduce a runtime the catalog deliberately avoids. Full analysis: [docs/releases/v3/v3.8/comparisons/v3.8.0-comparison-ralph-claude-code.md](../v3.8.0/comparison-ralph-claude-code.md). |
| n/a (external loop runtime + config, not MCP) | A per-project loop runtime config file plus a standalone Bash loop runtime that drives iterations | Ships a loop runtime and its config file to drive the loop | None inherent; presupposes a shipped loop runtime | `drop-outright` / out-of-scope | n/a -- the declarative loop definition is the local-first equivalent | **Dropped.** Presupposes a shipped loop runtime, reversing the v3.1.0 decision that `/loop` and `/goal` are HOST commands Nexus-Hub references but never reimplements. The declarative loop definition in `references/loop-schema.md` plus the host driver is the local-first answer. | None planned. | Shipping a loop runtime contradicts the standing v3.1.0 host-driver decision; the loop-schema definition + host `/loop` / `/goal` already covers it. Full analysis: [docs/releases/v3/v3.8/comparisons/v3.8.0-comparison-ralph-claude-code.md](../v3.8.0/comparison-ralph-claude-code.md). |

---

### Declined or flagged in the v3.9.0 adoption cycle (loop-design coach + cross-model egress hygiene)

These rows record the capabilities the v3.9.0 adoption cycle evaluated and **deliberately declined or flagged**, so the decisions are durable -- a future comparison reads these rows and recognizes the candidates as already-adjudicated rather than re-surfacing them as fresh gaps. The genuinely-new contributions of that cycle were folded into `cross-model-orchestrator`, `loop-engineering`, `agent-access-policy`, and `context-pack-builder` as skill-native doctrine (no new MCP, outbound call, dependency, or credential). The upstream references live only in the comparison reports per the Reverse-Engineering Attribution Rule. Full analyses: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-looper.md](../v3.9.0/comparison-looper.md) (the design-time loop coach) and [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-deer-flow.md](../v3.9.0/comparison-deer-flow.md) (the cross-model agent runtime). The action columns are scoped to the declining release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | Action | Later action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (external loop runner script, not MCP) | A portable thin loop runner script that drives loop iterations from a resolved spec file | Ships a standalone runner that executes the loop body and re-invokes the agent each iteration | None inherent; presupposes a shipped loop runtime | `drop-outright` | n/a -- the declarative loop definition plus the host driver is the local-first equivalent | **Dropped.** Reverses the v3.1.0 host-driver decision; the "per-project loop runtime config + standalone loop runtime" row above (v3.8.0) is the direct precedent. `/loop` and `/goal` are HOST commands Nexus-Hub references but never reimplements, and the declarative loop definition in `references/loop-schema.md` already covers the loop body. | None planned. | A shipped loop runner contradicts the standing v3.1.0 host-driver decision, already adjudicated for the equivalent v3.8.0 candidate. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-looper.md](../v3.9.0/comparison-looper.md). |
| n/a (external spec-compile step, not MCP) | A design-spec-to-resolved-spec compile step that lowers a human-authored loop design into a machine-resolved spec for the runner above | Pre-processes a loop design file into a resolved spec consumed only by the declined runner | None inherent; exists only to feed the declined runner | `drop-outright` | n/a -- the declarative loop definition is authored directly, with no compile step needed | **Dropped.** Bundled with the runner decline: a compile step that exists only to feed a runtime Nexus-Hub does not ship has no standalone purpose. The loop definition in `references/loop-schema.md` is authored directly and read by the host driver; there is nothing to "compile". | None planned. | Same precedent as the runner row: it presupposes the declined runtime. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-looper.md](../v3.9.0/comparison-looper.md). |
| n/a (advisory cost caps, not MCP) | Advisory-only per-run cost caps that warn when a loop's spend crosses a soft threshold | Emits a warning when estimated spend exceeds an advisory limit, but does not stop the run | None inherent | not recommended (weaker than the existing control) | n/a | **Flagged, not adopted.** This is the opposite of an adoption: advisory-only caps are weaker than the HARD spending controls and budget gates the `ai-billing-safeguards` skill already provides. Adopting an advisory cap as the headline control would regress the posture; operators bound loop cost with the hard-cap skill, with an advisory warning at most a redundant secondary signal. | None planned. | Advisory caps under-protect relative to the existing hard-cap skill; recording the weaker alternative prevents it from being re-proposed as a gap. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-looper.md](../v3.9.0/comparison-looper.md). |
| n/a (convergent-validation finding, not a candidate) | A separate large agent runtime that independently authors agent capability as Markdown skill files loaded in tiers | Validates Nexus-Hub's own authoring model rather than introducing a gap | None relevant -- the runtime itself is out-of-category for a catalog | convergent validation (no action) | n/a | **Recorded as a finding, not a gap.** A separate large agent runtime independently converged on the same Markdown-`SKILL.md`-plus-three-tier-loading authoring model Nexus-Hub already uses; that is convergent-design validation of the existing model, not a missing capability. The runtime itself (a hosted multi-agent execution engine) is out-of-category for a skill catalog. The two low-priority doctrine notes it did surface (a default-deny host-execution posture and an optional typed-fact memory schema) were adopted into `agent-access-policy` and `context-pack-builder`. | None planned. | The finding strengthens confidence in the current authoring model; no registry or skill gap follows from it. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-deer-flow.md](../v3.9.0/comparison-deer-flow.md). |

---

### Declined in the v3.9.0 adoption cycle (local git-proxy verification-gate runtime + default-on telemetry)

These rows record the two capabilities the v3.9.0 pre-merge-verification adoption cycle evaluated and **deliberately declined**, so the decisions are durable -- a future comparison reads these rows and recognizes the candidates as already-adjudicated rather than re-surfacing them as fresh gaps. The genuinely-new contributions of that cycle were folded into `intent-based-review`, `receiving-code-review`, `multi-agent-code-review`, `shipping-and-launch`, `loop-engineering`, and `pr-description-writer` as skill-native doctrine (no new MCP, outbound call, dependency, or credential). The upstream reference lives only in the comparison report per the Reverse-Engineering Attribution Rule. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-no-mistakes.md](../v3.9.0/comparison-no-mistakes.md). The action columns are scoped to the declining release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | Action | Later action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (external host-side verification-gate runtime, not MCP) | A compiled host-side verification-gate runtime: a local bare-repo git proxy plus a background daemon, a terminal UI, an IPC layer, and a local SQLite state machine, sitting in the git push path to run a fixed verification pipeline in a disposable worktree before forwarding a branch to the real remote | Enforces a verification sequence in the git push path that the developer cannot accidentally skip | The user's own configured git remote and PR/CI host (their existing relationship); none added by reimplementation since it is declined | `drop-outright` | n/a (host-side runtime; not reimplemented) | **Dropped.** Not adopted in any form. It reverses no decision: the catalog references host runtimes and never reimplements them. The doctrine this runtime encodes was imported as the skill-native items in this cycle (the finding-action taxonomy + verbatim-escalation rule, the canonical pre-merge gate, the intent-as-verbatim-oracle guidance, round-history hygiene, the deterministic-PR-body pattern, and the stop-at-the-human-boundary doctrine). | None planned. Reconsider only if host-side verification enforcement becomes a stated product goal, and even then prefer referencing a host runtime over shipping one. | A compiled daemon + git-plumbing proxy is a control plane, not Markdown-plus-thin-scripts catalog content. Pre-adjudicated by the v3.1.0 `/loop` host-command decision and the v3.8.0 standalone-loop-runtime `drop-outright` row above. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-no-mistakes.md](../v3.9.0/comparison-no-mistakes.md). |
| n/a (default-on usage telemetry, not MCP) | Default-on usage telemetry in the same runtime: an analytics client that POSTs usage events (event name, OS/arch, build version, timestamp) to a third-party-owned analytics endpoint, opt-out only via an environment variable | Emits anonymous usage events to the author's analytics server by default | Egress-by-default to a third-party-owned analytics endpoint; the payload observed is usage-shaped (not source code), but the send happens unless the user opts out | `drop-outright` + cautionary | n/a | **Flagged as a cautionary not-recommended item, not adopted.** This is the egress-by-default analytics posture the MCP Registry Policy classifies as a hard-no, and it is the principal reason the comparison recommends importing the doctrine but not the tool. Nexus-Hub keeps its zero-telemetry posture; no telemetry of any kind was imported. | None planned. Cite this as a concrete example in the MCP Registry Policy rationale if useful. | Egress-by-default to a third-party-owned analytics endpoint is exactly the posture the MCP Registry Policy exists to prevent (outbound-by-default analytics is on the hard-no spectrum). Recording it durably documents why the tool itself is declined even though its doctrine is adopted. Full analysis: [docs/releases/v3/v3.9/comparisons/v3.9.0-comparison-no-mistakes.md](../v3.9.0/comparison-no-mistakes.md). |

---

### Declined in the v3.10.0 ruflo adoption cycle (six runtime drops)

These rows record the six runtime components the v3.10.0 adoption cycle evaluated and **deliberately dropped outright** under the MCP Registry Policy, so the decisions are durable -- a future comparison reads these rows and recognizes the candidates as already-adjudicated rather than re-surfacing them as fresh gaps. None of these reverses a standing decision: the catalog references runtimes and never reimplements them (the v3.1.0 `/loop` host-command decision and the v3.8.0 standalone-loop-runtime row above are the direct precedents). The adoptable substance of the source was imported in the same cycle as the skill-native and re-partial items -- a typed egress / PII-redaction skill (`egress-redaction`), a prompt-injection-defense skill (`prompt-injection-defense`), an iterative-rounds enrichment of `competitive-generation`, a local `nexus-hub verify` supply-chain command + release manifest, an agent-setup grade + regression diff in `harness_audit.py`, and two advisory worker-check hooks (`test-gap-notice`, `dependency-staleness-notice`). Per the Reverse-Engineering Attribution Rule, the prose stays generic: the upstream product and component brand names appear nowhere here; the cycle is identified only by the comparison report subject. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). The action columns are scoped to the declining release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | Action | Later action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (runtime meta-harness + MCP daemon, not an adoptable MCP) | A runtime meta-harness for an AI assistant: an MCP server plus always-on background-worker daemons | Runs an always-on control plane (the MCP server) and a set of background-worker daemons that auto-trigger checks on a timer | Always-on, multi-outbound runtime reaching multiple model and data providers | `drop-outright` | n/a (a control plane, not Markdown-plus-thin-scripts catalog content) | **Dropped.** Not adopted in any form. The adoptable *check ideas* from the worker catalog were imported instead as the two advisory, event-driven tool-event hooks in Phase 6.1 (`test-gap-notice`, `dependency-staleness-notice`), which exit 0 and never run as a daemon. | None planned. Reconsider only in the architecturally-absent event that an always-on runtime becomes a stated product goal, and even then prefer referencing a host runtime over shipping one. | An always-on, multi-outbound runtime inverts the catalog's zero-outbound-by-default posture; this is exactly the MCP-server-as-daemon plus background-worker-daemon surface the MCP Registry Policy governs. Pre-adjudicated by the v3.1.0 `/loop` host-command decision and the v3.8.0 standalone-loop-runtime `drop-outright` row above. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |
| n/a (GPU vector database + graph-RAG runtime, not MCP) | A GPU-accelerated vector database with a graph-augmented retrieval layer | Indexes content into a GPU-backed vector store and serves graph-augmented retrieval | Embeddings generation plus a heavy persistent runtime; embeddings-as-service on the policy hard-no spectrum | `drop-outright` | n/a (the retrieval doctrine is already local) | **Dropped.** The retrieval doctrine is already delivered locally by the `rag-implementation` and `code-semantic-search` skills plus the internal `nexus-code-search` MCP; no GPU vector DB or graph-RAG runtime is added. | None planned. Dense / hybrid retrieval on `nexus-code-search` (the existing v1.1.0+ backlog item) remains the local-first path if richer retrieval is wanted. | Embeddings-as-service plus a heavy persistent runtime is on the MCP Registry Policy hard-no list; the local-first equivalent already exists. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |
| n/a (multi-provider LLM router runtime, not MCP) | A self-learning multi-provider LLM router that runs as a service | Routes generation requests across several model providers from a running router process | Generation-as-service via a running router reaching multiple third-party model providers | `drop-outright` | n/a (covered as skills, not a runtime) | **Dropped.** The model- and provider-routing doctrine is already delivered as skills by `multi-provider-ai` and `model-routing` (the latter detects the platform and routes a task without a running router); no router runtime is added. | None planned. | Generation-as-service via a running router is on the MCP Registry Policy hard-no spectrum; routing is a skill-native capability the catalog already covers without a runtime. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |
| n/a (cross-machine federation layer, not MCP) | A cross-machine federation layer: an mTLS / ed25519 device-trust layer plus an optional encrypted mesh network | Federates agent state and work across machines over an authenticated, optionally mesh-networked transport | Cross-machine outbound plus a device-credential / key-management surface | `drop-outright` | n/a (out of scope for a local-first template catalog) | **Dropped.** Not adopted. Cross-machine federation is out of scope for a local-first template catalog and introduces cross-machine outbound plus credential sprawl. | None planned. | A device-trust plus mesh transport is cross-machine outbound and a standing credential surface, the opposite of the catalog's local-first, zero-outbound-by-default posture. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |
| n/a (hosted web UIs, not MCP) | Two hosted web applications fronting the runtime | Serve hosted, browser-based generation / search / management UIs | Hosted generation / search-as-service over the network | `drop-outright` | n/a (a hosted service, not catalog content) | **Dropped.** No hosted web UI is added; the catalog ships content, not a hosted service. | None planned. | Hosted generation / search-as-service is on the MCP Registry Policy hard-no list. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |
| n/a (WASM agent sandbox runtime, not MCP) | A WebAssembly agent-sandbox execution runtime | Executes agent code inside a bundled WASM sandbox engine | None inherent, but presupposes a shipped sandbox-engine runtime | `drop-outright` | n/a (the sandbox doctrine is already a skill) | **Dropped.** The sandbox *doctrine* (default-deny host execution, isolated sandbox tiers escalating with risk) is already delivered by `agent-access-policy`; the bundled runtime engine itself is non-adoptable catalog content. | None planned. | A bundled execution-engine runtime is not Markdown-plus-thin-scripts catalog content; the adoptable doctrine already lives in `agent-access-policy`. Full analysis: [docs/releases/v3/v3.10/comparisons/v3.10.0-comparison-ruflo.md](../v3.10.0/comparison-ruflo.md). |

---

### Declined in the v3.11.0 adoption cycle (optical / image-token compression proxy runtime)

This row records the optical / image-token context-compression proxy mechanism that the v3.11.0 adoption cycle (comparison dated 2026-07-06) evaluated and **deliberately dropped outright** under the MCP Registry Policy, so the decision is durable -- a future comparison reads this row and recognizes the candidate as already-adjudicated rather than re-surfacing it as a fresh gap. It reverses no standing decision: the catalog references runtimes and never reimplements them (the v3.1.0 `/loop` host-command decision, the v3.8.0 standalone-loop-runtime row, and the v3.10.0 ruflo MCP-server-as-daemon drops above are the direct precedents). The adoptable substance of the source was imported in the same cycle as skill-native doctrine: the optical / image-token compression treatment in `prompt-token-optimization` (the mechanism, the `ceil(w/28) * ceil(h/28)` per-patch billing math, the high-resolution-tier cost inversion, the silent-exact-string-confabulation failure, and the byte-exact-stays-text rule) plus a model-specificity note in `model-routing`. This file is not installer-distributed, so the source is named here; the distributed skill artifacts stay generic per the Reverse-Engineering Attribution Rule. Full analysis: [docs/releases/v3/v3.11/comparisons/v3.11.0-comparison-pxpipe.md](../v3.11.0/comparison-pxpipe.md). The action columns are scoped to the declining release.

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.11.0 action | v3.12.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (transport-layer reverse-proxy, not MCP) | An always-on local reverse-proxy (`teamchong/pxpipe`, npm `pxpipe-proxy`) that points the client at `ANTHROPIC_BASE_URL=127.0.0.1`, intercepts each `/v1/messages` request, lossily re-renders bulky static context (system slab, tool docs, large tool results, closed history) as dense PNG image blocks, and forwards upstream | Sits in the API critical path and mutates every eligible request body lossily before it reaches the provider; proxies and re-injects the user's Anthropic / OpenAI credentials | Runtime interception of all API traffic through a local proxy that re-injects the user's provider credentials and lossily rewrites request bodies in the critical path | `drop-outright` | n/a (the mechanism itself is the disqualifier; it runs locally, but a lossy request-mutating proxy in the API path is precisely what must not be shipped) | **Dropped.** Not adopted in any form -- no proxy, no request-mutation layer, no credential handling, no image-rendering runtime is added. The adoptable substance was imported as the skill-native doctrine in `prompt-token-optimization` (Phase 1) and the `model-routing` note (Phase 2). | None planned. Reconsider only if Anthropic changes image-token billing so legible renders become cheaper than text on strong models, or if a lossless-fidelity variant (a verified anchor sidecar that passes byte-exact strings as text beside the image) becomes proven. | Declined under the MCP Registry Policy on the same grounds as the v3.10.0 ruflo MCP-server-as-daemon and standalone-loop-runtime drops: an always-on runtime component in the API critical path that lossily mutates requests and handles credentials inverts the catalog's zero-outbound, no-runtime posture. Independently declined on **correctness** grounds -- a correctness-first coding catalog whose ethos is binary Verification and "no temporary fixes" must not ship or endorse a lossy context transform whose errors are silent confabulations (published tests report 0% 12-character hex verbatim recall on Opus-class models). Independently declined on **economics** -- the savings invert on the high-resolution image tier strong models use (a legible page costs roughly 1.5x-5x more tokens than the equivalent text) and can be net-negative once completion tokens are counted. Full analysis: [docs/releases/v3/v3.11/comparisons/v3.11.0-comparison-pxpipe.md](../v3.11.0/comparison-pxpipe.md). |

---

### Adopted and declined in the v3.15.6 agentic-endpoint-hardening cycle (sandbox escapes)

These rows record the seven candidates from the v3.15.6 adoption cycle, whose source is a public security-research series on coding-agent sandbox escapes. The cycle is materially different from most: one of the disclosed advisories (CVE-2026-48124, a workspace-controlled agent-harness hook configuration that triggered unsandboxed execution) names **Nexus-Hub's own distribution channel** as the attack surface, since the catalog distributes exactly that artifact class. Every adopted candidate is local: none introduces an outbound call, an API key, a third-party data processor, or a runtime dependency. The single candidate that would have (the vendor's commercial endpoint product) is dropped under the MCP Registry Policy hard-no list, and its valuable concept (provenance plus seam monitoring) is reverse-engineered locally as a best-effort ledger with the un-achievable portion documented as an explicit boundary. Per the Reverse-Engineering Attribution Rule, the vendor, its framework, and its product are named only in this column and in the adopted skill's `references/standards.md`; they appear nowhere in the distributed skill body. Full analysis: [docs/releases/v3/v3.15/comparisons/v3.15.6-comparison-sandbox-escapes.md](../releases/v3/v3.15/comparisons/v3.15.6-comparison-sandbox-escapes.md). Plan: [docs/releases/v3/v3.15/plans/v3.15.6-adoption-sandbox-escapes.md](../releases/v3/v3.15/plans/v3.15.6-adoption-sandbox-escapes.md).

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.15.6 action | v3.17.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (skill, not MCP) | Public security-research series: Pillar Security, "The Week of Sandbox Escapes" (https://www.pillar.security/blog/the-week-of-sandbox-escapes) | AC1: a layered agentic-endpoint threat model (the config-write-then-executed pattern, a six-form escape taxonomy, the normative execution-trigger surface list, nine control layers, a seven-question platform audit checklist) | None. Pure Markdown instructions to the agent's own LLM: no code, no dependency, no credential, no outbound call | `skill-native` | n/a (already skill-native) | **Adopted** as `catalog/skills/security-operations/agentic-endpoint-hardening/SKILL.md` (Phase 1, sub-task 1.1) and registered in the three `data/` files (sub-task 1.3). Source content generalized to component classes and rewritten to Nexus-Hub tone; the vendor, its framework, and its product are stripped from every user-facing line | Refresh the taxonomy if a materially new escape form is disclosed. There is no runtime to track | Classified `skill-native` because the entire artifact is a knowledge product in the Agent Skill format. The distributed body names no product version, so it stays accurate as vendors patch; advisory identifiers live in `references/standards.md`. The catalog had the deny-by-default DOCTRINE (`agent-access-policy`) but no consolidated threat model for this seam, and no coverage of the specific config-write-then-executed pattern |
| n/a (reference doc, not MCP) | Same series, plus the public MITRE ATT&CK / D3FEND and NIST CSF catalogs | AC7: source attribution and the framework mapping (ATT&CK T1546 / T1059 / T1611, D3FEND D3-FA / D3-FH / D3-PA, NIST CSF PR.PS / DE.CM) with per-ID rationale and public source URLs | None. Reference documentation only | `skill-native` | n/a | **Adopted** as the skill's `references/standards.md` (Phase 1, sub-task 1.2). This is the one distributed file where the vendor and its framework are named, which is what keeps the skill body generic | Re-verify framework identifiers if ATT&CK or D3FEND renumber | The vendor's own framework is referenced by name only and deliberately NOT reproduced (it is that vendor's intellectual property). Only independently-derivable engineering conclusions were adopted. Every declared identifier was cross-checked against existing verified in-repo usage rather than asserted from memory |
| n/a (local hook edits, not MCP) | Same series | AC2 and AC4: extend the sensitive-path advisory hook (`escalation-trigger`) to cover the normative execution-trigger surfaces, and harden the command guardrail (`git-guardrails`) for the version-control metadata indirection patterns, each with a PowerShell sibling and pytest coverage | None. Local bash / PowerShell hooks reading a tool-event path or command string. No network, no credential | `re-full` | Low-Medium | **Adopted** in Phase 2 (sub-tasks 2.1 and 2.2). Default action stays advisory (`warn`, not `block`) so the catalog's own `nexus-hub init` writes are not self-blocked | Extend the surface list if the project-extension candidates in the skill are promoted | Classified `re-full` because the sensitive-path list and the pattern denylist are entirely local logic with no third-party data destination. The honest deliverable is the specific known-bad patterns plus a documented argv-decomposition limitation, not a claim of completeness: the source's own thesis (denylists cannot keep pace) applies to this hook, and the skill states that it is defense-in-depth rather than a boundary |
| n/a (local config artifact plus installer flag, not MCP) | Same series | AC5: an opt-in deny-by-default permission stub whose deny and ask entries reuse the normative surface list, installed behind a `--strict-permissions` / `-StrictPermissions` flag in both installers | None. Local permission-config emission only. No network, no credential | `re-full` | Medium | **Adopted** in Phase 3 (sub-task 3.1), opt-in only. The distributed default stub stays allow-only and the install stays no-prompt, preserving the v3.7.0 install UX | Revisit the default posture only if the convenience default is reconsidered project-wide | Classified `re-full`: a local config artifact plus an installer flag, no external dependency. The comparison flagged the conflict explicitly: Nexus-Hub's install philosophy is no-prompt install-all, so a default deny stub would contradict it. Shipping it opt-in is a deliberate posture split (convenience default, security-hardened opt-in), not a regression. Touches the installers, an ask-first surface per `AGENTS.md`, pre-authorized by the comparison's Full scope |
| n/a (local PostToolUse hook, not MCP) | Same series (concept generalized from the vendor's endpoint-monitoring component) | AC3: a best-effort file-provenance / trust-seam ledger. On a write it records the path and a content hash; on a later command it flags execution of a recently agent-written path | None. Writes a local ledger file only. Records paths and content hashes, never file bodies or secret material, per the `egress-redaction` discipline | `re-partial` | High | **Adopted** in Phase 3 (sub-task 3.2), bounded: paths and hashes only, correlation capped to the current session, small per-write overhead | None planned. The cross-executor gap below is a documented boundary, not a backlog item | Classified `re-partial` because the local correlation (agent write, then a command executing that path) is fully reverse-engineerable and ships, while full cross-executor seam instrumentation across every trusted host component is NOT locally achievable: a hook observes only the tool calls of the harness it is installed in, and cannot instrument an editor extension, a language server, or a version-control integration in another process. That un-achievable portion is the explicitly dropped part, recorded as a tracked known gap rather than a silent omission |
| n/a (skill guidance plus local deny globs, not MCP) | Same series | AC6: privileged-local-daemon guidance (container, build, package-manager, service-manager, and orchestration endpoints as trust bridges) plus the narrow deny globs a user opts into | None. Guidance text plus local config globs | `skill-native` (guidance) + `re-full` (deny globs) | Low | **Adopted**: the guidance and enumeration checklist ship in the Phase 1 skill's "Privileged Local Daemons" section; the deny globs and least-privilege scoping remain `agent-access-policy`'s territory and are cross-linked rather than duplicated | Add specific daemon deny globs to the access-policy templates if demand emerges | Mixed classification because the two halves differ: enumerating which daemons are reachable and why they are a full escape path is a knowledge product, while a deny glob is a local config line. Nothing previously restricted privileged local daemons: `agent-access-policy` could deny a socket glob, but there was no guidance identifying daemons as trust bridges |
| n/a (commercial endpoint product, not an adoptable MCP) | The same vendor's four-component commercial platform (discovery and posture, red teaming, runtime guardrails, governance and compliance) | Instruments the developer endpoint and receives trust-handoff telemetry | Egress of endpoint trust-handoff telemetry to a third-party-owned platform; requires a commercial relationship and a credential | `drop-outright` | n/a (the local best-effort ledger is the reverse-engineered answer) | **Dropped.** Not adopted in any form. No telemetry, no credential, no third-party processor is added. The valuable concept is reverse-engineered locally as AC3 above, with the un-achievable cross-executor portion documented as a boundary | None planned. Reconsider only if endpoint-telemetry-as-a-service ever becomes a stated goal, which would reverse the catalog's zero-telemetry posture | A third-party data processor watching the developer endpoint sits on the MCP Registry Policy hard-no list (posture, scanning, and runtime-guardrail-as-service). It is not vendor-intrinsic: Nexus-Hub is not a customer of this vendor, and the capability is reverse-engineerable in concept. Same grounds as the v3.9.0 default-on-telemetry decline above |
| n/a (external framework, not MCP) | The same vendor's SAIL 2.0 framework | A structured AI-security framework published by the research vendor | None inherent; the objection is intellectual property, not data flow | `drop-outright` (verbatim adoption) | n/a | **Dropped as catalog content.** Referenced by name in `references/standards.md` as provenance and deliberately NOT reproduced. Only independently-derivable engineering conclusions were adopted | None planned | Adopting a vendor's framework verbatim would redistribute that vendor's intellectual property and would violate the Reverse-Engineering Attribution Rule in the distributed artifact. The same treatment the catalog gives MITRE and NIST text applies: cite the public source, do not redistribute the prose |

### Adopted in the v3.15.7 execution-authorization cycle

This row records the local execution-authorization broker adopted in the v3.15.7 cycle. It adds no MCP, outbound call, API key, third-party processor, or runtime dependency. Full analysis: [docs/releases/v3/v3.15/comparisons/v3.15.7-comparison-raptor-loop-hunt.md](../releases/v3/v3.15/comparisons/v3.15.7-comparison-raptor-loop-hunt.md). Plan: [docs/releases/v3/v3.15/plans/v3.15.7-adoption-raptor-loop-hunt.md](../releases/v3/v3.15/plans/v3.15.7-adoption-raptor-loop-hunt.md).

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.15.7 action | v3.17.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (skill-bundled local script, not MCP) | External pure-standard-library execution-broker pattern | Converts an explicit authorization source, typed capability grant, absolute writable roots, and exact network destinations into a least-privilege execution plan; execution is opt-in and requires a reachable sandbox prefix | None in authorization mode. Optional execution can reach only what the separately supplied sandbox permits; the broker itself adds no credential or network client | `re-full` | Medium | **Adopted** as `catalog/skills/orchestration/agent-access-policy/scripts/capability-grant-broker.py`, with denial-first tests and recursive installer-distribution proof | Extend typed adapters only when a new owned sandbox surface requires one; do not turn the broker into a general host executor | Reverse-engineered from the execution-broker concept in `dinosn/raptor-loop-hunt` (`raptor-loop-exec`, https://github.com/dinosn/raptor-loop-hunt). No source code was copied. The useful boundary is entirely local and expressible with the Python standard library: authorization is pure, execution remains separate and opt-in, and no outbound service, API key, third-party processor, or added dependency is required |

### Adopted in the v3.15.14 spec-driven-development cycle (spec artifact reconciliation and proportional depth)

These rows record the seven candidates from the v3.15.14 adoption cycle. Every adopted item is prose: a Markdown edit to a template, a checklist, or a skill body already owned by this repository. **No MCP, outbound call, API key, third-party data processor, or runtime dependency is introduced by any of them**, and no new skill was created, so the catalog count is unchanged at 270. Three external sources informed the cycle: a spec-driven-development article attributed to Gregor Ojstersek, the `obra/superpowers` example specification (https://github.com/obra/superpowers), and `github/spec-kit` (https://github.com/github/spec-kit). Per the Reverse-Engineering Attribution Rule, none of the three is named in any distributed artifact; they appear only in this column. Full analysis: [docs/releases/v3/v3.15/comparisons/v3.15.14-comparison-spec-driven-development.md](../releases/v3/v3.15/comparisons/v3.15.14-comparison-spec-driven-development.md). Plan: [docs/releases/v3/v3.15/plans/v3.15.14-spec-driven-development.md](../releases/v3/v3.15/plans/v3.15.14-spec-driven-development.md).

| MCP key | Current source | What it does | Outbound-call surface | Classification | Effort if RE'd | v3.15.14 action | v3.17.0+ action | Rationale / citation |
|---|---|---|---|---|---|---|---|---|
| n/a (owned template plus skill edit, not MCP) | Article insight I1 (problem statement at the top) | D1: resolves two competing spec templates to one canonical skeleton, adds `## Problem Statement` carrying the actor and observable outcome forward from `idea-refine`, and repoints the skill's Verification checklist at the canonical template's sections | None. Markdown edits to files this repository already owns | `re-full` | Low-Medium | **Adopted** in Phase 1 (sub-tasks 1.1 and 1.3). The rival inline template was removed in both its code-block and prose forms and its project-onboarding content relocated to a labelled "Project-level context" section rather than deleted | Re-verify only if a second spec skeleton is ever introduced | Classified `re-full` because the artifacts are entirely local and the defect was internal: the skill's own completion gate validated a template the workflow does not produce. Nothing external is depended on; the article supplied the observation, not an implementation |
| n/a (owned template plus checklist edit, not MCP) | Article insight I2 (non-goals) | D2: a mandatory `## Non-Goals` section in `spec-template.md` with a reason required per entry, bound to `spec-quality-checklist.md`'s existing "Scope is clearly bounded" item and to the `scope-guardian-reviewer` agent | None. Markdown only | `re-full` | Low | **Adopted** in Phase 1 (sub-tasks 1.1, 1.2, 1.4). The reason-per-entry discipline reuses conventions this catalog already carries at `product-manager` SKILL.md:137 and `test-strategy-doc` SKILL.md:74 | None planned | Classified `re-full`: a local template section closing a confirmed internal inconsistency in which three owned surfaces audited for a boundary the owned template could not express. `idea-refine` was reconciled as the upstream producer so the hand-off is a copy rather than a rewrite |
| n/a (owned template edit, not MCP) | Article insight I10 (invariants) | D7: an `## Invariants` section declaring existing behavior a change must not break, distinct from non-goals and conditional on the change touching existing behavior | None. Markdown only | `re-full` | Low | **Adopted** in Phase 1 (sub-task 1.1), in the same template region as D2 | None planned | Classified `re-full` for the same reason as D2. The Non-Goals / Invariants boundary is stated at the point of use, because the distinction (will not be built, versus must not break) is the part authors get wrong |
| n/a (skill, not MCP) | Article insight I8 (proportionality) | D6: a spec-depth tier rule keyed on blast radius, placed inside the Hard Gate section, with the approval gate left unconditional | None. Pure Markdown instructions to the agent's own LLM | `skill-native` | n/a (already skill-native) | **Adopted** in Phase 2 (sub-task 2.1). Full proportionality (a size threshold below which the spec step is skipped) was considered and **declined**; only artifact depth scales | Revisit only if the depth tiers prove mis-calibrated in practice | Classified `skill-native` under MCP Registry Policy tier 2: the capability is achieved by instructing the agent directly, so it ships as prose rather than as any tool. The governance risk was managed structurally rather than by wording alone: the rule is nested under the gate's own heading so it cannot be read detached from it, and two rationalization rows rebut the misreadings it invites |
| n/a (skill, not MCP) | Article insight I7 (the scaling argument) | D5: one paragraph stating why guess cost scales with codebase size rather than change size, tied to the `[NEEDS CLARIFICATION]` marker and the mandatory `## Assumptions` section | None. Markdown only | `skill-native` | n/a | **Adopted** in Phase 2 (sub-task 2.2) | None planned | Classified `skill-native` (tier 2). The source article is not named in the skill body, per the attribution rule; only the engineering conclusion was adopted |
| n/a (skill, not MCP) | Article insight I5 (error handling), narrowed | D4: mandatory per-component failure-mode coverage at the plan layer across three named situations (malformed or absent inputs, unreachable or slow dependency, conflicting operations), keyed off the spec's existing `### Edge Cases` prompts | None. Markdown only | `skill-native` | n/a | **Adopted** in Phase 3 (sub-task 3.1) into `implementation-plan`, chosen over `plan-before-code` and recorded as decision DC-1. This is the one narrow slice of the declined architecture-in-the-spec insight that was a real gap | Extend only if a fourth failure situation proves as common as the three named | Classified `skill-native` (tier 2). The layering is the whole point: the spec names the user-visible edge case, the plan names the handling, and the skill states the prohibition explicitly so a future author does not close the gap by pushing error handling back into the spec |
| n/a (skill, not MCP) | Article insight I4 (shortcuts versus load-bearing proof) | D3: a one-line `scaffolding` versus `load-bearing` build class per plan sub-task, so a reviewer can tell disposable code from the code that proves the design | None. Markdown only | `skill-native` | n/a | **Adopted** in Phase 3 (sub-task 3.2) into the same skill as D4 | None planned | Classified `skill-native` (tier 2). **Adopted on merit, not on the cited evidence.** The article cited the `obra/superpowers` example specification as demonstrating this convention; that document contains no code blocks and no such labeling, so the cited evidence does not support the claim. The idea was adopted anyway because the review problem it names is real and observable, and the skill body records that honestly without naming the source |

**Two policy declines in this cycle**, both recorded with grounds:

- **The Spec Kit CLI (`github/spec-kit`) is declined as redundant, not as a trust concern.** All ten `speckit.*` commands the source recommends already have first-party equivalents in this catalog, so the Spec Kit half of the comparison produced zero candidates. Declined under MCP Registry Policy tier 2, which prefers an LLM-native skill over an external tool dependency wherever the capability is already achievable by instructing the agent. Adding the CLI would introduce an external runtime to deliver behavior the catalog already ships.
- **Architecture-in-the-spec is declined on layering grounds, not on trust grounds.** The source recommends putting the data model, module boundaries, interfaces, and error handling into the spec. That conflicts with this catalog's normative-spec-versus-free-form-context split and would fail `spec-quality-checklist.md`'s implementation-detail checks (lines 9, 19, 30), so a spec written to the source's shape would fail its own quality gate. The one narrow slice that was a genuine gap (error handling) was adopted at the plan layer instead, as D4 above. No security or data-flow objection applies; this is a decision about which artifact owns which content.

**Cycle-wide confirmation**: no adopted item introduces an MCP entry, an outbound call, an API key, a third-party data processor, or a runtime dependency. `catalog/mcp-configs/mcp-servers.json` is untouched, and the registry size is unchanged.

---

## Summary

| Bucket | Count | Notes |
|---|---:|---|
| Kept: internal or local-only | 5 | `nexus-skill-server`, `filesystem`, `memory`, `sequential-thinking`, `sqlite` |
| Kept: vendor-intrinsic (your-own-account) | 6 | `github`, `postgres`, `supabase`, `railway`, `vercel`, `cloudflare` - each with a 5-question audit in its `_comment` |
| Dropped in v1.0.0 | 4 | `context7` (replaced by `local-docs-lookup` skill), `exa-web-search` (drop-outright), `firecrawl` (replaced by `nexus-web-fetch`), `magic-ui` (replaced by `ui-component-generation` skill) |
| New internal MCPs in v1.0.0 | 2 | `nexus-code-search` (keyword-only MVP), `nexus-web-fetch` (SSRF-guarded HTTP fetch + readability) |
| Reverted in v1.0.0 | 1 | `claude-context` (reverse-engineered into `nexus-code-search` + the `code-semantic-search` skill) |
| Deferred to v1.1.0+ | 5+ | dense/hybrid retrieval on `nexus-code-search`; Playwright rendering in `nexus-web-fetch`; vendor-wrapper rebuilds (`nexus-github`, `nexus-postgres`, `nexus-supabase`, `nexus-railway`, `nexus-vercel`, `nexus-cloudflare`) |

**Post-v1.0.0 registry size**: 13 entries (5 already-local + 6 vendor-intrinsic + 2 new internal).

**Post-v3.2.0 registry size**: 14 entries (adds `nexus-context-compressor`, the third new internal MCP, via the `adoption-headroom` plan).

---

## 2026-07-03 third-cycle re-comparison (dispositions held)

The 2026-07-03 re-comparison ([docs/releases/v3/v3.11/comparisons/v3.11.0-comparison-spec-kit.md](../releases/v3/v3.11/comparisons/v3.11.0-comparison-spec-kit.md)) re-encountered the previously-declined surfaces in expanded form, and every disposition held under the MCP Registry Policy:

- **Community-installable workflow step catalog** - inherits the third-party-extension-install `drop-outright` above (same code-distribution-channel character; the capability is already met by the scanned local skill catalog + `/skills import` hardening).
- **Bundle remote-catalog fetch + trust-indicator machinery** - inherits the remote-catalog / plaintext-PAT-auth `drop-outright` above (remote credentialed catalog fetch is N/A for a local-first catalog that ships in the repo).
- **CI agent runtime (gh-aw-style)** - recorded as a non-shipped pattern; Nexus-Hub ships the label-gated pipeline as instructions only (`label-gated-agent-pipelines` skill), never a runtime or workflow file.

No new MCP, runtime, credential, or outbound surface was added in the v3.11.0 spec-kit cycle; the adopted items are skill-native or local `re-full` builds over owned artifacts.

---

## How to Add a New Row

1. Walk the [decision tree](../../AGENTS.md#decision-tree-stop-at-the-first-bucket-that-fits) in `AGENTS.md` and pick the highest-precedence bucket that applies.
2. Cite upstream evidence in the Rationale column (a file path in the upstream repo, a docs URL, or a README section).
3. For `re-full` / `re-partial` classifications, name the internal deliverable (package name, skill name) and the target release.
4. For `vendor-intrinsic` classifications, write the 5-question audit paragraph that Phase 3 copies into the entry's `_comment`.
5. Record the row before opening a PR that touches `catalog/mcp-configs/mcp-servers.json`. No MCP is added without a matrix row.
